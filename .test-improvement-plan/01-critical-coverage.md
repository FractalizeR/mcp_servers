# Этап 1: Повышение Coverage до 80%

**Приоритет:** 🔴 КРИТИЧНО
**Estimate:** 2-3 дня
**Impact:** HIGH
**Effort:** MEDIUM

---

## 📊 Текущее состояние

| Метрика | Текущий | Целевой | Gap |
|---------|---------|---------|-----|
| Branches | 59% | 80% | -21% |
| Functions | 70% | 80% | -10% |
| Lines | 61% | 80% | -19% |
| Statements | 61% | 80% | -19% |

**Конфигурация:** `vitest.config.ts`

```typescript
thresholds: {
  // TODO: Повысить до 80% когда будет больше тестов
  branches: 59,
  functions: 70,
  lines: 61,
  statements: 61,
}
```

---

## 🎯 Цели

1. Достичь 80%+ coverage по всем метрикам
2. Идентифицировать и покрыть uncovered branches
3. Добавить тесты для некорректно покрытых функций
4. Обновить пороги в `vitest.config.ts`

---

## 📋 План действий

### Шаг 1: Анализ текущего coverage (30 мин)

**Задачи:**
1. Сгенерировать HTML отчет coverage
2. Идентифицировать файлы с низким coverage (<80%)
3. Составить список приоритетных областей

**Команды:**
```bash
# Генерация отчета
npm run test:coverage

# Открыть HTML отчет
open coverage/index.html

# Фильтр по низкому coverage
# В браузере: сортировать по % coverage, найти красные файлы
```

**Ожидаемый результат:**
- Список файлов с coverage <80%
- Список uncovered branches/lines
- Приоритизированный backlog

**Шаблон для документирования:**
```markdown
## Uncovered Areas

### High Priority (Core Logic)
- [ ] src/tracker_api/facade/yandex-tracker.facade.ts - 65% (lines 120-145)
- [ ] src/infrastructure/http/client/http-client.ts - 70% (error paths)

### Medium Priority (Edge Cases)
- [ ] src/mcp/tools/api/issues/update/update-issue.tool.ts - 75%

### Low Priority (Utils)
- [ ] src/mcp/utils/response-field-filter.ts - 78%
```

---

### Шаг 2: Покрытие CLI (1 день)

**Текущий статус:** CLI исключен из coverage (`src/cli/` в exclude)

**Задачи:**
1. Создать `tests/unit/cli/` структуру
2. Написать тесты для CLI команд
3. Убрать `src/cli/` из coverage exclude

**Структура тестов:**
```
tests/unit/cli/
├── mcp-connect.test.ts        # Тесты для CLI команд
└── config-loader.test.ts      # Если есть конфиг логика
```

**Пример теста для CLI:**
```typescript
// tests/unit/cli/mcp-connect.test.ts
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { spawn } from 'child_process';

describe('mcp-connect CLI', () => {
  describe('Argument parsing', () => {
    it('должен парсить --log-level аргумент', () => {
      // Arrange
      const args = ['--log-level', 'debug'];

      // Act
      const config = parseCliArgs(args);

      // Assert
      expect(config.logLevel).toBe('debug');
    });

    it('должен использовать дефолтный log level', () => {
      const config = parseCliArgs([]);
      expect(config.logLevel).toBe('info');
    });

    it('должен валидировать некорректный log level', () => {
      expect(() => parseCliArgs(['--log-level', 'invalid']))
        .toThrow('Invalid log level');
    });
  });

  describe('MCP Server initialization', () => {
    it('должен запустить MCP сервер с корректной конфигурацией', async () => {
      // Тест зависит от реализации CLI
      // Может требовать mock для server.start()
    });
  });

  describe('Error handling', () => {
    it('должен обработать ошибку инициализации DI контейнера', async () => {
      // Mock container.init() to throw
      // Verify error is logged and process exits
    });

    it('должен обработать ошибку запуска сервера', async () => {
      // Mock server.start() to throw
      // Verify graceful shutdown
    });
  });
});
```

**Чек-лист:**
- [ ] Создать tests/unit/cli/mcp-connect.test.ts
- [ ] Тесты для парсинга аргументов
- [ ] Тесты для инициализации сервера
- [ ] Тесты для error handling
- [ ] Убрать src/cli/ из vitest.config.ts exclude
- [ ] Запустить coverage и проверить результат

---

### Шаг 3: Покрытие DTO/Entities (3-4 часа)

**Текущий статус:** Нет dedicated тестов, покрываются косвенно

**Задачи:**
1. Проверить наличие логики в DTO/Entities
2. Если есть валидация/методы → создать тесты
3. Если только интерфейсы → оставить как есть

**Анализ:**
```bash
# Поиск методов в entities
grep -r "class\|function" src/tracker_api/entities/

# Поиск методов в dto
grep -r "class\|function" src/tracker_api/dto/
```

**Если есть логика, создать тесты:**
```typescript
// Пример: если есть валидация в DTO
// tests/unit/tracker_api/dto/issue/create-issue.dto.test.ts
describe('CreateIssueDto', () => {
  it('должен валидировать обязательные поля', () => {
    const dto = new CreateIssueDto({});
    expect(dto.validate()).toHaveProperty('errors');
  });

  it('должен принять валидные данные', () => {
    const dto = new CreateIssueDto({
      summary: 'Test',
      queue: 'QUEUE',
    });
    expect(dto.validate()).toEqual({ valid: true });
  });
});
```

**Чек-лист:**
- [ ] Проанализировать DTO на наличие логики
- [ ] Проанализировать Entities на наличие методов
- [ ] Создать тесты если есть логика
- [ ] Обновить coverage

---

### Шаг 4: Покрытие uncovered branches (1 день)

**Стратегия:**
1. Открыть HTML coverage отчет
2. Найти файлы с красными/желтыми линиями
3. Для каждого uncovered branch добавить тест

**Типичные uncovered branches:**

#### 1. Error paths
```typescript
// Код
try {
  await apiCall();
} catch (error) {
  logger.error('Failed'); // ⚠️ Может быть не покрыто
  throw new CustomError();
}

// Тест
it('должен обработать ошибку API', async () => {
  mockApi.call.mockRejectedValue(new Error('API Error'));

  await expect(service.execute()).rejects.toThrow(CustomError);
  expect(mockLogger.error).toHaveBeenCalled();
});
```

#### 2. Условные ветки
```typescript
// Код
if (config.enableCache) { // ⚠️ Может быть не покрыто обе ветки
  return cache.get(key);
}
return await api.get(key);

// Тесты
it('должен использовать кеш если включен', () => {
  config.enableCache = true;
  // ...
});

it('должен пропустить кеш если выключен', () => {
  config.enableCache = false;
  // ...
});
```

#### 3. Edge cases в логике
```typescript
// Код
const items = data?.items ?? []; // ⚠️ Обе ветки?
if (items.length === 0) {
  return defaultValue;
}

// Тесты
it('должен вернуть default для пустого массива', () => { ... });
it('должен вернуть default для null data', () => { ... });
it('должен вернуть default для undefined data', () => { ... });
```

**Процесс:**
```bash
# 1. Запустить coverage
npm run test:coverage

# 2. Открыть отчет
open coverage/index.html

# 3. Для каждого файла с <80%:
#    - Найти красные линии (uncovered)
#    - Найти желтые линии (partially covered branches)
#    - Добавить тесты для покрытия

# 4. Проверить прогресс
npm run test:coverage
```

**Чек-лист:**
- [ ] Проанализировать все файлы с coverage <80%
- [ ] Добавить тесты для error paths
- [ ] Добавить тесты для условных веток
- [ ] Добавить тесты для edge cases
- [ ] Достичь 80%+ coverage

---

### Шаг 5: Обновление конфигурации (15 мин)

**Задачи:**
1. Обновить пороги в `vitest.config.ts`
2. Убрать TODO комментарий
3. Проверить что CI проходит

**Изменения в `vitest.config.ts`:**
```typescript
coverage: {
  provider: 'v8',
  reporter: ['text', 'json', 'html'],
  exclude: [
    'node_modules/',
    'dist/',
    'tests/',
    'src/index.ts', // Entry point, не тестируется
    // Убрать: 'src/cli/' — теперь покрыто тестами
  ],
  thresholds: {
    branches: 80,    // ✅ Было: 59
    functions: 80,   // ✅ Было: 70
    lines: 80,       // ✅ Было: 61
    statements: 80,  // ✅ Было: 61
  },
},
```

**Чек-лист:**
- [ ] Обновить thresholds до 80
- [ ] Убрать TODO комментарий
- [ ] Убрать src/cli/ из exclude если добавлены тесты
- [ ] Запустить `npm run validate` — должно пройти
- [ ] Закоммитить изменения

---

## ✅ Критерии завершения

### Must Have
- [x] Coverage ≥80% по всем метрикам
- [x] `npm run validate` проходит без ошибок
- [x] Обновлены пороги в vitest.config.ts
- [x] CLI покрыт тестами (если логика есть)

### Should Have
- [x] Все uncovered branches покрыты
- [x] DTO/Entities покрыты (если есть логика)
- [x] HTML coverage отчет показывает зеленые файлы

### Nice to Have
- [ ] Coverage >85% (превышение цели)
- [ ] Документация обновлена (README.md)

---

## 🚨 Возможные проблемы

### Проблема 1: Трудно достичь 80% для некоторых файлов

**Причина:** Файл содержит много edge cases или сложную логику

**Решение:**
1. Проверить если это действительно нужная логика
2. Рефакторинг для упрощения тестирования
3. Допустить исключение для конкретного файла:
   ```typescript
   // vitest.config.ts
   exclude: [
     'src/complex-legacy-code.ts', // TODO: рефакторинг и тесты
   ]
   ```

### Проблема 2: CLI тесты требуют integration подход

**Причина:** CLI запускает полный сервер, сложно мокать

**Решение:**
1. Использовать unit тесты для парсинга аргументов
2. Использовать integration тесты для запуска сервера
3. Допустить более низкий coverage для CLI (70%+)

### Проблема 3: Coverage падает при добавлении новых тестов

**Причина:** Новые тесты импортируют код, который увеличивает total lines

**Решение:**
1. Это нормально, продолжить добавлять тесты
2. Фокусироваться на абсолютных числах, не только процентах

---

## 📚 Ресурсы

- [Vitest Coverage](https://vitest.dev/guide/coverage.html)
- [V8 Coverage](https://v8.dev/blog/javascript-code-coverage)
- [Coverage Best Practices](https://testing.googleblog.com/2020/08/code-coverage-best-practices.html)

---

## 📝 Шаблон для PR

```markdown
# Повышение coverage до 80%

## Изменения
- ✅ Добавлены тесты для CLI (tests/unit/cli/)
- ✅ Добавлены тесты для uncovered branches в {files}
- ✅ Обновлены пороги coverage до 80%

## Метрики
| Метрика | До | После | Изменение |
|---------|-----|-------|-----------|
| Branches | 59% | 82% | +23% |
| Functions | 70% | 83% | +13% |
| Lines | 61% | 81% | +20% |
| Statements | 61% | 81% | +20% |

## Проверка
- [x] `npm run validate` проходит
- [x] Coverage отчет показывает ≥80%
- [x] Все тесты зеленые

🤖 Generated with Claude Code
Co-Authored-By: Claude <noreply@anthropic.com>
```

---

**Следующий этап:** [02-critical-e2e.md](./02-critical-e2e.md)
