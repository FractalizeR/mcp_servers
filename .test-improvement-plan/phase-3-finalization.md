# Фаза 3: Финализация (ПОСЛЕДОВАТЕЛЬНО)

**Зависимости:** Все фазы 0, 1, 2 смержены в main
**Estimate:** 2-3 дня
**Приоритет:** 🟡 ВАЖНО
**Параллельность:** НЕТ (выполняется последовательно)

---

## 🎯 Цель фазы

Завершить улучшение тестов:
- **F1:** Обновить coverage thresholds до достигнутых значений
- **F2:** (ОПЦИОНАЛЬНО) Мигрировать существующие тесты на новые паттерны

**⚠️ ВАЖНО:** Эта фаза выполняется ТОЛЬКО после мержа всех предыдущих веток.

---

## 📋 Этап F1: Coverage thresholds

**Ветка:** `claude/test-phase-3-thresholds-<session-id>`
**Estimate:** 1 час
**Обязательность:** MUST

### Цель
Обновить `vitest.config.ts` с финальными достигнутыми значениями coverage.

### Шаги

#### 1. Проверить текущий coverage (10 мин)

```bash
# Убедись что ты на актуальном main
git checkout main
git pull origin main

# Запусти coverage
npm run test:coverage
```

**Ожидаемые значения (примерно):**
```
Branches    : 75-80%
Functions   : 78-82%
Lines       : 75-80%
Statements  : 75-80%
```

#### 2. Определить новые thresholds (5 мин)

**Правило:** Установить чуть НИЖЕ текущих значений (на 2-3%)

Пример:
```
Если текущий coverage:
- Branches: 78% → threshold: 75%
- Functions: 81% → threshold: 78%
- Lines: 77% → threshold: 74%
- Statements: 77% → threshold: 74%
```

**Почему ниже?** Чтобы оставить небольшой буфер для будущих изменений.

#### 3. Обновить vitest.config.ts (10 мин)

```typescript
// vitest.config.ts

export default defineConfig({
  // ... other config ...
  test: {
    coverage: {
      provider: 'v8',
      reporter: ['text', 'json', 'html'],
      exclude: [
        'node_modules/',
        'dist/',
        'tests/',
        'src/index.ts',
        // CLI теперь покрыт (убрано в Фазе 1B)
      ],
      thresholds: {
        // ✅ ОБНОВЛЕНО: было 59/70/61/61
        branches: 75,    // Установи на основе шага 2
        functions: 78,   // Установи на основе шага 2
        lines: 74,       // Установи на основе шага 2
        statements: 74,  // Установи на основе шага 2
      },
    },
  },
});
```

#### 4. Проверить что build проходит (5 мин)

```bash
# Проверить что новые thresholds не ломают build
npm run test:coverage

# Должен пройти успешно
npm run validate
```

#### 5. Закоммитить (30 мин)

```bash
# Убедись что изменён только vitest.config.ts
git status

# Закоммитить
git add vitest.config.ts
git commit -m "$(cat <<'EOF'
test: обновить coverage thresholds до достигнутых значений

После завершения Фаз 0-2 coverage вырос:
- Branches: 59% → 75%
- Functions: 70% → 78%
- Lines: 61% → 74%
- Statements: 61% → 74%

Обновлены пороги в vitest.config.ts для защиты от регрессии.

Детали:
- Фаза 0: Mock factories и E2E helpers
- Фаза 1A: Skip тесты закрыты
- Фаза 1B: CLI coverage 70%+
- Фаза 1C: Uncovered branches покрыты
- Фаза 2D: E2E workflows
- Фаза 2E: Integration для mutation tools

🤖 Generated with Claude Code
Co-Authored-By: Claude <noreply@anthropic.com>
EOF
)"

# Push
git push -u origin claude/test-phase-3-thresholds-<session-id>
```

### Критерии завершения
- [x] Coverage запущен и проанализирован
- [x] Thresholds обновлены в vitest.config.ts
- [x] `npm run validate` проходит
- [x] PR создан и смержен

### PR Checklist
- [ ] Только vitest.config.ts изменён
- [ ] Новые thresholds проходят
- [ ] В описании указаны старые и новые значения

---

## 📋 Этап F2: Рефакторинг паттернов (ОПЦИОНАЛЬНО)

**Ветка:** `claude/test-phase-3-refactoring-<session-id>`
**Estimate:** 1-2 дня
**Обязательность:** OPTIONAL (можно отложить)

### Цель
Мигрировать СУЩЕСТВУЮЩИЕ тесты на паттерны из Фазы 0:
- Заменить `as any` на DI или mock factories
- Добавить ESLint правила
- Улучшить типобезопасность

### ⚠️ Важно
- Этот этап **НЕ КРИТИЧЕН** для достижения 80% coverage
- Можно выполнить позже или пропустить
- Фокус на существующих тестах, не на новых

### Шаги

#### 1. Поиск проблемных мест (30 мин)

```bash
# Поиск всех 'as any' в тестах
grep -r "as any" tests/ | grep -v node_modules > /tmp/as-any-list.txt

# Поиск обращений к приватным полям
grep -r "as any\)\\." tests/ | grep -v node_modules > /tmp/private-access.txt

# Анализ
cat /tmp/as-any-list.txt
cat /tmp/private-access.txt
```

**Создать список:**
```markdown
## Найденные проблемы

### Критичные (обход инкапсуляции)
- [ ] tests/unit/tracker_api/api_operations/issue/get-issues.operation.test.ts
  - `(operation as any).parallelExecutor = ...`

### Некритичные (только type casting)
- [ ] tests/unit/mcp/tools/...
  - `mockFacade = {...} as unknown as YandexTrackerFacade`
```

#### 2. Рефакторинг критичных (1 день)

См. [05-important-refactoring.md](./05-important-refactoring.md), Шаг 2:
- Использовать DI для зависимостей
- Или создать публичные setters для тестов
- Обновить регистрацию в DI контейнере

**Пример рефакторинга:**

```typescript
// До (плохо)
const operation = new GetIssuesOperation(mockHttpClient, mockLogger);
(operation as any).parallelExecutor = mockParallelExecutor; // ❌

// После (хорошо)
const operation = new GetIssuesOperation(
  mockHttpClient,
  mockLogger,
  mockParallelExecutor  // ✅ DI
);
```

#### 3. Рефакторинг некритичных (0.5 дня)

Заменить на mock factories:

```typescript
// До (допустимо, но не идеально)
const mockFacade = {
  getIssues: vi.fn(),
} as unknown as YandexTrackerFacade; // ⚠️

// После (лучше)
import { createMockFacade } from '@tests/helpers/mock-factories.js';
const mockFacade = createMockFacade(); // ✅
mockFacade.getIssues = vi.fn();
```

#### 4. Добавить ESLint правила (1 час)

```javascript
// .eslintrc.cjs
module.exports = {
  rules: {
    '@typescript-eslint/no-explicit-any': ['error', {
      ignoreRestArgs: false,
      fixToUnknown: true,
    }],
  },
  overrides: [
    {
      files: ['tests/**/*.ts'],
      rules: {
        '@typescript-eslint/no-explicit-any': 'error',
      },
    },
  ],
};
```

```bash
# Проверить
npm run lint

# Исправить найденные проблемы
```

#### 5. Обновить документацию (30 мин)

Добавить в `tests/README.md` раздел "Migration to new patterns":

```markdown
## Migration to New Patterns

### Existing Tests
Старые тесты (до Фазы 0) используют `as any` / `as unknown as`.
Постепенная миграция на mock factories.

### New Tests
Все новые тесты (с Фазы 1) ДОЛЖНЫ использовать mock factories.

### Status
- ✅ Новые тесты (Фазы 1-2): mock factories
- ⚠️ Старые тесты: в процессе миграции
- 🎯 Цель: 100% тестов на mock factories
```

### Критерии завершения
- [x] Критичные `as any` заменены (обход инкапсуляции)
- [x] ESLint правила добавлены
- [x] Документация обновлена
- [x] `npm run lint` — 0 errors

### Опциональные критерии
- [ ] Все `as any` заменены (100%)
- [ ] Все тесты используют mock factories

---

## ✅ Критерии завершения Фазы 3

### Must Have (F1)
- [x] Coverage thresholds обновлены
- [x] `npm run validate` проходит
- [x] F1 PR смержен

### Should Have (F2, опционально)
- [ ] Критичные `as any` устранены
- [ ] ESLint правила добавлены
- [ ] F2 PR смержен

### Nice to Have (F2, опционально)
- [ ] 100% тестов на mock factories
- [ ] 0 `as any` во всём проекте

---

## 📊 Финальные метрики

### После F1
| Метрика | Начало | После Фазы 3-F1 | Прирост |
|---------|--------|-----------------|---------|
| Branches | 59% | 75%+ | +16% |
| Functions | 70% | 78%+ | +8% |
| Lines | 61% | 74%+ | +13% |
| Statements | 61% | 74%+ | +13% |
| Skip тестов | 2 | 0 | -2 |
| CLI coverage | 0% | 70%+ | +70% |
| E2E файлов | 1 | 4+ | +3 |
| Integration файлов | 6 | 11+ | +5 |

### После F2 (опционально)
| Метрика | До F2 | После F2 |
|---------|-------|----------|
| `as any` в тестах | 10+ | 0 |
| Mock factories | 3 | 3 |
| ESLint warnings | ? | 0 |

---

## 🚨 Важные замечания

### ⚠️ F1 обязателен, F2 опционален
- **F1:** MUST - защищает coverage от регрессии
- **F2:** OPTIONAL - улучшает качество, но не критично

### ⚠️ Не повышай thresholds до 100%
Оставь буфер 2-3% для будущих изменений.

### ⚠️ F2 можно делать постепенно
Необязательно мигрировать все тесты сразу. Можно делать по 5-10 файлов в неделю.

---

## 📝 Шаблон для PR (F1)

```markdown
# Фаза 3-F1: Обновление coverage thresholds

## Изменения
- ✅ Обновлены пороги в vitest.config.ts

## Метрики
| Метрика | До | После | Прирост |
|---------|-----|-------|---------|
| Branches | 59% | 75% | +16% |
| Functions | 70% | 78% | +8% |
| Lines | 61% | 74% | +13% |
| Statements | 61% | 74% | +13% |

## Детали
После завершения Фаз 0-2:
- Фаза 0: Mock factories, E2E helpers
- Фаза 1A: Skip тесты (0)
- Фаза 1B: CLI coverage (70%+)
- Фаза 1C: Uncovered branches
- Фаза 2D: E2E workflows (4 файла)
- Фаза 2E: Integration mutation (5 файлов)

Общий прирост coverage: +13-16%

## Проверка
- [x] `npm run validate` проходит
- [x] Новые thresholds корректны

🤖 Generated with Claude Code
Co-Authored-By: Claude <noreply@anthropic.com>
```

---

## 📝 Шаблон для PR (F2, если выполняется)

```markdown
# Фаза 3-F2: Рефакторинг тестовых паттернов

## Изменения
- ✅ Рефакторинг критичных `as any` (DI)
- ✅ Добавлены ESLint правила
- ✅ Обновлена документация

## Метрики
| Метрика | До | После |
|---------|-----|-------|
| `as any` (критичные) | 5 | 0 |
| `as any` (всего) | 10+ | 3 |
| ESLint warnings | ? | 0 |

## Детали
- Критичные места (обход инкапсуляции) устранены через DI
- Добавлены ESLint правила для предотвращения в будущем
- Документация обновлена с migration guide

## Проверка
- [x] `npm run lint` — 0 errors
- [x] `npm run validate` проходит
- [x] Все тесты зелёные

🤖 Generated with Claude Code
Co-Authored-By: Claude <noreply@anthropic.com>
```

---

## 🎉 Поздравления!

После завершения Фазы 3 проект достиг:
- ✅ 75%+ coverage (цель 80% почти достигнута)
- ✅ 0 skip тестов
- ✅ Полное покрытие CLI
- ✅ E2E workflows
- ✅ Integration для всех mutation tools
- ✅ Единые паттерны тестирования

**Следующие шаги (будущее):**
- [07-future-performance.md](./07-future-performance.md)
- [08-future-mutation.md](./08-future-mutation.md)
- [09-future-mocks.md](./09-future-mocks.md)
