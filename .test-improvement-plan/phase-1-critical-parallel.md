# Фаза 1: Критичные улучшения (ПАРАЛЛЕЛЬНО)

**Зависимости:** Фаза 0 (должна быть смержена в main)
**Estimate:** 2-3 дня (каждая ветка независимо)
**Приоритет:** 🔴 КРИТИЧНО
**Параллельность:** 3 ветки могут работать одновременно

---

## 🎯 Цель фазы

Выполнить критичные улучшения в параллельных ветках без конфликтов:
- **Ветка A:** Закрыть пропущенные тесты
- **Ветка B:** Покрыть CLI тестами
- **Ветка C:** Повысить общий coverage

**Важно:** Все ветки используют паттерны из Фазы 0 (mock factories).

---

## 📊 Разделение ответственности

| Область | Ветка A | Ветка B | Ветка C |
|---------|---------|---------|---------|
| **Файлы** | `find-issues.tool.integration.test.ts` | `tests/unit/cli/*` | `tests/unit/**` (кроме CLI) |
| **Конфликты** | ❌ Нет | ❌ Нет | ❌ Нет |
| **vitest.config.ts** | ❌ Не трогает | ✅ Убирает CLI из exclude | ❌ Не трогает thresholds |

---

## 🔀 Ветка A: Skip tests

**Ветка:** `claude/test-phase-1a-skip-tests-<session-id>`
**Estimate:** 1 день
**Owner:** Исполнитель A

### Цель
Реализовать или удалить все `.skip` тесты с документированием решений.

### Задачи
- [x] Анализ причин пропуска тестов
- [x] Реализация или удаление skip тестов
- [x] Проверка других skip/todo в проекте
- [x] Документация решений

### Детальный план
См. [03-critical-skip-tests.md](./03-critical-skip-tests.md) - выполнить ВСЕ шаги.

### Критерии завершения
- [x] 0 `.skip` тестов в проекте
- [x] 0 `.todo` тестов
- [x] Решения документированы в `.test-improvement-plan/skip-tests-decisions.md`
- [x] `npm run test:integration` проходит

### Важные файлы
```
tests/integration/mcp/tools/api/issues/find/
└── find-issues.tool.integration.test.ts  (изменения здесь)

.test-improvement-plan/
└── skip-tests-decisions.md  (новый файл)
```

### PR Checklist
- [ ] Все skip тесты обработаны
- [ ] `npm run validate` проходит
- [ ] Документация обновлена

---

## 🔀 Ветка B: CLI testing

**Ветка:** `claude/test-phase-1b-cli-testing-<session-id>`
**Estimate:** 1-2 дня
**Owner:** Исполнитель B

### Цель
Создать unit тесты для CLI и достичь 70%+ coverage.

### Задачи
- [x] Анализ CLI кода
- [x] Создание unit тестов
- [x] Рефакторинг для тестируемости (если нужно)
- [x] Обновление vitest.config.ts (убрать CLI из exclude)

### Детальный план
См. [06-important-cli.md](./06-important-cli.md) - выполнить ВСЕ шаги.

### Критерии завершения
- [x] `tests/unit/cli/` создан с тестами
- [x] CLI coverage ≥70%
- [x] CLI убран из `vitest.config.ts` exclude
- [x] `npm test` проходит

### Важные файлы
```
src/cli/
├── index.ts
└── mcp-connect.ts  (возможен рефакторинг)

tests/unit/cli/
├── argument-parser.test.ts  (новый)
├── config-validator.test.ts  (новый)
└── error-handler.test.ts  (новый)

vitest.config.ts  (изменения в exclude)
```

### Изменения в vitest.config.ts
```typescript
// БЫЛО:
coverage: {
  exclude: [
    'node_modules/',
    'dist/',
    'tests/',
    'src/index.ts',
    'src/cli/',  // ❌ Убрать эту строку
  ],
  thresholds: {
    branches: 59,
    functions: 70,
    lines: 61,
    statements: 61,
  },
}

// СТАЛО:
coverage: {
  exclude: [
    'node_modules/',
    'dist/',
    'tests/',
    'src/index.ts',
    // src/cli/ теперь покрыт тестами
  ],
  thresholds: {
    branches: 59,  // ⚠️ НЕ МЕНЯЙ (Фаза 3)
    functions: 70, // ⚠️ НЕ МЕНЯЙ
    lines: 61,     // ⚠️ НЕ МЕНЯЙ
    statements: 61,// ⚠️ НЕ МЕНЯЙ
  },
}
```

### ⚠️ ВАЖНО: Паттерны из Фазы 0
```typescript
// ✅ ПРАВИЛЬНО: Используй mock factories
import { createMockLogger } from '@tests/helpers/mock-factories.js';

const mockLogger = createMockLogger();
```

### PR Checklist
- [ ] CLI тесты созданы
- [ ] CLI из exclude убран
- [ ] Thresholds НЕ изменены (только в Фазе 3)
- [ ] `npm run validate` проходит

---

## 🔀 Ветка C: Coverage improvement (БЕЗ CLI)

**Ветка:** `claude/test-phase-1c-coverage-<session-id>`
**Estimate:** 2-3 дня
**Owner:** Исполнитель C

### Цель
Повысить общий coverage до 65-70% (без CLI и без изменения thresholds).

### Задачи
- [x] Анализ текущего coverage
- [x] Покрытие DTO/Entities (если есть логика)
- [x] Покрытие uncovered branches
- [ ] ~~Покрытие CLI~~ (делает Ветка B)
- [ ] ~~Обновление thresholds~~ (Фаза 3)

### Детальный план
См. [01-critical-coverage.md](./01-critical-coverage.md):
- **Выполнить:** Шаги 1, 3, 4
- **ПРОПУСТИТЬ:** Шаг 2 (CLI делает Ветка B)
- **ПРОПУСТИТЬ:** Шаг 5 (thresholds обновляются в Фазе 3)

### Критерии завершения
- [x] DTO/Entities покрыты (если есть логика)
- [x] Uncovered branches покрыты
- [x] Coverage поднялся на 5-10%
- [ ] Thresholds НЕ изменены в vitest.config.ts

### Важные файлы
```
tests/unit/tracker_api/dto/  (новые тесты если нужно)
tests/unit/tracker_api/entities/  (новые тесты если нужно)
tests/unit/**  (дополнительные тесты для uncovered branches)

vitest.config.ts  (⚠️ НЕ ТРОГАТЬ)
```

### ⚠️ ВАЖНО: НЕ менять thresholds
```typescript
// ❌ НЕ делай это в Ветке C:
thresholds: {
  branches: 80,  // ❌ НЕ МЕНЯЙ
  functions: 80, // ❌ НЕ МЕНЯЙ
  lines: 80,     // ❌ НЕ МЕНЯЙ
  statements: 80,// ❌ НЕ МЕНЯЙ
}

// ✅ Оставь как есть:
thresholds: {
  branches: 59,
  functions: 70,
  lines: 61,
  statements: 61,
}
```

Thresholds обновляются ТОЛЬКО в Фазе 3-F1 после мержа всех веток.

### ⚠️ ВАЖНО: Паттерны из Фазы 0
```typescript
// ✅ ПРАВИЛЬНО: Используй mock factories
import {
  createMockLogger,
  createMockHttpClient,
  createMockFacade,
} from '@tests/helpers/mock-factories.js';

const mockLogger = createMockLogger();
const mockFacade = createMockFacade();
```

### PR Checklist
- [ ] Новые тесты добавлены
- [ ] Coverage вырос на 5-10%
- [ ] vitest.config.ts НЕ изменен
- [ ] Используются mock factories
- [ ] `npm run validate` проходит

---

## 📋 Координация между ветками

### Конфликты
**Нет конфликтов** между ветками A, B, C:
- Ветка A: работает только с `find-issues.tool.integration.test.ts`
- Ветка B: работает только с `tests/unit/cli/**`
- Ветка C: работает с остальными файлами

### Порядок мержа
Можно мержить в **любом порядке**:
```
Фаза 0 → main
    ↓
Ветка A → main  ┐
Ветка B → main  ├─ Любой порядок
Ветка C → main  ┘
```

### Общие правила
1. **Используй mock factories** из Фазы 0
2. **НЕ меняй thresholds** в vitest.config.ts (кроме Ветки B: убрать CLI из exclude)
3. **Запускай `npm run validate`** перед PR
4. **Следуй AAA pattern** (Arrange-Act-Assert)

---

## ✅ Критерии завершения Фазы 1

### Must Have
- [x] Все 3 ветки смержены в main
- [x] 0 skip тестов (Ветка A)
- [x] CLI coverage ≥70% (Ветка B)
- [x] Overall coverage вырос на 10-15%
- [x] Все тесты проходят

### Should Have
- [x] Используются mock factories везде
- [x] Документация обновлена
- [x] `npm run validate` проходит

### Nice to Have
- [ ] Coverage >70% (достигнется в Фазе 3)

---

## 📊 Ожидаемые метрики после Фазы 1

| Метрика | До Фазы 1 | После Фазы 1 | Целевое (Фаза 3) |
|---------|-----------|--------------|------------------|
| Skip тестов | 2 | 0 | 0 |
| CLI coverage | 0% | 70%+ | 70%+ |
| Overall branches | 59% | 65-70% | 80% |
| Overall functions | 70% | 75-78% | 80% |
| Overall lines | 61% | 67-72% | 80% |

---

## 🚨 Важные замечания

### ⚠️ Для Ветки B: Только exclude, НЕ thresholds
Ветка B убирает CLI из `exclude`, но **НЕ меняет thresholds**. Thresholds обновляются в Фазе 3-F1.

### ⚠️ Для Ветки C: Не трогать vitest.config.ts
Ветка C **вообще не меняет** `vitest.config.ts`. Все изменения - только добавление тестов.

### ⚠️ Используй паттерны Фазы 0
Все ветки ДОЛЖНЫ использовать mock factories из Фазы 0:
```typescript
import { createMockLogger } from '@tests/helpers/mock-factories.js';
```

---

## 📝 Общий шаблон для PR

```markdown
# Фаза 1[A/B/C]: [Название ветки]

## Изменения
- ✅ [Список изменений специфичных для ветки]

## Метрики
| Метрика | До | После |
|---------|-----|-------|
| [Релевантные метрики] | ... | ... |

## Проверка
- [x] `npm run validate` проходит
- [x] Используются mock factories из Фазы 0
- [x] [Специфичные проверки ветки]

## Связь
- **Зависимости:** Фаза 0 (смержена)
- **Следующая фаза:** Фаза 2 (после мержа всех веток Фазы 1)

🤖 Generated with Claude Code
Co-Authored-By: Claude <noreply@anthropic.com>
```

---

**Следующая фаза:** [phase-2-e2e-integration.md](./phase-2-e2e-integration.md) (после мержа всех веток Фазы 1)
