# Этап 3: Закрытие пропущенных тестов

**Приоритет:** 🔴 КРИТИЧНО
**Estimate:** 1 день
**Impact:** MEDIUM
**Effort:** LOW

---

## 📊 Текущее состояние

**Пропущенные тесты:** 2 (в find-issues.tool.integration.test.ts)

```typescript
// tests/integration/mcp/tools/api/issues/find/find-issues.tool.integration.test.ts

it.skip('должен поддерживать пагинацию (perPage, page)', async () => {
  // TODO: Реализовать после добавления пагинации в API
});

it.skip('должен принять валидные параметры', async () => {
  // TODO: Проверить какие параметры реально поддерживает API
});
```

---

## 🎯 Цели

1. Реализовать или удалить все `.skip` тесты
2. Добавить комментарии с причиной если реализация невозможна
3. Достичь 0 пропущенных тестов

---

## 📋 План действий

### Шаг 1: Анализ причин пропуска (30 мин)

**Задачи:**
1. Прочитать код пропущенных тестов
2. Проверить API документацию Яндекс.Трекера
3. Определить возможность реализации

**Анализ:**

#### Тест 1: Пагинация
```typescript
it.skip('должен поддерживать пагинацию (perPage, page)', async () => {
  mockServer.mockFindIssuesSuccess(['TEST-1', 'TEST-2'], {
    page: 1,
    perPage: 10,
  });

  const result = await client.callTool(
    'fractalizer_mcp_yandex_tracker_find_issues',
    {
      query: 'queue: TEST',
      perPage: 10,
      page: 1,
    }
  );

  expect(result.isError).toBeUndefined();
  // ...
});
```

**Вопросы:**
- Поддерживает ли Яндекс.Трекер API v3 пагинацию?
- Реализована ли пагинация в find-issues.tool?
- Есть ли параметры perPage/page в схеме?

**Проверка:**
```bash
# Проверить схему find-issues
cat src/mcp/tools/api/issues/find/find-issues.schema.ts

# Проверить operation
cat src/tracker_api/api_operations/issue/find/find-issues.operation.ts

# Проверить документацию API
cat yandex_tracker_client/docs/find_issues.md
```

#### Тест 2: Валидные параметры
```typescript
it.skip('должен принять валидные параметры', async () => {
  const validParams = {
    query: 'queue: TEST',
    orderBy: 'created',
    orderDirection: 'asc',
    expand: ['transitions', 'attachments'],
  };

  const result = await client.callTool(
    'fractalizer_mcp_yandex_tracker_find_issues',
    validParams
  );

  expect(result.isError).toBeUndefined();
});
```

**Вопросы:**
- Какие параметры поддерживает find-issues tool?
- Есть ли валидация в схеме?

**Чек-лист:**
- [ ] Проверить find-issues.schema.ts
- [ ] Проверить API документацию
- [ ] Определить возможность реализации

---

### Шаг 2: Реализация или удаление тестов (4-6 часов)

**Сценарий A: Пагинация НЕ поддерживается API**

Если Яндекс.Трекер API v3 не поддерживает пагинацию:

```typescript
// Удалить тест и добавить комментарий
describe('Пагинация', () => {
  // NOTE: Яндекс.Трекер API v3 не поддерживает пагинацию для /v3/issues/_search
  // Пагинация доступна только через scrolling API (v3/issues/_scroll)
  // См. документацию: yandex_tracker_client/docs/scrolling.md
  //
  // Если пагинация потребуется в будущем, рассмотреть:
  // 1. Реализацию scroll-based пагинации
  // 2. Использование курсоров вместо page/perPage
});
```

**Сценарий B: Пагинация поддерживается, но не реализована**

Если API поддерживает, но tool не реализован:

1. Обновить find-issues.schema.ts:
```typescript
// src/mcp/tools/api/issues/find/find-issues.schema.ts
export const FindIssuesParamsSchema = z.object({
  query: z.string().min(1),
  fields: FieldsSchema.optional(),

  // Добавить параметры пагинации
  perPage: z.number().int().min(1).max(100).optional()
    .describe('Количество задач на странице (1-100)'),
  page: z.number().int().min(1).optional()
    .describe('Номер страницы (начиная с 1)'),
});
```

2. Обновить find-issues.operation.ts:
```typescript
// src/tracker_api/api_operations/issue/find/find-issues.operation.ts
export interface FindIssuesInput {
  query: string;
  perPage?: number;
  page?: number;
}

async execute(input: FindIssuesInput): Promise<IssueWithUnknownFields[]> {
  const params: Record<string, unknown> = {
    query: input.query,
  };

  if (input.perPage !== undefined) {
    params.perPage = input.perPage;
  }

  if (input.page !== undefined) {
    params.page = input.page;
  }

  const response = await this.httpClient.post<IssueWithUnknownFields[]>(
    '/v3/issues/_search',
    params
  );

  return response.data;
}
```

3. Обновить find-issues.tool.ts:
```typescript
// src/mcp/tools/api/issues/find/find-issues.tool.ts
async execute(params: FindIssuesParams): Promise<CallToolResult> {
  const issues = await this.trackerFacade.findIssues({
    query: params.query,
    perPage: params.perPage,
    page: params.page,
  });

  // ...
}
```

4. Убрать `.skip` из теста:
```typescript
it('должен поддерживать пагинацию (perPage, page)', async () => {
  // Реализация теста
});
```

5. Добавить unit тесты для новой функциональности

**Сценарий C: Реализация валидных параметров**

Если тест просто проверяет валидацию:

1. Проверить текущую схему:
```typescript
// Какие параметры уже поддерживаются?
cat src/mcp/tools/api/issues/find/find-issues.schema.ts
```

2. Убрать `.skip` и обновить тест:
```typescript
it('должен принять валидные параметры', async () => {
  // Arrange: используй РЕАЛЬНО поддерживаемые параметры
  const validParams = {
    query: 'queue: TEST',
    fields: ['key', 'summary', 'status'],
    // Добавить другие поддерживаемые параметры из схемы
  };

  mockServer.mockFindIssuesSuccess(['TEST-1']);

  // Act
  const result = await client.callTool(
    'fractalizer_mcp_yandex_tracker_find_issues',
    validParams
  );

  // Assert
  expect(result.isError).toBeUndefined();
  const response = JSON.parse(result.content[0]!.text);
  expect(response.data).toHaveLength(1);
});
```

**Чек-лист:**
- [ ] Определить сценарий (A, B или C)
- [ ] Реализовать или удалить тест 1 (пагинация)
- [ ] Реализовать или удалить тест 2 (валидные параметры)
- [ ] Запустить integration тесты
- [ ] Обновить unit тесты если добавлена новая функциональность

---

### Шаг 3: Проверка других skip-тестов (30 мин)

**Задача:** Убедиться что нет других `.skip` или `.todo` в проекте

**Команды:**
```bash
# Поиск .skip
grep -r "it.skip\|describe.skip" tests/

# Поиск .todo
grep -r "it.todo\|describe.todo" tests/

# Поиск TODO комментариев в тестах
grep -r "TODO" tests/ | grep -v node_modules
```

**Если найдены другие:**
- Повторить Шаг 2 для каждого
- Документировать причины
- Создать issues если требуется будущая реализация

**Чек-лист:**
- [ ] Поиск всех .skip в проекте
- [ ] Поиск всех .todo в проекте
- [ ] Поиск TODO комментариев
- [ ] Обработать все найденные случаи

---

### Шаг 4: Документация решений (30 мин)

**Задача:** Создать лог принятых решений

**Файл:** `.test-improvement-plan/skip-tests-decisions.md`

**Содержание:**
```markdown
# Решения по пропущенным тестам

## Дата: 2025-11-16

### 1. Пагинация в find-issues

**Статус:** [Реализовано / Удалено]

**Причина пропуска:**
- API не поддерживает пагинацию
- ИЛИ: Не было времени реализовать
- ИЛИ: Требуется рефакторинг

**Решение:**
- Удалено: API не поддерживает, добавлен комментарий
- ИЛИ: Реализовано: добавлены параметры perPage/page

**Ссылки:**
- Commit: [hash]
- API Docs: yandex_tracker_client/docs/find_issues.md

---

### 2. Валидные параметры find-issues

**Статус:** [Реализовано / Удалено]

**Причина пропуска:**
...

**Решение:**
...
```

**Чек-лист:**
- [ ] Создать skip-tests-decisions.md
- [ ] Документировать каждое решение
- [ ] Добавить ссылки на commits/docs

---

## ✅ Критерии завершения

### Must Have
- [x] Все `.skip` тесты реализованы или удалены с комментариями
- [x] `npm run test:integration` проходит без skip
- [x] Решения документированы

### Should Have
- [x] 0 `.skip` тестов во всем проекте
- [x] 0 `.todo` тестов
- [x] Все TODO в тестах обработаны

### Nice to Have
- [ ] Созданы issues для будущей реализации
- [ ] Обновлена документация tests/README.md

---

## 🚨 Возможные проблемы

### Проблема 1: API не поддерживает функцию

**Решение:**
- Удалить тест
- Добавить подробный комментарий ПОЧЕМУ
- Создать issue "Feature request" если нужно

### Проблема 2: Требуется большой рефакторинг

**Решение:**
- Оценить сложность (если >1 день)
- Удалить тест с комментарием
- Создать issue для будущей реализации
- Добавить в backlog

---

## 📝 Шаблон для PR

```markdown
# Закрытие пропущенных тестов

## Изменения
- ✅ Реализован тест пагинации в find-issues
- ✅ Реализован тест валидных параметров
- ✅ Проверены все skip/todo в проекте

ИЛИ

- ✅ Удален тест пагинации (API не поддерживает)
- ✅ Реализован тест валидных параметров
- ✅ Добавлены комментарии с причинами

## Метрики
| Метрика | До | После |
|---------|-----|-------|
| Skip тестов | 2 | 0 |
| TODO тестов | 0 | 0 |

## Проверка
- [x] `npm run test:integration` проходит
- [x] Нет .skip в проекте
- [x] Решения документированы

🤖 Generated with Claude Code
Co-Authored-By: Claude <noreply@anthropic.com>
```

---

**Следующий этап:** [04-important-integration.md](./04-important-integration.md)
