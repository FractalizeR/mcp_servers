# ⚠️ Риски и стратегии митигации

---

## Риск 1: Циклические зависимости между пакетами

**Вероятность:** Средняя
**Влияние:** Высокое (блокирует сборку)

### Описание
При разделении кода могут возникнуть циклические зависимости:
- `core` импортирует `search`
- `search` импортирует `core`
- Невозможно собрать ни один из пакетов

### Признаки
```
Error: Circular dependency detected:
@mcp-framework/core -> @mcp-framework/search -> @mcp-framework/core
```

### Митигация

**Превентивные меры:**
1. ✅ Чёткая иерархия зависимостей (см. OVERVIEW.md граф)
2. ✅ `infrastructure` не зависит ни от кого
3. ✅ `core` зависит только от infrastructure (через npm deps)
4. ✅ `search` зависит только от `core`
5. ✅ `cli` независим
6. ✅ `yandex-tracker` зависит от всех (но никто не зависит от него)

**Проверка:**
```bash
npm run depcruise
# Должно пройти без ошибок о циклах
```

**Исправление при обнаружении:**
1. Найти, какой модуль создаёт цикл
2. Вынести общую функциональность в более низкий уровень (infrastructure или core)
3. Использовать dependency injection вместо прямого импорта
4. Рефакторить интерфейсы для разрыва цикла

---

## Риск 2: Сломанные импорты после миграции

**Вероятность:** Очень высокая
**Влияние:** Высокое (не компилируется)

### Описание
При массовом обновлении импортов легко пропустить файлы:
- Забыть обновить импорт в тесте
- Не заменить вложенный путь (`@mcp/tools/base/utils/something`)
- Оставить старый alias в comments или strings

### Признаки
```
Error: Cannot find module '@infrastructure/http/client'
Error: Cannot find module '@mcp/tools/base'
```

### Митигация

**Превентивные меры:**
1. ✅ Поэтапная миграция (по одному пакету)
2. ✅ После каждого этапа: `npm run typecheck`
3. ✅ Использовать find/sed для массовой замены
4. ✅ IDE mass rename где возможно

**Инструменты проверки:**
```bash
# Найти все старые импорты
grep -r "@infrastructure" packages/*/src
grep -r "@mcp/tools/base" packages/*/src
grep -r "@mcp/utils" packages/*/src
grep -r "@types'" packages/*/src  # С кавычкой!

# Не должно найти ничего (или только в yandex-tracker для локальных алиасов)
```

**Исправление:**
1. Использовать вывод grep для поиска файлов
2. Заменить вручную или через sed
3. Запустить typecheck после каждой замены

---

## Риск 3: Потеря функциональности при перемещении кода

**Вероятность:** Средняя
**Влияние:** Среднее (missing features)

### Описание
При копировании файлов можно:
- Забыть скопировать файл
- Скопировать не все файлы из директории
- Потерять вспомогательные утилиты

### Признаки
```
Error: Cannot find module './some-helper'
Missing function in production
```

### Митигация

**Превентивные меры:**
1. ✅ Чек-лист всех директорий/файлов
2. ✅ Сравнение количества файлов до/после
3. ✅ Полный прогон тестов после миграции

**Проверка:**
```bash
# До миграции
find src -name "*.ts" | wc -l  # 160 файлов

# После миграции
find packages -name "*.ts" | grep -v node_modules | wc -l
# Должно быть ~160 (±5 для новых index.ts)

# Проверить что ничего не потерялось
diff <(find src -name "*.ts" | sort) <(find packages -name "*.ts" | grep -v node_modules | sed 's|packages/[^/]*/src/||' | sort)
```

**Исправление:**
1. Найти потерянный файл в src/
2. Скопировать в правильный пакет
3. Обновить импорты

---

## Риск 4: Проблемы с TypeScript path aliases

**Вероятность:** Низкая
**Влияние:** Среднее (усложняет разработку)

### Описание
npm workspaces и TypeScript path aliases могут конфликтовать:
- `workspace:*` не разрешается в IDE
- Path aliases не работают в runtime
- tsc-alias не обрабатывает все случаи

### Признаки
```
Error [ERR_MODULE_NOT_FOUND]: Cannot find package '@mcp-framework/core'
IDE не показывает autocomplete для workspace пакетов
```

### Митигация

**Превентивные меры:**
1. ✅ Использовать `workspace:*` для внутренних зависимостей
2. ✅ Минимизировать path aliases (только для локальных путей внутри пакета)
3. ✅ Использовать `tsc-alias` для разрешения в build time
4. ✅ Тестировать импорты после сборки

**Конфигурация:**
```json
// tsconfig.json каждого пакета
{
  "compilerOptions": {
    "paths": {
      // ТОЛЬКО для внутренних локальных алиасов
      "@utils/*": ["./src/utils/*"]
    }
  },
  "references": [
    // Ссылки на другие пакеты
    { "path": "../core" }
  ]
}
```

**Проверка:**
```bash
# После сборки проверить что импорты разрешены
node packages/yandex-tracker/dist/index.js --help
# Не должно быть ошибок импорта
```

**Исправление:**
1. Использовать относительные пути вместо aliases
2. Добавить недостающий пакет в dependencies
3. Проверить что tsc-alias настроен правильно

---

## Риск 5: Breaking changes для пользователей

**Вероятность:** Высокая (если есть пользователи)
**Влияние:** Высокое (недовольство пользователей)

### Описание
Текущие пользователи mcp-server-yandex-tracker столкнутся с:
- Изменением структуры пакета
- Новыми импортами (если расширяют код)
- Необходимостью обновления конфигурации

### Признаки
- GitHub issues от пользователей
- Низкий adoption rate v2.0

### Митигация

**Превентивные меры:**
1. ✅ Семантическое версионирование (v1.x → v2.0.0)
2. ✅ Подробный MIGRATION.md гайд
3. ✅ Сохранение обратной совместимости где возможно
4. ✅ Переходный период (поддержка v1.x 3-6 месяцев)

**План коммуникации:**
1. GitHub Release с детальным changelog
2. Примеры миграции в README
3. Deprecation warnings в v1.x (если возможно)
4. Announcement в community каналах

**MIGRATION.md должен содержать:**
- Список breaking changes
- До/после примеры кода
- Автоматизированные скрипты миграции (если возможно)
- FAQ с частыми проблемами

---

## Риск 6: Большие размеры пакетов при публикации

**Вероятность:** Средняя
**Влияние:** Низкое (медленная установка)

### Описание
Если не настроить `.npmignore`, пакеты могут включать:
- Исходные файлы (`src/`)
- Тесты
- Config файлы
- Раздутый `node_modules` из-за dev deps

### Признаки
```bash
npm pack --dry-run
# Показывает большой размер (>1MB для инфраструктуры)
```

### Митигация

**Превентивные меры:**
1. ✅ `.npmignore` для каждого пакета
2. ✅ Проверка `npm pack --dry-run` перед публикацией
3. ✅ `files` поле в package.json (whitelist подход)

**Правильный .npmignore:**
```
src/
tests/
tsconfig.json
vitest.config.ts
*.test.ts
*.spec.ts
.eslintrc
.prettierrc
node_modules/
coverage/
```

**Проверка:**
```bash
cd packages/infrastructure
npm pack --dry-run
# Должно показать только dist/, package.json, README.md, LICENSE

# Проверить размер
ls -lh *.tgz  # Должно быть <500KB для большинства пакетов
```

---

## Риск 7: Конфликты версий зависимостей

**Вероятность:** Низкая
**Влияние:** Среднее (hoisting проблемы)

### Описание
Разные пакеты могут требовать разные версии одной зависимости:
- `core` требует `pino@10.1.0`
- `infrastructure` требует `pino@10.2.0`
- npm workspaces не может разрешить

### Признаки
```
npm ERR! peer dependency conflict
npm WARN ERESOLVE overriding peer dependency
```

### Митигация

**Превентивные меры:**
1. ✅ Использовать одинаковые версии зависимостей везде
2. ✅ Вынести общие зависимости в корень (devDependencies)
3. ✅ Использовать `^` для минорных версий (flexibilty)

**Проверка:**
```bash
# Проверить duplicate dependencies
npm ls --all | grep -i "duplicate"

# Проверить версии общих зависимостей
grep -r "\"pino\":" packages/*/package.json
# Все должны иметь одну версию
```

**Исправление:**
1. Выбрать единую версию (обычно latest)
2. Обновить во всех package.json
3. `npm install` для обновления lockfile

---

## Риск 8: Проблемы с Git history

**Вероятность:** Низкая
**Влияние:** Низкое (неудобство)

### Описание
При перемещении файлов Git может не распознать их как renamed:
- Потеря git history для файла
- Сложность с git blame

### Признаки
```bash
git log --follow packages/core/src/tools/base/base-tool.ts
# Не показывает историю из src/mcp/tools/base/base-tool.ts
```

### Митигация

**Превентивные меры:**
1. ⚠️ Использовать `git mv` вместо `cp` (но сложно для структурных изменений)
2. ✅ Документировать mapping старых путей → новых в MIGRATION.md

**Для сохранения history (если критично):**
```bash
# Вместо cp использовать git mv (но требует больше работы)
git mv src/infrastructure packages/infrastructure/src
git commit -m "move: infrastructure to packages/"

# Повторить для каждой директории
```

**Альтернатива:**
- Принять потерю истории (для больших рефакторингов это нормально)
- Создать mapping файл `FILE_MAPPING.md`:
  ```
  packages/core/src/tools/base/base-tool.ts ← src/mcp/tools/base/base-tool.ts
  packages/infrastructure/src/http/client.ts ← src/infrastructure/http/client/http-client.ts
  ```

---

## Риск 9: Проблемы с CI/CD при публикации

**Вероятность:** Средняя
**Влияние:** Высокое (не можем релизить)

### Описание
GitHub Actions может столкнуться с:
- Отсутствие NPM_TOKEN secret
- Ошибки авторизации
- Проблемы с build в CI environment

### Признаки
```
Error: npm publish failed
Error: 401 Unauthorized
```

### Митигация

**Превентивные меры:**
1. ✅ Тестировать CI локально (act)
2. ✅ Настроить NPM_TOKEN в GitHub secrets
3. ✅ Dry-run публикации перед автоматизацией

**Настройка:**
```bash
# 1. Создать npm access token (на npmjs.com)
# Settings → Access Tokens → Generate New Token → Automation

# 2. Добавить в GitHub secrets
# Repository → Settings → Secrets → Actions → New secret
# Name: NPM_TOKEN
# Value: [ваш токен]

# 3. Тестировать локально
npm publish --dry-run --workspace=@mcp-framework/core
```

**Проверка:**
```bash
# Симуляция CI build
npm ci
npm run build
npm run test
npm publish --dry-run --workspaces
```

---

## 📊 Матрица рисков

| Риск | Вероятность | Влияние | Приоритет | Статус |
|------|-------------|---------|-----------|--------|
| Циклические зависимости | Средняя | Высокое | 🔴 Высокий | Готов |
| Сломанные импорты | Очень высокая | Высокое | 🔴 Высокий | Готов |
| Потеря функциональности | Средняя | Среднее | 🟡 Средний | Готов |
| Path aliases | Низкая | Среднее | 🟡 Средний | Готов |
| Breaking changes | Высокая | Высокое | 🔴 Высокий | Готов |
| Большие пакеты | Средняя | Низкое | 🟢 Низкий | Готов |
| Конфликты версий | Низкая | Среднее | 🟡 Средний | Готов |
| Git history | Низкая | Низкое | 🟢 Низкий | Принято |
| CI/CD проблемы | Средняя | Высокое | 🔴 Высокий | Готов |

---

## 🆘 План действий при критической проблеме

Если что-то пошло не так:

### 1. Остановиться и оценить ситуацию
```bash
# Не паниковать!
# Сохранить текущее состояние
git status
git stash  # Если есть незакоммиченные изменения
```

### 2. Откатиться на предыдущий рабочий commit
```bash
git log --oneline  # Найти последний рабочий commit
git checkout <commit-hash>  # Временно вернуться назад
```

### 3. Проанализировать проблему
```bash
# Сравнить с рабочим состоянием
git diff <working-commit> <broken-commit>

# Найти проблемный файл/изменение
```

### 4. Исправить и продолжить
```bash
git checkout refactor/split-into-packages  # Вернуться на ветку
# Исправить проблему
git add .
git commit -m "fix: исправление проблемы с [описание]"
```

### 5. Если всё очень плохо — начать фазу заново
```bash
# Откатить всю фазу
git reset --hard <commit-before-phase>
# Повторить фазу с учётом ошибок
```

---

**Помни:** Все риски управляемы если следовать плану и проверять каждый шаг!
