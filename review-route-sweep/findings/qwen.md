# Находки — reviewer: qwen

**Важное замечание о материале (проверено фасилитатором).** `changes.diff`/`diff_files.txt`
сняты в 23:05, находки `claude.md`/`codex.md` записаны в 23:16/23:24, а коммит
`b1198193` ("fix(tracker)!: находки ревью — дыра в рубеже, issueTypes у правки очереди")
лёг в 23:29 — **после** снятия материала, но **до** запуска Qwen, и он уже в диапазоне
`fe3bcbe4..HEAD`. Qwen работал в текущей директории (не только по статичному диффу) и сам
заметил расхождение снимка с рабочим деревом. Дополнительно в рабочем дереве есть
**незакоммиченные** правки `update-component.operation.ts` (числовая проверка `version`
перед PATCH) и `sweep-doc-routes.ts` (сужение поиска ключей до раздела запроса). Это
означает: часть находок Qwen (qwen-01, qwen-06) описывает дефект, реально бывший в
снятом диффе, но **уже устранённый** в коммите `b1198193` или незакоммиченной правке —
я это подтвердил чтением текущего кода и `git show`/`git diff`, отмечено в
`verification_note` каждой находки. qwen-05 указывает на реальный, **всё ещё не
устранённый** остаток этого же фикса.

### qwen-01

- **reviewer**: qwen
- **severity**: HIGH
- **kind**: pattern
- **domain**: reliability
- **title**: `update_queue` в снятом диапазоне рекламировал `issueTypes`, который никогда не доставлялся в API — D9-класс «молчаливого игнора», пропущенный всеми тремя пакетами
- **mechanism**: Пакет починки убрал `issueTypes` из `create_queue` (живая проба: `400 issueTypes: Incorrect data format`), а семейство очереди-правки не тронул. В снятом диапазоне `update-queue.schema.ts` объявлял опциональный `issueTypes`, `update-queue.tool.ts` его не деструктурировал и не клал в `updates` — значит вызов «успешен», а типы задач очереди молча не меняются. Маршрут `PATCH /v3/queues/{id}` не имеет страницы в справочнике, поэтому сверка ключей тела для него структурно не срабатывает (см. qwen-02) — расхождение не поймал и инструмент сверки.
- **trigger**: воспроизводится в нормальной работе — агент передаёт единственный объявленный параметр, вызов завершается «успехом» без эффекта (на момент снятого диффа).
- **in_scope**: частично — якорь в диффе (`outgoing-requests.md`, `organization-rules.ts`, `3_OPEN_ITEMS.md`); сами `update-queue.schema.ts`/`update-queue.tool.ts` в `diff_files.txt` отсутствуют.
- **anchor**: `packages/servers/yandex-tracker/outgoing-requests.md` (строка `fr_yandex_tracker_update_queue`, состояние на момент снятия диффа); `packages/servers/yandex-tracker/src/tools/api/queues/update-queue.tool.ts:31-47` (состояние на момент снятия диффа)
- **evidence**:
  ```
  | `fr_yandex_tracker_update_queue` | … | PATCH | `/v3/queues/probe_queueId` | name, lead, defaultType, defaultPriority, description, issueTypes |
  ```
- **verification**: confirmed
- **verification_note**: ФАСИЛИТАТОР проверил напрямую: дефект был реален для снятого диапазона (`git show b1198193` показывает, что до этого коммита `update-queue.schema.ts` содержал `issueTypes: z.array(z.string()).optional()`, а `update-queue.tool.ts` его не читал). **Коммит `b1198193` (23:29, уже на HEAD) устранил это** — убрал параметр из схемы/tool/DTO/тестов, коммит-месседж прямо это называет находкой ревью. На момент запуска Qwen (после 23:29) в текущей рабочей копии дефекта уже нет — Qwen сам это заметил и указал в преамбуле ответа. Открытый остаток — qwen-05.
- **fix_direction**: Уже исправлено в `b1198193`; для недокументированных пишущих маршрутов (нет страницы в справочнике) завести явный чек-лист живых проб ключей тела, чтобы «нет страницы» не значило «ключи никто не сверял».

### qwen-02

- **reviewer**: qwen
- **severity**: MEDIUM
- **kind**: point
- **domain**: reliability
- **title**: Сверка маршрутов полностью пропускает ключи тела у маршрутов без страницы справочника
- **mechanism**: В `judge()` (`sweep-doc-routes.ts`) ключи тела сверяются со страницей только при `hitPages.length > 0`; при ненайденном маршруте `unknownBodyKeys` принудительно пуст (`empty`). На таких маршрутах (пример — `PATCH /v3/queues/{id}`, реализовалось в qwen-01) класс дефекта «ключ принят и молча проигнорирован» целиком вне зоны действия инструмента: отчёт помечает маршрут как «нет в документации», но состав ключей тела не печатается.
- **trigger**: воспроизводится в нормальной работе инструмента сверки — любой вызов с недокументированным маршрутом проходит проверку ключей тела молча.
- **in_scope**: да
- **anchor**: `packages/servers/yandex-tracker/scripts/sweep-doc-routes.ts:322-324` (текущие строки; в снятом диффе — 302-304, номер сместился из-за незакоммиченной правки `requestSection`)
- **evidence**:
  ```ts
  unknownBodyKeys:
    hitPages.length > 0 ? absent.filter((key) => verified?.has(key) !== true) : empty,
  settledBodyKeys: hitPages.length > 0 ? absent.filter((key) => verified?.has(key)) : empty,
  ```
- **verification**: confirmed
- **verification_note**: ФАСИЛИТАТОР перечитал текущий `sweep-doc-routes.ts` (строки 316-326 на момент проверки) — логика `hitPages.length > 0 ? ... : empty` присутствует без изменений; незакоммиченная правка добавила `requestSection()` (сужение поиска до раздела запроса), но условие "страницы нет → ключи не сверяются" не тронула. Находка НЕ устранена.
- **fix_direction**: Для маршрутов без страницы печатать в отчёте заявленные ключи тела с пометкой «не сверено — страницы нет», дополнить список «Чего не находит» в документации скрипта.

### qwen-03

- **reviewer**: qwen
- **severity**: LOW
- **kind**: point
- **domain**: tests
- **title**: `samePath` в сверке маскирует недокументированный маршрут под документированный при совпадении арности
- **mechanism**: Сегмент пути считается совпавшим, если наш или документный сегмент похож на переменный (`{…}`, `<…>`, `probe_*`, числа и т.п.). Недокументированный маршрут с тем же методом и числом сегментов, что у документированного параметризованного, пройдёт как «страница есть». В текущем наборе действующих коллизий нет — риск на будущее.
- **trigger**: недостижим в текущем наборе инструментов; воспроизводится при добавлении маршрута, арного существующему документированному.
- **in_scope**: да
- **anchor**: `packages/servers/yandex-tracker/scripts/sweep-doc-routes.ts:255-268`
- **evidence**:
  ```ts
  const VARIABLE_SEGMENT = /^(\{.*\}|<.*>|probe_.*|[A-Z]+-\d+|\d+|__.*|.*\*\*\*.*)$/;
  …
  if (VARIABLE_SEGMENT.test(segment) || VARIABLE_SEGMENT.test(other)) return true;
  ```
- **verification**: confirmed
- **verification_note**: Логика прочитана фасилитатором в текущем файле — совпадает с цитатой Qwen. Отсутствие текущих коллизий не перепроверялось повторно (полагаюсь на разбор Qwen по 86 вызовам из `outgoing-requests.md`).
- **fix_direction**: Совпадение с переменным сегментом документа засчитывать только при совпадении литеральных сегментов; для подозрительных пар печатать пометку для ручной проверки.

### qwen-04

- **reviewer**: qwen
- **severity**: LOW
- **kind**: pattern
- **domain**: style
- **title**: Остаточные упоминания удалённых маршрутов и семейства после пакетов удаления
- **mechanism**: Пакеты удаления вычистили код, тесты, рубеж и основные доки, но в соседних файлах остались утверждения, противоречащие основанию удаления: entity-doc-комментарии описывают `DELETE /v3/components/{id}` как часть контракта; мёртвый тег курсора `projects: 'proj'`; ссылка на удалённый класс `GetProjectOperation`; расхождение счётчика «4»/«(3 инструмента)» у Components в README; «три провайдера» при двух.
- **trigger**: недостижим — документация и мёртвая конфигурация, на корректность не влияет.
- **in_scope**: смешанно — `README.md`, `component.entity.ts`, `component.output.ts` в диффе; `cursor-codec.util.ts`, `get-entity.operation.ts`, `json-resource-contents.util.ts`, `tracker-paginator.util.ts` — нет.
- **anchor**: `src/tracker_api/utils/cursor-codec.util.ts:62`; `README.md:188`; `src/tracker_api/entities/component.entity.ts:2-9`; `src/tracker_api/dto/component/component.output.ts:4-9`; `src/tracker_api/api_operations/entity-api/get-entity.operation.ts:10`
- **evidence**:
  ```ts
  projects: 'proj', // cursor-codec.util.ts — тег без потребителя после удаления get_projects
  ```
- **verification**: confirmed
- **verification_note**: Принято по разбору Qwen без построчной перепроверки каждого из пяти якорей фасилитатором (низкая severity, стиль/доки — не требует security-уровня верификации). Направление правдоподобно и согласуется с характером пакетов удаления, описанных в BRIEF.md.
- **fix_direction**: Пройтись по инвентарю ссылок удаления точечно: мёртвый тег курсора убрать, счётчики свести, док-комментарии привести в соответствие с решением пакетов удаления.

### qwen-05

- **reviewer**: qwen
- **severity**: LOW
- **kind**: point
- **domain**: style
- **title**: `issueTypes` остался в `redactionAllowlist` метаданных `update_queue` после фикса
- **mechanism**: Фикс `update_queue` (коммит `b1198193`) убрал параметр из схемы, инструмента и тестов, но `redactionAllowlist` в метаданных инструмента его всё ещё перечисляет.
- **trigger**: недостижим — редакция несуществующего поля невозможна; только рассинхрон документации инструмента.
- **in_scope**: нет — файл вне `diff_files.txt`/`changes.diff` (остаток фикса, пришедшего после снятия материала).
- **anchor**: `packages/servers/yandex-tracker/src/tools/api/queues/update-queue.metadata.ts:28`
- **evidence**:
  ```ts
  redactionAllowlist: ['queueId', 'lead', 'defaultType', 'defaultPriority', 'issueTypes', 'fields'],
  ```
- **verification**: confirmed
- **verification_note**: ФАСИЛИТАТОР перепроверил `grep -n issueTypes` по каталогу `queues/` — строка присутствует буквально как процитировано, схема (`update-queue.schema.ts`) и инструмент (`update-queue.tool.ts`) параметр уже не содержат (коммит `b1198193`). Расхождение реально и не устранено.
- **fix_direction**: Убрать `issueTypes` из `redactionAllowlist`; при регенерации `outgoing-requests.md` строка `update_queue` обновится сама.

### qwen-06

- **reviewer**: qwen
- **severity**: LOW
- **kind**: point
- **domain**: reliability
- **title**: `readCurrentVersion` не проверяла числовое `version` перед подстановкой в URL (на момент снятого диффа)
- **mechanism**: Операция читала версию прямым `GET /v3/components/{id}` и интерполировала `component.version` в `?version=` без проверки типа — при отсутствии поля в ответе запрос уходил бы с `?version=undefined` и получал неясный 400.
- **trigger**: недостижим в нормальной работе — тип объявляет `version` обязательным, живая проба отвечает полным компонентом.
- **in_scope**: да
- **anchor**: `packages/servers/yandex-tracker/src/tracker_api/api_operations/component/update-component.operation.ts:79-81`
- **evidence**:
  ```ts
  // до правки:
  const component = await this.httpClient.get<ComponentOutput>(`/v3/components/${componentId}`);
  return component.version;
  ```
- **verification**: confirmed
- **verification_note**: ФАСИЛИТАТОР прочитал текущий файл и `git diff` (незакоммиченная правка): проверка `typeof version !== 'number'` с явным `throw new Error(...)` **уже добавлена** в рабочем дереве — ровно то, что рекомендует `fix_direction` Qwen. Находка описывает состояние снятого диффа корректно, но на момент составления этого отчёта уже устранена (не закоммичена).
- **fix_direction**: Уже исправлено (незакоммиченная правка `update-component.operation.ts:79-88`) — зафиксировать коммитом.

# Coverage

- **Швы между пакетами** (`tool-definitions.ts`/`operation-definitions.ts`, фасад, DI-контейнеры, баррелы `dto/index.ts`/`api_operations/index.ts`/`entities/index.ts`, `mock-server`, счётчик 92→85, легаси-моки 33→30) — проверены Qwen попарно, хвостов удалённых символов не найдено. Не перепроверено фасилитатором построчно.
- **Снятие правила `/v3/projects` из рубежа живого прогона** — Qwen подтвердил fail-closed на трёх уровнях (`RAW_API_METHODS = ['GET']` в константе фреймворка, `z.literal('GET')` в схеме, switch в операции); дыры не открыто, рубеж стал строже. Не перепроверено фасилитатором отдельно (принято по разбору Qwen).
- **Переписанные тесты рубежа (12 файлов)** — Qwen подтвердил сохранность механик (fail-closed, дуальная адресация, ссылки на людей в теле, белые списки ключей, удержание префикса). Не перепроверено фасилитатором построчно.
- **Чтение версии перед PATCH** — фасилитатор лично подтвердил текущее состояние `update-component.operation.ts`: лишний GET осознан и закомментирован, идёт мимо кеша (`httpClient.get` напрямую, не `withCache`), гонка закрывается версией в query (оптимистичная блокировка), проверка на `undefined`-версию уже добавлена (см. qwen-06).
- **Скрипт сверки `sweep-doc-routes.ts`** — фасилитатор лично подтвердил актуальность qwen-02 (гейт `hitPages.length > 0` не устранён) и правдоподобность qwen-03 (regex `VARIABLE_SEGMENT`). Остальные механики скрипта (ретраи, кеш страниц, `MANUAL_ROUTES`, `SAMPLE_ARTEFACT_KEYS`, `LIVE_VERIFIED_KEYS`) — по разбору Qwen, не перепроверены построчно.
- **Починенные контракты** (`update_component`, `update_board`, `create_queue`) — по разбору Qwen подтверждена согласованность схема→инструмент→операция→DTO→белые списки рубежа. `update_queue`/`issueTypes` фасилитатор перепроверил лично и нашёл остаток (qwen-05).
- **Удаление промпта `project-summary` и ресурса `tracker://project/{id}`** — принято по разбору Qwen: перенос на Entity API невозможен честно (Entity API не гарантирует нужные поля), висячих ссылок не найдено. Не перепроверено фасилитатором.

**Дополнительно найдено фасилитатором вне рамок ответа Qwen (контекст материала, не отдельная находка):**
Материал (`changes.diff`/`diff_files.txt`) снят в 23:05 и статичен; текущее рабочее дерево уже
на коммит `b1198193` (23:29) впереди снимка и содержит незакоммиченные правки поверх него.
Три из шести находок Qwen (qwen-01, qwen-05, qwen-06) касаются именно этого шва
«снимок vs. текущее состояние» — подробности в каждой находке выше.

**Не покрыто (ни Qwen, ни фасилитатором в рамках этой сессии):**
- Ничего не запускалось (read-only задание) — `npm run validate`/тесты/сборка не прогонялись.
- Содержимое `COVERAGE_MATRIX.md` и документов `.agentic-planning/` сверх цитируемых Qwen выдержек.
- Живые пробы (отчёты F-серии) приняты как зафиксированные, независимо не воспроизводились.
- ~30 файлов диапазона (массовые удаления проектных фикстур/тестов/DTO/операций) — просмотрены Qwen только структурно (баррели + поиск висячих ссылок), фасилитатором не перепроверялись.
- Пакеты `yandex-wiki` и `dev-client` вне инвентаря — не рассматривались.
- Полная построчная верификация qwen-03, qwen-04 и секций coverage, помеченных «по разбору Qwen» — фасилитатор доверился прочитанным фрагментам без независимого grep/чтения каждого якоря целиком.

# Отклонённые

| id | title | причина |
|---|---|---|
| ref-1 | Снятие правила `/v3/projects` открыло дыру в рубеже | опровергнуто Qwen: `raw_api_request` заперт на GET тремя уровнями, мутации к пути отклоняются fail-closed остатком |
| ref-2 | `?version=` в пути сломает матчинг правил рубежа | опровергнуто Qwen: `canonicalRequestPath` отсекает query-строку до сопоставления |
| ref-3 | Чтение версии перед PATCH может взять устаревшую версию из кеша | опровергнуто Qwen: `readCurrentVersion` использует прямой `httpClient.get` без `withCache` |
| ref-4 | Переписанные тесты рубежа выхолощены | опровергнуто Qwen: механики сохранены эквивалентными заменами |
| ref-5 | `BOARD_KEYS` после фикса `update_board` всё ещё пропускает `version` | опровергнуто Qwen: `version` в этом файле принадлежит только `SPRINT_KEYS` |
| ref-6 | Новая форма фильтра `update_board` позволяет привязать доску прогона к чужой очереди мимо рубежа (`filter.queue` вместо `body.queue`) | опровергнуто Qwen **для текущего состояния**; ФАСИЛИТАТОР уточняет: это была реальная дыра, устранённая коммитом `b1198193` (23:29) — до этого коммита `boardEditViolation` в `organization-rules.ts` действительно проверял только `body.queue` и пропускал `filter.queue`, что коммит-месседж прямо называет находкой ревью («доску прогона можно было молча перевесить на боевую очередь»). Отклонение Qwen корректно описывает СЕЙЧАС, но не описывает риск, бывший в снятом диффе |
