# Этап 2.1: Анализ Framework пакетов (PARALLEL)

**Время:** 2-3 часа
**Зависимости:** Этапы 1.1, 1.2 (базовые метрики и граф зависимостей)
**Выполняется:** Параллельно с 2.2, 2.3, 2.4

---

## 🎯 Цель

Детально проанализировать framework пакеты (infrastructure, core, search) на соответствие SOLID и best practices для переиспользуемых библиотек.

---

## 📋 Задачи

### ✅ 1. Infrastructure Package анализ

**Файлы:**
- `packages/framework/infrastructure/src/`

**Что проверить:**

#### 1.1 Single Responsibility

- [ ] HTTP client - одна ответственность (HTTP requests)
- [ ] Cache - одна ответственность (кэширование)
- [ ] Logger - одна ответственность (логирование)
- [ ] Async utilities - одна ответственность (параллелизм)

**Команды:**
```bash
# Структура infrastructure
tree packages/framework/infrastructure/src -L 2

# Размеры файлов (не должно быть >400 строк)
find packages/framework/infrastructure/src -name "*.ts" -exec wc -l {} \; | \
  awk '$1 > 400 {print}'
```

#### 1.2 Zero dependencies on domain

- [ ] Нет импортов из core/search/yandex-tracker
- [ ] Нет упоминаний доменных понятий (Yandex, Tracker, Issue, etc)

```bash
# Проверка утечки домена
grep -ri "yandex\|tracker\|issue\|queue" \
  packages/framework/infrastructure/src --include="*.ts" || echo "OK"
```

#### 1.3 Open/Closed Principle

- [ ] Интерфейсы для всех основных компонентов
- [ ] Легко расширяется без изменений (через наследование/композицию)

**Проверить вручную:**
- Есть ли интерфейсы для HttpClient, Cache, Logger?
- Можно ли заменить реализацию без изменения кода?

#### 1.4 Type Safety

- [ ] Отсутствие `any` (кроме обоснованных случаев)
- [ ] Строгие generic типы
- [ ] Правильные Promise типы

```bash
# Поиск any (исключая комментарии)
grep -r ": any\|<any>" packages/framework/infrastructure/src --include="*.ts" | \
  grep -v "^[[:space:]]*\*\|^[[:space:]]*//"
```

**Сохранить в:** `results/2.1_infrastructure_analysis.md`

---

### ✅ 2. Core Package анализ

**Файлы:**
- `packages/framework/core/src/`

**Что проверить:**

#### 2.1 BaseTool architecture

- [ ] Четкая абстракция для всех tools
- [ ] Правильное использование шаблонного метода (Template Method pattern)
- [ ] Разделение concerns (validation, execution, transformation)

**Файлы для анализа:**
- `src/tools/base/base-tool.ts`
- `src/tools/base/base-definition.ts`

**Вопросы:**
- Насколько легко создать новый tool?
- Сколько кода нужно дублировать?
- Есть ли hooks для расширения?

#### 2.2 Type System

- [ ] Правильное использование Zod schemas
- [ ] Type safety для input/output
- [ ] Правильная генерация MCP definitions из schemas

**Файлы для анализа:**
- `src/types/`
- `src/tools/base/definition-generator.ts` (если есть)

#### 2.3 Dependency Inversion

- [ ] Зависимость от абстракций (interfaces), не конкретных классов
- [ ] Правильная инверсия контроля

```bash
# Поиск прямых new Class() (должно быть через DI)
grep -r "new [A-Z]" packages/framework/core/src --include="*.ts" | \
  grep -v "new Error\|new Promise\|new Map\|new Set\|new Date" | head -20
```

#### 2.4 Cohesion & Coupling

- [ ] Высокая cohesion (связанные вещи вместе)
- [ ] Низкий coupling (минимум зависимостей между модулями)

**Метрика:** Посчитать среднее количество импортов на файл

```bash
# Средние импорты на файл
find packages/framework/core/src -name "*.ts" -exec grep -c "^import" {} \; | \
  awk '{sum+=$1; count++} END {print "Average imports per file:", sum/count}'
```

**Сохранить в:** `results/2.1_core_analysis.md`

---

### ✅ 3. Search Package анализ

**Файлы:**
- `packages/framework/search/src/`

**Что проверить:**

#### 3.1 Search Engine architecture

- [ ] Правильная абстракция для поисковых движков
- [ ] Возможность добавления новых движков без изменения кода (OCP)
- [ ] Четкое разделение indexing и searching

**Файлы для анализа:**
- `src/search/` (основная логика)
- `src/tools/search-tools.tool.ts`

#### 3.2 Compile-time indexing

- [ ] Правильность подхода с compile-time генерацией
- [ ] Нет runtime overhead
- [ ] Правильная регенерация при изменениях

**Вопросы:**
- Когда происходит индексация?
- Как обновляется индекс?
- Есть ли cache invalidation?

#### 3.3 Performance considerations

- [ ] Эффективность поиска (O(log n) или лучше)
- [ ] Размер индекса разумен
- [ ] Нет memory leaks

**Команды:**
```bash
# Проверить размер сгенерированного индекса
find packages/framework/search -name "*index*.json" -o -name "*search*.json" | \
  xargs ls -lh
```

#### 3.4 Integration с Core

- [ ] Правильное использование BaseTool
- [ ] Нет дублирования логики из core
- [ ] Четкая граница ответственности

**Сохранить в:** `results/2.1_search_analysis.md`

---

### ✅ 4. Cross-package patterns

**Что проверить:**

- [ ] Консистентность паттернов между пакетами
- [ ] Единый стиль кода и архитектуры
- [ ] Правильное переиспользование (DRY vs WET trade-off)

**Вопросы для анализа:**

1. **Error Handling:**
   - Единый подход к ошибкам?
   - Правильная типизация errors?
   - Логирование ошибок консистентно?

2. **Testing patterns:**
   - Единый подход к mock/stub?
   - Консистентность test helpers?
   - Правильная изоляция тестов?

3. **Configuration:**
   - Единый подход к конфигурации?
   - Правильная валидация config?
   - Environment variables vs config files?

**Команды:**
```bash
# Поиск паттернов error handling
grep -r "throw new\|Error(" packages/framework/*/src --include="*.ts" | \
  head -20

# Проверка test helpers
find packages/framework/*/tests -name "*helper*" -o -name "*util*"
```

**Сохранить в:** `results/2.1_cross_package_patterns.md`

---

### ✅ 5. API Design оценка

**Что проверить:**

- [ ] Публичное API минимально и понятно
- [ ] Правильная экспортация (index.ts)
- [ ] Нет утечки internal деталей

**Файлы для анализа:**
```bash
# Проверить, что экспортируется
cat packages/framework/infrastructure/src/index.ts
cat packages/framework/core/src/index.ts
cat packages/framework/search/src/index.ts
```

**Вопросы:**
- Легко ли понять, что делает пакет по его exports?
- Нет ли избыточных экспортов?
- Правильно ли скрыты internal детали?

**Сохранить в:** `results/2.1_api_design.md`

---

## 📊 Формат результата

Создать файл `results/2.1_framework_packages_summary.md`:

```markdown
# Framework Packages Analysis Summary

**Дата:** YYYY-MM-DD

## Infrastructure Package

### ✅ Сильные стороны
- [список]

### ⚠️ Проблемы
| Приоритет | Проблема | SOLID | Рекомендация |
|-----------|----------|-------|--------------|
| 🔴/🟡/🟢 | ...      | ...   | ...          |

### 📈 Метрики
- LoC: X
- Max file size: X строк
- Average imports: X
- Any usage: X мест

## Core Package

### ✅ Сильные стороны
- [список]

### ⚠️ Проблемы
[таблица как выше]

### 📈 Метрики
[метрики как выше]

## Search Package

### ✅ Сильные стороны
- [список]

### ⚠️ Проблемы
[таблица как выше]

### 📈 Метрики
[метрики как выше]

## Cross-Package Analysis

### Консистентность
- Error Handling: ✅/⚠️/❌
- Testing Patterns: ✅/⚠️/❌
- Configuration: ✅/⚠️/❌

### API Design
- Infrastructure: оценка 1-5
- Core: оценка 1-5
- Search: оценка 1-5

## 🎯 SOLID Compliance

| Принцип | Infrastructure | Core | Search | Комментарий |
|---------|---------------|------|--------|-------------|
| SRP     | ✅/⚠️/❌      | ...  | ...    | ...         |
| OCP     | ✅/⚠️/❌      | ...  | ...    | ...         |
| LSP     | ✅/⚠️/❌      | ...  | ...    | ...         |
| ISP     | ✅/⚠️/❌      | ...  | ...    | ...         |
| DIP     | ✅/⚠️/❌      | ...  | ...    | ...         |

## 💡 Ключевые выводы

1. **Сильные стороны framework:**
   - [список топ-3]

2. **Основные проблемы:**
   - [список топ-3]

3. **Приоритетные улучшения:**
   - [список с приоритетами]

## 📋 Рекомендации

### 🔴 Критические (блокируют переиспользование)
- [список]

### 🟡 Важные (снижают качество)
- [список]

### 🟢 Улучшения (nice to have)
- [список]
```

---

## 📋 Рекомендации и план действий

**После завершения анализа - обязательная секция!**

### ✅ 6. Формирование рекомендаций

**Для каждой найденной проблемы:**

1. **Оценка критичности:**
   - 🔴 CRITICAL - блокирует переиспользование framework
   - 🟡 IMPORTANT - снижает качество API
   - 🟢 NICE TO HAVE - улучшения

2. **Оценка усилий:**
   - LOW (<4 часов)
   - MEDIUM (1-2 дня)
   - HIGH (>2 дней)

3. **Dependency analysis:**
   - Блокирует ли эта проблема другие улучшения?
   - Зависит ли от других изменений?
   - Можно ли решить независимо?

**Формат рекомендации:**
```markdown
### [ID] Название проблемы

**Категория:** Infrastructure / Core / Search / Cross-package
**SOLID нарушение:** SRP / OCP / LSP / ISP / DIP
**Приоритет:** 🔴/🟡/🟢
**Effort:** LOW / MEDIUM / HIGH
**Блокирует:** [список других проблем/улучшений]
**Зависит от:** [список зависимостей]

**Проблема:**
[Что не так и почему это проблема]

**Рекомендация:**
[Конкретные шаги для решения]

**Trade-offs:**
[Что мы получим vs что потеряем]
```

### ✅ 7. План решения проблем Framework

**После формирования рекомендаций:**

Создать файл `results/2.1_framework_execution_plan.md`:

```markdown
# Framework Packages - Execution Plan

## Dependency Graph

```
[Проблема A] ──блокирует──> [Проблема B] ──блокирует──> [Проблема C]
     │
     └──блокирует──> [Проблема D]
```

## Фазы выполнения

### Phase 0: Prerequisites (если нужны)
- [ ] [Подготовительные работы]

### Phase 1: Foundation (первоочередные проблемы)
- [ ] [Проблема A] - Effort: X, Impact: HIGH
- [ ] [Проблема D] - Effort: X, Impact: HIGH

**Почему сначала:** Блокируют остальные улучшения

### Phase 2: Improvements (независимые улучшения)
**Можно параллельно:**
- [ ] [Проблема B] - Effort: X, Impact: MEDIUM
- [ ] [Проблема C] - Effort: X, Impact: MEDIUM

### Phase 3: Polish (опционально)
- [ ] [Nice to have улучшения]

## Estimated Timeline
- Phase 0: X hours
- Phase 1: X hours (sequential)
- Phase 2: X hours (can be parallel)
- Phase 3: X hours (optional)
- **Total:** X-Y hours
```

---

## 🤝 Согласование с пользователем

**ОБЯЗАТЕЛЬНЫЙ шаг перед переходом к следующему этапу!**

### Вопросы для обсуждения:

1. **Согласны ли вы с приоритетами?**
   - Может быть что-то критично для вас, что я оценил как MEDIUM?
   - Может быть что-то можно отложить?

2. **Согласны с trade-offs?**
   - Например: "Оставить небольшое дублирование вместо сложной абстракции"
   - Есть ли возражения?

3. **Реалистичны ли оценки effort?**
   - Учитывая вашу команду и процессы?

4. **Есть ли ограничения?**
   - Breaking changes допустимы?
   - Есть ли дедлайны?
   - Есть ли зависимости от внешних систем?

5. **Утверждаем execution plan?**
   - Согласны с порядком решения проблем?
   - Готовы начать с Phase 1?

### После обсуждения:

- [ ] Обновить приоритеты на основе feedback
- [ ] Скорректировать execution plan
- [ ] Зафиксировать согласованный план
- [ ] Закоммитить финальную версию

---

## ⚠️ Важные замечания

1. **Фокус на переиспользуемость** - framework пакеты должны быть универсальными
2. **Zero domain coupling** - критически важно для infrastructure
3. **API simplicity** - чем проще публичное API, тем лучше
4. **Pragmatic approach** - не требовать идеального покрытия интерфейсами где не нужно
5. **Dependencies matter** - порядок решения проблем критичен

---

## ✅ Definition of Done

- [ ] Все 5 категорий проанализированы
- [ ] Создан summary с оценкой SOLID
- [ ] Проблемы приоритизированы
- [ ] **Сформированы рекомендации для каждой проблемы**
- [ ] **Создан execution plan с учетом dependencies**
- [ ] **Проведено обсуждение с пользователем**
- [ ] **План согласован и утвержден**
- [ ] Результаты закоммичены в git
- [ ] Обновлен README.md плана со статусом

---

## 🔗 Параллельные этапы

Можно выполнять одновременно:
- **2.2** Yandex-Tracker Analysis
- **2.3** Code Quality Analysis
- **2.4** Test Coverage Analysis
