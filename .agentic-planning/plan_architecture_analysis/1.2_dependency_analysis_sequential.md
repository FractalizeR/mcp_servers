# Этап 1.2: Анализ зависимостей и границ (SEQUENTIAL)

**Время:** 1-2 часа
**Зависимости:** Этап 1.1 (базовые метрики)
**Выполняется:** Последовательно (блокирует Фазу 2)

---

## 🎯 Цель

Проанализировать граф зависимостей между пакетами и внутри них, проверить соблюдение архитектурных границ.

---

## 📋 Задачи

### ✅ 1. Monorepo dependency graph

**Что проверить:**

- [ ] Соблюдение иерархии: infrastructure → core → search → yandex-tracker
- [ ] Отсутствие циклических зависимостей между пакетами
- [ ] Отсутствие обратных зависимостей
- [ ] Правильность импортов (через npm package names)

**Команды:**
```bash
# Запустить dependency-cruiser
npm run depcruise

# Создать визуальный граф (опционально, если нужно)
npx depcruise packages --output-type dot | dot -T svg > dependency-graph.svg
```

**Проверить вручную:**

1. Открыть `packages/framework/infrastructure/src/**/*.ts`
   - Не должно быть импортов из `@mcp-framework/core` или `@mcp-framework/search`

2. Открыть `packages/framework/core/src/**/*.ts`
   - Разрешены импорты из `@mcp-framework/infrastructure`
   - Запрещены импорты из `@mcp-framework/search`

3. Открыть `packages/framework/search/src/**/*.ts`
   - Разрешены импорты из `@mcp-framework/core` и `@mcp-framework/infrastructure`
   - Запрещены импорты из `mcp-server-yandex-tracker`

4. Открыть `packages/servers/yandex-tracker/src/**/*.ts`
   - Может импортировать все framework пакеты

**Сохранить в:** `results/1.2_monorepo_dependencies.md`

---

### ✅ 2. Yandex-Tracker internal layering

**Что проверить:**

- [ ] Изоляция `tracker_api` от `mcp` слоя
- [ ] MCP tools используют только Facade (не Operations напрямую)
- [ ] Operations изолированы (импортируются через Facade или DI)
- [ ] Composition Root на вершине (ничто его не импортирует кроме entry point)

**Файлы для проверки:**

```bash
# 1. tracker_api не должен импортировать mcp
grep -r "from.*mcp" packages/servers/yandex-tracker/src/tracker_api/ || echo "OK"

# 2. MCP tools должны импортировать только facade/entities/dto
grep -r "from.*api_operations" packages/servers/yandex-tracker/src/tools/ || echo "Check manually"

# 3. Composition root импортируется только из index.ts
grep -r "from.*composition-root" packages/servers/yandex-tracker/src/ \
  --exclude-dir=composition-root | grep -v "index.ts" || echo "OK"
```

**Визуализация слоев:**

```
┌─────────────────────────────────────┐
│     Entry Point (index.ts)          │
└──────────────┬──────────────────────┘
               │
┌──────────────▼──────────────────────┐
│     Composition Root (DI)            │ ← Высший уровень
└──────────────┬──────────────────────┘
               │
┌──────────────▼──────────────────────┐
│     MCP Layer (tools)                │
└──────────────┬──────────────────────┘
               │ (через Facade)
┌──────────────▼──────────────────────┐
│     Facade (координация)             │
└──────────────┬──────────────────────┘
               │
┌──────────────▼──────────────────────┐
│     Tracker API (operations/dto)     │ ← Низший уровень
└─────────────────────────────────────┘
```

**Проверить:**
- Стрелки идут только вниз
- Нет обратных зависимостей
- Нет "перепрыгиваний" слоев

**Сохранить в:** `results/1.2_internal_layering.md`

---

### ✅ 3. Import patterns анализ

**Что проверить:**

- [ ] Использование npm package names между пакетами
- [ ] Использование # префиксов внутри yandex-tracker
- [ ] Отсутствие относительных импортов через `../../..` между пакетами
- [ ] Правильность `import type` для type-only импортов

**Команды:**
```bash
# 1. Поиск неправильных импортов между пакетами (через относительные пути)
grep -r "from '\.\./\.\./\.\." packages/*/src/ --include="*.ts" || echo "OK"

# 2. Поиск @ алиасов вместо # префиксов в yandex-tracker
grep -r "from '@tracker_api\|from '@tools\|from '@cli'" \
  packages/servers/yandex-tracker/src/ --include="*.ts" || echo "OK"

# 3. Проверка использования import type
grep -r "^import {.*}" packages/*/src/ --include="*.ts" | \
  grep -v "import type" | grep "Type\|Interface" | head -20
```

**Сохранить в:** `results/1.2_import_patterns.md`

---

### ✅ 4. Circular dependencies детальный анализ

**Что проверить:**

- [ ] Циклы в dependency-cruiser отчете
- [ ] Намеренные циклы (Definition ↔ Tool паттерн)
- [ ] Случайные циклы (нужно исправлять)

**Команды:**
```bash
# Запустить depcruise с focus на circular deps
npm run depcruise 2>&1 | grep -A 10 "circular"

# Найти все циклы через madge (если установлен)
npx madge --circular packages/framework/infrastructure/src
npx madge --circular packages/framework/core/src
npx madge --circular packages/framework/search/src
npx madge --circular packages/servers/yandex-tracker/src
```

**Разрешенные циклы (из .dependency-cruiser.cjs):**
- `BaseDefinition ↔ ToolMetadata` (framework pattern)
- `SearchToolsDefinition ↔ SearchToolsTool` (pairing pattern)
- `DemoDefinition ↔ DemoTool` (pairing pattern)

**Любые другие циклы = проблема!**

**Сохранить в:** `results/1.2_circular_dependencies.md`

---

### ✅ 5. External dependencies анализ

**Что проверить:**

- [ ] Разумность использования зависимостей
- [ ] Отсутствие дублирования функционала
- [ ] Правильность peer dependencies

**Проверить:**

1. **Infrastructure пакет:**
   - Должен иметь минимум зависимостей
   - Только базовые утилиты (axios, pino, etc)

2. **Core пакет:**
   - Зависит от infrastructure
   - Минимум дополнительных deps

3. **Search пакет:**
   - Зависит от core
   - Специфичные search deps (lunr, fuse, etc)

4. **Yandex-Tracker:**
   - Зависит от всех framework пакетов
   - Доменные зависимости (MCP SDK, etc)

**Команды:**
```bash
# Для каждого пакета
for pkg in infrastructure core search yandex-tracker; do
  echo "=== $pkg ==="
  if [ "$pkg" = "yandex-tracker" ]; then
    jq '.dependencies' packages/servers/$pkg/package.json
  else
    jq '.dependencies' packages/framework/$pkg/package.json
  fi
done
```

**Сохранить в:** `results/1.2_external_dependencies.md`

---

### ✅ 6. Boundary violations поиск

**Что проверить:**

- [ ] Утечка доменной логики в framework пакеты
- [ ] Прямое использование HTTP в бизнес-логике (должно быть через facade)
- [ ] Bypass DI контейнера (прямые `new` вместо inject)

**Поиск проблем:**

```bash
# 1. Поиск "Yandex" или "Tracker" в framework пакетах (утечка домена)
grep -ri "yandex\|tracker" packages/framework/*/src/ --include="*.ts" \
  --exclude-dir=node_modules || echo "OK"

# 2. Поиск прямых HTTP вызовов в tools (должны быть через facade)
grep -r "axios\|fetch\|http\." packages/servers/yandex-tracker/src/tools/ \
  --include="*.ts" || echo "OK"

# 3. Поиск прямых new Operation() вместо inject
grep -r "new.*Operation" packages/servers/yandex-tracker/src/tools/ \
  --include="*.ts" | grep -v "test" || echo "Check manually"
```

**Сохранить в:** `results/1.2_boundary_violations.md`

---

## 📊 Формат результата

Создать файл `results/1.2_dependency_analysis_summary.md`:

```markdown
# Dependency Analysis Summary

**Дата:** YYYY-MM-DD

## Monorepo Graph

- ✅/❌ Иерархия пакетов соблюдена
- ✅/❌ Нет циклических зависимостей между пакетами
- ✅/❌ Нет обратных зависимостей
- Найдено проблем: X

## Internal Layering (Yandex-Tracker)

- ✅/❌ tracker_api изолирован от mcp
- ✅/❌ MCP tools используют только Facade
- ✅/❌ Operations изолированы
- ✅/❌ Composition Root на вершине
- Найдено проблем: X

## Import Patterns

- ✅/❌ Правильные npm package names
- ✅/❌ Правильные # префиксы
- ✅/❌ Нет относительных импортов между пакетами
- ✅/❌ Правильное использование import type
- Найдено проблем: X

## Circular Dependencies

- Общее количество циклов: X
- Разрешенные циклы: X
- Проблемные циклы: X
- **Список проблемных:**
  - [список]

## Boundary Violations

- Утечек домена в framework: X
- Bypass facade: X
- Bypass DI: X
- **Критические нарушения:**
  - [список]

## 🎯 SOLID Compliance (на уровне архитектуры)

- **SRP (пакеты):** ✅/❌ Каждый пакет имеет одну ответственность
- **OCP (расширяемость):** ✅/❌ Framework пакеты расширяемы без изменений
- **LSP (подстановка):** N/A (на уровне пакетов не применяется)
- **ISP (интерфейсы):** ✅/❌ Нет избыточных зависимостей между пакетами
- **DIP (абстракции):** ✅/❌ Зависимость от абстракций, не конкретики

## 📈 Рекомендации

### 🔴 Критические (нужно исправить)
- [список]

### 🟡 Важные (желательно исправить)
- [список]

### 🟢 Улучшения (nice to have)
- [список]
```

---

## ⚠️ Важные замечания

1. **Визуализация** - если depcruise показывает проблемы, создать граф для наглядности
2. **Pragmatic approach** - небольшие нарушения допустимы если есть веская причина
3. **Focus на критических** - boundary violations важнее мелких проблем с импортами
4. **Документировать причины** - если нарушение намеренное, добавить в комментарий

---

## ✅ Definition of Done

- [ ] Все 6 категорий проанализированы
- [ ] Создан summary файл с оценкой SOLID compliance
- [ ] Проблемы приоритизированы (критические, важные, улучшения)
- [ ] Результаты закоммичены в git
- [ ] Обновлен README.md плана со статусом
- [ ] **Готово к запуску Фазы 2 (parallel анализ)**

---

## 🔗 Следующий этап

После завершения → **Фаза 2** (можно запускать 2.1, 2.2, 2.3, 2.4 параллельно)
