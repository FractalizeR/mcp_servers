# Этап 2.3: Анализ качества кода (PARALLEL)

**Время:** 2-3 часа
**Зависимости:** Этап 1.1 (базовые метрики)
**Выполняется:** Параллельно с 2.1, 2.2, 2.4

---

## 🎯 Цель

Детально проанализировать качество кода с точки зрения complexity, duplication, type safety и code smells.

---

## 📋 Задачи

### ✅ 1. Complexity Analysis

**Что проверить:**

#### 1.1 Cyclomatic Complexity

- [ ] Найти функции с complexity >10
- [ ] Оценить критичность (можно ли упростить?)
- [ ] Приоритизировать для рефакторинга

**Команды:**
```bash
# ESLint complexity violations
npm run lint 2>&1 | grep "complexity" | head -50

# Или запустить с focus на complexity
npx eslint packages --rule 'complexity: [error, 5]' --format compact 2>&1 | \
  grep "complexity" | wc -l
```

**Для каждой найденной функции:**
- Где находится?
- Какова сложность? (число)
- Можно ли упростить? (да/нет/частично)
- Приоритет рефакторинга (высокий/средний/низкий)

#### 1.2 Cognitive Complexity

**⚠️ Важнее чем cyclomatic!** - показывает реальную сложность понимания кода.

```bash
# SonarJS cognitive-complexity violations
npm run lint 2>&1 | grep "cognitive-complexity" | head -50
```

**Порог:** 15 (из ESLint config)

**Анализ:**
- Функции с cognitive complexity >15
- Насколько превышение критично?
- Есть ли паттерн (определенные типы функций)?

#### 1.3 Nesting Depth

```bash
# Max-depth violations
npm run lint 2>&1 | grep "max-depth" | head -20
```

**Порог:** 4 уровня

**Проблемы:**
- Глубокая вложенность = сложность понимания
- Обычно можно упростить через early returns или extraction

#### 1.4 Function/File Size

```bash
# Max-lines violations (files >400 строк)
npm run lint 2>&1 | grep "max-lines" | head -20

# Max-lines-per-function violations (>50 строк)
npm run lint 2>&1 | grep "max-lines-per-function" | head -20
```

**Известные нарушители:**
- YandexTrackerFacade (1080 строк) - уже в плане рефакторинга

**Анализ остальных:**
- Сколько файлов нарушают лимиты?
- Насколько критично?
- Можно ли разделить?

#### 1.5 Parameter Count

```bash
# Max-params violations (>4 параметров)
npm run lint 2>&1 | grep "max-params" | head -20
```

**Проблемы:**
- Много параметров = сложный API
- Решение: объекты параметров, builder pattern

**Сохранить в:** `results/2.3_complexity_analysis.md`

---

### ✅ 2. Code Duplication Analysis

**Что проверить:**

#### 2.1 CPD Metrics

```bash
# Запустить jscpd с детальным отчетом
npm run cpd:report

# Проверить порог (должно быть ≤5%)
npm run cpd 2>&1 | grep "percentage"
```

#### 2.2 Top Duplicates

**Анализ HTML отчета:**
- Открыть `jscpd-report/html/index.html`
- Найти топ-10 дубликатов
- Для каждого оценить:
  - Где дублируется?
  - Сколько раз? (2+ раза)
  - Можно ли абстрагировать?
  - Стоит ли? (trade-off: простота vs DRY)

#### 2.3 Pragmatic Evaluation

**НЕ требовать устранения дублирования если:**
- Дублируется <10 строк
- Код специфичен для каждого случая
- Абстракция усложнит код

**Требовать устранения если:**
- Дублируется >20 строк
- Очевидная общая логика
- Изменение требует правок в N местах

#### 2.4 Pattern Duplication

**Поиск паттернов для абстракций:**

```bash
# Повторяющиеся паттерны try-catch
grep -r "try {" packages/*/src --include="*.ts" -A 10 | \
  grep -c "} catch" | head -20

# Повторяющиеся валидации
grep -r "if (!.*) {" packages/*/src --include="*.ts" -A 2 | \
  grep "throw" | wc -l

# Повторяющиеся transformations
grep -r "\.map(\|\.filter(\|\.reduce(" packages/*/src --include="*.ts" | \
  wc -l
```

**Вопросы:**
- Есть ли общие паттерны?
- Можно ли создать helper utilities?
- Стоит ли создавать абстракции?

**Сохранить в:** `results/2.3_duplication_analysis.md`

---

### ✅ 3. Type Safety Analysis

**Что проверить:**

#### 3.1 `any` Usage

```bash
# Найти все использования any
grep -r ": any\|<any>\|as any" packages/*/src --include="*.ts" | \
  grep -v "^[[:space:]]*\*\|^[[:space:]]*//" > any-usage.txt

wc -l any-usage.txt
```

**Для каждого случая:**
- Обоснован ли `any`? (иногда нужен для external libs)
- Можно ли заменить на `unknown` + type guard?
- Можно ли типизировать точнее?

#### 3.2 `unknown` Usage

```bash
# Найти использования unknown
grep -r ": unknown" packages/*/src --include="*.ts" | wc -l
```

**Проверить:**
- Правильно ли используется с type guards?
- Не лучше ли конкретный тип?

#### 3.3 Type Assertions

```bash
# as Type assertions
grep -r " as [A-Z]" packages/*/src --include="*.ts" | wc -l

# Non-null assertions (!)
grep -r "!\." packages/*/src --include="*.ts" | head -30
```

**Проблемы:**
- Type assertions = отключение проверок
- Non-null assertions опасны
- Лучше использовать type guards

#### 3.4 Strict Mode Compliance

```bash
# Проверить tsconfig.json
grep -A 10 '"strict"' packages/*/tsconfig.json
grep -A 10 '"strictNullChecks"' packages/*/tsconfig.json
```

**Должно быть:**
- `"strict": true`
- `"strictNullChecks": true`
- `"noImplicitAny": true`

#### 3.5 Missing Type Annotations

```bash
# Функции без return type
grep -r "function.*)\s*{" packages/*/src --include="*.ts" | \
  grep -v ": [A-Z]\|: void\|: Promise" | head -20

# Параметры без типов (не должно быть в strict mode)
npm run typecheck 2>&1 | grep "implicitly has an 'any' type"
```

**Сохранить в:** `results/2.3_type_safety_analysis.md`

---

### ✅ 4. Code Smells Detection

**Что проверить:**

#### 4.1 Long Parameter Lists

**Уже проверено в 1.5**, но дополнительно:

```bash
# Найти функции с >4 параметрами
grep -r "function\|^\s*\w\+(" packages/*/src --include="*.ts" -A 1 | \
  grep -E "\w+\s*,\s*\w+\s*,\s*\w+\s*,\s*\w+\s*," | head -20
```

**Решения:**
- Parameter object
- Builder pattern
- Partial application

#### 4.2 God Objects/Functions

**Уже известно:**
- YandexTrackerFacade (God Object)

**Поиск других:**

```bash
# Классы с >20 методами
for file in $(find packages -name "*.ts" -path "*/src/*"); do
  count=$(grep -c "^\s*\(public\|private\|protected\)\s\+\(async\s\+\)\?\w\+" "$file" 2>/dev/null || echo 0)
  if [ "$count" -gt 20 ]; then
    echo "$file: $count methods"
  fi
done
```

#### 4.3 Dead Code

```bash
# Запустить knip для поиска unused exports
npm run knip 2>&1 | tee knip-output.txt

# Подсчет
grep "unused" knip-output.txt | wc -l
```

**Pragmatic approach:**
- Публичное API может быть "unused" внутри - это OK
- Private unused - точно можно удалить
- Internal unused - осторожно (может быть задел на будущее)

#### 4.4 Magic Numbers/Strings

```bash
# Поиск магических чисел (>1, не 0/1/-1)
grep -r "[^0-1]\d\{2,\}" packages/*/src --include="*.ts" | \
  grep -v "test\|spec\|//\|/\*" | head -30

# Повторяющиеся строковые литералы
grep -ro '"[^"]\{10,\}"' packages/*/src --include="*.ts" | \
  sort | uniq -c | sort -rn | head -20
```

**Решение:** Constants/Enums

#### 4.5 Nested Ternaries

```bash
# Поиск вложенных тернарных операторов
grep -r "?" packages/*/src --include="*.ts" | grep ".*?.*?.*:" | head -20
```

**Проблема:** Сложно читать
**Решение:** if-else или функция

#### 4.6 Callback Hell

```bash
# Поиск вложенных callbacks (не должно быть в async/await коде)
grep -r "\.then(\.then\|callback.*callback" packages/*/src --include="*.ts" || echo "OK"
```

**Должно быть:** async/await вместо callbacks/promises

**Сохранить в:** `results/2.3_code_smells.md`

---

### ✅ 5. Best Practices Compliance

**Что проверить:**

#### 5.1 Error Handling Patterns

```bash
# Все try-catch блоки
grep -r "try {" packages/*/src --include="*.ts" -A 15 | \
  grep -A 15 "} catch" | head -50
```

**Проверить:**
- Правильная типизация errors?
- Логирование ошибок?
- Re-throw с контекстом?
- Не глотаются ли ошибки? (empty catch)

#### 5.2 Async/Await Patterns

```bash
# Promise без await
grep -r "= \w\+\.\w\+(" packages/*/src --include="*.ts" | \
  grep -v "await\|\.then" | grep "Promise" | head -20

# Последовательные await (можно распараллелить?)
grep -r "await.*await" packages/*/src --include="*.ts" -A 1 | head -20
```

**Проблемы:**
- Забытый await = race condition
- Последовательные await = медленно (если можно параллельно)

#### 5.3 Immutability

```bash
# let vs const ratio
let_count=$(grep -r "^\s*let\s" packages/*/src --include="*.ts" | wc -l)
const_count=$(grep -r "^\s*const\s" packages/*/src --include="*.ts" | wc -l)
echo "let: $let_count, const: $const_count, ratio: $((let_count * 100 / const_count))%"
```

**Цель:** const > 80%

#### 5.4 Pure Functions

**Сложно автоматически**, но можно проверить признаки:
- Мутация параметров (плохо)
- Side effects в вычислениях (плохо)
- Зависимость от внешнего состояния (может быть OK)

```bash
# Поиск мутаций массивов (push, splice, etc)
grep -r "\.push(\|\.splice(\|\.pop(\|\.shift(" packages/*/src --include="*.ts" | \
  wc -l
```

**Не требовать 100% immutability** - pragmatic approach.

#### 5.5 Naming Conventions

```bash
# Проверка ESLint naming rules
npm run lint 2>&1 | grep "naming-convention" | head -20
```

**Проверить консистентность:**
- camelCase для переменных/функций
- PascalCase для классов/типов
- UPPER_CASE для констант
- Понятные имена (не i, j, k кроме loops)

**Сохранить в:** `results/2.3_best_practices.md`

---

## 📊 Формат результата

Создать файл `results/2.3_code_quality_summary.md`:

```markdown
# Code Quality Analysis Summary

**Дата:** YYYY-MM-DD

## Complexity Metrics

| Метрика | Текущее | Лимит | Нарушений | Статус |
|---------|---------|-------|-----------|--------|
| Cyclomatic Complexity | max X | 10 | X файлов | ✅/⚠️/❌ |
| Cognitive Complexity | max X | 15 | X файлов | ✅/⚠️/❌ |
| Nesting Depth | max X | 4 | X мест | ✅/⚠️/❌ |
| Max Lines (file) | max X | 400 | X файлов | ✅/⚠️/❌ |
| Max Lines (function) | max X | 50 | X функций | ✅/⚠️/❌ |
| Max Params | max X | 4 | X функций | ✅/⚠️/❌ |

### Критические нарушения complexity

| Файл | Функция | Метрика | Значение | Приоритет |
|------|---------|---------|----------|-----------|
| ... | ... | Cyclomatic | X | 🔴/🟡/🟢 |

## Code Duplication

- **Общий CPD:** X% (порог: 5%)
- **Статус:** ✅ Хорошо / ⚠️ Близко к порогу / ❌ Превышен
- **Дубликатов найдено:** X

### Топ-5 дубликатов

| Файлы | Строк | Оценка | Действие |
|-------|-------|--------|----------|
| A, B | X | Стоит абстрагировать / Оставить | Рефакторинг / Оставить |

## Type Safety

- **`any` usage:** X мест
  - Обоснованных: X
  - Можно исправить: X
- **Type assertions:** X мест
- **Non-null assertions:** X мест
- **Strict mode:** ✅ Enabled / ❌ Disabled

### Проблемы type safety

| Приоритет | Файл | Проблема | Рекомендация |
|-----------|------|----------|--------------|
| 🔴/🟡/🟢 | ... | ... | ... |

## Code Smells

- **God Objects:** X (YandexTrackerFacade + X других)
- **Long Parameter Lists:** X функций
- **Dead Code:** X exports (по knip)
- **Magic Numbers:** X мест
- **Nested Ternaries:** X мест
- **Callback Hell:** X мест (должно быть 0)

## Best Practices

- **Error Handling:** ✅/⚠️/❌ Консистентно
- **Async/Await:** ✅/⚠️/❌ Правильно используется
- **Immutability:** X% const usage (цель: >80%)
- **Naming:** ✅/⚠️/❌ Консистентно

## 📈 Общая оценка качества

| Категория | Оценка (1-5) | Комментарий |
|-----------|--------------|-------------|
| Complexity | X/5 | ... |
| Duplication | X/5 | ... |
| Type Safety | X/5 | ... |
| Code Smells | X/5 | ... |
| Best Practices | X/5 | ... |
| **ИТОГО** | **X/5** | ... |

## 💡 Ключевые выводы

### ✅ Сильные стороны
1. [топ-3 позитивных находок]

### ⚠️ Проблемные зоны
1. [топ-3 проблем]

### 🎯 Приоритетные улучшения
1. [топ-3 с максимальным impact]

## 📋 Рекомендации

### 🔴 Критические (блокируют качество)
- [список]

### 🟡 Важные (снижают maintainability)
- [список]

### 🟢 Улучшения (nice to have)
- [список]

### ⚪ Не требуют действий (trade-offs)
- [список вещей которые можно оставить как есть]
```

---

## ⚠️ Важные замечания

1. **Pragmatic over perfect** - не требовать идеальности где это не нужно
2. **Trade-offs matter** - иногда небольшое дублирование лучше сложной абстракции
3. **Context is key** - сложность в критическом коде хуже чем в утилитах
4. **Impact focus** - приоритизировать по влиянию на maintainability

---

## ✅ Definition of Done

- [ ] Все 5 категорий проанализированы
- [ ] Создан summary с общей оценкой качества
- [ ] Проблемы приоритизированы с учетом trade-offs
- [ ] Результаты закоммичены в git
- [ ] Обновлен README.md плана со статусом

---

## 🔗 Параллельные этапы

Можно выполнять одновременно:
- **2.1** Framework Packages Analysis
- **2.2** Yandex-Tracker Analysis
- **2.4** Test Coverage Analysis

---

## 📋 Рекомендации и план действий

**После завершения анализа - обязательная секция!**

### ✅ 6. Формирование рекомендаций

**Для каждой найденной проблемы:**

1. **Оценка критичности:**
   - 🔴 CRITICAL - критичные code smells, серьезные нарушения complexity
   - 🟡 IMPORTANT - ухудшает maintainability
   - 🟢 NICE TO HAVE - косметические улучшения

2. **Dependency analysis:**
   - Является ли следствием архитектурных проблем? (если да → зависит от 2.1/2.2)
   - Независимое улучшение? (можно делать сразу)

**⚠️ Важно про приоритеты:**
- Архитектурные проблемы (из 2.1, 2.2) ВАЖНЕЕ code quality
- Нет смысла улучшать код который будет рефакториться
- Фокус на код который останется стабильным

### ✅ 7. План решения проблем Code Quality

Создать файл `results/2.3_code_quality_execution_plan.md`:

```markdown
# Code Quality - Execution Plan

## Dependency Analysis

**Зависит от архитектуры:**
- [ ] [Проблемы в коде который будет рефакториться] → ждем архитектурного рефакторинга

**Независимые улучшения:**
- [ ] [Проблемы в стабильном коде] → можно делать сразу

## Фазы выполнения

### Phase 1: Quick Wins (независимые)
**Можно делать параллельно с архитектурными изменениями:**
- [ ] Исправить type safety issues
- [ ] Удалить dead code
- [ ] Добавить const вместо let
- [ ] Исправить simple code smells

### Phase 2: После архитектурных изменений
**Ждем завершения 2.1/2.2 execution plans:**
- [ ] Упростить complexity в отрефакторенном коде
- [ ] Устранить дублирование (если осталось)

## Estimated Timeline
- Phase 1: X hours (можно начать сразу)
- Phase 2: X hours (после архитектурных изменений)
```

---

## 📝 Примечание: обсуждение откладывается

**⚠️ ВАЖНО:** Обсуждение рекомендаций происходит в Фазе 3!

На этом этапе нужно:
- ✅ Собрать все проблемы и рекомендации
- ✅ Создать execution plan с оценками effort/impact
- ✅ Закоммитить результаты

**НЕ нужно:**
- ❌ Обсуждать с пользователем на этом этапе
- ❌ Согласовывать приоритеты
- ❌ Утверждать execution plan

**Причина:** Сначала соберем ВСЮ информацию (Фаза 1 + Фаза 2), затем обсудим все проблемы сразу в Фазе 3.

---

## ✅ Definition of Done (обновлен)

- [x] Все категории проанализированы
- [x] Создан code quality summary
- [x] **Сформированы рекомендации с dependency analysis**
- [x] **Создан execution plan с учетом архитектурных изменений**
- [ ] **Проведено обсуждение с пользователем** (отложено на Фазу 3)
- [ ] **План согласован и утвержден** (отложено на Фазу 3)
- [ ] Результаты закоммичены
- [ ] README.md обновлен
