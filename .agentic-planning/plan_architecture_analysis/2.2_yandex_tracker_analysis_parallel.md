# Этап 2.2: Анализ Yandex-Tracker пакета (PARALLEL)

**Время:** 3-4 часа
**Зависимости:** Этапы 1.1, 1.2 (базовые метрики и граф зависимостей)
**Выполняется:** Параллельно с 2.1, 2.3, 2.4

---

## 🎯 Цель

Детально проанализировать доменный пакет yandex-tracker на соответствие SOLID, layered architecture и best practices.

---

## 📋 Задачи

### ✅ 1. MCP Layer анализ [COMPLETED]

**Файлы:**
- `packages/servers/yandex-tracker/src/tools/`

**Что проверить:**

#### 1.1 Tool implementations

- [ ] Каждый tool наследует BaseTool правильно
- [ ] Правильное использование schemas (Zod)
- [ ] Автогенерация definitions из schemas (не ручные *.definition.ts)
- [ ] Размер файлов разумен (≤400 строк)

**Команды:**
```bash
# Размеры tool файлов
find packages/servers/yandex-tracker/src/tools -name "*.tool.ts" -exec wc -l {} \; | \
  sort -rn | head -20

# Поиск устаревших *.definition.ts файлов (должны быть удалены)
find packages/servers/yandex-tracker/src/tools -name "*.definition.ts"

# Проверка использования generateDefinitionFromSchema
grep -r "generateDefinitionFromSchema" packages/servers/yandex-tracker/src/tools
```

#### 1.2 Single Responsibility

- [ ] Каждый tool делает одну вещь
- [ ] Нет God Tools (>50 строк execute метода)
- [ ] Правильная декомпозиция сложных операций

**Команды:**
```bash
# Поиск длинных execute методов
for file in packages/servers/yandex-tracker/src/tools/**/*.tool.ts; do
  lines=$(sed -n '/execute(/,/^  }/p' "$file" 2>/dev/null | wc -l)
  if [ "$lines" -gt 50 ]; then
    echo "$file: $lines lines"
  fi
done
```

#### 1.3 Facade usage

- [ ] Все tools используют YandexTrackerFacade
- [ ] Нет прямых вызовов Operations
- [ ] Нет прямых HTTP calls

**Команды:**
```bash
# Проверка что tools НЕ импортируют operations напрямую
grep -r "from.*api_operations" packages/servers/yandex-tracker/src/tools/ | \
  grep -v "test\|spec" || echo "OK"

# Проверка использования facade
grep -r "@inject.*FACADE\|YandexTrackerFacade" \
  packages/servers/yandex-tracker/src/tools/ | wc -l
```

**Сохранить в:** `results/2.2_mcp_layer.md`

---

### ✅ 2. Tracker API Layer анализ [COMPLETED]

**Файлы:**
- `packages/servers/yandex-tracker/src/tracker_api/`

**Что проверить:**

#### 2.1 Operations architecture

- [ ] Все operations наследуют BaseOperation
- [ ] Правильное разделение на категории (issues, queues, etc)
- [ ] Размер файлов разумен
- [ ] Правильная обработка ошибок

**Файлы для анализа:**
```bash
# Структура operations
tree packages/servers/yandex-tracker/src/tracker_api/api_operations -L 2

# Найти самые большие operations
find packages/servers/yandex-tracker/src/tracker_api/api_operations \
  -name "*.ts" -exec wc -l {} \; | sort -rn | head -10
```

#### 2.2 Facade architecture

**⚠️ ИЗВЕСТНАЯ ПРОБЛЕМА из plan_architecture_refactoring:**
- YandexTrackerFacade = God Object (1080 строк, 61 метод)

**Проверить:**
- [ ] Текущее состояние Facade (размер, количество методов)
- [ ] Насколько критична проблема?
- [ ] Есть ли план исправления? (должен быть в plan_architecture_refactoring)

```bash
# Текущие метрики Facade
wc -l packages/servers/yandex-tracker/src/tracker_api/facade/*.ts

# Количество методов
grep -c "^\s*public\|^\s*async" \
  packages/servers/yandex-tracker/src/tracker_api/facade/yandex-tracker.facade.ts
```

**НЕ исправлять** - просто задокументировать текущее состояние и ссылку на план рефакторинга.

#### 2.3 DTO/Entity design

- [ ] Правильное разделение Input/Output DTO
- [ ] Entities используют WithUnknownFields для расширяемости
- [ ] Нет дублирования между DTO и Entity
- [ ] Правильная валидация через Zod

**Файлы для анализа:**
```bash
# Структура DTO
ls packages/servers/yandex-tracker/src/tracker_api/dto/

# Структура Entities
ls packages/servers/yandex-tracker/src/tracker_api/entities/

# Поиск WithUnknownFields usage
grep -r "WithUnknownFields" packages/servers/yandex-tracker/src/tracker_api/entities/
```

#### 2.4 Isolation от MCP layer

- [ ] tracker_api НЕ импортирует ничего из mcp/
- [ ] Нет утечки MCP concepts в tracker_api

**Команды:**
```bash
# Проверка изоляции
grep -r "from.*mcp\|from.*tools" \
  packages/servers/yandex-tracker/src/tracker_api/ || echo "OK"
```

**Сохранить в:** `results/2.2_tracker_api_layer.md`

---

### ✅ 3. Composition Root (DI) анализ [COMPLETED]

**Файлы:**
- `packages/servers/yandex-tracker/src/composition-root/`

**Что проверить:**

#### 3.1 DI Container setup

- [ ] Правильная регистрация зависимостей
- [ ] Symbol-based tokens (не строки)
- [ ] Автоматическая регистрация operations/tools
- [ ] Правильные scopes (singleton, transient, request)

**Файлы для анализа:**
- `container.ts` - основной контейнер
- `definitions/` - регистрация компонентов

#### 3.2 Isolation

- [ ] Composition Root не импортируется нигде кроме entry point
- [ ] Правильная топология (composition root = highest level)

**Команды:**
```bash
# Проверка что composition-root импортируется только из index.ts
grep -r "from.*composition-root" packages/servers/yandex-tracker/src/ \
  --exclude-dir=composition-root | grep -v "index.ts" || echo "OK"
```

#### 3.3 Dependency Inversion Principle

- [ ] Все зависимости через интерфейсы
- [ ] Нет прямых new Class() в бизнес-логике
- [ ] Правильное использование @inject decorators

**Сохранить в:** `results/2.2_composition_root.md`

---

### ✅ 4. CLI module анализ [COMPLETED]

**Файлы:**
- `packages/servers/yandex-tracker/src/cli/`

**Что проверить:**

#### 4.1 Responsibility

- [ ] CLI имеет четкую ответственность (управление подключениями)
- [ ] Нет смешивания с бизнес-логикой
- [ ] Правильная изоляция от MCP layer

#### 4.2 Architecture

- [ ] Правильное использование connectors
- [ ] Правильная структура команд
- [ ] Хорошее UX (понятные сообщения, help, etc)

**Команды:**
```bash
# Проверить размер CLI
find packages/servers/yandex-tracker/src/cli -name "*.ts" -exec wc -l {} \; | \
  awk '{sum+=$1} END {print "Total CLI LoC:", sum}'
```

**Сохранить в:** `results/2.2_cli_module.md`

---

### ✅ 5. Code Organization & Patterns [COMPLETED]

**Что проверить:**

#### 5.1 Module System

- [ ] Правильное использование # префиксов (subpath imports)
- [ ] Относительные импорты только для близких файлов (≤2 уровня)
- [ ] Нет @ алиасов (устарели)

**Команды:**
```bash
# Поиск @ алиасов (должны быть заменены на #)
grep -r "from '@tracker_api\|from '@tools'" \
  packages/servers/yandex-tracker/src/ || echo "OK"

# Проверка использования # префиксов
grep -r "from '#" packages/servers/yandex-tracker/src/ | wc -l
```

#### 5.2 Error Handling

- [ ] Консистентная обработка ошибок
- [ ] Правильная типизация errors
- [ ] Логирование критических ошибок

```bash
# Паттерны error handling
grep -r "throw new\|Error\|catch" \
  packages/servers/yandex-tracker/src --include="*.ts" | head -30
```

#### 5.3 Async patterns

- [ ] Правильное использование async/await
- [ ] Нет callback hell
- [ ] Правильная обработка Promise rejections

```bash
# Поиск проблемных паттернов
grep -r "\.then(\.then\|catch(catch" \
  packages/servers/yandex-tracker/src --include="*.ts" || echo "OK"
```

**Сохранить в:** `results/2.2_code_organization.md`

---

### ✅ 6. Integration points [COMPLETED]

**Что проверить:**

- [ ] Правильная интеграция с framework пакетами
- [ ] Нет дублирования функционала из framework
- [ ] Правильное использование HttpClient из infrastructure
- [ ] Правильное использование BaseTool из core

**Команды:**
```bash
# Импорты из framework пакетов
grep -r "from '@mcp-framework" packages/servers/yandex-tracker/src/ | \
  cut -d: -f2 | sort | uniq -c | sort -rn
```

**Сохранить в:** `results/2.2_integration_points.md`

---

## 📊 Формат результата

Создать файл `results/2.2_yandex_tracker_summary.md`:

```markdown
# Yandex-Tracker Package Analysis Summary

**Дата:** YYYY-MM-DD

## MCP Layer

### ✅ Сильные стороны
- [список]

### ⚠️ Проблемы
| Приоритет | Проблема | SOLID | Рекомендация |
|-----------|----------|-------|--------------|
| 🔴/🟡/🟢 | ...      | ...   | ...          |

### 📈 Метрики
- Tools count: X
- Average tool size: X строк
- Manual definitions: X (должно быть 0)
- Max tool size: X строк

## Tracker API Layer

### ⚠️ ИЗВЕСТНЫЕ ПРОБЛЕМЫ

**YandexTrackerFacade (God Object):**
- Размер: X строк (лимит: 400)
- Методов: X (рекомендуется: ~20)
- **Статус:** Запланирован рефакторинг в `plan_architecture_refactoring/1.1`

### Остальной Tracker API

### ✅ Сильные стороны
- [список]

### ⚠️ Проблемы
[таблица]

### 📈 Метрики
- Operations count: X
- Average operation size: X строк
- Entities count: X
- DTOs count: X

## Composition Root

### ✅ Сильные стороны
- [список]

### ⚠️ Проблемы
[таблица]

### 📈 Метрики
- Registered services: X
- Symbol tokens: X/X (должно быть 100%)

## CLI Module

### ✅ Сильные стороны
- [список]

### ⚠️ Проблемы
[таблица]

### 📈 Метрики
- Commands: X
- LoC: X

## 🎯 SOLID Compliance

| Принцип | MCP Layer | Tracker API | DI | CLI | Комментарий |
|---------|-----------|-------------|----|----|-------------|
| SRP     | ✅/⚠️/❌  | ❌ (Facade) | ... | ... | Facade = God Object |
| OCP     | ...       | ...         | ... | ... | ...         |
| LSP     | ...       | ...         | ... | ... | ...         |
| ISP     | ...       | ...         | ... | ... | ...         |
| DIP     | ...       | ...         | ... | ... | ...         |

## 🎯 Layered Architecture Compliance

- **MCP → Facade:** ✅/⚠️/❌
- **Facade → Operations:** ✅/⚠️/❌
- **Operations → HTTP:** ✅/⚠️/❌
- **Tracker API изоляция:** ✅/⚠️/❌
- **DI на вершине:** ✅/⚠️/❌

## 💡 Ключевые выводы

1. **Сильные стороны:**
   - [список топ-3]

2. **Критические проблемы:**
   - YandexTrackerFacade God Object (ИЗВЕСТНО, запланирован рефакторинг)
   - [другие проблемы]

3. **Приоритетные улучшения:**
   - [список кроме Facade - он уже в плане]

## 📋 Рекомендации

### 🔴 Критические
- ~~YandexTrackerFacade рефакторинг~~ (уже в плане: `plan_architecture_refactoring/1.1`)
- [другие критические]

### 🟡 Важные
- [список]

### 🟢 Улучшения
- [список]
```

---

## 📋 Рекомендации и план действий

**После завершения анализа - обязательная секция!**

### ✅ 7. Формирование рекомендаций [COMPLETED]

**Для каждой найденной проблемы:**

1. **Оценка критичности:**
   - 🔴 CRITICAL - блокирует развитие yandex-tracker, нарушает layering
   - 🟡 IMPORTANT - усложняет поддержку, снижает качество
   - 🟢 NICE TO HAVE - улучшения

2. **Оценка усилий:**
   - LOW (<4 часов)
   - MEDIUM (1-2 дня)
   - HIGH (>2 дней, архитектурные изменения)

3. **Dependency analysis:**
   - Связано ли с известной проблемой Facade? (может быть уже в плане!)
   - Блокирует ли добавление новых features?
   - Зависит ли от изменений в framework?

**⚠️ ВАЖНО про Facade:**
- YandexTrackerFacade God Object УЖЕ запланирован в `plan_architecture_refactoring/1.1`
- НЕ создавать дублирующий план!
- Можно ссылаться на существующий план
- Можно добавлять связанные проблемы которые зависят от рефакторинга Facade

### ✅ 8. План решения проблем Yandex-Tracker [COMPLETED]

Создать файл `results/2.2_yandex_tracker_execution_plan.md`:

```markdown
# Yandex-Tracker Package - Execution Plan

## Связь с существующими планами

**⚠️ YandexTrackerFacade рефакторинг:**
- Уже запланирован: `plan_architecture_refactoring/1.1`
- Статус: [проверить текущий статус]
- Наши рекомендации: [дополнения к существующему плану если есть]

## Dependency Graph (новые проблемы)

```
[Facade Refactoring] ──блокирует──> [MCP Tools улучшения]
      │                                    │
      │                                    └──> [Tool Error Handling]
      │
      └──блокирует──> [Operations optimization]
```

## Фазы выполнения

### Phase 0: Ожидание Facade рефакторинга (если не завершен)
- [ ] Дождаться завершения `plan_architecture_refactoring/1.1`
- [ ] Или работать над независимыми улучшениями (см. Phase 2)

### Phase 1: Проблемы зависящие от Facade
- [ ] [Проблема A] - требует новый Facade API
- [ ] [Проблема B] - упрощается после Facade split

**Зависит от:** Завершения Facade рефакторинга

### Phase 2: Независимые улучшения
**Можно делать параллельно или до Facade рефакторинга:**
- [ ] [Проблема C] - CLI улучшения
- [ ] [Проблема D] - DTO/Entity improvements
- [ ] [Проблема E] - Test coverage

### Phase 3: Integration improvements
- [ ] Оптимизация после Facade рефакторинга
- [ ] Использование новых возможностей

## Estimated Timeline
- Phase 0: depends on existing plan
- Phase 1: X hours (after Phase 0)
- Phase 2: X hours (можно начать сразу)
- Phase 3: X hours
- **Total:** X-Y hours (+ ожидание Phase 0)
```

---

## 📝 Примечание: обсуждение откладывается

**⚠️ ВАЖНО:** Обсуждение рекомендаций происходит в Фазе 3!

На этом этапе нужно:
- ✅ Собрать все проблемы и рекомендации
- ✅ Создать execution plan с оценками effort/impact
- ✅ Закоммитить результаты

**НЕ нужно:**
- ❌ Обсуждать с пользователем на этом этапе
- ❌ Согласовывать приоритеты
- ❌ Утверждать execution plan

**Причина:** Сначала соберем ВСЮ информацию (Фаза 1 + Фаза 2), затем обсудим все проблемы сразу в Фазе 3.

---

## ⚠️ Важные замечания

1. **Facade проблема известна** - ссылаться на существующий план, не дублировать
2. **Фокус на layering** - критически важно для доменного пакета
3. **Integration quality** - насколько хорошо используются framework пакеты
4. **Pragmatic approach** - если работает и читабельно, возможно не требует изменений
5. **Coordination** - синхронизация с существующими планами критична

---

## ✅ Definition of Done

- [ ] Все 6 категорий проанализированы
- [ ] Создан summary с оценкой SOLID и layering
- [ ] Проблемы приоритизированы (с учетом Facade плана)
- [ ] **Сформированы рекомендации для каждой проблемы**
- [ ] **Создан execution plan с учетом существующих планов**
- [ ] **Проверена синхронизация с plan_architecture_refactoring**
- [ ] **Проведено обсуждение с пользователем**
- [ ] **План согласован и утвержден**
- [ ] Результаты закоммичены в git
- [ ] Обновлен README.md плана со статусом

---

## 🔗 Параллельные этапы

Можно выполнять одновременно:
- **2.1** Framework Packages Analysis
- **2.3** Code Quality Analysis
- **2.4** Test Coverage Analysis
