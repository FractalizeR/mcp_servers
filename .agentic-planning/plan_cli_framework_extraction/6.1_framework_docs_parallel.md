# 6.1 Документация framework/cli

**Тип:** Parallel (можно выполнять одновременно с 6.2 и 6.3)
**Зависимости:** Этап 5 (все)
**Время:** ~1.5 часа

---

## Задача

Создать полную документацию для `@mcp-framework/cli` пакета.

---

## Действия

### 1. Создать README.md

**Файл:** `packages/framework/cli/README.md`

**Структура:**
```markdown
# @mcp-framework/cli

Generic CLI для управления подключениями MCP серверов к различным клиентам.

## 🎯 Возможности

- Универсальный для любого MCP сервера
- Поддержка множества MCP клиентов (Claude Desktop, Claude Code, Codex, Gemini, Qwen)
- Типобезопасность через TypeScript generics
- Декларативная конфигурация промптов
- Безопасное хранение конфигурации (секреты не сохраняются)
- Расширяемость — легко добавить новый клиент

## 📦 Установка

```bash
npm install @mcp-framework/cli
```

## 🚀 Быстрый старт

См. примеры использования для разных сценариев.

## 📚 API

### Типы

- `BaseMCPServerConfig` — базовая конфигурация
- `MCPClientInfo` — информация о клиенте
- `MCPConnector<TConfig>` — интерфейс коннектора
- `ConfigPromptDefinition<TConfig>` — определение промпта

### Классы

- `BaseConnector<TConfig>` — базовый класс для коннекторов
- `ConnectorRegistry<TConfig>` — реестр коннекторов
- `ConfigManager<TConfig>` — менеджер конфигурации
- `InteractivePrompter<TConfig>` — сборщик конфигурации

### Команды

- `connectCommand(options)` — подключение
- `disconnectCommand(options)` — отключение
- `statusCommand(options)` — статус
- `listCommand(options)` — список клиентов

### Утилиты

- `CommandExecutor` — выполнение shell команд
- `FileManager` — работа с файлами
- `Logger` — CLI логирование

## 🎨 Примеры

### Базовый MCP сервер

[Пример кода с комментариями]

### Расширенная конфигурация

[Пример с кастомными промптами]

### Добавление нового клиента

[Пример создания коннектора]

## 🏗️ Архитектура

[Диаграмма компонентов]

## 🔗 См. также

- [API Reference](./API.md)
- [@mcp-framework/infrastructure](../infrastructure/README.md)
- [Yandex Tracker CLI example](../../servers/yandex-tracker/src/cli/README.md)
```

**Соблюдать лимит:** ≤600 строк

### 2. Создать API.md

**Файл:** `packages/framework/cli/API.md`

**Содержание:**
- Полное описание всех public типов
- Сигнатуры всех функций и методов
- Параметры и возвращаемые значения
- Примеры использования для каждого API

### 3. Добавить примеры

**Файл:** `packages/framework/cli/examples/`

Создать несколько примеров:
- `basic-server/` — простой MCP сервер
- `advanced-prompts/` — сложные промпты
- `custom-connector/` — свой коннектор

### 4. Обновить CLAUDE.md проекта

**Файл:** `CLAUDE.md`

**Добавить секцию:**
```markdown
## Framework CLI

- **CLI пакет:** [packages/framework/cli/README.md](packages/framework/cli/README.md)
- Использование в yandex-tracker: [packages/servers/yandex-tracker/src/cli/README.md](...)
```

### 5. Обновить DOCS.md

**Файл:** `DOCS.md`

**Добавить ссылку на CLI:**
```markdown
## 📦 Framework Packages

- **[CLI](./packages/framework/cli/README.md)** — Generic CLI для MCP подключений
- **[Infrastructure](./packages/framework/infrastructure/README.md)** — ...
- ...
```

---

## Результат

- [ ] README.md создан (≤600 строк)
- [ ] API.md создан
- [ ] Примеры добавлены
- [ ] CLAUDE.md обновлен
- [ ] DOCS.md обновлен
- [ ] `npm run validate:docs` проходит

---

## Критерии готовности

- [ ] Документация полная и понятная
- [ ] Примеры рабочие
- [ ] Лимиты размера соблюдены
- [ ] Ссылки корректны
