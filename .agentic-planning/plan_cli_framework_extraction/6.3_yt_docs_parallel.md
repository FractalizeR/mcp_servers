# 6.3 Обновление yandex-tracker документации

**Тип:** Parallel (можно выполнять одновременно с 6.1 и 6.2)
**Зависимости:** Этап 5 (все)
**Время:** ~30 минут

---

## Задача

Обновить документацию yandex-tracker после выноса CLI в framework.

---

## Действия

### 1. Обновить yandex-tracker/src/cli/README.md

**Уже сделано в этапе 5.3, но проверить:**
- Ссылка на framework/cli документацию
- Описание только YT-специфичных компонентов
- Размер ≤600 строк

### 2. Обновить yandex-tracker/README.md

**Секция "CLI":**
```markdown
## 🔌 CLI для подключения к MCP клиентам

Этот пакет использует `@mcp-framework/cli` — универсальный CLI для управления MCP подключениями.

### Установка и использование

См. [src/cli/README.md](./src/cli/README.md) для деталей.

### Быстрый старт

```bash
# Собрать проект
npm run build

# Подключить к MCP клиенту
npm run mcp:connect

# Проверить статус
npm run mcp:status
```

### Конфигурация

Yandex Tracker требует:
- OAuth токен (не сохраняется)
- ID организации (сохраняется)

Подробнее: [Framework CLI](../../framework/cli/README.md)
```

### 3. Обновить yandex-tracker/CLAUDE.md

**Проверить, что правила CLI упоминают framework:**
```markdown
## CLI

- Использует `@mcp-framework/cli` для универсального управления подключениями
- YT-специфичная конфигурация в `src/cli/types.ts` и `src/cli/prompts.ts`
- См. framework/cli для деталей архитектуры
```

### 4. Проверить ARCHITECTURE.md

**Если есть упоминания CLI — обновить:**
```markdown
## CLI Layer

CLI построен на `@mcp-framework/cli` — универсальном framework для MCP подключений.

**YT-специфичные компоненты:**
- `src/cli/types.ts` — YandexTrackerMCPConfig
- `src/cli/prompts.ts` — конфигурация промптов
- `src/cli/bin/mcp-connect.ts` — entry point

См. [packages/framework/cli/README.md](../../../framework/cli/README.md) для деталей.
```

### 5. Проверить размеры документов

```bash
npm run validate:docs
```

**Ожидаемый результат:**
- Все документы в пределах лимитов
- Особенно `src/cli/README.md` должен уменьшиться (было много описания коннекторов)

---

## Результат

- [x] `src/cli/README.md` обновлен и уменьшен (139/600 строк)
- [x] `README.md` обновлен (добавлена секция CLI)
- [x] `CLAUDE.md` обновлен (добавлены правила CLI)
- [x] `ARCHITECTURE.md` обновлен (упоминания CLI корректны)
- [x] `npm run validate:docs` проходит
- [x] Все ссылки корректны

---

## Критерии готовности

- [x] Документация отражает новую архитектуру
- [x] Ссылки на framework/cli добавлены
- [x] Размеры в пределах лимитов
- [x] Документация понятна и полна

---

## ✅ Этап завершен

Все задачи этапа 6.3 выполнены успешно.
