# Phase 1.1: Перемещение Framework пакетов

**Цель:** Переместить infrastructure, core, search в `packages/framework/`

**Ветка:** `claude/migrate-framework-packages-{session-id}`

**Зависимости:** Phase 0.1 завершена

**Может выполняться параллельно с:** 1.2, 1.3, 1.4

**Конфликты:** минимальные (работаем в packages/framework/, другие ветки там не трогают)

---

## ✅ Чек-лист

### 1. Создать ветку

```bash
git checkout main
git pull origin main
git checkout -b claude/migrate-framework-packages-{session-id}
```

### 2. Перемещение пакетов (с сохранением истории git)

- [ ] Переместить infrastructure:
```bash
git mv packages/infrastructure packages/framework/infrastructure
```

- [ ] Переместить core:
```bash
git mv packages/core packages/framework/core
```

- [ ] Переместить search:
```bash
git mv packages/search packages/framework/search
```

### 3. Обновление package.json в каждом пакете

- [ ] `packages/framework/infrastructure/package.json`:
  - ✅ Проверить name: `@mcp-framework/infrastructure` (без изменений)
  - ✅ Обновить пути в scripts если нужно (обычно не нужно)

- [ ] `packages/framework/core/package.json`:
  - ✅ Проверить dependencies на `@mcp-framework/infrastructure` (должны остаться)
  - ✅ Проверить пути в scripts

- [ ] `packages/framework/search/package.json`:
  - ✅ Проверить dependencies на `@mcp-framework/core`
  - ✅ Обновить пути к тестам если используются абсолютные пути

### 4. Обновление tsconfig.json в пакетах

- [ ] `packages/framework/infrastructure/tsconfig.json`:
  - Обновить `extends`: `"../../tsconfig.base.json"` → `"../../../tsconfig.base.json"`
  - Или использовать `../tsconfig.base.json` если создали локальный

- [ ] `packages/framework/core/tsconfig.json`:
  - То же самое для extends

- [ ] `packages/framework/search/tsconfig.json`:
  - То же самое для extends

### 5. Обновление внутренних импортов (если есть абсолютные пути)

- [ ] Проверить все `import` statements в framework пакетах
- [ ] Убедиться что используются npm package names (`@mcp-framework/*`), а не относительные пути между пакетами

### 6. Локальная валидация

- [ ] `npm install` (переустановить симлинки workspace)
- [ ] `npm run build --workspace=@mcp-framework/infrastructure` — успешно
- [ ] `npm run build --workspace=@mcp-framework/core` — успешно
- [ ] `npm run build --workspace=@mcp-framework/search` — успешно
- [ ] `npm run test --workspace=@mcp-framework/infrastructure` — тесты проходят
- [ ] `npm run test --workspace=@mcp-framework/core` — тесты проходят
- [ ] `npm run test --workspace=@mcp-framework/search` — тесты проходят

### 7. Проверка деревьев зависимостей

- [ ] `npm run depcruise` — нет новых ошибок
- [ ] Граф зависимостей сохранен:
  ```
  infrastructure (0 deps)
      ↓
  core (deps: infrastructure)
      ↓
  search (deps: core)
  ```

---

## 📤 Результат этапа

- ✅ Пакеты перемещены в `packages/framework/`
- ✅ Все конфиги обновлены
- ✅ Сборка и тесты проходят
- ✅ История git сохранена (`git mv`)

---

## 🚀 Коммит и пуш

```bash
git add packages/framework
git commit -m "refactor: переместить framework пакеты в packages/framework/

- Переместить infrastructure, core, search
- Обновить tsconfig extends пути
- Сохранить историю git через git mv

Ref: Phase 1.1 migration plan

🤖 Generated with Claude Code
Co-Authored-By: Claude <noreply@anthropic.com>"

git push -u origin claude/migrate-framework-packages-{session-id}
```

**Статус:** ✅ Готово к мерджу (после завершения Phase 2.1)
