# Phase 1.2: Перемещение Server пакета

**Цель:** Переместить yandex-tracker в `packages/servers/` и обновить scope на `@mcp-server/`

**Ветка:** `claude/migrate-server-package-{session-id}`

**Зависимости:** Phase 0.1 завершена

**Может выполняться параллельно с:** 1.1, 1.3, 1.4

**Конфликты:** минимальные (работаем в packages/servers/, другие ветки там не трогают)

---

## ✅ Чек-лист

### 1. Создать ветку

```bash
git checkout main
git pull origin main
git checkout -b claude/migrate-server-package-{session-id}
```

### 2. Перемещение пакета

- [ ] Переместить yandex-tracker с сохранением истории:
```bash
git mv packages/yandex-tracker packages/servers/yandex-tracker
```

### 3. Обновление package.json

**`packages/servers/yandex-tracker/package.json`:**

- [ ] Изменить `name`:
  ```json
  {
    "name": "@mcp-server/yandex-tracker",
    "version": "2.1.0"
  }
  ```

- [ ] Обновить `bin` (пока оставить bundle.js, переименуем в 1.3):
  ```json
  {
    "bin": {
      "mcp-server-yandex-tracker": "./dist/bundle.js",
      "mcp-connect": "./dist/cli/mcp-connect.js"
    }
  }
  ```

- [ ] Проверить dependencies на `@mcp-framework/*` (должны остаться без изменений):
  ```json
  {
    "dependencies": {
      "@mcp-framework/infrastructure": "workspace:*",
      "@mcp-framework/core": "workspace:*",
      "@mcp-framework/search": "workspace:*"
    }
  }
  ```

### 4. Обновление tsconfig.json

- [ ] `packages/servers/yandex-tracker/tsconfig.json`:
  - Обновить `extends`: `"../../tsconfig.base.json"` → `"../../../tsconfig.base.json"`
  - Или использовать `../tsconfig.base.json` если создали локальный в servers/

### 5. Обновление импортов в коде

- [ ] Проверить все импорты используют npm package names:
  ```typescript
  // ✅ Правильно
  import { BaseTool } from '@mcp-framework/core';
  import { HttpClient } from '@mcp-framework/infrastructure';

  // ❌ Неправильно (если есть такие - исправить)
  import { BaseTool } from '../../../core/src/...';
  ```

### 6. Обновление .dependency-cruiser.cjs (для yandex-tracker)

- [ ] Если есть специфичные правила для yandex-tracker, обновить пути:
  - `packages/yandex-tracker` → `packages/servers/yandex-tracker`

### 7. Локальная валидация

- [ ] `npm install` (переустановить симлинки)
- [ ] `npm run build --workspace=@mcp-server/yandex-tracker` — успешно
- [ ] `npm run test --workspace=@mcp-server/yandex-tracker` — тесты проходят
- [ ] `npm run lint --workspace=@mcp-server/yandex-tracker` — без ошибок

### 8. Проверка CLI инструментов

- [ ] Проверить `mcp-connect` работает:
  ```bash
  node packages/servers/yandex-tracker/dist/cli/mcp-connect.js --help
  ```

- [ ] Проверить основной бандл:
  ```bash
  node packages/servers/yandex-tracker/dist/bundle.js
  # (должен запуститься MCP сервер, Ctrl+C для остановки)
  ```

---

## 📤 Результат этапа

- ✅ Пакет перемещен в `packages/servers/yandex-tracker`
- ✅ Scope изменен на `@mcp-server/yandex-tracker`
- ✅ Все зависимости на framework пакеты работают
- ✅ Сборка, тесты, CLI работают
- ✅ История git сохранена

---

## 🚀 Коммит и пуш

```bash
git add packages/servers
git commit -m "refactor: переместить yandex-tracker в packages/servers/

- Переместить пакет в packages/servers/yandex-tracker
- Изменить scope: mcp-server-yandex-tracker → @mcp-server/yandex-tracker
- Обновить tsconfig extends пути
- Сохранить зависимости на @mcp-framework/* пакеты

Ref: Phase 1.2 migration plan

🤖 Generated with Claude Code
Co-Authored-By: Claude <noreply@anthropic.com>"

git push -u origin claude/migrate-server-package-{session-id}
```

**Статус:** ✅ Готово к мерджу (после завершения Phase 2.1)
