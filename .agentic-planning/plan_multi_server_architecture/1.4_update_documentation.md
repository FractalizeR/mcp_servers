# Phase 1.4: Обновление документации

**Цель:** Обновить всю документацию под новую multi-server структуру

**Ветка:** `claude/update-docs-multi-server-{session-id}`

**Зависимости:** Phase 0.1 завершена

**Может выполняться параллельно с:** 1.1, 1.2, 1.3

**Конфликты:** минимальные (работаем с разными .md файлами)

---

## ✅ Чек-лист

### 1. Создать ветку

```bash
git checkout main
git pull origin main
git checkout -b claude/update-docs-multi-server-{session-id}
```

### 2. Обновить корневой README.md

**`README.md`:**

- [ ] Обновить секцию "Структура проекта":

```markdown
## Структура проекта

```
packages/
├── framework/              → MCP Framework (переиспользуемые пакеты)
│   ├── infrastructure/     → @mcp-framework/infrastructure
│   │   └── HTTP, cache, logging, async utilities
│   ├── core/              → @mcp-framework/core
│   │   └── BaseTool, registry, type system
│   └── search/            → @mcp-framework/search
│       └── Tool Search Engine (compile-time indexing)
└── servers/               → MCP Servers (приложения)
    └── yandex-tracker/    → @mcp-server/yandex-tracker
        └── Yandex.Tracker API integration
```
```

- [ ] Обновить секцию "Быстрый старт" если упоминается имя пакета:
  - `mcp-server-yandex-tracker` → `@mcp-server/yandex-tracker`

### 3. Обновить ARCHITECTURE.md

**`ARCHITECTURE.md`:**

- [ ] Обновить секцию "Организация monorepo":

```markdown
## Организация monorepo

### Категории пакетов

**Framework (packages/framework/):**
- Переиспользуемые, универсальные компоненты
- Могут использоваться в любых MCP серверах
- НЕ зависят от доменной логики

**Servers (packages/servers/):**
- Готовые MCP серверы для конкретных API
- Используют framework пакеты
- Содержат доменную логику и интеграции

### Граф зависимостей

```
packages/framework/infrastructure (0 deps)
    ↓
packages/framework/core
    ↓
packages/framework/search
    ↓
packages/servers/yandex-tracker (зависит от всех framework)
```
```

- [ ] Обновить диаграммы если упоминаются старые пути
- [ ] Обновить секцию "Масштабирование" — добавить про новые серверы:

```markdown
## Добавление новых MCP серверов

Создание нового сервера (например, GitHub или Jira):

1. Создать `packages/servers/{server-name}/`
2. Использовать `yandex-tracker` как шаблон структуры
3. Добавить зависимости на `@mcp-framework/*` пакеты
4. Использовать `tsup.config.base.ts` для build конфигурации
5. Опубликовать как `@mcp-server/{server-name}`

Каждый сервер получает:
- Отдельный бандл: `dist/{server-name}.bundle.js`
- Независимое версионирование
- Общие framework возможности
```

### 4. Обновить CLAUDE.md

**`CLAUDE.md`:**

- [ ] Обновить секцию "📦 Структура monorepo" (аналогично README.md)
- [ ] Обновить секцию "Граф зависимостей":
  ```
  packages/framework/infrastructure (0 deps)
      ↓
  packages/framework/core
      ↓
  packages/framework/search
      ↓
  packages/servers/* (любой сервер может зависеть от framework)
  ```

- [ ] Обновить примеры импортов (если есть):
  - Проверить что используются `@mcp-framework/*` и `@mcp-server/*`

- [ ] Обновить секцию "Работа с конкретными компонентами":

```markdown
**Framework пакеты (packages/framework/):**
- Универсальные, переиспользуемые компоненты
- НЕ должны зависеть от доменной логики серверов
- См. README.md в каждом пакете для API

**MCP Servers (packages/servers/):**
- Готовые серверы для конкретных API
- Yandex Tracker: [packages/servers/yandex-tracker/CLAUDE.md](packages/servers/yandex-tracker/CLAUDE.md)
- (будущие: GitHub, Jira, etc.)
```

### 5. Обновить DOCS.md

**`DOCS.md`:**

- [ ] Обновить навигацию с новыми путями:
  - `packages/yandex-tracker/` → `packages/servers/yandex-tracker/`
  - Добавить секцию "Framework пакеты"
  - Добавить секцию "MCP Servers"

```markdown
## 🔧 Framework пакеты

| Файл | Назначение |
|------|-----------|
| **[packages/framework/infrastructure/README.md](packages/framework/infrastructure/README.md)** | HTTP, cache, logging, async |
| **[packages/framework/core/README.md](packages/framework/core/README.md)** | BaseTool, registry, types |
| **[packages/framework/search/README.md](packages/framework/search/README.md)** | Tool Search Engine |

## 🖥️ MCP Servers

| Файл | Назначение |
|------|-----------|
| **[packages/servers/yandex-tracker/CLAUDE.md](packages/servers/yandex-tracker/CLAUDE.md)** | Yandex.Tracker MCP Server (главный файл) |
| **[packages/servers/yandex-tracker/README.md](packages/servers/yandex-tracker/README.md)** | Установка и использование |
```

### 6. Обновить package-specific документацию

**`packages/servers/yandex-tracker/CLAUDE.md`:**

- [ ] Обновить путь к корневой документации:
  - `../../CLAUDE.md` → `../../../CLAUDE.md` (если используются относительные ссылки)

- [ ] Проверить все ссылки на framework пакеты (должны остаться корректными)

**Framework пакеты README.md:**

- [ ] Проверить что все ссылки на другие пакеты корректны
- [ ] Обновить примеры импортов если нужно

### 7. Создать packages/servers/README.md (если не создан в 1.3)

- [ ] См. Phase 1.3, шаг 6 для содержимого

### 8. Обновить GitHub templates (если есть)

- [ ] `.github/PULL_REQUEST_TEMPLATE.md` — обновить структуру если упоминается
- [ ] `.github/ISSUE_TEMPLATE/` — проверить примеры путей

### 9. Валидация документации

- [ ] Проверить все ссылки работают (относительные пути корректны)
- [ ] `npm run validate:docs` — если есть такая команда
- [ ] Проверить размеры файлов не превышают лимиты (см. CLAUDE.md)

---

## 📤 Результат этапа

- ✅ Вся документация обновлена под новую структуру
- ✅ Все ссылки корректны
- ✅ Диаграммы и примеры актуальны
- ✅ Новая структура документирована для будущих разработчиков

---

## 🚀 Коммит и пуш

```bash
git add *.md packages/**/*.md .github/
git commit -m "docs: обновить документацию для multi-server архитектуры

- Обновить структуру в README, ARCHITECTURE, CLAUDE
- Обновить граф зависимостей
- Добавить инструкции по созданию новых серверов
- Исправить все ссылки и пути

Ref: Phase 1.4 migration plan

🤖 Generated with Claude Code
Co-Authored-By: Claude <noreply@anthropic.com>"

git push -u origin claude/update-docs-multi-server-{session-id}
```

**Статус:** ✅ Готово к мерджу (после завершения Phase 2.1)
