# Phase 2.1: Интеграция и валидация

**Цель:** Смёрджить все параллельные ветки и провести интеграционное тестирование

**Ветка:** `main` (или `claude/integration-multi-server-{session-id}` для проверки перед финальным мерджем)

**Зависимости:** Phase 1.1, 1.2, 1.3, 1.4 завершены

**Блокирует:** Phase 2.2, 2.3

---

## ✅ Чек-лист

### 1. Подготовка к мерджу

- [ ] Убедиться что все ветки Phase 1 запушены:
  ```bash
  git branch -r | grep claude/migrate
  # Должны быть:
  # - claude/migrate-framework-packages-XXX
  # - claude/migrate-server-package-XXX
  # - claude/prepare-build-configs-XXX
  # - claude/update-docs-multi-server-XXX
  ```

- [ ] Создать интеграционную ветку:
  ```bash
  git checkout main
  git pull origin main
  git checkout -b claude/integration-multi-server-{session-id}
  ```

### 2. Последовательный мердж веток

**Порядок важен для минимизации конфликтов!**

- [ ] **Шаг 1:** Мердж framework packages (наименьший риск конфликтов):
  ```bash
  git merge origin/claude/migrate-framework-packages-XXX
  # Если конфликты — разрешить, затем:
  git add .
  git commit -m "Merge: framework packages migration"
  ```

- [ ] **Шаг 2:** Мердж server package:
  ```bash
  git merge origin/claude/migrate-server-package-XXX
  # Возможные конфликты: package.json (workspaces)
  # Разрешение: взять оба workspaces пути (старый + новый временно)
  ```

- [ ] **Шаг 3:** Мердж build configs:
  ```bash
  git merge origin/claude/prepare-build-configs-XXX
  # Возможные конфликты: packages/servers/yandex-tracker/package.json (bin section)
  # Разрешение: взять версию из build configs ветки (yandex-tracker.bundle.js)
  ```

- [ ] **Шаг 4:** Мердж documentation:
  ```bash
  git merge origin/claude/update-docs-multi-server-XXX
  # Обычно без конфликтов (разные файлы)
  ```

### 3. Разрешение известных конфликтов

**Конфликт в корневом `package.json` (workspaces):**

```json
{
  "workspaces": [
    "packages/framework/*",
    "packages/servers/*"
  ]
}
```

Старые пути временно НЕ удаляем (для обратной совместимости CI).

**Конфликт в `packages/servers/yandex-tracker/package.json` (bin):**

```json
{
  "bin": {
    "mcp-server-yandex-tracker": "./dist/yandex-tracker.bundle.js",
    "mcp-connect": "./dist/cli/mcp-connect.js"
  }
}
```

### 4. Обновление оставшихся конфигов

**Удаление старых workspace путей:**

- [ ] Корневой `package.json` — оставить только новые пути:
  ```json
  {
    "workspaces": [
      "packages/framework/*",
      "packages/servers/*"
    ]
  }
  ```

**Обновление `.dependency-cruiser.cjs`:**

- [ ] Удалить временные правила для старых путей
- [ ] Финализировать правила для новой структуры:

```javascript
{
  forbidden: [
    {
      name: 'framework-no-server-deps',
      comment: 'Framework пакеты не должны зависеть от серверов',
      severity: 'error',
      from: { path: '^packages/framework/' },
      to: { path: '^packages/servers/' }
    },
    {
      name: 'infrastructure-is-base',
      comment: 'Infrastructure не зависит ни от чего (кроме node_modules)',
      severity: 'error',
      from: { path: '^packages/framework/infrastructure/' },
      to: {
        path: '^packages/',
        pathNot: '^packages/framework/infrastructure/'
      }
    },
    // ... остальные правила
  ]
}
```

### 5. Проверка и обновление корневых scripts

**Проверить корневой `package.json` на старые пути:**

- [ ] Найти упоминания старых путей в scripts:
```bash
cat package.json | grep -A 1 '"scripts"' | grep -i "packages/" || echo "OK"
```

- [ ] Если есть scripts вроде:
  ```json
  {
    "test:infra": "npm run test --workspace=packages/infrastructure"
  }
  ```

  Обновить на новые пути (или удалить если не нужны):
  ```json
  {
    "test:infra": "npm run test --workspace=@mcp-framework/infrastructure"
  }
  ```

### 6. Полная переустановка зависимостей

**Стратегия без удаления package-lock.json (безопаснее):**

- [ ] Очистить старые артефакты и node_modules:
  ```bash
  npm run clean
  rm -rf node_modules packages/*/node_modules packages/framework/*/node_modules packages/servers/*/node_modules
  ```

- [ ] Переустановить с force (обновит симлинки):
  ```bash
  npm ci --force
  # Или если package-lock устарел:
  # npm install
  ```

- [ ] Если возникают проблемы с симлинками, ТОГДА удалить lock и переустановить:
  ```bash
  rm -f package-lock.json
  npm install
  ```

  **⚠️ Важно:** После этого закоммитить новый package-lock.json отдельным коммитом:
  ```bash
  git add package-lock.json
  git commit -m "chore: обновить package-lock.json после реструктуризации"
  ```

- [ ] Проверить симлинки созданы корректно:
  ```bash
  ls -la node_modules/@mcp-framework/
  ls -la node_modules/@mcp-server/
  ```

### 7. Полная сборка

- [ ] Собрать все пакеты:
  ```bash
  npm run build
  ```

- [ ] Проверить созданы файлы:
  ```bash
  # Framework packages
  ls -la packages/framework/infrastructure/dist/
  ls -la packages/framework/core/dist/
  ls -la packages/framework/search/dist/

  # Server package
  ls -la packages/servers/yandex-tracker/dist/
  # Должны быть:
  # - yandex-tracker.bundle.js (новое имя!)
  # - cli/mcp-connect.js
  ```

### 8. Запуск всех тестов

- [ ] Тесты framework пакетов:
  ```bash
  npm run test --workspace=@mcp-framework/infrastructure
  npm run test --workspace=@mcp-framework/core
  npm run test --workspace=@mcp-framework/search
  ```

- [ ] Тесты server пакета:
  ```bash
  npm run test --workspace=@mcp-server/yandex-tracker
  ```

- [ ] Интеграционные тесты (если есть):
  ```bash
  npm run test:integration
  ```

- [ ] Сравнить метрики до/после миграции:
  ```bash
  # Сохранить метрики после
  find packages/ -type f | wc -l > /tmp/migration-files-after.txt
  npm run test 2>&1 | grep -E "Test Files|Tests" | tee /tmp/migration-tests-after.txt
  ls -la packages/ > /tmp/migration-structure-after.txt

  # Сравнить количество файлов
  echo "=== Количество файлов ==="
  echo "До:  $(cat /tmp/migration-files-before.txt)"
  echo "После: $(cat /tmp/migration-files-after.txt)"
  diff /tmp/migration-files-before.txt /tmp/migration-files-after.txt && echo "✅ OK" || echo "⚠️ Изменилось - проверь что ничего не потеряно!"

  # Сравнить количество тестов
  echo ""
  echo "=== Количество тестов ==="
  diff /tmp/migration-tests-before.txt /tmp/migration-tests-after.txt && echo "✅ OK - тесты сохранены" || echo "⚠️ ПРОВЕРЬ: количество тестов изменилось!"

  # Показать новую структуру
  echo ""
  echo "=== Новая структура ==="
  diff /tmp/migration-structure-before.txt /tmp/migration-structure-after.txt || echo "Структура изменена (ожидаемо)"
  ```

### 9. Валидация архитектуры

- [ ] Проверка графа зависимостей:
  ```bash
  npm run depcruise
  # Должно быть 0 ошибок
  ```

- [ ] Проверка качества кода:
  ```bash
  npm run quality
  ```

- [ ] Полная валидация:
  ```bash
  npm run validate
  ```

### 10. Функциональная проверка

- [ ] Проверить MCP сервер запускается:
  ```bash
  node packages/servers/yandex-tracker/dist/yandex-tracker.bundle.js
  # Должен вывести MCP приветствие и ждать stdin
  # Ctrl+C для остановки
  ```

- [ ] Проверить CLI инструмент:
  ```bash
  node packages/servers/yandex-tracker/dist/cli/mcp-connect.js --help
  # Должна вывести справку
  ```

- [ ] Проверить установку через npm (локально):
  ```bash
  cd /tmp
  npm pack /home/user/mcp_server_yandex_tracker/packages/servers/yandex-tracker
  npm install -g ./mcp-server-yandex-tracker-*.tgz
  mcp-server-yandex-tracker --version
  npm uninstall -g @mcp-server/yandex-tracker
  ```

### 11. Проверка документации

- [ ] Все ссылки в README.md работают
- [ ] Примеры в ARCHITECTURE.md актуальны
- [ ] DOCS.md содержит корректные пути

---

## 📤 Результат этапа

- ✅ Все ветки смёржены без конфликтов
- ✅ Workspace корректно настроен
- ✅ Все пакеты собираются
- ✅ Все тесты проходят
- ✅ Валидация успешна
- ✅ MCP сервер работает
- ✅ Документация актуальна

---

## 🚀 Коммит и пуш

```bash
git add .
git commit -m "chore: интеграция multi-server архитектуры

Объединены ветки:
- migrate-framework-packages: перемещение в packages/framework/
- migrate-server-package: перемещение в packages/servers/
- prepare-build-configs: именованные бандлы
- update-docs: актуализация документации

Изменения:
- Новая структура: framework/ и servers/ категории
- Scope изменен: @mcp-server/yandex-tracker
- Именованный бандл: yandex-tracker.bundle.js
- Обновлены dependency rules
- Вся документация актуализирована

✅ Все тесты проходят
✅ Валидация успешна
✅ MCP сервер работает

Ref: Phase 2.1 integration plan

🤖 Generated with Claude Code
Co-Authored-By: Claude <noreply@anthropic.com>"

git push -u origin claude/integration-multi-server-{session-id}
```

**Следующий шаг:** Phase 2.2 — обновление CI/CD
