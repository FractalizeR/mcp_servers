# 2.4 Добавление отсутствующих файлов (PARALLEL)

**Приоритет:** 🟡 Средний
**Режим выполнения:** ПАРАЛЛЕЛЬНО (можно выполнять отдельным агентом)
**Расчетное время:** 20-30 минут

---

## Цель

Добавить важные конфигурационные файлы, которые отсутствуют в проекте:
- .editorconfig (уже создан в 1.0)
- .nvmrc (уже создан в 1.0)
- GitHub Actions workflows (опционально)
- VSCode settings (для consistency)

---

## Задачи

### 2.4.1 ✅ .editorconfig и .nvmrc

**Статус:** Уже созданы в этапе 1.0

**Проверка:**
```bash
ls -la .editorconfig .nvmrc
```

### 2.4.2 Создать .vscode/settings.json (рекомендуемые настройки)

**Создать файл:** `.vscode/settings.json`

```json
{
  "editor.formatOnSave": true,
  "editor.defaultFormatter": "esbenp.prettier-vscode",
  "editor.codeActionsOnSave": {
    "source.fixAll.eslint": "explicit",
    "source.organizeImports": "never"
  },
  "typescript.tsdk": "node_modules/typescript/lib",
  "typescript.enablePromptUseWorkspaceTsdk": true,
  "eslint.validate": ["javascript", "typescript"],
  "eslint.workingDirectories": [
    { "mode": "auto" }
  ],
  "files.associations": {
    "*.json": "jsonc"
  },
  "search.exclude": {
    "**/node_modules": true,
    "**/dist": true,
    "**/coverage": true,
    "**/.git": true,
    "**/jscpd-report": true
  },
  "files.exclude": {
    "**/.git": true,
    "**/.DS_Store": true,
    "**/Thumbs.db": true
  },
  "vitest.enable": true,
  "vitest.commandLine": "npx vitest",
  "vitest.include": ["**/*.{test,spec}.{ts,js}"]
}
```

**Обоснование:**
- Консистентность настроек между разработчиками
- Автоматическое форматирование при сохранении
- Правильная интеграция ESLint + Prettier
- Поддержка Vitest в VSCode

**Проверка:**
- Открыть проект в VSCode
- Должно появиться предложение использовать workspace TypeScript

### 2.4.3 Создать .vscode/extensions.json (рекомендуемые расширения)

**Создать файл:** `.vscode/extensions.json`

```json
{
  "recommendations": [
    "dbaeumer.vscode-eslint",
    "esbenp.prettier-vscode",
    "editorconfig.editorconfig",
    "vitest.explorer",
    "orta.vscode-jest",
    "ms-vscode.vscode-typescript-next",
    "usernamehw.errorlens",
    "streetsidesoftware.code-spell-checker",
    "eamodio.gitlens"
  ],
  "unwantedRecommendations": []
}
```

**Обоснование:**
- Новые разработчики сразу получат нужные расширения
- Консистентность IDE experience
- Улучшенная productivity

### 2.4.4 Обновить .gitignore (добавить .vscode исключения)

**Проблема:** `.gitignore` исключает весь `.vscode/`

**Решение:** Изменить правило:

```gitignore
# IDE
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
!.vscode/launch.json
!.vscode/tasks.json
.idea/
*.swp
*.swo
*~
```

**Обоснование:**
- Shared settings коммитятся
- Personal settings (.vscode/*.local.json) игнорируются
- Best practice для team projects

**Проверка:**
```bash
git status .vscode/
```

### 2.4.5 Создать .vscode/launch.json (debug конфигурация)

**Создать файл:** `.vscode/launch.json`

```json
{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug Current Test File",
      "type": "node",
      "request": "launch",
      "runtimeExecutable": "npm",
      "runtimeArgs": ["run", "test", "--", "${relativeFile}"],
      "console": "integratedTerminal",
      "internalConsoleOptions": "neverOpen"
    },
    {
      "name": "Debug Vitest (Current File)",
      "type": "node",
      "request": "launch",
      "runtimeExecutable": "npx",
      "runtimeArgs": ["vitest", "run", "${relativeFile}"],
      "console": "integratedTerminal",
      "internalConsoleOptions": "neverOpen",
      "sourceMaps": true
    },
    {
      "name": "Debug MCP Server (Yandex Tracker)",
      "type": "node",
      "request": "launch",
      "program": "${workspaceFolder}/packages/servers/yandex-tracker/dist/yandex-tracker.bundle.js",
      "preLaunchTask": "npm: build - packages/servers/yandex-tracker",
      "console": "integratedTerminal",
      "internalConsoleOptions": "neverOpen"
    }
  ]
}
```

**Обоснование:**
- Упрощает debugging тестов
- Debugging MCP server из VSCode
- Standard VSCode workflow

### 2.4.6 (Опционально) GitHub Actions workflow

**Создать файл:** `.github/workflows/ci.yml`

```yaml
name: CI

on:
  push:
    branches: [main, master, develop]
  pull_request:
    branches: [main, master, develop]

jobs:
  validate:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [22.x]

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Type check
        run: npm run typecheck

      - name: Test
        run: npm run test:coverage

      - name: Code quality
        run: npm run quality

      - name: Upload coverage
        uses: codecov/codecov-action@v4
        if: matrix.node-version == '22.x'
        with:
          files: ./coverage/lcov.info
          flags: unittests
          name: codecov-umbrella
          fail_ci_if_error: false

  security:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22.x
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Security audit
        run: npm run validate:security
```

**Обоснование:**
- Автоматическая валидация на каждый push/PR
- Security checks
- Code coverage tracking
- Best practice для open source

**ВАЖНО:** Этот файл опционален - создавать только если проект на GitHub и нужен CI/CD

---

## Чек-лист выполнения

- [x] 2.4.1 Проверить .editorconfig и .nvmrc (уже созданы)
- [x] 2.4.2 ~~Создать .vscode/settings.json~~ ПРОПУЩЕНО (VSCode не используется)
- [x] 2.4.3 ~~Создать .vscode/extensions.json~~ ПРОПУЩЕНО (VSCode не используется)
- [x] 2.4.4 ~~Обновить .gitignore для .vscode~~ ПРОПУЩЕНО (VSCode не используется)
- [x] 2.4.5 ~~Создать .vscode/launch.json~~ ПРОПУЩЕНО (VSCode не используется)
- [x] 2.4.6 ~~Создать .github/workflows/ci.yml~~ УЖЕ СУЩЕСТВУЕТ (.github/workflows/ci.yml, publish.yml, release.yml)
- [x] ~~Проверить работу в VSCode~~ ПРОПУЩЕНО
- [x] ~~Запустить `git status`~~ НЕ ТРЕБУЕТСЯ
- [x] Закоммитить изменения в план (коммит 5dd1e1c)

**Выполнено:** 2025-11-19
**Коммит:** 5dd1e1c
**Ветка:** claude/project-config-improvements-01AdkJQYuVcRCPHFAAADVvci

**Решение:** Этап 2.4 завершен. VSCode конфигурация не нужна, GitHub Actions уже настроен.

---

## Проблемы и решения

**Проблема:** .vscode/settings.json может конфликтовать с личными настройками
**Решение:** Использовать .vscode/settings.local.json для personal overrides

**Проблема:** Расширения могут не подходить всем разработчикам
**Решение:** Это рекомендации, не требования - можно игнорировать

**Проблема:** GitHub Actions может быть ненужным
**Решение:** Пропустить этап 2.4.6 если проект не на GitHub или не нужен CI/CD

**Проблема:** Debug конфигурация может не работать
**Решение:** Проверить пути в launch.json и preLaunchTask
