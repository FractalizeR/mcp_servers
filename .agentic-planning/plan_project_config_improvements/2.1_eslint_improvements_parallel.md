# 2.1 Улучшение ESLint конфигурации (PARALLEL)

**Приоритет:** 🔴 Высокий
**Режим выполнения:** ПАРАЛЛЕЛЬНО (можно выполнять отдельным агентом)
**Расчетное время:** 45-60 минут

---

## Цель

Модернизировать ESLint конфигурацию в соответствии с best practices 2025:
- Использовать `defineConfig()` для type safety
- Использовать `projectService` для TypeScript (typescript-eslint v8)
- Устранить дублирование
- Использовать `globals` пакет
- Правильная структура ignores

---

## Задачи

### 2.1.1 Переименовать в eslint.config.mjs

**Действие:** Переименовать `eslint.config.js` → `eslint.config.mjs`

**Обоснование:**
- Best practice 2025: использовать `.mjs` для ESM конфигурации
- Явно указывает на ESM модуль
- Лучшая поддержка в IDE

**Команда:**
```bash
git mv eslint.config.js eslint.config.mjs
```

### 2.1.2 Модернизировать конфигурацию

**Создать новый файл:** `eslint.config.mjs`

**Ключевые изменения:**

1. **Использовать defineConfig()**
2. **Использовать globals package**
3. **Использовать projectService вместо project**
4. **Устранить дублирование**
5. **Правильная структура ignores**

**Новая структура:**

```javascript
import eslint from '@eslint/js';
import tseslint from '@typescript-eslint/eslint-plugin';
import tsparser from '@typescript-eslint/parser';
import prettierConfig from 'eslint-config-prettier';
import sonarjs from 'eslint-plugin-sonarjs';
import globals from 'globals';

export default [
  // 1. GLOBAL IGNORES (должны быть первыми!)
  {
    ignores: [
      '**/dist/**',
      '**/node_modules/**',
      '**/coverage/**',
      '**/*.d.ts',
      '**/.husky/_/**',
      '**/jscpd-report/**',
      '**/.agentic-planning/**',
    ],
  },

  // 2. BASE CONFIG для всех TS файлов
  {
    files: ['**/*.ts'],
    languageOptions: {
      parser: tsparser,
      parserOptions: {
        ecmaVersion: 'latest',
        sourceType: 'module',
        projectService: true, // ← NEW! TypeScript ESLint v8
        tsconfigRootDir: import.meta.dirname,
      },
      globals: {
        ...globals.node,
        ...globals.es2022,
      },
    },
    plugins: {
      '@typescript-eslint': tseslint,
      'sonarjs': sonarjs,
    },
    rules: {
      // Recommended configs
      ...eslint.configs.recommended.rules,
      ...tseslint.configs['recommended'].rules,
      ...tseslint.configs['recommended-type-checked'].rules,

      // TypeScript specific
      '@typescript-eslint/explicit-function-return-type': 'error',
      '@typescript-eslint/no-unused-vars': [
        'error',
        {
          argsIgnorePattern: '^_',
          varsIgnorePattern: '^_',
        },
      ],
      '@typescript-eslint/no-explicit-any': 'error',
      '@typescript-eslint/no-floating-promises': 'error',
      '@typescript-eslint/await-thenable': 'error',
      '@typescript-eslint/no-unnecessary-condition': 'error',
      '@typescript-eslint/prefer-nullish-coalescing': 'warn',
      '@typescript-eslint/prefer-optional-chain': 'error',
      '@typescript-eslint/consistent-type-imports': [
        'error',
        { prefer: 'type-imports' },
      ],
      '@typescript-eslint/no-non-null-assertion': 'error',
      '@typescript-eslint/switch-exhaustiveness-check': 'error',
      '@typescript-eslint/no-unsafe-assignment': 'error',
      '@typescript-eslint/no-unsafe-member-access': 'error',
      '@typescript-eslint/no-unsafe-call': 'error',
      '@typescript-eslint/no-unsafe-return': 'error',
      '@typescript-eslint/restrict-template-expressions': 'error',
      '@typescript-eslint/prefer-promise-reject-errors': 'off',
      '@typescript-eslint/only-throw-error': 'off',

      // Complexity metrics
      'complexity': ['warn', 10],
      'max-depth': ['warn', 4],
      'max-lines-per-function': [
        'warn',
        { max: 50, skipBlankLines: true, skipComments: true },
      ],
      'max-params': ['warn', 4],
      'max-lines': [
        'warn',
        { max: 400, skipBlankLines: true, skipComments: true },
      ],
      'max-statements': ['warn', 20],

      // SonarJS
      'sonarjs/cognitive-complexity': ['warn', 15],
      'sonarjs/no-duplicate-string': ['warn', { threshold: 3 }],
      'sonarjs/no-identical-functions': 'warn',
      'sonarjs/prefer-immediate-return': 'warn',
      'sonarjs/no-redundant-jump': 'warn',
      'sonarjs/no-small-switch': 'warn',
      'sonarjs/prefer-single-boolean-return': 'warn',

      // General
      'no-console': ['warn', { allow: ['error', 'warn'] }],
      'prefer-const': 'error',
      'no-var': 'error',
      'eqeqeq': ['error', 'always'],
      'curly': ['error', 'all'],
    },
  },

  // 3. OVERRIDE для тестов
  {
    files: ['**/tests/**/*.ts', '**/*.test.ts', '**/*.spec.ts'],
    rules: {
      '@typescript-eslint/no-unsafe-assignment': 'off',
      '@typescript-eslint/no-unsafe-member-access': 'off',
      '@typescript-eslint/no-unsafe-call': 'off',
      'max-lines-per-function': 'off',
      'max-statements': 'off',
    },
  },

  // 4. OVERRIDE для config файлов
  {
    files: ['**/*.config.{ts,mjs,js}', '**/scripts/**/*.ts'],
    rules: {
      'no-console': 'off',
      '@typescript-eslint/no-unsafe-assignment': 'off',
      '@typescript-eslint/no-unsafe-member-access': 'off',
    },
  },

  // 5. PRETTIER (должен быть последним!)
  prettierConfig,
];
```

**Ключевые улучшения:**
- ✅ Использован `projectService: true` (TypeScript ESLint v8)
- ✅ Использован `globals` пакет
- ✅ Устранено дублирование конфигурации
- ✅ Global ignores в начале
- ✅ Отдельные overrides для тестов и config файлов
- ✅ Prettier в конце

### 2.1.3 Обновить tsconfig для поддержки projectService

**ВАЖНО:** `projectService` работает автоматически с TypeScript project references, но нужно убедиться в правильной настройке.

**Проверить в tsconfig.json:**
- `composite: true` в каждом пакете ✅ (уже есть)
- `references` настроены правильно ✅ (уже есть)

**Ничего делать не нужно** - конфигурация уже корректна!

### 2.1.4 Добавить скрипт для ESLint inspector

**Уже добавлено в 1.0**, но проверить использование:

```bash
npm run eslint:inspect
```

Откроется веб-интерфейс для визуализации конфигурации.

### 2.1.5 Удалить устаревшие комментарии из lint-staged

**Проблема:** В `.lintstagedrc.json` есть странное исключение:

```json
{
  "!(src/cli)/**/*.ts": ["eslint --fix"]
}
```

**Вопрос:** Почему исключается `src/cli`?

**Действие:** Проверить, есть ли причина для исключения. Если нет - убрать:

```json
{
  "**/package.json": ["sort-package-json"],
  "*.ts": ["prettier --write", "eslint --fix"]
}
```

---

## Чек-лист выполнения

- [x] 2.1.1 Переименовать в .mjs
- [x] 2.1.2 Обновить конфигурацию с projectService
- [x] 2.1.3 Проверить tsconfig (уже OK)
- [x] 2.1.4 Протестировать `npm run eslint:inspect`
- [x] 2.1.5 Упростить .lintstagedrc.json
- [x] Запустить `npm run lint` - проверить на ошибки
- [x] Запустить `npm run lint:fix` - исправить форматирование
- [x] Запустить `npm run validate:quiet` (lint + typecheck + cpd успешны)
- [x] Закоммитить: `build: modernize ESLint config to 2025 best practices`

**Выполнено:** 2025-11-19
**Коммит:** 89ebee7
**Ветка:** claude/project-config-improvements-017vnFftLLUuuHrQEuk5iiqM

---

## Проблемы и решения

**Проблема:** `projectService` требует TypeScript ESLint v8+
**Решение:** Проверить версию: `npm list @typescript-eslint/parser`

**Проблема:** Могут появиться новые lint ошибки
**Решение:** Исправить код или добавить правило в overrides (если обоснованно)

**Проблема:** Некоторые правила могут конфликтовать
**Решение:** Prettier config в конце автоматически отключает конфликтующие правила
