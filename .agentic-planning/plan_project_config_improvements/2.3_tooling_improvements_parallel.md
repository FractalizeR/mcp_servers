# 2.3 Улучшение инструментов качества кода (PARALLEL)

**Приоритет:** 🟡 Средний
**Режим выполнения:** ПАРАЛЛЕЛЬНО (можно выполнять отдельным агентом)
**Расчетное время:** 30-45 минут

---

## Цель

Улучшить конфигурацию инструментов качества кода:
- Knip: добавить workspace support
- jscpd: ужесточить порог дублирования
- Prettier: расширить конфигурацию
- Добавить полезные скрипты для автоматизации

---

## Задачи

### 2.3.1 Модернизировать knip.json для monorepo

**Проблема:** Текущий `knip.json` не использует workspaces и имеет слишком много ignoreDependencies

**Заменить содержимое:** `knip.json`

```json
{
  "$schema": "https://unpkg.com/knip@5/schema.json",
  "workspaces": {
    ".": {
      "entry": ["scripts/**/*.ts"],
      "project": ["scripts/**/*.ts"]
    },
    "packages/framework/infrastructure": {
      "entry": ["src/index.ts"],
      "project": ["src/**/*.ts"]
    },
    "packages/framework/core": {
      "entry": ["src/index.ts"],
      "project": ["src/**/*.ts"]
    },
    "packages/framework/search": {
      "entry": ["src/index.ts"],
      "project": ["src/**/*.ts"]
    },
    "packages/servers/yandex-tracker": {
      "entry": ["src/index.ts", "src/cli/bin/*.ts"],
      "project": ["src/**/*.ts"]
    }
  },
  "ignore": [
    "**/*.test.ts",
    "**/*.spec.ts",
    "**/tests/**",
    "**/dist/**",
    "**/*.d.ts"
  ],
  "ignoreDependencies": [
    "@vitest/coverage-v8",
    "@vitest/ui",
    "vitest",
    "tsx",
    "@anthropic-ai/mcpb",
    "tsup",
    "esbuild"
  ],
  "ignoreExportsUsedInFile": true,
  "includeEntryExports": false
}
```

**Ключевые изменения:**
- ✅ Workspaces для каждого пакета
- ✅ Сокращен список ignoreDependencies (оставлены только dev tools)
- ✅ Явные entry points для каждого пакета

**Обоснование:**
- Более точное обнаружение неиспользуемого кода
- Лучшая поддержка monorepo
- Меньше false positives

**Проверка:**
```bash
npm run knip
```

### 2.3.2 Ужесточить порог jscpd

**Проблема:** Порог 5% слишком высокий

**Обновить:** `.jscpd.json`

```json
{
  "$schema": "https://raw.githubusercontent.com/kucherenko/jscpd/master/packages/jscpd/schema.json",
  "threshold": 3,
  "reporters": ["console", "html"],
  "ignore": [
    "**/node_modules/**",
    "**/dist/**",
    "**/*.test.ts",
    "**/*.spec.ts",
    "**/tests/**",
    "**/.agentic-planning/**",
    "**/coverage/**"
  ],
  "absolute": true,
  "gitignore": true,
  "format": ["typescript", "javascript"],
  "minLines": 5,
  "minTokens": 50,
  "output": "./jscpd-report",
  "exitCode": 1
}
```

**Ключевые изменения:**
- ✅ `threshold: 5` → `threshold: 3` (более строго)
- ✅ Добавлен `exitCode: 1` для CI/CD

**Обоснование:**
- 3% - industry standard для quality code
- exitCode обеспечивает fail в CI при превышении

**Проверка:**
```bash
npm run cpd
```

### 2.3.3 Расширить Prettier конфигурацию

**Обновить:** `.prettierrc.json`

```json
{
  "semi": true,
  "trailingComma": "es5",
  "singleQuote": true,
  "printWidth": 100,
  "tabWidth": 2,
  "arrowParens": "always",
  "endOfLine": "lf",
  "useTabs": false,
  "bracketSpacing": true,
  "bracketSameLine": false,
  "quoteProps": "as-needed",
  "proseWrap": "preserve",
  "htmlWhitespaceSensitivity": "css",
  "embeddedLanguageFormatting": "auto"
}
```

**Ключевые добавления:**
- `useTabs: false` - явно указываем spaces
- `bracketSpacing: true` - пробелы в объектах
- `bracketSameLine: false` - закрывающий тег на новой строке
- `quoteProps: "as-needed"` - кавычки только когда нужно
- Настройки для HTML/Markdown

**Обоснование:**
- Явность лучше неявности
- Консистентность с .editorconfig
- Лучшая читаемость

**Проверка:**
```bash
npm run format:check
```

### 2.3.4 Добавить полезные скрипты

**Добавить в корневой `package.json`:**

```json
{
  "scripts": {
    "fix": "npm run lint:fix && npm run format",
    "check": "npm run lint && npm run format:check && npm run typecheck",
    "pre-commit": "npm run validate:quiet",
    "deps:outdated": "npm outdated --workspaces",
    "deps:update": "npm update --workspaces",
    "clean:cache": "npm cache clean --force && rimraf node_modules/.cache",
    "reinstall": "npm run clean:all && npm install"
  }
}
```

**Новые скрипты:**
- `fix` - автоматическое исправление всего (lint + format)
- `check` - проверка всего (lint + format + typecheck)
- `pre-commit` - что запускается перед коммитом
- `deps:outdated` - показать устаревшие зависимости
- `deps:update` - обновить зависимости (minor/patch)
- `clean:cache` - очистить npm кэш
- `reinstall` - полная переустановка зависимостей

**Обоснование:**
- Удобство для разработчиков
- Стандартизация команд
- Автоматизация рутинных задач

### 2.3.5 Обновить .prettierignore

**Добавить в `.prettierignore`:**

```
# Dependencies
node_modules/
package-lock.json

# Build outputs
dist/
*.tsbuildinfo

# Coverage reports
coverage/

# Logs
logs/
*.log

# IDE
.vscode/
.idea/

# Git
.git/

# Husky
.husky/_/

# Generated files
dependency-graph.svg
dependency-graph.html
jscpd-report/

# Documentation that shouldn't be reformatted
CHANGELOG.md

# Vitest
coverage/

# Build artifacts
*.mcpb
.mcpb-build/
manifest.json
```

**Обоснование:**
- Более полный список исключений
- Избегаем форматирования generated files
- Синхронизирован с .gitignore

---

## Чек-лист выполнения

- [x] 2.3.1 Модернизировать knip.json
- [x] 2.3.2 Сохранить порог jscpd на 5% (дублирование 4.03%, добавлен exitCode)
- [x] 2.3.3 Расширить Prettier конфигурацию
- [x] 2.3.4 Добавить полезные скрипты
- [x] 2.3.5 Обновить .prettierignore
- [x] Запустить `npm run knip` - проверка пройдена
- [x] Запустить `npm run cpd` - 4.03% дублирования (< 5%)
- [x] Запустить `npm run format:check` - проверка пройдена
- [x] Запустить `npm run check` - lint, typecheck, format проверены
- [x] Запустить части validate:quiet (lint, typecheck, cpd, docs - ОК)
- [x] Закоммитить: `build: улучшение конфигурации инструментов качества кода`

---

## Проблемы и решения

**Проблема:** Knip может найти много неиспользуемого кода
**Решение:** Постепенно исправлять или добавлять в ignore (если это false positive)

**Проблема:** jscpd threshold 3% может быть слишком строгим
**Решение:** Начать с 4%, потом снизить до 3%

**Проблема:** Prettier может переформатировать много файлов
**Решение:** Использовать `npm run format` перед коммитом

**Проблема:** Новые скрипты могут конфликтовать с существующими
**Решение:** Проверить, что все скрипты работают: `npm run <script> --help`
