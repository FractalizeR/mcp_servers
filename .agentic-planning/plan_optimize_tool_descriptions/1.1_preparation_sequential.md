# 1.1 Подготовка окружения (SEQUENTIAL)

**Цель:** Настроить инструменты для валидации и измерения изменений

**Исполнитель:** Один агент

---

## ✅ Задачи

### 1. Убедиться в работоспособности measure-tools-size скрипта

```bash
cd packages/servers/yandex-tracker
npx tsx scripts/measure-tools-size.ts
```

**Ожидаемый результат:** Скрипт выводит базовые метрики (~408 токенов descriptions)

### 2. Создать baseline метрики ПЕРЕД оптимизацией

```bash
# Сохранить текущие метрики
npx tsx scripts/measure-tools-size.ts > /tmp/baseline_metrics.txt
```

**Ожидаемый результат:** Файл `/tmp/baseline_metrics.txt` с текущими метриками

### 3. Собрать список всех файлов для оптимизации

```bash
# Найти все definition файлы с "Для:" паттерном
find src/tools -name "*.definition.ts" -exec grep -l "Для:" {} \; > /tmp/files_to_optimize.txt
```

**Ожидаемый результат:** Список ~47 файлов в `/tmp/files_to_optimize.txt`

### 4. Проверить что все тесты проходят ДО изменений

```bash
npm run validate
```

**Ожидаемый результат:** Все тесты зелёные

---

## 📝 Checklist

- [ ] measure-tools-size.ts работает корректно
- [ ] Baseline метрики сохранены в /tmp/baseline_metrics.txt
- [ ] Список файлов для оптимизации в /tmp/files_to_optimize.txt
- [ ] Все тесты проходят (npm run validate)
- [ ] Git статус чистый (working tree clean)

---

## 📊 Baseline метрики (ожидаемые)

- Всего инструментов: 41
- Descriptions: ~408 токенов (короткие из metadata)
- Full descriptions + schemas: ~14,836 токенов
- Files с "Для:": 47

---

**Время выполнения:** ~5-10 минут
**Зависимости:** Нет
**Следующий этап:** 2.1 или 2.2 (можно параллельно)
