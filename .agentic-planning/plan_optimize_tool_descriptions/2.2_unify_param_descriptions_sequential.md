# 2.2 Унификация описаний параметров (SEQUENTIAL)

**Цель:** Привести повторяющиеся описания параметров к единому краткому стилю

**Исполнитель:** Один агент (после завершения 2.1)

**Ожидаемая экономия:** ~200-300 токенов

---

## 🎯 Параметры для унификации

### 1. issueKey / issueId

**Текущие вариации (~15 файлов):**
- "Ключ задачи в формате QUEUE-123 (пример: PROJ-123, ABC-1)"
- "Идентификатор или ключ задачи (примеры: TEST-123, PROJ-456)"
- "Ключ существующей задачи в формате QUEUE-123 (пример: PROJ-123, ABC-1)"
- "⚠️ ОБЯЗАТЕЛЬНЫЙ. Идентификатор или ключ задачи (примеры: TEST-123, PROJ-456)."

**Новый единый стиль:**
```typescript
"Ключ задачи (QUEUE-123)"
```

**Файлы для обновления:**
- `src/tools/api/issues/update/update-issue.definition.ts`
- `src/tools/api/issues/transitions/execute/transition-issue.definition.ts`
- `src/tools/api/issues/links/create/create-link.definition.ts`
- `src/tools/api/issues/links/delete/delete-link.definition.ts`
- `src/tools/api/comments/add/add-comment.definition.ts`
- `src/tools/api/comments/edit/edit-comment.definition.ts`
- `src/tools/api/comments/delete/delete-comment.definition.ts`
- `src/tools/api/checklists/add/add-checklist-item.definition.ts`
- `src/tools/api/checklists/update/update-checklist-item.definition.ts`
- `src/tools/api/checklists/delete/delete-checklist-item.definition.ts`
- `src/tools/api/issues/attachments/upload/upload-attachment.definition.ts`
- `src/tools/api/issues/attachments/delete/delete-attachment.definition.ts`
- `src/tools/api/worklog/add/add-worklog.definition.ts`
- `src/tools/api/worklog/update/update-worklog.definition.ts`
- `src/tools/api/worklog/delete/delete-worklog.definition.ts`

### 2. issueKeys (массив)

**Текущие вариации (~3 файла):**
- "⚠️ ОБЯЗАТЕЛЬНО МАССИВ! Даже для одной задачи используй ["PROJ-123"], НЕ "PROJ-123". Массив ключей в формате QUEUE-123 (до 100 задач)."
- "Массив ключей задач в формате QUEUE-123"

**Новый единый стиль:**
```typescript
"Массив ключей задач (QUEUE-123), до 100 элементов"
```

**Файлы для обновления:**
- `src/tools/api/issues/get/get-issues.definition.ts`
- `src/tools/api/bulk-change/move/bulk-move-issues.definition.ts`
- `src/tools/api/bulk-change/update/bulk-update-issues.definition.ts`
- `src/tools/api/bulk-change/transition/bulk-transition-issues.definition.ts`

### 3. projectId

**Текущие вариации (~5 файлов):**
- "⚠️ ОБЯЗАТЕЛЬНЫЙ. ID или ключ проекта для удаления."
- "ID или ключ проекта (например: \"PROJ\", \"project123\")"
- "Идентификатор проекта (примеры: \"PROJ\", \"project123\")"

**Новый единый стиль:**
```typescript
"ID проекта (PROJ)"
```

**Файлы для обновления:**
- `src/tools/api/projects/get-project.definition.ts`
- `src/tools/api/projects/update-project.definition.ts`
- `src/tools/api/projects/delete-project.definition.ts`

### 4. queueId / queue

**Текущие вариации (~8 файлов):**
- "⚠️ ОБЯЗАТЕЛЬНЫЙ. Ключ очереди в формате 1-10 заглавных букв/цифр, начинается с буквы (примеры: PROJ, TEST, DEV2024)."
- "Идентификатор очереди (примеры: \"PROJ\", \"TEST\")"
- "Ключ очереди (примеры: PROJ, TEST)"

**Новый единый стиль:**
```typescript
"Ключ очереди (PROJ)"
```

**Файлы для обновления:**
- `src/tools/api/issues/create/create-issue.definition.ts`
- `src/tools/api/queues/get-queue.definition.ts`
- `src/tools/api/queues/update-queue.definition.ts`
- `src/tools/api/queues/manage-queue-access.definition.ts`
- `src/tools/api/queues/get-queue-fields.definition.ts`
- `src/tools/api/components/create-component.definition.ts`
- `src/tools/api/components/get-components.definition.ts`

### 5. fields (массив)

**Текущие вариации (~12 файлов):**
- "Фильтр полей ответа (опционально, по умолчанию все). Указывайте только необходимые поля для экономии токенов."
- "Фильтр полей ответа (опционально, по умолчанию все). Рекомендуется указывать только необходимые поля для экономии токенов (без фильтрации: ~2000-5000 токенов/задача, с фильтрацией: ~200-500)."
- "Массив полей для возврата в результате. Используйте только необходимые поля для экономии контекста."

**Новый единый стиль:**
```typescript
"Поля для возврата. Указывайте минимум для экономии токенов."
```

**Файлы для обновления:**
- `src/tools/api/issues/get/get-issues.definition.ts`
- `src/tools/api/issues/create/create-issue.definition.ts`
- `src/tools/api/issues/update/update-issue.definition.ts`
- `src/tools/api/projects/get-project.definition.ts`
- `src/tools/api/projects/get-projects.definition.ts`
- `src/tools/api/projects/create-project.definition.ts`
- `src/tools/api/projects/update-project.definition.ts`
- `src/tools/api/queues/get-queue.definition.ts`
- `src/tools/api/queues/get-queues.definition.ts`
- `src/tools/api/components/get-components.definition.ts`
- `src/tools/api/worklog/add/add-worklog.definition.ts`
- `src/tools/api/worklog/update/update-worklog.definition.ts`

### 6. summary

**Текущие вариации (~2 файла):**
- "⚠️ ОБЯЗАТЕЛЬНЫЙ. Краткое описание задачи (название). Обычно 5-10 слов."
- "Краткое описание задачи (заголовок). Обычно 5-10 слов."

**Новый единый стиль:**
```typescript
"Название задачи (5-10 слов)"
```

**Файлы для обновления:**
- `src/tools/api/issues/create/create-issue.definition.ts`
- `src/tools/api/issues/update/update-issue.definition.ts`

---

## 📝 Процесс унификации

### Шаг 1: issueKey / issueId (~15 файлов)
1. Найти все вхождения через grep
2. Заменить на единый стиль
3. Убедиться что `pattern` и `examples` сохранены
4. Lint каждого файла

### Шаг 2: issueKeys (~3 файла)
1. Сократить длинные предупреждения
2. Сохранить лимит "до 100"
3. Lint

### Шаг 3: projectId (~5 файлов)
1. Унифицировать к "ID проекта (PROJ)"
2. Lint

### Шаг 4: queueId / queue (~8 файлов)
1. Убрать избыточные объяснения формата
2. Оставить pattern для валидации
3. Lint

### Шаг 5: fields (~12 файлов)
1. Сократить до "Поля для возврата. Указывайте минимум для экономии токенов."
2. Убрать конкретные цифры токенов (они могут меняться)
3. Lint

### Шаг 6: summary (~2 файла)
1. Сократить до "Название задачи (5-10 слов)"
2. Lint

---

## 🔧 Команды для поиска

```bash
# Найти все issueKey вариации
grep -r "Ключ задачи" src/tools --include="*.definition.ts" | wc -l

# Найти все fields вариации
grep -r "Фильтр полей" src/tools --include="*.definition.ts" | wc -l

# Найти все projectId вариации
grep -r "ID.*проекта" src/tools --include="*.definition.ts" | wc -l
```

---

## ✅ Checklist

- [ ] issueKey унифицирован в 15 файлах
- [ ] issueKeys унифицирован в 3 файлах
- [ ] projectId унифицирован в 5 файлах
- [ ] queueId унифицирован в 8 файлах
- [ ] fields унифицирован в 12 файлах
- [ ] summary унифицирован в 2 файлах
- [ ] Все lint проверки прошли
- [ ] Typecheck прошёл

---

## 🧪 Валидация

```bash
# После всех изменений
npm run validate --workspace=@mcp-server/yandex-tracker

# Проверить что descriptions сократились
npx tsx scripts/measure-tools-size.ts
```

---

## 📊 Ожидаемые результаты

- **Унифицировано параметров:** 6 типов
- **Обновлено файлов:** ~45
- **Средняя экономия на параметр:** ~5-10 символов
- **Общая экономия:** ~200-300 токенов
- **Консистентность:** ✅

---

**Время выполнения:** ~1-2 часа
**Зависимости:** Желательно после 2.1 (чтобы не было конфликтов)
**Следующий этап:** 3.1 (валидация)
