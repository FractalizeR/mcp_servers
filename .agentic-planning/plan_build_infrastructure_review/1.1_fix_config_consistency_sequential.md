# 1.1 Исправление консистентности конфигураций (Sequential)

**Приоритет:** Высокий
**Тип:** Sequential (изменения влияют друг на друга)
**Оценка:** ~30 минут

---

## Чеклист

### 1. Исправить корневой tsconfig.json

- [x] Добавить `packages/framework/cli` в references
- [x] Добавить `packages/servers/yandex-wiki` в references
- [x] Добавить `packages/servers/ticktick` в references

**Файл:** `tsconfig.json`

```json
{
  "files": [],
  "references": [
    { "path": "./packages/framework/infrastructure" },
    { "path": "./packages/framework/cli" },
    { "path": "./packages/framework/core" },
    { "path": "./packages/framework/search" },
    { "path": "./packages/servers/yandex-tracker" },
    { "path": "./packages/servers/yandex-wiki" },
    { "path": "./packages/servers/ticktick" }
  ]
}
```

### 2. Исправить vitest.workspace.ts

- [x] Добавить `packages/framework/cli`
- [x] Добавить `packages/servers/yandex-wiki`
- [x] Добавить `packages/servers/ticktick`

**Файл:** `vitest.workspace.ts`

### 3. Исправить knip.json

- [x] Добавить workspace для `packages/framework/cli`
- [x] Добавить workspace для `packages/servers/yandex-wiki`
- [x] Добавить workspace для `packages/servers/ticktick`

**Файл:** `knip.json`

### 4. Исправить references в пакетах

- [x] `packages/framework/cli/tsconfig.json` — добавить reference на infrastructure
- [x] `packages/framework/search/tsconfig.json` — добавить reference на infrastructure (помимо core)

### 5. Валидация

- [x] Запустить `npm run build` — проверить сборку всех пакетов
- [x] Запустить `npm run typecheck` — проверить типы
- [x] Запустить `npm run test` — проверить тесты

---

## Критерии завершения

- Все пакеты включены в корневые конфигурации
- `npm run build` проходит без ошибок
- `npm run validate:quiet` проходит без ошибок
