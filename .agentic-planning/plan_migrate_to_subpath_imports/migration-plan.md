# План миграции на Node.js Subpath Imports

**Дата создания:** 2025-11-19
**Статус:** 📋 Готов к реализации
**Масштаб:** 686 файлов, **~958 импортов** для миграции (796 @ алиасов + 162 относительных пути)

---

## 🎯 Цель миграции

Перейти с TypeScript path aliases (`@prefix/*`) на Node.js Subpath Imports (`#prefix/*`) в соответствии с современными рекомендациями (2025) для TypeScript 5.4+ и Node.js 22+.

**Преимущества:**
- ✅ Нативная поддержка Node.js (не требует tsc-alias)
- ✅ Одна точка конфигурации (package.json)
- ✅ Рекомендация Turborepo team для monorepo
- ✅ Префикс `#` избегает конфликтов с npm scoped packages
- ✅ Полная поддержка в TypeScript 5.4+ (авто-импорты, Language Server)

---

## 📊 Текущее состояние проекта

### Технические характеристики:
- **TypeScript:** 5.9.3 ✅ (требуется 5.4+)
- **Node.js:** >= 22.0.0 ✅ (требуется 12.19.0+)
- **Файлов:** 686 TypeScript файлов в yandex-tracker
- **Импортов для миграции:** **~958 импортов** (796 @ алиасов + 162 относительных пути)

### Стратегия:
- Мигрируем @ алиасы → # префиксы (796 импортов)
- Мигрируем глубокие относительные пути → # префиксы (162 импорта)
- tsc-alias будет удалён (проверим, что не нужен)

### Распределение импортов:

| Префикс | Количество | Действие |
|---------|-----------|----------|
| `@mcp-framework/*` | 614 | ✅ Оставить (междупакетный) |
| `@tracker_api/*` | 439 | 🔄 Мигрировать → `#tracker_api/*` |
| `@integration/*` | 151 | 🔄 Мигрировать → `#integration/*` |
| `@tools/*` | 144 | 🔄 Мигрировать → `#tools/*` |
| `@constants` (tests) | 43 | 🔄 Мигрировать → `#constants` |
| `@cli/*` | 12 | 🔄 Мигрировать → `#cli/*` |
| `@composition-root/*` | 7 | 🔄 Мигрировать → `#composition-root/*` |
| **Относительные пути (для миграции):** |
| `../../../constants.js` (src) | 57 | 🔄 Мигрировать → `#constants` |
| `../../../common/*` (src) | 44 | 🔄 Мигрировать → `#common/*` |
| `../../helpers/*` (tests) | 61 | 🔄 Мигрировать → `#helpers/*` |
| **Внешние пакеты (не трогать):** |
| `@modelcontextprotocol/*` | 3 | ✅ Оставить (внешний пакет) |
| `@iarna/*` | 1 | ✅ Оставить (внешний пакет) |
| `@anthropic-ai/*` | ? | ✅ Оставить (внешний пакет) |

**Итого к миграции:** **958 импортов** (796 @ + 162 относительных)

---

## 📋 Этапы миграции

---

## Этап 1: Изучение и подготовка (sequential)

**Цель:** Полностью понять текущую структуру импортов и подготовить маппинг

### ✅ Подэтапы:

- [ ] Создать полный анализ всех `@` импортов в проекте
  - [ ] Сгруппировать по категориям (внутрипакетные vs междупакетные)
  - [ ] Создать таблицу маппинга: `@prefix/*` → `#prefix/*`
  - [ ] Определить edge cases (если есть)

- [ ] Проверить зависимости, которые могут сломаться:
  - [ ] eslint-plugin-import (настройки path resolution)
  - [ ] vitest (resolve.alias конфигурация)
  - [ ] IDE конфигурация (VSCode, WebStorm)
  - [ ] tsconfig-paths (больше не нужен!)

- [ ] Создать тестовую стратегию:
  - [ ] Определить smoke tests для быстрой проверки
  - [ ] Запланировать полный `npm run validate` после миграции

**Ожидаемый результат:**
- Детальная таблица маппинга всех алиасов
- Список потенциальных проблем и их решений
- Чек-лист проверки после миграции

**Время:** ~1-2 часа

---

## Этап 2: Обновление конфигурации (sequential)

**Цель:** Добавить Node.js subpath imports в package.json, сохранив старую конфигурацию

### ✅ Подэтапы:

- [ ] **2.1. Обновить `packages/servers/yandex-tracker/package.json`**

  Добавить секцию `"imports"`:
  ```json
  {
    "imports": {
      "#tracker_api/*": "./src/tracker_api/*",
      "#tools/*": "./src/tools/*",
      "#composition-root/*": "./src/composition-root/*",
      "#cli/*": "./src/cli/*",
      "#constants": "./src/constants.ts",
      "#common/*": "./src/common/*",
      "#integration/*": "./tests/integration/*",
      "#helpers/*": "./tests/helpers/*"
    }
  }
  ```

- [ ] **2.2. Проверить, что новые импорты работают**
  - [ ] Создать тестовый файл с `#` импортами
  - [ ] Убедиться, что TypeScript их резолвит
  - [ ] Убедиться, что IDE показывает автокомплит

- [ ] **2.3. Пока НЕ удалять старую конфигурацию**
  - ⚠️ Оставить `paths` в `tsconfig.json` и `tsconfig.tests.json`
  - ⚠️ Оставить `resolve.alias` в `vitest.config.ts`
  - Это обеспечит плавную миграцию (старые и новые импорты будут работать одновременно)

**Ожидаемый результат:**
- И `@prefix/*`, и `#prefix/*` импорты работают одновременно
- TypeScript компилируется без ошибок
- IDE поддерживает автокомплит для `#prefix/*`

**Время:** ~30 минут

---

## Этап 3: Написание миграционного скрипта (sequential)

**Цель:** Создать надёжный скрипт для автоматической замены импортов

### ✅ Подэтапы:

- [ ] **3.1. Создать скрипт `scripts/migrate-to-subpath-imports.ts`**

  **Требования к скрипту:**

  1. **Dry-run режим по умолчанию:**
     ```bash
     npm run migrate:subpath-imports -- --dry-run
     npm run migrate:subpath-imports -- --dry-run --verbose
     ```

  2. **Apply режим (после проверки):**
     ```bash
     npm run migrate:subpath-imports -- --apply
     ```

  3. **Фильтры:**
     - `--src-only` — только src/
     - `--tests-only` — только tests/
     - `--file <path>` — один конкретный файл

  4. **Безопасность:**
     - Создание backup перед изменениями (опционально)
     - Валидация синтаксиса после замены
     - Проверка, что файл существует перед модификацией

  5. **Отчётность:**
     - Сколько файлов изменено
     - Сколько импортов заменено
     - Какие паттерны заменены (статистика по префиксам)
     - Список изменённых файлов

- [ ] **3.2. Реализовать функции скрипта:**

  ```typescript
  // Паттерны замены (ВАРИАНТ B: @ алиасы + относительные пути)
  const MIGRATION_PATTERNS = [
    // ========================================
    // 1. Существующие @ алиасы → # префиксы
    // ========================================
    {
      name: 'tracker_api',
      regex: /from ['"]@tracker_api\/([^'"]+)['"]/g,
      replacement: 'from \'#tracker_api/$1\'',
      description: 'Tracker API modules',
    },
    {
      name: 'tools',
      regex: /from ['"]@tools\/([^'"]+)['"]/g,
      replacement: 'from \'#tools/$1\'',
      description: 'MCP Tools',
    },
    {
      name: 'integration',
      regex: /from ['"]@integration\/([^'"]+)['"]/g,
      replacement: 'from \'#integration/$1\'',
      description: 'Integration test helpers',
    },
    {
      name: 'constants_alias',
      regex: /from ['"]@constants['"]/g,
      replacement: 'from \'#constants\'',
      description: 'Constants (@ alias in tests)',
    },
    {
      name: 'composition-root',
      regex: /from ['"]@composition-root\/([^'"]+)['"]/g,
      replacement: 'from \'#composition-root/$1\'',
      description: 'DI Composition Root',
    },
    {
      name: 'cli',
      regex: /from ['"]@cli\/([^'"]+)['"]/g,
      replacement: 'from \'#cli/$1\'',
      description: 'CLI modules',
    },

    // ========================================
    // 2. Относительные пути → # префиксы (НОВОЕ!)
    // ========================================
    {
      name: 'constants_relative',
      regex: /from ['"](?:\.\.\/)+constants\.js['"]/g,
      replacement: 'from \'#constants\'',
      description: 'Constants (relative paths in src) - 57 imports',
    },
    {
      name: 'common_relative',
      regex: /from ['"](?:\.\.\/)+common\/([^'"]+)['"]/g,
      replacement: 'from \'#common/$1\'',
      description: 'Common modules (relative paths in src) - 44 imports',
    },
    {
      name: 'helpers_relative',
      regex: /from ['"](?:\.\.\/)+helpers\/([^'"]+)['"]/g,
      replacement: 'from \'#helpers/$1\'',
      description: 'Test helpers (relative paths in tests) - 61 imports',
    },

    // ========================================
    // 3. Dynamic imports (edge case)
    // ========================================
    {
      name: 'tracker_api_dynamic',
      regex: /import\(['"]@tracker_api\/([^'"]+)['"]\)/g,
      replacement: 'import(\'#tracker_api/$1\')',
      description: 'Dynamic imports for Tracker API',
    },
    {
      name: 'tools_dynamic',
      regex: /import\(['"]@tools\/([^'"]+)['"]\)/g,
      replacement: 'import(\'#tools/$1\')',
      description: 'Dynamic imports for Tools',
    },
  ];

  // ⚠️ ВАЖНО: НЕ заменять @mcp-framework/*, @modelcontextprotocol/*, @iarna/*
  const EXCLUDED_PATTERNS = [
    /from ['"]@mcp-framework\//,
    /from ['"]@modelcontextprotocol\//,
    /from ['"]@iarna\//,
    /from ['"]@anthropic-ai\//,
  ];
  ```

  **Функции:**
  - `findFilesToMigrate(directory: string): string[]` — найти все .ts файлы
  - `isExcludedImport(line: string): boolean` — проверка на исключения
  - `migrateFile(filePath: string, dryRun: boolean): MigrationResult`
  - `validateSyntax(filePath: string): boolean` — проверка после замены
  - `generateReport(results: MigrationResult[]): void` — красивый отчёт

- [ ] **3.3. Добавить npm скрипт:**

  В `packages/servers/yandex-tracker/package.json`:
  ```json
  {
    "scripts": {
      "migrate:subpath-imports": "tsx scripts/migrate-to-subpath-imports.ts"
    }
  }
  ```

**Ожидаемый результат:**
- Работающий скрипт с dry-run режимом
- Тесты скрипта (unit tests для regex паттернов)
- Детальная документация использования

**Время:** ~4-5 часов (включая тестирование и edge cases)

---

## Этап 4: Тестирование скрипта (sequential)

**Цель:** Убедиться, что скрипт работает корректно на реальных файлах

### ✅ Подэтапы:

- [ ] **4.1. Dry-run на всём проекте:**
  ```bash
  cd packages/servers/yandex-tracker
  npm run migrate:subpath-imports -- --dry-run --verbose
  ```

  **Проверить:**
  - Правильность паттернов замены
  - Что НЕ заменяются междупакетные импорты (@mcp-framework/*)
  - Количество изменений соответствует ожиданиям (~958 импортов: 796 @ + 162 относительных)

- [ ] **4.2. Тест на одном файле:**
  ```bash
  npm run migrate:subpath-imports -- --file src/index.ts --apply
  ```

  **Проверить:**
  - Файл изменён корректно
  - TypeScript компилируется: `npm run typecheck`
  - Синтаксис валиден

- [ ] **4.3. Тест на директории tests/:**
  ```bash
  npm run migrate:subpath-imports -- --tests-only --apply
  ```

  **Проверить:**
  - Все тестовые файлы мигрированы
  - Тесты проходят: `npm run test`

- [ ] **4.4. Откат изменений:**
  ```bash
  git checkout -- .
  ```

  Убедиться, что можно безопасно откатить изменения.

**Ожидаемый результат:**
- Скрипт работает корректно
- Изменения не ломают компиляцию и тесты
- Готовность к полной миграции

**Время:** ~2-3 часа (проверка относительных импортов)

---

## Этап 5: Применение миграции (sequential, но можно разбить на части)

**Цель:** Мигрировать весь проект на Node.js subpath imports

### ✅ Подэтапы:

- [ ] **5.1. Создать отдельную ветку для миграции:**
  ```bash
  git checkout -b migrate/subpath-imports-implementation
  ```

- [ ] **5.2. Запустить полную миграцию:**
  ```bash
  cd packages/servers/yandex-tracker
  npm run migrate:subpath-imports -- --apply
  ```

- [ ] **5.3. Проверить результаты:**
  - [ ] Посмотреть git diff (выборочно, чтобы убедиться в корректности)
  - [ ] Проверить статистику замен из отчёта скрипта
  - [ ] Убедиться, что междупакетные импорты не тронуты

- [ ] **5.4. Запустить typecheck:**
  ```bash
  npm run typecheck
  ```

  **Если есть ошибки:**
  - Проанализировать причину
  - Исправить вручную или доработать скрипт
  - Повторить миграцию

- [ ] **5.5. Запустить тесты:**
  ```bash
  npm run test:quiet
  ```

  **Если есть падения:**
  - Проанализировать причину
  - Проверить, не связано ли с импортами в test helpers
  - Исправить

- [ ] **5.6. Запустить полную валидацию:**
  ```bash
  npm run validate:quiet
  ```

  **Ожидаемый результат:** ✅ Все проверки проходят

**Время:** ~2-3 часа (включая исправление потенциальных проблем)

---

## Этап 6: Очистка конфигурации (sequential)

**Цель:** Удалить старую конфигурацию TypeScript path aliases

### ✅ Подэтапы:

- [ ] **6.1. Удалить `paths` из `tsconfig.json`:**

  **Было:**
  ```json
  {
    "compilerOptions": {
      "paths": {
        "@tracker_api/*": ["./src/tracker_api/*"],
        "@tools/*": ["./src/tools/*"],
        "@composition-root/*": ["./src/composition-root/*"],
        "@cli/*": ["./src/cli/*"],
        "@constants": ["./src/constants.ts"]
      }
    }
  }
  ```

  **Станет:**
  ```json
  {
    "compilerOptions": {
      // paths удалены - используем Node.js subpath imports
    }
  }
  ```

- [ ] **6.2. Удалить `paths` из `tsconfig.tests.json`:**

  Аналогично удалить секцию `paths` (оставить только `extends`)

- [ ] **6.3. Очистить `vitest.config.ts`:**

  **Было:**
  ```typescript
  resolve: {
    alias: {
      '@tracker_api': path.resolve(__dirname, './src/tracker_api'),
      '@tracker_api/*': path.resolve(__dirname, './src/tracker_api/*'),
      '@tools': path.resolve(__dirname, './src/tools'),
      '@tools/*': path.resolve(__dirname, './src/tools/*'),
      // ... и т.д.
    }
  }
  ```

  **Станет:**
  ```typescript
  resolve: {
    alias: {
      // Алиасы больше не нужны - Node.js subpath imports работают нативно
      // Оставляем только алиасы для framework пакетов (если нужны для vitest)
      '@mcp-framework/search': path.resolve(__dirname, '../../framework/search/src'),
      // ... и т.д.
    }
  }
  ```

- [ ] **6.4. Проверить зависимости:**

  **Проверить и удалить (если больше не нужны):**
  - `tsconfig-paths` — больше не требуется!
  - `tsc-alias` — ⚠️ **Проверить!** Возможно, больше не нужен для внутрипакетных импортов
    - Framework пакеты (`@mcp-framework/*`) — обычные npm зависимости, не требуют tsc-alias
    - Subpath imports (`#*`) — нативно поддерживаются Node.js, не требуют tsc-alias
    - **Действие:** Попробовать билд без tsc-alias, если работает — удалить

  **Оставить:**
  - `vite-tsconfig-paths` — может потребоваться для других целей

- [ ] **6.5. Обновить package.json scripts:**

  Проверить, что `build` скрипт всё ещё работает:
  ```bash
  npm run build
  ```

- [ ] **6.6. Финальная валидация после очистки:**
  ```bash
  npm run validate:quiet
  ```

**Ожидаемый результат:**
- Конфигурация очищена от старых path aliases
- Проект компилируется и тесты проходят
- Билд работает корректно

**Время:** ~1-2 часа (эксперименты с tsc-alias)

---

## Этап 7: Обновление документации (sequential)

**Цель:** Задокументировать новые правила импортов в проекте

### ✅ Подэтапы:

- [ ] **7.1. Обновить ARCHITECTURE.md:**

  Добавить новый раздел в секцию "Module System":

  ```markdown
  ### Node.js Subpath Imports

  **Решение (2025):** Проект использует Node.js Subpath Imports вместо TypeScript path aliases.

  **Причины выбора:**
  - Нативная поддержка Node.js (не требует tsc-alias для внутренних импортов)
  - Рекомендация Turborepo team для monorepo
  - Одна точка конфигурации (package.json)
  - Полная поддержка в TypeScript 5.4+ (авто-импорты)
  - Префикс `#` избегает конфликтов с npm scoped packages

  **Конфигурация:** См. `package.json` секцию `"imports"`

  **Префиксы:**
  - `#tracker_api/*` — Tracker API слой
  - `#tools/*` — MCP Tools
  - `#constants` — Константы проекта
  - `#common/*` — Общие утилиты
  - `#helpers/*` — Тестовые helpers
  - `#integration/*` — Интеграционные тесты

  **Важно:**
  - Междупакетные импорты используют npm names: `@mcp-framework/core`
  - Внутрипакетные короткие импорты (≤2 уровня) используют относительные пути: `./utils.js`
  ```

- [ ] **7.2. Обновить CLAUDE.md:**

  Добавить/обновить правило в секции "Импорты между пакетами":

  ```markdown
  ### 2. Импорты между пакетами

  **✅ Используй npm package names:**
  ```typescript
  import { BaseTool } from '@mcp-framework/core';
  import { HttpClient } from '@mcp-framework/infrastructure';
  ```

  **❌ НЕ используй:**
  ```typescript
  import { BaseTool } from '../../../core/src/tools/base/base-tool.js';
  ```

  ### 3. Внутрипакетные импорты

  **Короткие (≤2 уровня) — относительные пути:**
  ```typescript
  import { validateInput } from './utils.js';
  import { BaseOperation } from '../base-operation.js';
  ```

  **Глубокие (≥3 уровня) — Node.js subpath imports:**
  ```typescript
  import { MCP_TOOL_PREFIX } from '#constants';
  import { YandexTrackerFacade } from '#tracker_api/facade/yandex-tracker.facade.js';
  import { createFixture } from '#helpers/queue.fixture.js';
  ```

  **Префикс `#` обязателен** — это требование Node.js subpath imports.

  **Конфигурация:** `package.json` секция `"imports"`
  ```

- [ ] **7.3. Обновить yandex-tracker/CLAUDE.md (если есть специфика):**

  Добавить примеры использования `#` префиксов для конкретных модулей проекта.

- [ ] **7.4. Обновить import-conventions.md:**

  Добавить секцию "Migration Completed":
  ```markdown
  ## ✅ Миграция завершена (2025-11-19)

  Проект полностью мигрирован на Node.js Subpath Imports.

  **Статистика миграции:**
  - Файлов изменено: XXX
  - Импортов заменено: ~796
  - Время миграции: ~X часов

  **Commit:** [ссылка на commit]
  ```

- [ ] **7.5. Создать MIGRATION.md (опционально):**

  Если нужна история миграций для будущих разработчиков.

**Ожидаемый результат:**
- Документация актуализирована
- Правила импортов чётко описаны
- Примеры кода обновлены

**Время:** ~1-2 часа

---

## Этап 8: Финальная проверка и коммит (sequential)

**Цель:** Убедиться, что всё работает, и закоммитить изменения

### ✅ Подэтапы:

- [ ] **8.1. Финальная валидация:**
  ```bash
  # В корне monorepo
  npm run validate:quiet

  # В yandex-tracker пакете
  cd packages/servers/yandex-tracker
  npm run validate:quiet
  npm run test:smoke
  ```

- [ ] **8.2. Проверить, что не осталось старых импортов:**
  ```bash
  # Проверить, что нет @tracker_api, @tools и т.д. (кроме междупакетных)
  npm run check-internal-aliases  # создать этот скрипт
  ```

  Скрипт должен найти и показать ошибку, если остались `@tracker_api/*` и подобные.

- [ ] **8.3. Проверить git diff:**
  ```bash
  git status
  git diff --stat
  ```

  **Ожидаемые изменения:**
  - ~686 файлов изменено
  - package.json (добавлена секция "imports")
  - tsconfig.json (удалены paths)
  - tsconfig.tests.json (удалены paths)
  - vitest.config.ts (очищены алиасы)
  - ARCHITECTURE.md (обновлена документация)
  - CLAUDE.md (обновлены правила)
  - scripts/migrate-to-subpath-imports.ts (новый скрипт)

- [ ] **8.4. Создать коммит:**
  ```bash
  git add .
  git commit -m "$(cat <<'EOF'
  refactor: мигрировать на Node.js Subpath Imports (# префикс)

  Детали:
  - Заменено ~958 импортов: 796 @ алиасов + 162 относительных пути
  - Все глубокие импорты теперь используют # префикс
  - Добавлена секция "imports" в package.json
  - Удалены TypeScript path aliases из tsconfig.json
  - Очищена конфигурация vitest.config.ts
  - Обновлена документация (ARCHITECTURE.md, CLAUDE.md)
  - Создан миграционный скрипт для будущих пакетов

  Причины миграции:
  - Нативная поддержка Node.js (TypeScript 5.4+, Node.js 22+)
  - Рекомендация Turborepo team для monorepo
  - Префикс # избегает конфликтов с npm scoped packages
  - Упрощение конфигурации (одна точка в package.json)

  Ссылки:
  - https://turborepo.com/docs/guides/tools/typescript
  - https://nodejs.org/api/packages.html#subpath-imports

  🤖 Generated with Claude Code
  Co-Authored-By: Claude <noreply@anthropic.com>
  EOF
  )"
  ```

- [ ] **8.5. Запушить в ветку:**
  ```bash
  git push -u origin migrate/subpath-imports-implementation
  ```

**Ожидаемый результат:**
- Все изменения закоммичены
- Ветка запушена
- Готово к review / merge

**Время:** ~30 минут

---

## 📊 Итоговая статистика (заполнить после миграции)

- **Файлов изменено:** ___
- **Импортов заменено:** ___
- **Время затрачено:** ___
- **Проблем обнаружено:** ___
- **Commit hash:** ___

---

## 🚨 Потенциальные проблемы и решения

### Проблема 1: IDE не подхватывает `#` импорты

**Симптом:** Красные подчёркивания в VSCode/WebStorm

**Решение:**
- Перезапустить TypeScript Language Server
- VSCode: Cmd+Shift+P → "TypeScript: Restart TS Server"
- WebStorm: Invalidate Caches and Restart

### Проблема 2: Vitest не резолвит `#` импорты

**Симптом:** Ошибки импортов при запуске тестов

**Решение:**
- Проверить, что vitest версии 4.0+
- Убрать старые алиасы из vitest.config.ts полностью
- Убедиться, что используется `vite-tsconfig-paths` plugin

### Проблема 3: Остались ошибки после миграции

**Симптом:** TypeScript показывает ошибки импортов

**Решение:**
- Проверить секцию "imports" в package.json на опечатки
- Убедиться, что пути в "imports" правильные (относительно package.json)
- Проверить, что все файлы сохранены

### Проблема 4: Конфликт старых и новых импортов

**Симптом:** Некоторые файлы остались со старыми `@` импортами

**Решение:**
- Повторно запустить скрипт миграции
- Проверить git diff, чтобы убедиться, что все файлы обновлены
- Создать скрипт проверки для CI

---

## 🔗 Ссылки и ресурсы

- **Turborepo TypeScript Guide:** https://turborepo.com/docs/guides/tools/typescript
- **Node.js Subpath Imports Docs:** https://nodejs.org/api/packages.html#subpath-imports
- **TypeScript 5.4 Release:** https://devblogs.microsoft.com/typescript/announcing-typescript-5-4/
- **GitHub Discussion #620:** https://github.com/vercel/turborepo/discussions/620

---

## 📝 Чек-лист готовности к следующей сессии

- [ ] План согласован
- [ ] Определён формат миграционного скрипта
- [ ] Понятна стратегия тестирования
- [ ] Текущая ветка готова для экспериментов

---

## 🎯 Критерии успеха миграции

1. ✅ Все внутрипакетные импорты используют `#` префикс
2. ✅ Междупакетные импорты остались с `@mcp-framework/*`
3. ✅ `npm run validate` проходит успешно
4. ✅ `npm run build` собирает проект
5. ✅ Все тесты проходят (включая smoke tests)
6. ✅ TypeScript path aliases удалены из конфигурации
7. ✅ Документация обновлена
8. ✅ Создан миграционный скрипт для повторного использования

---

**Следующий шаг:** Начать реализацию в новой сессии (Этап 1: Изучение и подготовка).
