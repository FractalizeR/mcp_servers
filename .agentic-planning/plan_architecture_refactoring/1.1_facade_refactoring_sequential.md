# Этап 1.1: Рефакторинг YandexTrackerFacade - Разделение на доменные фасады

**Приоритет:** 🔴 CRITICAL
**Тип выполнения:** SEQUENTIAL (последовательно)
**Оценка:** 10-12 часов (обновлено после ревью)

---

## 🎯 Цель

Разделить God Object `YandexTrackerFacade` (1080 строк, 61 метод) на несколько специализированных фасадов по доменам API.

---

## 📊 Текущее состояние

**Файл:** `packages/servers/yandex-tracker/src/tracker_api/facade/yandex-tracker.facade.ts`

**Проблемы:**
- 1080 строк кода (лимит: 400) - превышение в 2.7 раза!
- 61 публичный метод (было 30+ в первоначальном плане)
- Смешаны все домены API: Issues, Queues, Projects, Components, Boards, Fields, Sprints, Links, Comments, Checklists, Worklogs, Attachments
- Нарушает SRP, ISP, max-lines
- Есть TODO в коде: "Рефакторинг - разделить на несколько фасадов по доменам"
- Использует ленивую инициализацию через DI (важно сохранить этот паттерн!)

**Текущая структура методов:**
```typescript
class YandexTrackerFacade {
  // User (1 метод)
  ping()

  // Issues (6 методов)
  getIssues(), findIssues(), createIssue(), updateIssue()
  getIssueChangelog(), getIssueTransitions(), transitionIssue()

  // Queues (5 методов)
  getQueues(), getQueue(), createQueue(), updateQueue()
  getQueueFields(), manageQueueAccess()

  // Projects (5 методов)
  getProjects(), getProject(), createProject(), updateProject(), deleteProject()

  // Components (4 метода)
  getComponents(), createComponent(), updateComponent(), deleteComponent()

  // Links (3 метода)
  getIssueLinks(), createLink(), deleteLink()

  // Comments (4 метода)
  addComment(), getComments(), editComment(), deleteComment()

  // Checklists (4 метода)
  getChecklist(), addChecklistItem(), updateChecklistItem(), deleteChecklistItem()

  // Worklogs (4 метода)
  getWorklogs(), addWorklog(), updateWorklog(), deleteWorklog()

  // Attachments (5 методов)
  getAttachments(), uploadAttachment(), downloadAttachment()
  deleteAttachment(), getThumbnail()

  // Boards (4 метода) - ДОБАВЛЕНО в ревью
  getBoards(), getBoard(), createBoard(), updateBoard(), deleteBoard()

  // Fields (5 методов) - ДОБАВЛЕНО в ревью
  getFields(), getField(), createField(), updateField(), deleteField()

  // Sprints (3 метода) - ДОБАВЛЕНО в ревью
  getSprint(), createSprint(), updateSprint()
}
```

**ИТОГО:** 61 метод в 12 доменах (было 30+ методов в 9 доменах в первоначальном плане)

---

## 🏗️ Целевая архитектура

### Новая структура фасадов

```
tracker_api/facade/
├── index.ts                          # Barrel exports
├── base-domain-facade.ts             # Базовый класс для доменных фасадов (рекомендуется)
├── yandex-tracker.facade.ts          # Главный фасад (агрегатор, ~150-200 строк)
├── domains/                          # Доменные фасады (12 шт)
│   ├── issues.facade.ts              # IssueFacade (~150 строк, 7 методов)
│   ├── queues.facade.ts              # QueueFacade (~100 строк, 6 методов)
│   ├── projects.facade.ts            # ProjectFacade (~80 строк, 5 методов)
│   ├── components.facade.ts          # ComponentFacade (~70 строк, 4 метода)
│   ├── boards.facade.ts              # BoardFacade (~90 строк, 5 методов) - ДОБАВЛЕНО
│   ├── fields.facade.ts              # FieldFacade (~90 строк, 5 методов) - ДОБАВЛЕНО
│   ├── sprints.facade.ts             # SprintFacade (~60 строк, 3 метода) - ДОБАВЛЕНО
│   ├── links.facade.ts               # LinkFacade (~60 строк, 3 метода)
│   ├── comments.facade.ts            # CommentFacade (~70 строк, 4 метода)
│   ├── checklists.facade.ts          # ChecklistFacade (~70 строк, 4 метода)
│   ├── worklogs.facade.ts            # WorklogFacade (~70 строк, 4 метода)
│   └── attachments.facade.ts         # AttachmentFacade (~100 строк, 5 методов)
```

### Новый главный фасад (агрегатор)

```typescript
/**
 * Главный фасад Яндекс.Трекера (агрегатор доменных фасадов)
 *
 * ВАЖНО: Использует lazy initialization через DI контейнер (как в текущей архитектуре)
 * НЕ создаёт фасады через `new` - извлекает из контейнера при первом обращении!
 *
 * Делегирует вызовы специализированным фасадам.
 * Обеспечивает единую точку входа для tools.
 */
export class YandexTrackerFacade {
  // Lazy initialization через getters + DI контейнер
  private _issues?: IssueFacade;
  private _queues?: QueueFacade;
  private _projects?: ProjectFacade;
  private _components?: ComponentFacade;
  private _boards?: BoardFacade;          // ДОБАВЛЕНО
  private _fields?: FieldFacade;          // ДОБАВЛЕНО
  private _sprints?: SprintFacade;        // ДОБАВЛЕНО
  private _links?: LinkFacade;
  private _comments?: CommentFacade;
  private _checklists?: ChecklistFacade;
  private _worklogs?: WorklogFacade;
  private _attachments?: AttachmentFacade;

  constructor(private readonly container: Container) {
    // НИЧЕГО не создаём в конструкторе - только сохраняем контейнер
  }

  // Lazy getters - извлекают из DI при первом обращении
  get issues(): IssueFacade {
    if (!this._issues) {
      this._issues = this.container.get<IssueFacade>(TYPES.IssueFacade);
    }
    return this._issues;
  }

  get queues(): QueueFacade {
    if (!this._queues) {
      this._queues = this.container.get<QueueFacade>(TYPES.QueueFacade);
    }
    return this._queues;
  }

  get projects(): ProjectFacade {
    if (!this._projects) {
      this._projects = this.container.get<ProjectFacade>(TYPES.ProjectFacade);
    }
    return this._projects;
  }

  // ... аналогично для components, boards, fields, sprints, links, comments, checklists, worklogs, attachments

  // Backward compatibility (deprecated)
  /** @deprecated Use facade.issues.get() instead */
  async getIssues(issueKeys: string[]): Promise<BatchIssueResult[]> {
    return this.issues.get(issueKeys);
  }

  // ... остальные deprecated методы для обратной совместимости (61 метод!)
}
```

---

## ✅ Чек-лист выполнения

### Подготовка (1-2 часа)

- [ ] Проанализировать текущие зависимости между методами
- [ ] Создать типы для всех доменных фасадов
- [ ] Подготовить тесты для текущей функциональности (snapshot tests)
- [ ] Создать новую структуру директорий

### Создание доменных фасадов (3-4 часа)

- [ ] **IssueFacade** - методы работы с задачами
  - [ ] Создать `issues.facade.ts`
  - [ ] Перенести методы: `get()`, `find()`, `create()`, `update()`
  - [ ] Перенести методы: `getChangelog()`, `getTransitions()`, `transition()`
  - [ ] Написать unit тесты
  - [ ] Размер файла: ~150 строк

- [ ] **QueueFacade** - методы работы с очередями
  - [ ] Создать `queues.facade.ts`
  - [ ] Перенести методы: `list()`, `get()`, `create()`, `update()`
  - [ ] Перенести методы: `getFields()`, `manageAccess()`
  - [ ] Написать unit тесты
  - [ ] Размер файла: ~100 строк

- [ ] **ProjectFacade** - методы работы с проектами
  - [ ] Создать `projects.facade.ts`
  - [ ] Перенести методы: `list()`, `get()`, `create()`, `update()`, `delete()`
  - [ ] Написать unit тесты
  - [ ] Размер файла: ~80 строк

- [ ] **ComponentFacade** - методы работы с компонентами
  - [ ] Создать `components.facade.ts`
  - [ ] Перенести методы: `list()`, `create()`, `update()`, `delete()`
  - [ ] Написать unit тесты
  - [ ] Размер файла: ~70 строк

- [ ] **LinkFacade** - методы работы со связями
  - [ ] Создать `links.facade.ts`
  - [ ] Перенести методы: `list()`, `create()`, `delete()`
  - [ ] Написать unit тесты
  - [ ] Размер файла: ~60 строк

- [ ] **CommentFacade** - методы работы с комментариями
  - [ ] Создать `comments.facade.ts`
  - [ ] Перенести методы: `add()`, `list()`, `edit()`, `delete()`
  - [ ] Написать unit тесты
  - [ ] Размер файла: ~70 строк

- [ ] **ChecklistFacade** - методы работы с чеклистами
  - [ ] Создать `checklists.facade.ts`
  - [ ] Перенести методы: `list()`, `addItem()`, `updateItem()`, `deleteItem()`
  - [ ] Написать unit тесты
  - [ ] Размер файла: ~70 строк

- [ ] **WorklogFacade** - методы работы с учётом времени
  - [ ] Создать `worklogs.facade.ts`
  - [ ] Перенести методы: `list()`, `add()`, `update()`, `delete()`
  - [ ] Написать unit тесты
  - [ ] Размер файла: ~70 строк

- [ ] **AttachmentFacade** - методы работы с вложениями
  - [ ] Создать `attachments.facade.ts`
  - [ ] Перенести методы: `list()`, `upload()`, `download()`, `delete()`, `getThumbnail()`
  - [ ] Написать unit тесты
  - [ ] Размер файла: ~100 строк

### Рефакторинг главного фасада (1 час)

- [ ] Создать агрегатор `YandexTrackerFacade`
- [ ] Добавить свойства для всех доменных фасадов
- [ ] Добавить deprecated методы для обратной совместимости
- [ ] Размер файла: ~200 строк (вместо 745)

### Обновление DI контейнера (30 минут)

- [ ] Зарегистрировать все доменные фасады в `container.ts`
- [ ] Обновить `TYPES` символы
- [ ] Протестировать регистрацию зависимостей

### Обновление tools (1 час)

- [ ] Обновить импорты в tools (использовать доменные фасады)
- [ ] Например: `facade.getIssues()` → `facade.issues.get()`
- [ ] Запустить тесты tools
- [ ] Исправить ошибки

### Финализация (30 минут)

- [ ] Обновить barrel exports (`index.ts`)
- [ ] Обновить документацию (README.md)
- [ ] Запустить полный набор тестов: `npm run validate`
- [ ] Проверить покрытие кода: `npm run test:coverage`
- [ ] Обновить CHANGELOG.md

---

## 🔧 Пример кода

### До рефакторинга

```typescript
// tools/api/issues/get/get-issues.tool.ts
export class GetIssuesTool extends BaseTool<YandexTrackerFacade> {
  async execute(params: ToolCallParams): Promise<ToolResult> {
    const results = await this.facade.getIssues(issueKeys);
    // ...
  }
}
```

### После рефакторинга

```typescript
// tracker_api/facade/issues/issues.facade.ts
export class IssueFacade {
  constructor(private readonly container: Container) {}

  private getOperation<T>(operationName: string): T {
    return this.container.get<T>(Symbol.for(operationName));
  }

  async get(issueKeys: string[]): Promise<BatchIssueResult[]> {
    const operation = this.getOperation<{
      execute: (keys: string[]) => Promise<BatchIssueResult[]>;
    }>('GetIssuesOperation');
    return operation.execute(issueKeys);
  }

  async find(params: FindIssuesInputDto): Promise<FindIssuesResult> {
    const operation = this.getOperation<{
      execute: (params: FindIssuesInputDto) => Promise<FindIssuesResult>;
    }>('FindIssuesOperation');
    return operation.execute(params);
  }

  // ... остальные методы
}

// tools/api/issues/get/get-issues.tool.ts
export class GetIssuesTool extends BaseTool<YandexTrackerFacade> {
  async execute(params: ToolCallParams): Promise<ToolResult> {
    // Используем доменный фасад
    const results = await this.facade.issues.get(issueKeys);
    // ...
  }
}
```

---

## 📐 Метрики успеха

### До (актуальное состояние после ревью)
- YandexTrackerFacade: **1080 строк** (было 745 в первоначальном плане)
- Количество методов: **61 метод** (было 30+ в первоначальном плане)
- Количество доменов: **12** (было 9 в первоначальном плане)
- SRP нарушения: 1 критическое
- max-lines нарушения: 1 критическое (превышение в 2.7 раза!)

### После
- YandexTrackerFacade: ~150-200 строк (агрегатор с lazy getters)
- Доменные фасады: **12 файлов** по 60-150 строк
- Количество методов на класс: 3-7
- SRP нарушения: 0
- max-lines нарушения: 0
- Соответствие текущей DI архитектуре: ✅ (lazy initialization)

---

## ⚠️ Важные замечания

1. **Обратная совместимость:**
   - Сохранить deprecated методы в главном фасаде
   - Добавить JSDoc с предупреждениями и миграционными путями
   - Планировать удаление в следующей major версии

2. **DI контейнер:**
   - Каждый доменный фасад регистрируется отдельно
   - Главный фасад создаёт доменные фасады в конструкторе
   - Все используют один контейнер для получения операций

3. **Импорты в tools:**
   - Типы остаются прежними (`YandexTrackerFacade`)
   - Доступ к методам через свойства: `facade.issues.get()`
   - Минимальные изменения в существующем коде

4. **Тестирование:**
   - Сначала snapshot tests текущей функциональности
   - Unit тесты для каждого доменного фасада
   - Integration tests остаются без изменений

---

## 🚀 Следующий этап

После завершения этого этапа переходить к **1.2_tool_registry_refactoring_sequential.md**
