# Этап 1.1: Завершение Comments API (Parallel)

**Статус:** ✅ Завершен
**Приоритет:** Высокий
**Время:** 1-2 дня → Выполнено за 1 сессию
**Зависимости:** Этап 0 (Critical Fixes) завершен
**Параллельность:** ✅ ДА (независимо от других этапов)
**Дата завершения:** 2025-11-18

## 🎯 Цель

Довести Comments API до production ready: дописать недостающие тесты и обновить документацию.

## 📊 Текущее состояние

**✅ Реализовано:**
- Entity: `Comment` (src/tracker_api/entities/comment/)
- DTO: 4 типа (Add, Edit, Get, Output)
- Operations: 4 операции (Add, Edit, Get, Delete)
- MCP Tools: 4 инструмента
- Unit-тесты для operations: ✅ 5 файлов

**❌ Отсутствует:**
- Unit-тесты для MCP tools (0 из 4)
- Integration-тесты для MCP tools (0 из 4)
- Документация (entities/README.md, operations/README.md, tools/README.md)
- Test fixtures

**Покрытие:** ~60% → Цель: ≥90%

---

## ✅ Чек-лист задач

### 1. Test Fixtures

- [ ] **1.1** Создать `tests/fixtures/entities/comment.fixture.ts`
  ```typescript
  import type { Comment } from '@tracker_api/entities/comment/index.js';
  import { createUserRefFixture } from './user-ref.fixture.js';

  export const createCommentFixture = (overrides?: Partial<Comment>): Comment => ({
    id: '12345',
    self: 'https://api.tracker.yandex.net/v2/issues/TEST-1/comments/12345',
    text: 'Test comment text',
    createdBy: createUserRefFixture(),
    createdAt: '2025-01-18T10:00:00.000+0000',
    version: 1,
    transport: 'internal',
    ...overrides,
  });

  export const createCommentWithAttachmentsFixture = (): Comment => ({
    ...createCommentFixture(),
    attachments: [
      { id: 'att1', name: 'file.pdf', size: 1024 },
    ],
  });
  ```

- [ ] **1.2** Создать `tests/fixtures/dto/comment.dto.fixture.ts`
  ```typescript
  export const createAddCommentInputFixture = () => ({
    text: 'New comment',
    attachmentIds: [],
  });

  export const createEditCommentInputFixture = () => ({
    text: 'Updated comment',
  });
  ```

### 2. Unit-тесты для MCP Tools

#### 2.1 Add Comment Tool

- [ ] **2.1.1** Создать `tests/unit/tools/api/comments/add/add-comment.tool.test.ts`
  ```typescript
  describe('AddCommentTool', () => {
    it('should have correct name yandex_tracker_add_comment', () => {
      // ...
    });

    it('should have correct category', () => {
      // ...
    });

    it('should validate schema correctly', () => {
      // valid input
      // invalid input (missing issueId, empty text, etc.)
    });

    it('should call AddCommentOperation with correct params', async () => {
      // mock operation
      // execute tool
      // verify operation called with correct args
    });

    it('should format output correctly', async () => {
      // ...
    });

    it('should handle errors from operation', async () => {
      // operation throws error
      // tool should propagate error with context
    });
  });
  ```

#### 2.2 Get Comments Tool

- [ ] **2.2.1** Создать `tests/unit/tools/api/comments/get/get-comments.tool.test.ts`
  - Тесты для pagination (perPage, page)
  - Тесты для expand параметра
  - Тесты для пустого результата
  - Тесты для множественных комментариев

#### 2.3 Edit Comment Tool

- [ ] **2.3.1** Создать `tests/unit/tools/api/comments/edit/edit-comment.tool.test.ts`
  - Тесты для обновления текста
  - Валидация commentId
  - Error handling

#### 2.4 Delete Comment Tool

- [ ] **2.4.1** Создать `tests/unit/tools/api/comments/delete/delete-comment.tool.test.ts`
  - Тесты для успешного удаления
  - Валидация обязательных параметров
  - Error handling для несуществующего комментария

### 3. Integration-тесты для MCP Tools

⚠️ **Важно:** Integration тесты работают с реальным API Яндекс.Трекера!

#### 3.1 Подготовка

- [ ] **3.1.1** Создать директорию `tests/integration/tools/api/comments/`
- [ ] **3.1.2** Проверить наличие тестовой очереди и задачи
  ```bash
  # В integration тестах использовать env переменные:
  # TEST_ISSUE_KEY - ключ тестовой задачи для комментариев
  ```

#### 3.2 Add Comment Integration Test

- [ ] **3.2.1** Создать `tests/integration/tools/api/comments/add/add-comment.tool.integration.test.ts`
  ```typescript
  describe('AddCommentTool Integration', () => {
    it('should add comment to real issue', async () => {
      const tool = container.get(AddCommentTool);
      const result = await tool.execute({
        issueId: process.env.TEST_ISSUE_KEY!,
        text: `Integration test comment ${Date.now()}`,
      });

      expect(result).toBeDefined();
      expect(result.comment.id).toBeDefined();
      expect(result.comment.text).toContain('Integration test');

      // Cleanup: удалить созданный комментарий
    });

    it('should add comment with attachment IDs', async () => {
      // ...
    });
  });
  ```

#### 3.3 Get Comments Integration Test

- [ ] **3.3.1** Создать `tests/integration/tools/api/comments/get/get-comments.tool.integration.test.ts`
  - Тест: получить список комментариев реальной задачи
  - Тест: pagination работает корректно
  - Тест: expand параметр возвращает attachments

#### 3.4 Edit Comment Integration Test

- [ ] **3.4.1** Создать `tests/integration/tools/api/comments/edit/edit-comment.tool.integration.test.ts`
  - Тест: создать комментарий, затем отредактировать
  - Проверить, что updatedAt изменился
  - Cleanup после теста

#### 3.5 Delete Comment Integration Test

- [ ] **3.5.1** Создать `tests/integration/tools/api/comments/delete/delete-comment.tool.integration.test.ts`
  - Тест: создать комментарий, затем удалить
  - Проверить, что комментарий действительно удален (GET возвращает 404)

### 4. Документация

#### 4.1 Entities README

- [ ] **4.1.1** Обновить `packages/servers/yandex-tracker/src/tracker_api/entities/README.md`

  Добавить секцию:
  ```markdown
  ## Comment Entity

  Представляет комментарий к задаче в Яндекс.Трекере.

  **Расположение:** `entities/comment/comment.entity.ts`

  ### Поля

  | Поле | Тип | Обязательное | Описание |
  |------|-----|--------------|----------|
  | id | string | ✅ | Идентификатор комментария |
  | self | string | ✅ | URL комментария |
  | text | string | ✅ | Текст комментария |
  | createdBy | UserRef | ✅ | Автор комментария |
  | createdAt | string | ✅ | Дата создания (ISO 8601) |
  | updatedBy | UserRef | ❌ | Кто обновил |
  | updatedAt | string | ❌ | Дата обновления |
  | version | number | ❌ | Версия комментария |
  | transport | string | ❌ | 'internal' или 'email' |
  | attachments | array | ❌ | Прикрепленные файлы |

  ### Пример использования

  \`\`\`typescript
  import type { Comment } from '@tracker_api/entities/comment/index.js';

  const comment: Comment = {
    id: '12345',
    self: 'https://api.tracker.yandex.net/v2/issues/TEST-1/comments/12345',
    text: 'This is a comment',
    createdBy: { id: 'user1', display: 'John Doe', self: '...' },
    createdAt: '2025-01-18T10:00:00.000+0000',
  };
  \`\`\`
  ```

#### 4.2 Operations README

- [ ] **4.2.1** Обновить `packages/servers/yandex-tracker/src/tracker_api/api_operations/README.md`

  Добавить секцию:
  ```markdown
  ## Comment Operations

  ### AddCommentOperation

  Добавляет комментарий к задаче.

  **API Endpoint:** `POST /v2/issues/{issueId}/comments`

  **Пример:**
  \`\`\`typescript
  const operation = container.get(AddCommentOperation);
  const result = await operation.execute('TEST-123', {
    text: 'My comment',
    attachmentIds: ['att1', 'att2'],
  });
  \`\`\`

  ### GetCommentsOperation

  Получает список комментариев задачи.

  **API Endpoint:** `GET /v2/issues/{issueId}/comments`

  **Пример с пагинацией:**
  \`\`\`typescript
  const operation = container.get(GetCommentsOperation);
  const result = await operation.execute('TEST-123', {
    perPage: 50,
    page: 1,
    expand: 'attachments',
  });
  \`\`\`

  ### EditCommentOperation
  [Аналогично...]

  ### DeleteCommentOperation
  [Аналогично...]
  ```

#### 4.3 Tools README

- [ ] **4.3.1** Обновить/создать `packages/servers/yandex-tracker/src/tools/README.md`

  Добавить секцию:
  ```markdown
  ## Comments Tools

  ### yandex_tracker_add_comment

  Добавить комментарий к задаче.

  **Параметры:**
  - `issueId` (string, required) - ключ или ID задачи (например, TEST-123)
  - `text` (string, required) - текст комментария
  - `attachmentIds` (string[], optional) - массив ID прикрепленных файлов

  **Пример MCP запроса:**
  \`\`\`json
  {
    "name": "yandex_tracker_add_comment",
    "arguments": {
      "issueId": "TEST-123",
      "text": "This is a test comment"
    }
  }
  \`\`\`

  **Пример ответа:**
  \`\`\`json
  {
    "comment": {
      "id": "12345",
      "text": "This is a test comment",
      "createdBy": {...},
      "createdAt": "2025-01-18T10:00:00.000+0000"
    }
  }
  \`\`\`

  ### yandex_tracker_get_comments
  [Аналогично...]

  ### yandex_tracker_edit_comment
  [Аналогично...]

  ### yandex_tracker_delete_comment
  [Аналогично...]
  ```

### 5. Валидация и покрытие

- [ ] **5.1** Запустить unit-тесты
  ```bash
  npm run test --workspace=@mcp-server/yandex-tracker
  ```
  - Все новые тесты должны проходить

- [ ] **5.2** Запустить integration-тесты
  ```bash
  npm run test:integration --workspace=@mcp-server/yandex-tracker
  ```
  - Убедиться, что integration тесты проходят
  - Cleanup тестовых данных

- [ ] **5.3** Проверить покрытие Comments API
  ```bash
  npm run test:coverage --workspace=@mcp-server/yandex-tracker
  ```
  - Comment entity: ≥90%
  - Comment operations: ≥90%
  - Comment tools: ≥90%

- [ ] **5.4** Запустить валидацию
  ```bash
  npm run validate --workspace=@mcp-server/yandex-tracker
  ```
  - Lint: успешно
  - TypeCheck: успешно
  - Tests: успешно

### 6. Финальная проверка

- [ ] **6.1** Проверить архитектуру
  ```bash
  npm run depcruise
  ```
  - Нет нарушений dependency graph

- [ ] **6.2** Проверить дублирование
  ```bash
  npm run cpd
  ```
  - ≤5% дублирования

- [ ] **6.3** Проверить документацию
  - Все примеры работают
  - Нет битых ссылок
  - Все секции актуальны

### 7. Коммит и обновление статуса

- [ ] **7.1** Создать коммит
  ```
  test(comments): добавить полное покрытие тестами для Comments API

  Добавлены тесты:
  - Unit-тесты для 4 MCP tools (add, get, edit, delete)
  - Integration-тесты для 4 MCP tools
  - Fixtures для Comment entity и DTO

  Обновлена документация:
  - entities/README.md - секция Comment Entity
  - api_operations/README.md - секция Comment Operations
  - tools/README.md - секция Comments Tools с примерами

  Результат:
  - Покрытие Comments API: 60% → 90%+
  - Все тесты проходят
  - Документация актуальна

  🤖 Generated with Claude Code
  Co-Authored-By: Claude <noreply@anthropic.com>
  ```

- [ ] **7.2** Запушить изменения
  ```bash
  git push -u origin claude/complete-comments-api-[SESSION_ID]
  ```

- [ ] **7.3** Обновить статус в `0.0_overview.md`
  ```markdown
  | **1.1 Comments** | `2.1_complete_comments_api_parallel.md` | ✅ | 1-2д | Claude | 2025-11-XX |
  ```

  И в таблице API:
  ```markdown
  | **Comments** | 4 | 4 | ✅ | ✅ 90%+ | ✅ | ✅ Готов |
  ```

---

## 📊 Метрики успеха

**До:**
- Operations tests: ✅ 5 файлов
- Tools unit tests: ❌ 0
- Tools integration tests: ❌ 0
- Документация: ❌ Нет
- **Покрытие: ~60%**

**После:**
- Operations tests: ✅ 5 файлов
- Tools unit tests: ✅ 4 файла (add, get, edit, delete)
- Tools integration tests: ✅ 4 файла
- Fixtures: ✅ 2 файла
- Документация: ✅ Полная
- **Покрытие: ≥90%**

---

## ⏭️ Следующие шаги

После завершения этого этапа:
- Comments API полностью готов к production
- Можно использовать как reference для других API
- Продолжить с другими этапами (2.2, 2.3, 2.4) параллельно

---

**Создано:** 2025-11-18
**Версия:** 1.0
