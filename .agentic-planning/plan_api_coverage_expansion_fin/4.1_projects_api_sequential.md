# Этап 3.1: Projects API - Полная реализация (Sequential)

**Статус:** ⏪ Не начат
**Приоритет:** Средний
**Время:** 4-5 дней
**Зависимости:** Queues API ПОЛНОСТЬЮ завершен (Этап 1.3)
**Параллельность:** ⚠️ Может выполняться параллельно с Components API

## 🎯 Цель

Реализовать Projects API (зависит от Queues).

## API Endpoints

| Метод | Endpoint | Описание |
|-------|----------|----------|
| GET | `/v2/projects` | Список проектов |
| GET | `/v2/projects/{projectId}` | Параметры проекта |
| POST | `/v2/projects` | Создать проект |
| PATCH | `/v2/projects/{projectId}` | Обновить проект |
| DELETE | `/v2/projects/{projectId}` | Удалить проект |

## MCP Tools (5)

1. `yandex_tracker_get_projects`
2. `yandex_tracker_get_project`
3. `yandex_tracker_create_project`
4. `yandex_tracker_update_project`
5. `yandex_tracker_delete_project`

---

## ⚠️ Зависимость от Queues API

**Требует:**
- `Queue` entity для типизации
- `CreateProjectInput.queueIds: string[]` - массив ключей очередей

**НЕЛЬЗЯ НАЧИНАТЬ** пока Queues API не завершен полностью (Этап 1.3)!

---

## ✅ Чек-лист задач

### 1. Entities

- [ ] **1.1** Создать `project.entity.ts`
```typescript
export interface Project extends WithUnknownFields {
  id: string;
  self: string;
  key: string;
  name: string;
  lead: UserRef;
  status: ProjectStatus; // 'draft' | 'in_progress' | 'launched' | 'postponed' | 'at_risk'
  startDate?: string;
  endDate?: string;
  queues?: Array<{ id: string; key: string; display: string }>;
}

export type ProjectStatus = 'draft' | 'in_progress' | 'launched' | 'postponed' | 'at_risk';
```

### 2. DTO

- [ ] **2.1** `create-project.input.ts` (name, queueIds[], lead, status, dates)
- [ ] **2.2** `update-project.input.ts` (Partial)
- [ ] **2.3** `get-projects.input.ts` (фильтры, пагинация)
- [ ] **2.4** Output DTO

### 3. Operations (5)

- [ ] **3.1-3.5** GetProjects, GetProject, CreateProject, UpdateProject, DeleteProject
- [ ] **3.6** Unit-тесты для всех

### 4. MCP Tools (5)

- [ ] **4.1-4.5** Создать 5 tools (get-projects, get-project, create-project, update-project, delete-project)
  - Schema с enum для ProjectStatus
  - Unit + Integration тесты

### 5. Infrastructure + Docs + Validation

- [ ] **5.1** DI, Fixtures
- [ ] **5.2** Документация (связь с очередями!)
- [ ] **5.3** Coverage ≥90%, validate

### 6. Коммит

```
feat: реализовать полную поддержку Projects API

Добавлены:
- Project entity с ProjectStatus типами
- 5 operations (GetProjects, GetProject, CreateProject, UpdateProject, DeleteProject)
- 5 MCP tools
- Поддержка связи проектов с очередями
- Полное покрытие тестами ≥90%

Зависимости: Queues API

🤖 Generated with Claude Code
Co-Authored-By: Claude <noreply@anthropic.com>
```

---

**Создано:** 2025-11-18
