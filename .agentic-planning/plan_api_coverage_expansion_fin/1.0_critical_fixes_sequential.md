# Этап 0: Критические исправления (Sequential)

**Статус:** ⏪ Не начат
**Приоритет:** 🔴 КРИТИЧЕСКИЙ (БЛОКИРУЕТ ВСЁ!)
**Время:** 1-2 дня
**Зависимости:** Нет
**Параллельность:** ❌ НЕТ (обязательно последовательно)

## 🎯 Цель

Исправить критические проблемы с тестовой инфраструктурой, чтобы разблокировать всю дальнейшую работу.

## 🔥 Проблемы

1. **39 тестов падают** из 62 из-за ошибок импорта:
   ```
   Error: Failed to resolve entry for package "@mcp-framework/core"
   Error: Failed to resolve entry for package "@mcp-framework/infrastructure"
   ```

2. **`npm run validate` не проходит** из-за падающих тестов

3. **Невозможно проверить покрытие** новых модулей

---

## ✅ Чек-лист задач

### 1. Диагностика проблемы

- [ ] **1.1** Проанализировать ошибки импортов
  ```bash
  npm run test:coverage --workspace=@mcp-server/yandex-tracker
  ```
  - Записать все уникальные ошибки
  - Определить паттерн проблемы

- [ ] **1.2** Проверить package.json конфигурацию @mcp-framework пакетов
  ```bash
  cat packages/framework/core/package.json | grep -A5 "exports"
  cat packages/framework/infrastructure/package.json | grep -A5 "exports"
  ```
  - Проверить наличие `exports` полей
  - Проверить `main`, `module`, `types` поля
  - Сравнить с рабочими пакетами

- [ ] **1.3** Проверить vitest.config.ts
  ```bash
  cat packages/servers/yandex-tracker/vitest.config.ts
  ```
  - Проверить resolve.alias настройки
  - Проверить resolve.conditions
  - Проверить deps.optimizer

- [ ] **1.4** Проверить tsconfig.json
  ```bash
  cat packages/servers/yandex-tracker/tsconfig.json | grep -A10 "paths"
  ```
  - Проверить paths mapping
  - Проверить moduleResolution

### 2. Исправление импортов @mcp-framework

**Возможные решения (попробовать по порядку):**

#### 2.1 Вариант A: Исправить exports в package.json

- [ ] **2.1.1** Проверить, собраны ли @mcp-framework пакеты
  ```bash
  npm run build --workspace=@mcp-framework/core
  npm run build --workspace=@mcp-framework/infrastructure
  ```

- [ ] **2.1.2** Если нужно, обновить `exports` в @mcp-framework/core/package.json
  ```json
  {
    "exports": {
      ".": {
        "import": "./dist/index.js",
        "types": "./dist/index.d.ts"
      }
    }
  }
  ```

- [ ] **2.1.3** Если нужно, обновить `exports` в @mcp-framework/infrastructure/package.json

#### 2.2 Вариант B: Настроить vitest.config.ts

- [ ] **2.2.1** Добавить в `vitest.config.ts`:
  ```typescript
  export default defineConfig({
    test: {
      // ...
    },
    resolve: {
      alias: {
        '@mcp-framework/core': path.resolve(__dirname, '../../../framework/core/src/index.ts'),
        '@mcp-framework/infrastructure': path.resolve(__dirname, '../../../framework/infrastructure/src/index.ts'),
      },
      conditions: ['node', 'import', 'default'],
    },
  });
  ```

#### 2.3 Вариант C: Использовать deps.optimizer

- [ ] **2.3.1** Настроить deps в vitest.config.ts:
  ```typescript
  export default defineConfig({
    test: {
      deps: {
        optimizer: {
          ssr: {
            enabled: true,
            include: ['@mcp-framework/core', '@mcp-framework/infrastructure'],
          },
        },
      },
    },
  });
  ```

### 3. Запустить тесты и проверить

- [ ] **3.1** Запустить все тесты в yandex-tracker
  ```bash
  cd packages/servers/yandex-tracker
  npm run test
  ```
  - Цель: 0 падающих тестов
  - Если есть падающие - вернуться к шагу 2

- [ ] **3.2** Проверить покрытие
  ```bash
  npm run test:coverage
  ```
  - Должно генерироваться без ошибок
  - Проверить текущее покрытие

- [ ] **3.3** Запустить валидацию в yandex-tracker
  ```bash
  npm run validate
  ```
  - Lint должен пройти
  - TypeCheck должен пройти
  - Tests должны пройти

### 4. Проверить другие workspace

- [ ] **4.1** Запустить тесты в @mcp-framework/core
  ```bash
  npm run test --workspace=@mcp-framework/core
  ```

- [ ] **4.2** Запустить тесты в @mcp-framework/infrastructure
  ```bash
  npm run test --workspace=@mcp-framework/infrastructure
  ```

- [ ] **4.3** Запустить валидацию всего monorepo
  ```bash
  npm run validate
  ```
  - Из корня monorepo
  - Все workspace должны пройти

### 5. Исправить дополнительные проблемы (если есть)

- [ ] **5.1** Если есть lint ошибки - исправить
  ```bash
  npm run lint:fix --workspace=@mcp-server/yandex-tracker
  ```

- [ ] **5.2** Если есть TypeScript ошибки - исправить
  ```bash
  npm run typecheck --workspace=@mcp-server/yandex-tracker
  ```

- [ ] **5.3** Если есть другие проблемы - документировать и исправить

### 6. Документирование решения

- [ ] **6.1** Создать файл `docs/troubleshooting/vitest-imports.md`
  - Описать проблему
  - Описать решение
  - Примеры конфигурации

- [ ] **6.2** Обновить `packages/servers/yandex-tracker/tests/README.md`
  - Добавить секцию "Known Issues"
  - Добавить решение проблемы с импортами

### 7. Финальная валидация

- [ ] **7.1** Очистить всё и пересобрать с нуля
  ```bash
  npm run clean:all
  npm install
  npm run build
  npm run test
  ```
  - Убедиться, что всё работает "с чистого листа"

- [ ] **7.2** Запустить полную валидацию
  ```bash
  npm run validate
  npm run test:coverage --workspace=@mcp-server/yandex-tracker
  npm run depcruise
  ```
  - Всё должно быть зелёным

- [ ] **7.3** Проверить текущие метрики покрытия
  ```bash
  npm run test:coverage --workspace=@mcp-server/yandex-tracker 2>&1 | grep -A5 "Coverage"
  ```
  - Записать базовую линию для дальнейшего улучшения

### 8. Коммит и документация

- [ ] **8.1** Создать коммит
  ```
  fix: исправить падающие тесты из-за проблем с импортами @mcp-framework

  Проблема:
  - 39 тестов падали с ошибкой "Failed to resolve entry for package"
  - Невозможно было запустить test:coverage
  - npm run validate не проходил

  Решение:
  - [Описать конкретное решение, которое сработало]
  - Обновлен vitest.config.ts / package.json [что конкретно]
  - Добавлена документация по troubleshooting

  Результат:
  - 0 падающих тестов
  - test:coverage работает
  - validate проходит успешно

  🤖 Generated with Claude Code
  Co-Authored-By: Claude <noreply@anthropic.com>
  ```

- [ ] **8.2** Запушить изменения
  ```bash
  git push -u origin claude/review-api-coverage-plan-01LheHKvLfoPLcNowGcSXYwR
  ```

- [ ] **8.3** Обновить статус в `0.0_overview.md`
  - Изменить статус Этапа 0 с ⏪ на ✅
  - Добавить дату завершения и имя агента
  - Обновить метрику "Падающие тесты": 39 → 0

---

## 📊 Метрики успеха

**До:**
- ❌ 39 падающих тестов из 62
- ❌ `npm run validate` не проходит
- ❌ Невозможно проверить coverage

**После:**
- ✅ 0 падающих тестов
- ✅ `npm run validate` успешно
- ✅ Coverage report генерируется
- ✅ Все workspace тестируются

---

## ⚠️ Важные заметки

### Этот этап БЛОКИРУЕТ всю дальнейшую работу!

Без исправления этих проблем:
- ❌ Нельзя писать новые тесты (не будут запускаться)
- ❌ Нельзя проверить покрытие (будет падать)
- ❌ Нельзя коммитить (pre-commit hooks могут падать)
- ❌ Нельзя гарантировать качество кода

### Если проблема сложнее, чем кажется

1. **Не тратить больше 3 часов** на один подход
2. **Попробовать альтернативные решения** (Варианты A, B, C)
3. **Спросить у пользователя** если все варианты не работают
4. **Документировать процесс** для других агентов

### Полезные команды для отладки

```bash
# Проверить, какие файлы резолвятся
node --experimental-vm-modules node_modules/vitest/vitest.mjs --run --reporter=verbose

# Проверить exports в runtime
node -e "import('@mcp-framework/core').then(console.log).catch(console.error)"

# Проверить TypeScript resolution
npx tsc --traceResolution | grep "@mcp-framework"

# Очистить все кэши
rm -rf node_modules/.vite
rm -rf node_modules/.vitest
npm run clean
```

---

## 🔗 Следующие шаги

После успешного завершения этого этапа можно **параллельно** запускать:
- `2.1_complete_comments_api_parallel.md`
- `2.2_complete_links_api_parallel.md`
- `2.3_complete_queues_api_parallel.md`
- `2.4_finalize_attachments_api_parallel.md`

И даже начинать:
- `3.1_checklists_api_parallel.md`
- `3.2_worklog_api_parallel.md`

---

## 📝 Шаблон для обновления overview

После завершения, обновить в `0.0_overview.md`:

```markdown
| **0. Critical Fixes** | `1.0_critical_fixes_sequential.md` | ✅ | 1-2д | Claude | 2025-11-XX |
```

И обновить метрики:
```markdown
| Падающие тесты | 🔴 39 из 62 | ✅ 0 из ~100 |
| Validate | 🔴 Не проходит | ✅ Успешно |
```

---

**Создано:** 2025-11-18
**Обновлено:** -
**Версия:** 1.0
