# Разбор находок ревью плана (2026-08-26)

Ревьюер — отдельный агент, читал план, отчёт прогона, инвентарь и код. Каждая находка
перепроверена по коду мной; ниже — вердикт и что с ней сделано в редакции 2 плана.

| № | Sev | Вердикт | Что сделано |
|---|---|---|---|
| 1 | HIGH | **подтверждена** | `users` входит в `PERSON_FIELDS` (`people-in-body.ts:47`), обход тела глобальный (`live-scope.guard.ts:55`), `personRefs` обёртку `{add}` не распознаёт (`people-in-body.ts:58-73`) → любое новое тело отклонялось бы. `people-in-body.ts` добавлен в пакет C1, ложное утверждение из плана убрано |
| 2 | HIGH | **подтверждена** | Журнал подписан меткой и чужой не принимает. Живая проверка переписана: та же метка `sweep7-2026-08-26`, тот же файл журнала; журнал проверен — цел, все шесть родов сущностей на месте |
| 3 | HIGH | **подтверждена** | Матрица генерируется, исключение категорийное (`live-exempt-categories.ts`), `coverage:check` в `validate` не входит. Выделено в отдельный пакет E, с оговоркой, что посылка устарела шире, чем на два инструмента |
| 4 | MEDIUM | **подтверждена** | `Permissions.fields` референсного клиента (`collections.py:394-403`) действительно без `deny`. Пакет C разбит на C1 (запрос) и C2 (ответ после живого наблюдения) |
| 5 | MEDIUM | **подтверждена** | Документация даёт `"groups": {"add": [4]}` — числа. Контракт: `subjects: (string \| number)[]` |
| 6 | MEDIUM | **подтверждена** | Четыре артефакта проверены поимённо (`test-fields.ts:70`, `legacy-mock-tests.ts:72,84`, оба `index.ts`); добавлены в наборы C1/C2 |
| 7 | MEDIUM | **подтверждена** | Требование объявлять подготовительный GET в каждом кейсе внесено в пакет B, DoD переформулирован |
| 8 | MEDIUM | **подтверждена** | Индексная сигнатура `[key: string]: unknown` схлопывает `Omit`. Гарантия переведена из типа в явную деструктуризацию и тест |
| 9 | MEDIUM | **подтверждена** | Снятие `version` из `SPRINT_KEYS` внесено в пакет B |
| 10 | MEDIUM | **подтверждена** | `CHANGELOG` генерируется semantic-release; заменено на футер `BREAKING CHANGE:` и мерж `--no-ff` |
| 11 | LOW | **подтверждена** | `manifest.json` схем не содержит — убран из пакета D |
| 12 | LOW | **подтверждена** | `update-board.metadata.ts:22` до сих пор перечисляет снятый в 4.0.0 `version`. Приведение `redactionAllowlist` внесено в A и C; общий машинный барьер — отдельной задачей |
| 13 | LOW | **подтверждена** | Сужение контракта объявлено решением с причиной в комментарии схемы |
| 14 | LOW | **подтверждена** | DoD пакета A переписан на `grep -rn` |
| 15 | LOW | **подтверждена живой пробой, и хуже, чем предполагал ревьюер** | `manage_sprint_lifecycle` `start` и `archive` дают `428` на спринтах `236` и `237`; `delete` работает (спринт `237` удалён). Инструмент включён в пакет B, а не выведен за область |

**Ни одна находка не отклонена.** Тестовый план дополнен всеми семью кейсами из
раздела «чего не названо», включая негативный кейс на старую форму тела доступов.
