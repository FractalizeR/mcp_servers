# Этап 3.1 — Документация, валидация, ревью (SEQUENTIAL, после этапа 2)

Один агент/оркестратор, после слияния всех групп этапа 2.

## Шаги

1. **Слияние и проверка изоляции**
   - `git diff --stat`; убедиться, что группы 2.x не пересеклись по файлам.
   - X2: `composition-root/definitions/*` НЕ должны меняться (рефакторинг правит
     существующие компоненты, новых tool/operation нет; `CursorCodec` — статический
     util без DI). Если эти файлы в диффе — расследовать.
   - Общий прогон `npm run validate:quiet` — должен быть 0.

2. **Удаление legacy `page` (R5)** — отдельным коммитом ПОСЛЕ миграции всех 10:
   - Удалить `@deprecated` `PageSchema`/`noPageFetchAllConflict` из common schema,
     `PaginationMeta.page`/`PaginationParams.page` из entity, остатки `page` в DTO/
     операциях. Перепроверить сборку/knip (мёртвые экспорты не должны остаться).

2. **Документация (под cursor)**
   - `src/tools/README.md` — секция пагинации: page → cursor; правило seek-gating
     (`total`/`totalPages` только у seekable); описание `nextCursor`.
   - `src/tracker_api/api_operations/README.md` — секция пагинации операций.
   - `src/tracker_api/utils/README.md` — `CursorCodec` + обновлённый `TrackerPaginator`.
   - `packages/servers/yandex-tracker/CLAUDE.md` — раздел 2.2 «Пагинация» переписать
     на cursor; явно отметить breaking change (`page` удалён).
   - Проверить лимиты доков: `npm run validate:docs`.

3. **Регрессии вживую (опционально, ручная верификация)**
   - Через MCP-инструменты повторить кейсы из диагностики:
     comments/changelog ручным курсором отдают РАЗНЫЕ записи;
     queues/projects листаются курсором; components/attachments — всё разом,
     `hasNextPage=false`.

4. **Ревью реализации (расширенное)**
   Уровень — расширенный (новый кросс-доменный контракт + изменение 10 контрактов):
   Claude + Codex + DeepSeek параллельно из оркестрирующего контекста.
   Фокус ревью:
   - Корректность `CursorCodec` (guard, round-trip, тег эндпоинта, decode БРОСАЕТ).
   - seek-gating: позитив (total СОХРАНЯЕТСЯ у queues/projects, в т.ч. последняя
     страница — R8) И негатив (нет total у comments — регрессия).
   - batch-курсор только при одном issueId (R1); кросс-эндпоинт курсор → ошибка (R13).
   - find_issues: replay POST + сверка хеша тела (R2); fallback не сломан.
   - Сохранность budget/maxTotalItems/ParallelExecutor в batch fetchAll-операциях.
   - Кеш-инварианты links (bypass при cursor — R10); непагинируемые без `pagination`.
   - Каждый issue верифицировать, ложные — отклонять с обоснованием.
   - При необходимости — второй раунд по диффу фиксов.

5. **Финализация**
   - Повторный `npm run validate:quiet` = 0 после фиксов ревью.
   - Обновить память `project_pagination_work` (статус: cursor реализован).
   - Удалить `.agentic-planning/plan_pagination_cursor/` после успешной валидации.
   - Коммиты по этапам на русском; пуш — с разрешения (ветка main).

## Definition of Done (весь план)
- См. `0_overview_and_decisions.md` → раздел DoD.
- Документация консистентна; план удалён; ревью пройдено, issues закрыты.
