# Этап 2.2: Тесты для file-download.util.ts (Parallel)

**✅ СТАТУС: ЗАВЕРШЕН**

**Цель:** Покрыть тестами file-download.util.ts (7% → 90%+)

**Результат:** Покрытие 100% (43 теста, все проходят)

**Тип выполнения:** Parallel (можно выполнять одновременно с 2.1)

**Файл для тестирования:** `packages/servers/yandex-tracker/src/tracker_api/utils/file-download.util.ts`

**Создаваемый файл:** `packages/servers/yandex-tracker/tests/tracker_api/utils/file-download.util.test.ts`

**Оценка времени:** 2-3 часа

**Приоритет:** 🔴 КРИТИЧНО

---

## Чек-лист задач

### 1. Создание структуры тестового файла

- [ ] Создать файл `tests/tracker_api/utils/file-download.util.test.ts`
- [ ] Импортировать необходимые функции из vitest
- [ ] Импортировать тестируемые функции из `#tracker_api/utils/file-download.util.js`
- [ ] Импортировать моки для `fs.promises` (writeFile, mkdir)
- [ ] Импортировать `path` для работы с путями
- [ ] Создать основной `describe('File Download Utilities', ...)` блок

### 2. Тесты для `bufferToBase64(buffer)` (уже покрыто)

**Примечание:** Эта функция уже покрыта на 100%, но нужно убедиться, что тесты существуют

- [ ] **describe('bufferToBase64'):**
  - [ ] Проверить наличие теста: конвертация Buffer в base64
  - [ ] Если тестов нет — добавить:
    - [ ] Тест: пустой Buffer
    - [ ] Тест: Buffer с данными
    - [ ] Тест: проверка корректности base64 строки

### 3. Тесты для `base64ToBuffer(base64)`

**Цель:** Проверить конвертацию base64 строки в Buffer

- [ ] **describe('base64ToBuffer'):**
  - [ ] Тест: валидная base64 строка
    - Входные: `'SGVsbG8gV29ybGQ='` (Hello World в base64)
    - Проверить: результат — Buffer, содержимое корректно
  - [ ] Тест: пустая base64 строка
    - Входные: `''`
    - Проверить: пустой Buffer
  - [ ] Тест: невалидная base64 строка
    - Входные: `'invalid!!!base64'`
    - Проверить: корректная обработка (может быть ошибка или некорректный Buffer)
  - [ ] Тест: двусторонняя конвертация (buffer → base64 → buffer)
    - Проверить: исходный Buffer = результат после двух конвертаций

### 4. Тесты для `saveBufferToFile(buffer, path)`

**Цель:** Проверить сохранение Buffer в файл с созданием директорий

**Важно:** Использовать временные директории для изоляции тестов!

- [ ] **describe('saveBufferToFile'):**
  - [ ] Настроить beforeEach:
    - Создать временную директорию через `fs.mkdtemp(join(tmpdir(), 'test-'))`
    - Сохранить путь в переменную
  - [ ] Настроить afterEach:
    - Удалить временную директорию рекурсивно
  - [ ] Тест: сохранение в существующую директорию
    - Входные: Buffer, путь в существующую temp директорию
    - Проверить: файл создан, содержимое совпадает
  - [ ] Тест: сохранение с созданием вложенных директорий
    - Входные: Buffer, путь `tempDir/nested/deep/file.txt`
    - Проверить: все директории созданы, файл существует
  - [ ] Тест: сохранение пустого Buffer
    - Входные: пустой Buffer
    - Проверить: файл создан, размер 0 байт
  - [ ] Тест: перезапись существующего файла
    - Сначала создать файл с содержимым A
    - Затем сохранить Buffer с содержимым B
    - Проверить: файл перезаписан, содержимое = B
  - [ ] Тест: невалидный путь (содержит null bytes или недопустимые символы)
    - Входные: путь с `\0` или другими недопустимыми символами
    - Ожидаемое поведение: выброс ошибки EINVAL
  - [ ] Тест: путь без прав на запись (если возможно в тестовом окружении)
    - Использовать директорию с правами read-only
    - Ожидаемое поведение: выброс ошибки EACCES

### 5. Тесты для `getFileExtension(filename)`

**Цель:** Проверить извлечение расширения файла

- [ ] **describe('getFileExtension'):**
  - [ ] Тест: файл с расширением
    - Входные: `'document.pdf'`
    - Ожидаемый результат: `'pdf'`
  - [ ] Тест: файл с несколькими точками
    - Входные: `'archive.tar.gz'`
    - Ожидаемый результат: `'gz'` (последнее расширение)
  - [ ] Тест: файл без расширения
    - Входные: `'README'`
    - Ожидаемый результат: `''` (пустая строка)
  - [ ] Тест: файл с точкой в конце
    - Входные: `'file.'`
    - Ожидаемый результат: `''` (нет расширения после точки)
  - [ ] Тест: пустая строка
    - Входные: `''`
    - Ожидаемый результат: `''`
  - [ ] Тест: только расширение (начинается с точки)
    - Входные: `'.gitignore'`
    - Ожидаемый результат: `'gitignore'` или `''` (в зависимости от логики)
  - [ ] Тест: путь с директориями
    - Входные: `'/path/to/file.txt'`
    - Ожидаемый результат: `'txt'` (игнорировать путь, только имя файла)
  - [ ] Тест: Windows путь
    - Входные: `'C:\\Users\\file.docx'`
    - Ожидаемый результат: `'docx'`

### 6. Тесты для `isImage(mimetype)`

**Цель:** Проверить определение MIME типа изображения

- [ ] **describe('isImage'):**
  - [ ] Тест: валидные MIME типы изображений
    - Входные: `'image/png'`, `'image/jpeg'`, `'image/gif'`, `'image/webp'`, `'image/svg+xml'`
    - Ожидаемый результат: `true` для всех
  - [ ] Тест: невалидные MIME типы (не изображения)
    - Входные: `'application/pdf'`, `'text/plain'`, `'video/mp4'`, `'application/json'`
    - Ожидаемый результат: `false` для всех
  - [ ] Тест: пустая строка
    - Входные: `''`
    - Ожидаемый результат: `false`
  - [ ] Тест: undefined/null
    - Входные: `undefined`, `null`
    - Ожидаемый результат: `false`
  - [ ] Тест: MIME тип в верхнем регистре
    - Входные: `'IMAGE/PNG'`
    - Проверить: регистронезависимая проверка (должен быть `true`)
  - [ ] Тест: MIME тип с параметрами
    - Входные: `'image/png; charset=utf-8'`
    - Проверить: корректная обработка (может быть `true` или требовать парсинга)

### 7. Тесты для `isImageByExtension(filename)`

**Цель:** Проверить определение изображения по расширению файла

- [ ] **describe('isImageByExtension'):**
  - [ ] Тест: известные расширения изображений
    - Входные: `'photo.jpg'`, `'image.png'`, `'icon.gif'`, `'logo.svg'`, `'pic.webp'`, `'photo.jpeg'`
    - Ожидаемый результат: `true` для всех
  - [ ] Тест: неизвестные расширения (не изображения)
    - Входные: `'document.pdf'`, `'text.txt'`, `'video.mp4'`, `'archive.zip'`
    - Ожидаемый результат: `false` для всех
  - [ ] Тест: без расширения
    - Входные: `'README'`
    - Ожидаемый результат: `false`
  - [ ] Тест: расширение в верхнем регистре
    - Входные: `'photo.JPG'`, `'image.PNG'`
    - Проверить: регистронезависимая проверка (должен быть `true`)
  - [ ] Тест: расширение со смешанным регистром
    - Входные: `'photo.JpG'`
    - Ожидаемый результат: `true`
  - [ ] Тест: пустая строка
    - Входные: `''`
    - Ожидаемый результат: `false`
  - [ ] Тест: файл с несколькими точками
    - Входные: `'image.backup.png'`
    - Ожидаемый результат: `true` (проверяется последнее расширение)

### 8. Интеграционные тесты (комбинации функций)

- [ ] Тест: сохранение изображения и проверка типа
  - Buffer → сохранить → проверить isImageByExtension
- [ ] Тест: base64 → Buffer → сохранение в файл
  - base64ToBuffer → saveBufferToFile → проверить содержимое файла
- [ ] Тест: проверка типа файла по MIME и расширению
  - Убедиться, что isImage и isImageByExtension дают согласованные результаты

### 9. Валидация и финализация

- [ ] Запустить тесты: `npm test file-download.util.test.ts`
- [ ] Проверить покрытие: должно быть ≥ 90%
- [ ] Исправить failing тесты
- [ ] Проверить ESLint: `npm run lint`
- [ ] Проверить TypeScript: `npm run typecheck`
- [ ] Проверить изоляцию тестов (cleanup временных файлов)

---

## Архитектурные требования

### Изоляция тестов
- **ОБЯЗАТЕЛЬНО:** Использовать временные директории для file system операций
- Создавать temp директорию в `beforeEach` через `fs.mkdtemp(join(tmpdir(), 'test-'))`
- Удалять temp директорию в `afterEach` через `fs.rm(path, { recursive: true })`
- НЕ использовать директории проекта (`src/`, `tests/`) для записи файлов

### Моки vs реальные операции
- Для `saveBufferToFile` — использовать реальный file system (в temp директории)
- Это integration-like подход для проверки реальной работы с FS
- Альтернатива: мокировать `fs.promises.writeFile` и `fs.promises.mkdir`

### Error Handling
- Проверять специфичные ошибки FS: ENOENT, EACCES, EINVAL
- Использовать `expect().rejects.toThrow()` для async функций

### TypeScript
- Импортировать типы для Buffer, PathLike
- Использовать явные типы для тестовых данных

---

## Критерии готовности

✅ Все 25+ тест-кейсов написаны и проходят
✅ Покрытие file-download.util.ts ≥ 90%
✅ Временные файлы корректно очищаются (нет утечек)
✅ Нет ESLint/TypeScript ошибок
✅ Тесты запускаются параллельно и изолированно

---

## Следующий шаг

После завершения:
1. Коммит: `chore: добавить тесты для file-download.util.ts (7% → 90%+)`
2. Пуш в ветку
3. Можно переходить к этапу 3.x (если этап 2.1 тоже завершен)
