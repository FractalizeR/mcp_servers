# 2.4 Обновление DOCS.md (Parallel)

**Тип:** Parallel (можно выполнять параллельно с 2.1, 2.2, 2.3)
**Приоритет:** 🟢 НИЗКИЙ
**Время:** ~15 минут
**Сложность:** Очень низкая

---

## Цель

Актуализировать `DOCS.md` с корректными размерами файлов и автоматизировать обновление.

---

## Проблема

DOCS.md показывает неактуальные размеры (строки 11-12):
- CLAUDE.md: указано "~250 строк", фактически 333
- ARCHITECTURE.md: указано "~400 строк", фактически 564

**Воздействие:** Вводит в заблуждение при навигации.

---

## Решение

1. Обновить размеры файлов до актуальных
2. Добавить примечание о необходимости проверки через `npm run validate:docs`
3. (Опционально) Создать скрипт автоматического обновления

---

## Задачи

### ✅ Задача 2.4.1: Обновить размеры в таблице

**Файл:** `DOCS.md`

**Изменения (строки 9-12):**

Было:
```markdown
| Файл | Назначение | Размер |
|------|-----------|--------|
| **[CLAUDE.md](./CLAUDE.md)** | ✅ **Главный файл** — правила, чек-листы, критические требования | ~250 строк |
| **[ARCHITECTURE.md](./ARCHITECTURE.md)** | Архитектура проекта — концепции, паттерны, поток данных | ~400 строк |
```

Стало:
```markdown
| Файл | Назначение | Размер |
|------|-----------|--------|
| **[CLAUDE.md](./CLAUDE.md)** | ✅ **Главный файл** — правила, чек-листы, критические требования | 333 строки |
| **[ARCHITECTURE.md](./ARCHITECTURE.md)** | Архитектура проекта — концепции, паттерны, поток данных | 564 строки |
```

---

### ✅ Задача 2.4.2: Добавить примечание об актуальности

**Добавить после таблицы (строка 13):**

```markdown
> **Примечание:** Размеры актуальны на 2025-11-18.
> Для проверки текущих размеров используй: `npm run validate:docs`
```

---

### ✅ Задача 2.4.3: Добавить модульную документацию в список

**Добавить в секцию "🔧 Модульная документация" информацию о размерах (опционально):**

Было:
```markdown
| Файл | Назначение | Размер |
|------|-----------|--------|
| **[packages/servers/yandex-tracker/src/tools/README.md](./packages/servers/yandex-tracker/src/tools/README.md)** | MCP Tools — конвенции, шаблоны, чек-листы |  |
```

Стало:
```markdown
| Файл | Назначение | Размер |
|------|-----------|--------|
| **[packages/servers/yandex-tracker/src/tools/README.md](./packages/servers/yandex-tracker/src/tools/README.md)** | MCP Tools — конвенции, шаблоны, чек-листы | 486 строк |
```

**Аналогично для других модульных READMEs.**

**Примечание:** Это опционально, так как увеличит размер DOCS.md.

---

### ✅ Задача 2.4.4: (Опционально) Создать скрипт автообновления

**Файл:** `scripts/update-docs-sizes.ts` (новый)

```typescript
#!/usr/bin/env tsx
/**
 * Автоматическое обновление размеров в DOCS.md
 */

import { readFileSync, writeFileSync } from 'node:fs';

const files = {
  'CLAUDE.md': './CLAUDE.md',
  'ARCHITECTURE.md': './ARCHITECTURE.md',
};

function countLines(path: string): number {
  const content = readFileSync(path, 'utf-8');
  return content.split('\n').length;
}

function updateDocsFile(): void {
  const docsPath = './DOCS.md';
  let content = readFileSync(docsPath, 'utf-8');

  // Обновляем размеры
  for (const [name, path] of Object.entries(files)) {
    const lines = countLines(path);
    const regex = new RegExp(`(\\*\\*\\[${name}\\].*?\\|.*?\\|)\\s*~?\\d+\\s*строк[иы]?`, 'g');
    content = content.replace(regex, `$1 ${lines} строк${lines === 1 ? 'а' : lines < 5 ? 'и' : ''}`);
  }

  // Обновляем дату
  const today = new Date().toISOString().split('T')[0];
  content = content.replace(
    /Размеры актуальны на \d{4}-\d{2}-\d{2}/,
    `Размеры актуальны на ${today}`
  );

  writeFileSync(docsPath, content, 'utf-8');
  console.log('✅ DOCS.md обновлен');
}

updateDocsFile();
```

**Добавить скрипт в package.json:**
```json
"docs:update-sizes": "tsx scripts/update-docs-sizes.ts"
```

**Примечание:** Это опционально, можно обновлять вручную.

---

### ✅ Задача 2.4.5: Валидировать изменения

```bash
# Проверить, что DOCS.md корректен
cat DOCS.md | grep -A 2 "CLAUDE.md"
cat DOCS.md | grep -A 2 "ARCHITECTURE.md"

# Проверить размер самого DOCS.md
wc -l DOCS.md
# Должно быть: ~90-100 строк (без добавления модульных размеров)
```

---

## Критерии завершения

- [ ] DOCS.md показывает актуальные размеры CLAUDE.md и ARCHITECTURE.md
- [ ] Добавлено примечание о проверке через `npm run validate:docs`
- [ ] (Опционально) Добавлены размеры модульной документации
- [ ] (Опционально) Создан скрипт `npm run docs:update-sizes`
- [ ] Размер DOCS.md не превышает разумных пределов (≤150 строк)

---

## Зависимости

**Требует:**
- ✅ 1.1 Унификация лимитов (для корректных размеров в примечании)

**Блокирует:**
- Нет (параллельная задача)

**Можно выполнять параллельно с:**
- 2.1 Сокращение tests/README.md
- 2.2 Сокращение yandex-tracker/README.md
- 2.3 Оптимизация ARCHITECTURE.md

**Примечание:** Желательно выполнить ПОСЛЕ сокращения документов, чтобы указать актуальные размеры.

---

## Риски

- **Нет рисков** (простое обновление метаданных)

---

## Валидация

После выполнения проверить:

1. **Актуальность размеров:**
```bash
wc -l CLAUDE.md ARCHITECTURE.md
# Сравнить с размерами в DOCS.md
```

2. **Размер DOCS.md:**
```bash
wc -l DOCS.md
# Должно быть: ≤150 строк (если добавлены модульные размеры)
# Или: ~90-100 строк (если без модульных размеров)
```

3. **Корректность ссылок:**
```bash
# Проверить, что все ссылки работают
grep -E '\[.*\]\(.*\.md\)' DOCS.md
```

---

## Рекомендация

**Выполнить эту задачу ПОСЛЕДНЕЙ** среди параллельных задач, после завершения:
- 2.1 Сокращение tests/README.md
- 2.2 Сокращение yandex-tracker/README.md
- 2.3 Оптимизация ARCHITECTURE.md

Это позволит указать финальные размеры документов после оптимизации.

---

## Альтернативный подход

Вместо указания конкретных размеров, можно использовать динамическую проверку:

```markdown
| Файл | Назначение | Размер |
|------|-----------|--------|
| **[CLAUDE.md](./CLAUDE.md)** | ✅ **Главный файл** — правила, чек-листы, критические требования | См. `npm run validate:docs` |
| **[ARCHITECTURE.md](./ARCHITECTURE.md)** | Архитектура проекта — концепции, паттерны, поток данных | См. `npm run validate:docs` |
```

**Преимущество:** Не требует обновления при изменении размеров.
**Недостаток:** Менее удобно для быстрой навигации.
