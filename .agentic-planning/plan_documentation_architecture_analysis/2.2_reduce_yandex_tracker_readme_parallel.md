# 2.2 Сокращение yandex-tracker/README.md (Parallel)

**Тип:** Parallel (можно выполнять параллельно с 2.1, 2.3, 2.4)
**Приоритет:** 🟠 ВЫСОКИЙ
**Время:** ~1.5 часа
**Сложность:** Низкая-Средняя

---

## Цель

Сократить `packages/servers/yandex-tracker/README.md` с 514 строк до ≤500 строк (цель: ~500 строк).

**Требуется сократить:** минимум 14 строк (до лимита 500), рекомендуется сократить до цели 500

---

## Проблема

- **Текущий размер:** 514 строк
- **Лимит:** 600 строк (новая категория Package README)
- **Цель:** 500 строк
- **Превышение:** +14 строк (некритично, но желательно сократить)

---

## Анализ содержимого

`yandex-tracker/README.md` — это **User Guide** (документация для пользователей), а не developer guide.

**Содержит:**
- Установка и конфигурация
- Получение OAuth токена
- Примеры использования
- Troubleshooting
- FAQ

**Не содержит (это в CLAUDE.md):**
- Правила разработки
- Архитектурные паттерны
- Конвенции кода

---

## Стратегия сокращения

### Приоритет 1: Сократить примеры использования (экономия ~20-30 строк)

**Что сократить:**
- Длинные диалоги с Claude → показать только ключевой запрос
- Повторяющиеся примеры → оставить 2-3 самых показательных
- Избыточные комментарии → сократить до минимума

**Примерное сокращение:** 20-30 строк

---

### Приоритет 2: Оптимизировать инструкции по установке (экономия ~10-15 строк)

**Что оптимизировать:**
- Объединить похожие шаги
- Использовать более компактный формат
- Убрать повторяющиеся пояснения

**Примерное сокращение:** 10-15 строк

---

### Приоритет 3: Сократить Troubleshooting (экономия ~5-10 строк)

**Что сократить:**
- FAQ в формате вопрос-ответ → таблица
- Длинные объяснения → краткие решения
- Повторяющиеся инструкции → ссылки на секции выше

**Примерное сокращение:** 5-10 строк

---

## Задачи

### ✅ Задача 2.2.1: Сократить примеры использования

**Секция:** "💬 Примеры использования"

**Действие:**

Было (пример длинного диалога):
```markdown
**Запрос:**
> "Найди все задачи в очереди MYPROJECT, которые назначены на меня и находятся в статусе 'В работе'. Покажи их приоритеты и дедлайны."

**Ответ Claude:**
> "Использую инструмент find_issues для поиска задач..."
> (результат)

**Запрос:**
> "Теперь обнови дедлайн для задачи MYPROJECT-123 на завтра"

**Ответ Claude:**
> "Использую update_issue для обновления..."
```

Стало:
```markdown
**Запрос:** Найди мои задачи в очереди MYPROJECT со статусом "В работе"

**Запрос:** Обнови дедлайн MYPROJECT-123 на завтра
```

**Экономия:** ~8-10 строк на каждый длинный пример

---

### ✅ Задача 2.2.2: Оптимизировать инструкции по установке

**Секция:** "📦 Установка"

**Действие:**

Было:
```markdown
1. **Установи пакет глобально:**
   ```bash
   npm install -g mcp-server-yandex-tracker
   ```

2. **Настрой Claude Desktop** — добавь конфигурацию в `claude_desktop_config.json`:

   **macOS:** `~/Library/Application Support/Claude/claude_desktop_config.json`

   **Windows:** `%APPDATA%\Claude\claude_desktop_config.json`

   **Linux:** `~/.config/Claude/claude_desktop_config.json`

   ```json
   {
     "mcpServers": {
       "yandex-tracker": {
         ...
       }
     }
   }
   ```

3. **Перезапусти Claude Desktop** — готово!
```

Стало:
```markdown
1. **Установи пакет:** `npm install -g mcp-server-yandex-tracker`

2. **Настрой Claude Desktop** в `claude_desktop_config.json`:
   - macOS: `~/Library/Application Support/Claude/`
   - Windows: `%APPDATA%\Claude\`
   - Linux: `~/.config/Claude/`

   ```json
   {
     "mcpServers": {
       "yandex-tracker": {
         ...
       }
     }
   }
   ```

3. **Перезапусти Claude Desktop**
```

**Экономия:** ~5-7 строк

---

### ✅ Задача 2.2.3: Сократить секцию Troubleshooting

**Секция:** "🔧 Troubleshooting"

**Действие:** Преобразовать в таблицу

Было:
```markdown
### Проблема: Сервер не подключается

**Симптомы:** Claude не видит инструменты Яндекс.Трекера

**Решение:**
1. Проверь логи в `~/.mcp/logs/yandex-tracker.log`
2. Убедись, что токен и orgId правильные
3. Перезапусти Claude Desktop

### Проблема: Ошибка авторизации

**Симптомы:** "401 Unauthorized"

**Решение:**
1. Проверь, что токен начинается с `y0_`
2. Убедись, что у приложения есть права tracker:read и tracker:write
```

Стало:
```markdown
### Troubleshooting

| Проблема | Решение |
|----------|---------|
| Сервер не подключается | Проверь логи `~/.mcp/logs/yandex-tracker.log`, токен/orgId, перезапусти Claude |
| 401 Unauthorized | Проверь токен (начинается с `y0_`) и права (tracker:read, tracker:write) |
```

**Экономия:** ~10-12 строк

---

### ✅ Задача 2.2.4: Убрать избыточные разделители

**Действие:** Заменить длинные разделители на короткие

Было:
```markdown
---

## 🎯 Преимущества

---

## 📦 Установка

---
```

Стало:
```markdown
## 🎯 Преимущества

## 📦 Установка
```

**Экономия:** ~3-5 строк

---

### ✅ Задача 2.2.5: Валидировать результат

```bash
wc -l packages/servers/yandex-tracker/README.md
# Должно быть: ≤500 строк

npm run validate:docs
```

---

## Критерии завершения

- [ ] Файл сокращен до ≤500 строк
- [ ] Примеры использования компактны
- [ ] Инструкции по установке оптимизированы
- [ ] Troubleshooting в табличном формате
- [ ] Удалены избыточные разделители
- [ ] `npm run validate:docs` не показывает warnings для этого файла
- [ ] Документ остается понятным для пользователей

---

## Зависимости

**Требует:**
- ✅ 1.1 Унификация лимитов
- ✅ 1.2 Исправление скрипта валидации

**Блокирует:**
- Нет (параллельная задача)

**Можно выполнять параллельно с:**
- 2.1 Сокращение tests/README.md
- 2.3 Оптимизация ARCHITECTURE.md
- 2.4 Обновление DOCS.md

---

## Риски

- **Низкий:** Потеря важной информации для пользователей
  - **Митигация:** Сокращать примеры, а не инструкции

- **Низкий:** Ухудшение UX для новых пользователей
  - **Митигация:** Оставить все ключевые шаги установки

---

## Валидация

После выполнения проверить:

1. **Размер:**
```bash
wc -l packages/servers/yandex-tracker/README.md
# Ожидается: 490-500 строк
```

2. **Качество:**
- Инструкции по установке полные
- Примеры понятны
- Troubleshooting покрывает основные проблемы

3. **Автоматическая валидация:**
```bash
npm run validate:docs
# yandex-tracker/README.md должен показывать OK
```

---

## Примечание

Этот файл имеет **новый лимит 600 строк** (Package README), но рекомендуется держать его близким к **500 строкам** для лучшей читаемости пользователями.

Текущие 514 строк технически в пределах нового лимита (600), но сокращение до 500 улучшит качество документации.
