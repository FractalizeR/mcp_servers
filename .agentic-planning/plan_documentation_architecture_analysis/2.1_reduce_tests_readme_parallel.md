# 2.1 Сокращение tests/README.md (Parallel)

**Тип:** Parallel (можно выполнять параллельно с 2.2, 2.3, 2.4)
**Приоритет:** 🟠 ВЫСОКИЙ
**Время:** ~2 часа
**Сложность:** Средняя

---

## Цель

Сократить `tests/README.md` с 622 строк до ≤500 строк (цель: ~400 строк).

**Требуется сократить:** минимум 122 строки (фактически ~220 для достижения цели)

---

## Проблема

- **Текущий размер:** 622 строки
- **Лимит:** 500 строк
- **Цель:** 400 строк
- **Превышение:** +122 строки (+24%)

---

## Анализ содержимого

Нужно прочитать файл и определить, что можно сократить:

1. **Примеры кода** — заменить ссылками на актуальные тесты
2. **Дублирование** — убрать повторяющуюся информацию
3. **Подробные объяснения** — сократить до чек-листов
4. **Устаревшие секции** — удалить deprecated контент

---

## Стратегия сокращения

### Приоритет 1: Удалить избыточные примеры (экономия ~80-100 строк)

**Что удалить:**
- Длинные примеры кода (>15 строк) → заменить ссылкой на файл
- Повторяющиеся паттерны → описать один раз
- Примеры, которые есть в реальных тестах → дать ссылку

**Примерное сокращение:** 80-100 строк

---

### Приоритет 2: Сократить объяснения (экономия ~50-70 строк)

**Что сократить:**
- Развернутые объяснения "почему" → краткие формулировки
- Пошаговые инструкции → чек-листы
- Описания паттернов → краткие определения + ссылка на примеры

**Примерное сокращение:** 50-70 строк

---

### Приоритет 3: Удалить дублирование (экономия ~30-40 строк)

**Что удалить:**
- Информация, которая есть в других README
- Повторение концепций из ARCHITECTURE.md
- Дублирующиеся чек-листы

**Примерное сокращение:** 30-40 строк

---

### Приоритет 4: Реструктуризация (экономия ~20-30 строк)

**Что сделать:**
- Объединить похожие секции
- Использовать таблицы вместо списков
- Сократить заголовки и разделители

**Примерное сокращение:** 20-30 строк

---

## Задачи

### ✅ Задача 2.1.1: Прочитать и проанализировать файл

```bash
# Прочитать файл полностью
cat packages/servers/yandex-tracker/tests/README.md | less

# Посчитать строки по секциям
grep -n "^##" packages/servers/yandex-tracker/tests/README.md
```

**Действие:** Определить самые объемные секции.

---

### ✅ Задача 2.1.2: Выделить примеры кода для замены

**Критерий:** Примеры кода >10 строк

**Действие:**
1. Найти все блоки кода
2. Проверить, есть ли аналогичный реальный тест
3. Заменить пример ссылкой:

Было:
```markdown
### Пример unit теста

```typescript
describe('GetIssuesTool', () => {
  it('should get issues by keys', async () => {
    const mockFacade = mock<YandexTrackerFacade>();
    // ... 20 строк кода
  });
});
```
```

Стало:
```markdown
### Пример unit теста

См. [get-issues.tool.test.ts](../../tests/unit/mcp/tools/api/issues/get/get-issues.tool.test.ts)
```

---

### ✅ Задача 2.1.3: Сократить объяснения до чек-листов

Было:
```markdown
### Как писать unit тесты

При написании unit тестов важно помнить о следующих принципах:

1. **Изоляция:** Каждый тест должен быть полностью изолирован от других. Это означает, что тест не должен зависеть от порядка выполнения или результатов других тестов.

2. **Моки:** Все внешние зависимости должны быть замокированы. Используйте библиотеку vitest для создания моков.

3. **Структура AAA:** Arrange (подготовка), Act (действие), Assert (проверка). Каждый тест должен следовать этой структуре для лучшей читаемости.
```

Стало:
```markdown
### Чек-лист unit тестов

- [ ] Изоляция — тест не зависит от других
- [ ] Моки — все внешние зависимости замокированы (vitest)
- [ ] Структура AAA — Arrange, Act, Assert
```

**Экономия:** ~10-15 строк на каждую подобную секцию

---

### ✅ Задача 2.1.4: Удалить дублирование

**Проверить дублирование с:**
- `ARCHITECTURE.md` — описание слоев
- `yandex-tracker/CLAUDE.md` — правила тестирования
- Module READMEs — конвенции компонентов

**Действие:** Оставить только тестово-специфичную информацию, остальное заменить ссылками.

---

### ✅ Задача 2.1.5: Оптимизировать структуру

**Объединить похожие секции:**
- "Unit тесты" + "Примеры unit тестов" → одна секция
- "Интеграционные тесты" + "Примеры интеграционных тестов" → одна секция

**Использовать таблицы:**

Было:
```markdown
### Типы тестов

**Unit тесты:**
- Назначение: изоляция
- Путь: tests/unit/
- Запуск: npm test

**Integration тесты:**
- Назначение: взаимодействие
- Путь: tests/integration/
- Запуск: npm run test:integration
```

Стало:
```markdown
### Типы тестов

| Тип | Назначение | Путь | Запуск |
|-----|-----------|------|--------|
| Unit | Изоляция | tests/unit/ | npm test |
| Integration | Взаимодействие | tests/integration/ | npm run test:integration |
```

---

### ✅ Задача 2.1.6: Валидировать результат

```bash
# Проверить размер
wc -l packages/servers/yandex-tracker/tests/README.md

# Должно быть: ≤500 строк (цель: ~400)

# Проверить через валидацию
npm run validate:docs
```

---

## Критерии завершения

- [x] Файл сокращен до ≤500 строк (цель: ~400) — **291 строк** ✅
- [x] Все длинные примеры кода заменены ссылками
- [x] Объяснения сокращены до чек-листов
- [x] Удалено дублирование с другими документами
- [x] Структура оптимизирована (таблицы, объединение секций)
- [x] `npm run validate:docs` не показывает ошибки для этого файла
- [x] Сохранена вся критическая информация

---

## Зависимости

**Требует:**
- ✅ 1.1 Унификация лимитов
- ✅ 1.2 Исправление скрипта валидации

**Блокирует:**
- Нет (параллельная задача)

**Можно выполнять параллельно с:**
- 2.2 Сокращение yandex-tracker/README.md
- 2.3 Оптимизация ARCHITECTURE.md
- 2.4 Обновление DOCS.md

---

## Риски

- **Средний:** Потеря важной информации при сокращении
  - **Митигация:** Заменять длинные примеры ссылками, а не удалять

- **Низкий:** Нарушение структуры документа
  - **Митигация:** Валидировать readability после сокращения

---

## Примеры сокращений

### Пример 1: Длинный код → ссылка

**Было (25 строк):**
```markdown
### Пример integration теста

```typescript
import { describe, it, expect } from 'vitest';
import { TestMCPClient } from '../helpers/mcp-client.js';
import { MockServer } from '../helpers/mock-server.js';

describe('GetIssuesTool Integration', () => {
  it('should get issues from real server', async () => {
    const server = new MockServer();
    await server.start();

    const client = new TestMCPClient(server.url);
    await client.connect();

    const result = await client.callTool('get_issues', {
      keys: ['TEST-1', 'TEST-2']
    });

    expect(result.content).toHaveLength(2);

    await client.disconnect();
    await server.stop();
  });
});
```
```

**Стало (3 строки):**
```markdown
### Пример integration теста

См. [get-issues.tool.integration.test.ts](../../tests/integration/mcp/tools/api/issues/get/get-issues.tool.integration.test.ts)
```

**Экономия:** 22 строки

---

### Пример 2: Подробное объяснение → чек-лист

**Было (18 строк):**
```markdown
### Правила написания тестов

При написании тестов важно соблюдать следующие правила:

1. **Именование:** Имя теста должно четко описывать, что тестируется. Используйте формат "should [expected behavior] when [condition]".

2. **Один assert на тест:** Каждый тест должен проверять только одну вещь. Если нужно проверить несколько аспектов, создайте несколько тестов.

3. **Независимость:** Тесты не должны зависеть друг от друга. Порядок выполнения тестов не должен влиять на результаты.

4. **Чистота:** После выполнения теста должна производиться очистка (cleanup). Используйте afterEach/afterAll hooks.

5. **Читаемость:** Код теста должен быть понятен даже без комментариев. Используйте говорящие имена переменных.
```

**Стало (7 строк):**
```markdown
### Правила написания тестов

- [ ] Именование: `should [behavior] when [condition]`
- [ ] Один assert на тест
- [ ] Независимость тестов
- [ ] Cleanup в afterEach/afterAll
- [ ] Читаемый код без комментариев
```

**Экономия:** 11 строк

---

## Валидация

После выполнения проверить:

1. **Размер:**
```bash
wc -l packages/servers/yandex-tracker/tests/README.md
# Ожидается: 400-500 строк
```

2. **Качество:**
- Вся критическая информация сохранена
- Документ легко читается
- Есть ссылки на примеры вместо дублирования кода

3. **Автоматическая валидация:**
```bash
npm run validate:docs
# tests/README.md должен показывать OK или Warning (не Error)
```
