# 1.1 Унификация лимитов (Sequential)

**Тип:** Sequential (должен быть выполнен перед остальными задачами)
**Приоритет:** 🔴 КРИТИЧНЫЙ
**Время:** ~30 минут
**Сложность:** Низкая

---

## Цель

Привести все источники правды о лимитах документации к единому стандарту.

---

## Проблема

Три разных источника дают разные лимиты:
- Скрипт `validate-docs-size.ts`: 350/600
- `CLAUDE.md`: 400/700
- `DOCS.md`: 300/500

---

## Решение

Установить единый источник правды с разумными лимитами:

| Файл | Лимит (MUST) | Цель (SHOULD) | Критический порог |
|------|--------------|---------------|-------------------|
| CLAUDE.md | **400** | **350** | 480 |
| ARCHITECTURE.md | **700** | **600** | 840 |
| Module README.md | **500** | **400** | 600 |
| Package README.md | **600** | **500** | 720 |
| tests/README.md | **500** | **400** | 600 |

---

## Задачи

### ✅ Задача 1.1.1: Обновить скрипт валидации

**Файл:** `scripts/validate-docs-size.ts`

**Изменения:**

1. Обновить константы (строки 27-40):
```typescript
const DOC_LIMITS: DocLimit[] = [
  {
    path: 'CLAUDE.md',
    maxLines: 400,        // было: 350
    targetLines: 350,     // было: 300
    description: 'Руководство для ИИ агентов',
  },
  {
    path: 'ARCHITECTURE.md',
    maxLines: 700,        // было: 600
    targetLines: 600,     // было: 500
    description: 'Архитектурная документация',
  },
];
```

2. Добавить проверку Package README (новая категория):
```typescript
const PACKAGE_README_LIMITS: DocLimit[] = [
  {
    path: 'packages/servers/yandex-tracker/README.md',
    maxLines: 600,
    targetLines: 500,
    description: 'Package README (User guide)',
  },
];
```

3. Добавить проверку tests/README.md:
```typescript
const TEST_README_LIMITS: DocLimit[] = [
  {
    path: 'packages/servers/yandex-tracker/tests/README.md',
    maxLines: 500,
    targetLines: 400,
    description: 'Testing documentation',
  },
];
```

4. Обновить функцию `main()` для проверки всех категорий:
```typescript
function main(): void {
  const projectRoot = process.cwd();

  // Проверяем основные документы
  const mainDocsResults = DOC_LIMITS.map((doc) => validateDoc(doc, projectRoot));

  // Проверяем Package READMEs
  const packageReadmeResults = PACKAGE_README_LIMITS.map((doc) =>
    validateDoc(doc, projectRoot)
  );

  // Проверяем Test READMEs
  const testReadmeResults = TEST_README_LIMITS.map((doc) =>
    validateDoc(doc, projectRoot)
  );

  // Проверяем README файлы модулей
  const moduleReadmesResults = validateModuleReadmes(projectRoot);

  // Объединяем результаты
  const allResults = [
    ...mainDocsResults,
    ...packageReadmeResults,
    ...testReadmeResults,
    ...moduleReadmesResults,
  ];

  // Выводим результаты
  printResults(allResults);

  // Выходим с кодом ошибки, если есть ошибки
  const hasErrors = allResults.some((r) => r.status === 'error');
  if (hasErrors) {
    process.exit(1);
  }
}
```

---

### ✅ Задача 1.1.2: Обновить CLAUDE.md (корневой)

**Файл:** `CLAUDE.md`

**Изменения (строки 10-20):**

Заменить:
```markdown
**Жёсткие лимиты (MUST):**
- `CLAUDE.md` ≤ 400 строк
- `ARCHITECTURE.md` ≤ 700 строк
- Module `README.md` ≤ 500 строк

**Целевые значения (SHOULD):**
- `CLAUDE.md` ~350 строк
- `ARCHITECTURE.md` ~600 строк
- Module `README.md` ~400 строк
```

На:
```markdown
**Жёсткие лимиты (MUST):**
- `CLAUDE.md` ≤ 400 строк
- `ARCHITECTURE.md` ≤ 700 строк
- Module `README.md` ≤ 500 строк
- Package `README.md` ≤ 600 строк
- `tests/README.md` ≤ 500 строк

**Целевые значения (SHOULD):**
- `CLAUDE.md` ~350 строк
- `ARCHITECTURE.md` ~600 строк
- Module `README.md` ~400 строк
- Package `README.md` ~500 строк
- `tests/README.md` ~400 строк
```

---

### ✅ Задача 1.1.3: Обновить DOCS.md

**Файл:** `DOCS.md`

**Изменения (строки 63-79):**

Заменить:
```markdown
**Жёсткие лимиты (MUST):**

| Тип файла | Лимит | Проверка |
|-----------|-------|----------|
| CLAUDE.md | ≤300 строк | `npm run validate:docs` |
| ARCHITECTURE.md | ≤500 строк | `npm run validate:docs` |
| Module README.md | ≤500 строк | `npm run validate:docs` |

**Целевые значения (SHOULD):**

| Тип файла | Цель |
|-----------|------|
| CLAUDE.md | ~250 строк |
| ARCHITECTURE.md | ~400 строк |
| Module README.md | ~300-400 строк |
```

На:
```markdown
**Жёсткие лимиты (MUST):**

| Тип файла | Лимит | Проверка |
|-----------|-------|----------|
| CLAUDE.md | ≤400 строк | `npm run validate:docs` |
| ARCHITECTURE.md | ≤700 строк | `npm run validate:docs` |
| Module README.md | ≤500 строк | `npm run validate:docs` |
| Package README.md | ≤600 строк | `npm run validate:docs` |
| tests/README.md | ≤500 строк | `npm run validate:docs` |

**Целевые значения (SHOULD):**

| Тип файла | Цель |
|-----------|------|
| CLAUDE.md | ~350 строк |
| ARCHITECTURE.md | ~600 строк |
| Module README.md | ~400 строк |
| Package README.md | ~500 строк |
| tests/README.md | ~400 строк |
```

---

### ✅ Задача 1.1.4: Актуализировать размеры в DOCS.md

**Файл:** `DOCS.md`

**Изменения (строки 9-12):**

Заменить:
```markdown
| **[CLAUDE.md](./CLAUDE.md)** | ✅ **Главный файл** — правила, чек-листы, критические требования | ~250 строк |
| **[ARCHITECTURE.md](./ARCHITECTURE.md)** | Архитектура проекта — концепции, паттерны, поток данных | ~400 строк |
```

На:
```markdown
| **[CLAUDE.md](./CLAUDE.md)** | ✅ **Главный файл** — правила, чек-листы, критические требования | 333 строки |
| **[ARCHITECTURE.md](./ARCHITECTURE.md)** | Архитектура проекта — концепции, паттерны, поток данных | 564 строки |
```

**Добавить примечание:**
```markdown
> **Примечание:** Размеры актуальны на момент последней валидации.
> Текущие размеры проверяй через `npm run validate:docs`.
```

---

## Критерии завершения

- [x] Скрипт `validate-docs-size.ts` обновлен с новыми лимитами
- [x] Скрипт проверяет ВСЕ типы документов (main, package, module, tests)
- [x] `CLAUDE.md` обновлен с едиными лимитами
- [x] `DOCS.md` обновлен с едиными лимитами
- [x] `DOCS.md` показывает актуальные размеры файлов
- [x] Нет противоречий между источниками

---

## Зависимости

**Блокирует:**
- 1.2 Исправление скрипта валидации
- 2.1 Сокращение tests/README.md
- 2.2 Сокращение yandex-tracker/README.md

**Требует:**
- Нет зависимостей (можно начинать сразу)

---

## Риски

- **Низкий:** Чисто конфигурационные изменения, без кода

---

## Валидация

После выполнения:
```bash
# Проверить, что скрипт корректно читает новые лимиты
npm run validate:docs

# Ожидаемый результат:
# - CLAUDE.md: OK (333/350 цель, 333/400 лимит)
# - ARCHITECTURE.md: Warning (564/600 цель, 564/700 лимит)
# - tests/README.md: Error (622/400 цель, 622/500 лимит) — ПРЕВЫШЕНИЕ
# - yandex-tracker/README.md: Warning (514/500 цель, 514/600 лимит)
```
