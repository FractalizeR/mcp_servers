# 2.3 Оптимизация ARCHITECTURE.md (Parallel)

**Тип:** Parallel (можно выполнять параллельно с 2.1, 2.2, 2.4)
**Приоритет:** 🟡 СРЕДНИЙ (некритичное превышение)
**Время:** ~1 час
**Сложность:** Низкая

---

## Цель

Оптимизировать `ARCHITECTURE.md` с 564 строк ближе к цели ~600 строк.

**Текущий статус:**
- **Размер:** 564 строки
- **Лимит:** 700 строк ✅ (в пределах)
- **Цель:** 600 строк ⚠️ (можно улучшить)

**Примечание:** Это не критичная задача, так как файл в пределах лимита.

---

## Проблема

ARCHITECTURE.md находится в пределах лимита (700), но превышает целевое значение (600).

**Можно оптимизировать для:**
- Лучшей читаемости ИИ агентами
- Экономии токенов при чтении
- Соответствия целевым значениям

---

## Стратегия оптимизации

### Приоритет 1: Сократить примеры кода (экономия ~20-30 строк)

**Что сократить:**
- Длинные примеры (>10 строк) → ссылки на код
- Дублирующиеся примеры → оставить один
- ASCII art диаграммы → более компактные

**Примерное сокращение:** 20-30 строк (необязательно)

---

### Приоритет 2: Оптимизировать структуру (экономия ~10-20 строк)

**Что оптимизировать:**
- Длинные списки → таблицы
- Повторяющиеся разделители → убрать
- Объединить похожие секции

**Примерное сокращение:** 10-20 строк

---

### Приоритет 3: Убрать дублирование с README (экономия ~10-15 строк)

**Что проверить:**
- Дублирование установки/запуска → ссылка на README
- Повторяющиеся концепции → краткое описание + ссылка
- Примеры использования → ссылка на yandex-tracker/README.md

**Примерное сокращение:** 10-15 строк

---

## Задачи

### ✅ Задача 2.3.1: Сократить ASCII диаграммы

**Примеры:**

Было:
```markdown
```
┌─────────────────┐
│ infrastructure  │ ← Base layer (HTTP, logging, cache, async)
└────────┬────────┘
         │
         ↓
┌─────────────────┐
│      core       │ ← Framework core (BaseTool, registry, utilities)
└────────┬────────┘
         │
         ↓
┌─────────────────┐
│     search      │ ← Tool discovery (search engine, strategies)
└────────┬────────┘
         │
         ↓
┌─────────────────┐
│ yandex-tracker  │ ← Application (Yandex.Tracker integration)
└─────────────────┘
```
```

Стало:
```markdown
```
infrastructure → core → search → yandex-tracker
```
```

**Экономия:** ~10 строк

---

### ✅ Задача 2.3.2: Заменить длинные списки таблицами

**Пример:**

Было:
```markdown
### Framework Level
- **Strategy Pattern** — Search strategies, retry strategies
- **Null Object** — NoOpCache
- **Factory Pattern** — Tool creation in registry
- **Template Method** — BaseTool, BaseDefinition

### Application Level (Yandex Tracker)
- **Facade Pattern** — YandexTrackerFacade
- **Registry Pattern** — ToolRegistry
- **Dependency Injection** — InversifyJS container
- **Repository Pattern** — Operations as repositories
```

Стало:
```markdown
### Design Patterns

| Level | Pattern | Example |
|-------|---------|---------|
| Framework | Strategy | Search/retry strategies |
| Framework | Null Object | NoOpCache |
| Framework | Factory | Tool creation |
| Framework | Template Method | BaseTool |
| Application | Facade | YandexTrackerFacade |
| Application | Registry | ToolRegistry |
| Application | DI | InversifyJS |
| Application | Repository | Operations |
```

**Экономия:** ~3-5 строк

---

### ✅ Задача 2.3.3: Убрать избыточные разделители

Было:
```markdown
---

## 🔗 Dependency Graph

---
```

Стало:
```markdown
## 🔗 Dependency Graph
```

**Экономия:** ~10-15 строк (по всему документу)

---

### ✅ Задача 2.3.4: Сократить примеры кода (опционально)

**Длинные примеры кода заменить ссылками:**

Было (15+ строк кода):
```typescript
// Пример из ARCHITECTURE.md
export interface Issue { /* ... */ }
export type IssueWithUnknownFields = WithUnknownFields<Issue>;

// ... 15 строк
```

Стало:
```markdown
См. [issue.entity.ts](packages/servers/yandex-tracker/src/tracker_api/entities/issue.entity.ts)
```

**Экономия:** ~10-20 строк (опционально, если примеры длинные)

---

### ✅ Задача 2.3.5: Валидировать результат

```bash
wc -l ARCHITECTURE.md
# Желательно: ≤600 строк (не критично)

npm run validate:docs
```

---

## Критерии завершения

- [ ] Файл оптимизирован (желательно ≤600 строк, но не обязательно)
- [ ] ASCII диаграммы компактны
- [ ] Списки преобразованы в таблицы где уместно
- [ ] Удалены избыточные разделители
- [ ] Длинные примеры кода заменены ссылками (опционально)
- [ ] `npm run validate:docs` показывает OK или Warning (не Error)
- [ ] Архитектурная информация сохранена в полном объеме

---

## Зависимости

**Требует:**
- ✅ 1.1 Унификация лимитов
- ✅ 1.2 Исправление скрипта валидации

**Блокирует:**
- Нет (параллельная задача)

**Можно выполнять параллельно с:**
- 2.1 Сокращение tests/README.md
- 2.2 Сокращение yandex-tracker/README.md
- 2.4 Обновление DOCS.md

---

## Риски

- **Низкий:** Потеря архитектурной информации
  - **Митигация:** Не удалять контент, только оптимизировать формат

- **Низкий:** Ухудшение читаемости диаграмм
  - **Митигация:** Оставить диаграммы понятными, даже если компактнее

---

## Приоритет выполнения

**Этот документ имеет НИЗКИЙ приоритет** по сравнению с:
- 2.1 tests/README.md (превышает лимит на 122 строки) — ВЫСОКИЙ
- 2.2 yandex-tracker/README.md (превышает цель на 14 строк) — СРЕДНИЙ

**Рекомендация:** Выполнить эту задачу **ПОСЛЕДНЕЙ** среди параллельных задач или вообще пропустить, если времени мало.

---

## Валидация

После выполнения проверить:

1. **Размер:**
```bash
wc -l ARCHITECTURE.md
# Желательно: ≤600 строк
# Приемлемо: ≤700 строк (лимит)
```

2. **Качество:**
- Вся архитектурная информация сохранена
- Диаграммы понятны
- Документ легко читается

3. **Автоматическая валидация:**
```bash
npm run validate:docs
# ARCHITECTURE.md должен показывать OK или Warning (не Error)
```

---

## Примечание

**Необязательная задача!**

Если после выполнения 2.1 и 2.2 времени не хватает, эту задачу можно отложить.

ARCHITECTURE.md уже находится в пределах лимита (700 строк), превышение целевого значения (600) не критично.
