# Этап 1.1: Исправление ESLint конфигурации [SEQUENTIAL]

**Приоритет:** P0 (BLOCKER)
**Статус:** ⏪ Ожидает выполнения
**Время выполнения:** ~2-3 часа
**Зависимости:** Нет

---

## 📋 Описание проблемы

### Симптомы
- `npm run lint` падает с 226 ошибками парсинга
- Ошибки типа `Parsing error: Unexpected token {`, `Unexpected token type`
- Блокирует валидацию качества кода во всех пакетах monorepo

### Текущее состояние
```
> npm run lint
✖ 226 problems (226 errors, 0 warnings)

- infrastructure: 50+ errors
- core: 11 errors
- search: 14 errors
- yandex-tracker: 151 errors
```

---

## 🔍 ЭТАП 1: Детальный анализ проблемы

### 1.1. Анализ текущей конфигурации

#### Задачи анализа:
- [ ] Прочитать `eslint.config.js` и понять его структуру
- [ ] Изучить каждый блок конфигурации (files, languageOptions, plugins, rules)
- [ ] Проверить пути к tsconfig в каждом блоке
- [ ] Выявить, почему конфигурация работает только для yandex-tracker

#### Вопросы для анализа:
1. Какие `files` паттерны используются?
2. На какие `tsconfig.json` ссылается `parserOptions.project`?
3. Применяется ли конфигурация ко всем workspaces?

#### Ожидаемые находки:
```javascript
// Текущая конфигурация (проблемная)
{
  files: ['src/**/*.ts'],  // ❌ Только локальные файлы
  parserOptions: {
    project: './tsconfig.json'  // ❌ Только корневой tsconfig
  }
}
```

**Причина проблемы:**
- Относительные пути `./tsconfig.json` работают только для корня monorepo
- Паттерн `src/**/*.ts` не учитывает структуру `packages/*/src/**/*.ts`
- Parser не находит правильный tsconfig для каждого workspace

---

### 1.2. Анализ структуры monorepo

#### Задачи анализа:
- [ ] Найти все `tsconfig.json` в пакетах
- [ ] Проверить наличие `tsconfig.tests.json` в пакетах
- [ ] Изучить структуру каждого workspace

#### Команды для анализа:
```bash
# Найти все tsconfig
find packages -name "tsconfig*.json" -not -path "*/node_modules/*"

# Проверить структуру пакетов
tree -L 3 packages/

# Проверить наличие src/ в каждом пакете
ls -la packages/framework/*/src packages/servers/*/src
```

#### Ожидаемая структура:
```
packages/
├── framework/
│   ├── infrastructure/
│   │   ├── src/
│   │   ├── tests/
│   │   └── tsconfig.json
│   ├── core/
│   │   └── tsconfig.json
│   └── search/
│       └── tsconfig.json
└── servers/
    └── yandex-tracker/
        ├── src/
        ├── tests/
        ├── tsconfig.json
        └── tsconfig.tests.json
```

---

### 1.3. Анализ способов решения

#### Вариант 1: Мультипакетный корневой конфиг (рекомендуется)

**Плюсы:**
- Централизованное управление правилами
- Единый источник истины для ESLint
- Легко поддерживать синхронизацию

**Минусы:**
- Нужно правильно настроить паттерны для всех пакетов
- Сложнее настроить разные правила для разных пакетов

**Структура:**
```javascript
export default [
  // Конфигурация для framework пакетов
  {
    files: ['packages/framework/*/src/**/*.ts'],
    languageOptions: {
      parserOptions: {
        project: ['packages/framework/*/tsconfig.json']
      }
    }
  },
  // Конфигурация для server пакетов
  {
    files: ['packages/servers/*/src/**/*.ts'],
    languageOptions: {
      parserOptions: {
        project: ['packages/servers/*/tsconfig.json']
      }
    }
  }
]
```

#### Вариант 2: Отдельные конфиги в каждом workspace

**Плюсы:**
- Каждый пакет независим
- Можно настроить специфичные правила
- Проще понять конфигурацию конкретного пакета

**Минусы:**
- Дублирование конфигурации
- Сложнее поддерживать синхронизацию правил
- Больше файлов для управления

#### Вариант 3: Shared конфигурация + overrides

**Плюсы:**
- Переиспользуемая базовая конфигурация
- Возможность кастомизации per-package
- Меньше дублирования

**Минусы:**
- Дополнительная сложность настройки
- Нужен отдельный shared пакет

**Решение:** Выбираем Вариант 1 (мультипакетный корневой конфиг)

**Обоснование:**
- Простота управления
- Соответствует текущей архитектуре monorepo
- Минимум изменений в инфраструктуре

---

## 🛠️ ЭТАП 2: Реализация решения

### 2.1. Создание новой конфигурации

#### Шаги:
1. [ ] Сделать резервную копию `eslint.config.js`
   ```bash
   cp eslint.config.js eslint.config.js.backup
   ```

2. [ ] Проанализировать существующие правила и сгруппировать их
   - [ ] Правила для production кода (`src/**/*.ts`)
   - [ ] Правила для тестов (`tests/**/*.ts`)
   - [ ] Правила для CLI (`src/cli/**/*.ts`)

3. [ ] Создать новую структуру конфигурации:
   ```javascript
   export default [
     // 1. Global ignores
     { ignores: [...] },

     // 2. Framework packages (infrastructure, core, search)
     { files: ['packages/framework/*/src/**/*.ts'], ... },

     // 3. Server packages (yandex-tracker)
     { files: ['packages/servers/*/src/**/*.ts'], ... },

     // 4. Server packages - CLI specific
     { files: ['packages/servers/*/src/cli/**/*.ts'], ... },

     // 5. Tests (all packages)
     { files: ['packages/**/tests/**/*.ts'], ... },

     // 6. Config files
     { files: ['packages/**/*.config.ts'], ... },

     // 7. Prettier (last)
     prettierConfig
   ]
   ```

### 2.2. Настройка parserOptions для каждого блока

#### Ключевые моменты:
- Использовать glob patterns для `project`: `['packages/framework/*/tsconfig.json']`
- Для тестов использовать `tsconfig.tests.json` если есть
- Для CLI может быть отдельный `src/cli/tsconfig.json`

#### Проверить наличие tsconfig файлов:
```bash
# Какие tsconfig есть в каждом пакете?
find packages -name "tsconfig*.json" -not -path "*/node_modules/*"
```

#### Пример настройки:
```javascript
{
  files: ['packages/framework/*/src/**/*.ts'],
  languageOptions: {
    parser: tsparser,
    parserOptions: {
      ecmaVersion: 2022,
      sourceType: 'module',
      project: ['packages/framework/infrastructure/tsconfig.json',
                'packages/framework/core/tsconfig.json',
                'packages/framework/search/tsconfig.json']
    }
  }
}
```

### 2.3. Валидация конфигурации

#### Тестирование по шагам:
1. [ ] Проверить синтаксис конфигурации
   ```bash
   node eslint.config.js
   ```

2. [ ] Протестировать на одном файле из infrastructure
   ```bash
   npx eslint packages/framework/infrastructure/src/config.ts
   ```

3. [ ] Протестировать на одном файле из core
   ```bash
   npx eslint packages/framework/core/src/tool-registry.ts
   ```

4. [ ] Протестировать на одном файле из search
   ```bash
   npx eslint packages/framework/search/src/types.ts
   ```

5. [ ] Протестировать на всем yandex-tracker
   ```bash
   npm run lint --workspace=@mcp-server/yandex-tracker
   ```

6. [ ] Запустить полный lint для всех пакетов
   ```bash
   npm run lint
   ```

---

## ✅ ЭТАП 3: Проверка результата

### 3.1. Критерии успеха

- [ ] `npm run lint` завершается успешно (exit code 0)
- [ ] Нет ошибок парсинга в любом пакете
- [ ] Все 4 пакета проходят lint:
  - [ ] @mcp-framework/infrastructure
  - [ ] @mcp-framework/core
  - [ ] @mcp-framework/search
  - [ ] @mcp-server/yandex-tracker

### 3.2. Регрессионное тестирование

- [ ] Проверить, что правила работают корректно
  ```bash
  # Должен найти реальные ошибки, если они есть
  npm run lint
  ```

- [ ] Проверить метрики сложности
  ```bash
  # Не должно быть новых warnings по complexity
  npm run lint 2>&1 | grep -E "(complexity|max-depth|max-lines)"
  ```

- [ ] Проверить type-aware правила
  ```bash
  # Type-checking rules должны работать
  npm run lint 2>&1 | grep -E "(no-floating-promises|no-unsafe)"
  ```

### 3.3. Документирование изменений

- [ ] Обновить комментарии в `eslint.config.js`
  - Объяснить структуру конфигурации
  - Указать, какие блоки для каких пакетов

- [ ] Добавить примечание в CLAUDE.md если нужно
  - Как работает ESLint в monorepo
  - Как добавить новый workspace

---

## 🚨 Возможные проблемы и решения

### Проблема 1: Parser не находит tsconfig

**Симптом:**
```
Parsing error: Cannot read file 'tsconfig.json'
```

**Решение:**
- Проверить правильность путей в `project`
- Использовать абсолютные пути или правильные относительные
- Убедиться, что tsconfig.json существует

### Проблема 2: Конфликт правил между блоками

**Симптом:**
```
Warning: Rule 'X' was set multiple times
```

**Решение:**
- Проверить порядок блоков (более специфичные должны быть позже)
- Убедиться, что Prettier конфиг последний

### Проблема 3: Игнорирование файлов не работает

**Симптом:**
```
Lint runs on node_modules or dist files
```

**Решение:**
- Переместить `ignores` в первый блок
- Убедиться, что паттерны правильные

---

## 📊 Метрики до/после

### До исправления:
```
Errors: 226
Warnings: 0
Files analyzed: 0 (parsing failed)
```

### После исправления (ожидается):
```
Errors: 0-10 (реальные ошибки кода, если есть)
Warnings: 5-20 (complexity, max-lines)
Files analyzed: 297
```

---

## ⏭️ Следующий этап

После успешного завершения этого этапа можно переходить к:
- **1.2_fix_trivial_issues_sequential.md** (зависит от работающего lint)
