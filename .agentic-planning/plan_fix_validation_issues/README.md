# План исправления проблем валидации

**Дата создания:** 2025-11-18
**Обновлено:** 2025-11-19
**Создано:** Claude Code (Architectural Analysis)
**Статус:** ✅ ЗАВЕРШЕН (Все 4 этапа выполнены)

---

## 🎯 Цель плана

Исправить все проблемы, выявленные при полной валидации проекта:
- ✅ Критические (P0-P1): ESLint конфигурация, циркулярные зависимости, unlisted dependencies, упавший тест
- ⚠️ Некритические (P2-P3): Настройка knip для устранения ложноположительных warnings

---

## 📊 Результаты валидации

### ✅ Что работает хорошо:
- TypeScript компиляция успешна (после сборки)
- Тесты: 96/97 passed (99.8%)
- Дублирование кода: 4.87% (ниже порога 5%)
- Граф зависимостей monorepo правильный

### 🔴 Критические проблемы:
1. ~~**ESLint конфигурация** - 226 ошибок парсинга во всех пакетах~~ ✅ ИСПРАВЛЕНО
2. ~~**Циркулярные зависимости** - 30 warnings (definition ↔ tool паттерн)~~ ✅ ИСПРАВЛЕНО (0 нарушений)
3. ~~**Unlisted dependency** - mime-types не указан в dependencies~~ ✅ ИСПРАВЛЕНО
4. ~~**Упавший тест** - EntityCacheKey ожидает 8 типов, а их 10 (добавлены BOARD, FIELD)~~ ✅ ИСПРАВЛЕНО

### 🟡 Некритические проблемы:
5. ~~**Knip unused files** - 21 файл (scripts, test helpers)~~ ✅ НАСТРОЕН IGNORE
6. ~~**Knip unused exports** - 87 warnings (factories, constants, types)~~ ✅ НАСТРОЕН IGNORE (осталось 15 unused dependencies - приемлемо)

---

## 📋 Структура плана

План разбит на **4 этапа** с учетом зависимостей и возможности параллельного выполнения:

```
Этап 1 (Sequential - последовательно):
├── 1.1_fix_eslint_config_sequential.md      [P0] ~2-3ч
└── 1.2_fix_trivial_issues_sequential.md     [P1] ~1ч
         ↓
Этап 2 (Parallel - можно параллельно):
├── 2.1_refactor_circular_deps_parallel.md   [P1] ~4-6ч
└── 2.2_configure_knip_parallel.md           [P2] ~2ч
```

### Зависимости между этапами:
- **1.1 должен быть выполнен ПЕРВЫМ** (блокирует все остальное)
- **1.2 зависит от 1.1** (нужен работающий ESLint)
- **2.1 и 2.2 независимы** (можно выполнять параллельно)

---

## 📁 Файлы плана

### 1.1: Исправление ESLint конфигурации [SEQUENTIAL]
**Файл:** `1.1_fix_eslint_config_sequential.md`
**Приоритет:** P0 (BLOCKER)
**Время:** ~2-3 часа
**Статус:** ✅ ЗАВЕРШЕНО

**Проблема:**
- ~~Корневой `eslint.config.js` настроен только для yandex-tracker~~
- ~~Применяется ко всем workspaces, вызывая 226 ошибок парсинга~~

**Решение:**
- ✅ Создана мультипакетная конфигурация для всех workspaces
- ✅ Настроены правильные пути к tsconfig.json для каждого пакета
- ✅ Валидирована работа lint для всех 4 пакетов

**Результат:** `npm run lint:quiet` работает без ошибок

---

### 1.2: Исправление trivial проблем [SEQUENTIAL]
**Файл:** `1.2_fix_trivial_issues_sequential.md`
**Приоритет:** P1 (высокий)
**Время:** ~1 час
**Зависит от:** 1.1 ✅
**Статус:** ✅ ЗАВЕРШЕНО

**Проблемы:**
1. Упавший тест: EntityCacheKey ожидает 8 типов, а их 10 (добавлены BOARD, FIELD)
2. Unlisted dependency: mime-types используется, но не в dependencies

**Решения:**
1. Обновить тест: `toHaveLength(8)` → `toHaveLength(10)` + добавить проверки BOARD и FIELD
2. Добавить mime-types и @types/mime-types в package.json

**Включает:**
- Детальный анализ причины каждой проблемы
- Проверку side effects
- Регрессионное тестирование

---

### 2.1: Рефакторинг циркулярных зависимостей [PARALLEL]
**Файл:** `2.1_refactor_circular_deps_parallel.md`
**Приоритет:** P1 (высокий)
**Время:** ~6-8 часов
**Можно выполнять параллельно с:** 2.2
**Статус:** ✅ ЗАВЕРШЕНО

**Проблема:**
- 30 циркулярных зависимостей (28 в yandex-tracker, 2 в framework) ⚠️ БОЛЬШЕ ЧЕМ ОЖИДАЛОСЬ
- Паттерн: `definition.ts ↔ tool.ts`

**Решение:**
- Создать `*.types.ts` файлы с общими типами
- Рефакторить импорты: definition и tool импортируют types, но не друг друга
- Применить паттерн ко всем 17 случаям

**Включает:**
- Глубокий анализ корневой причины циркулярности
- Анализ всех 17 случаев с классификацией
- Сравнение 3 вариантов решения
- Пилотный рефакторинг (ping tool)
- Массовый рефакторинг 15 yandex-tracker tools
- Рефакторинг 2 framework случаев

---

### 2.2: Настройка Knip [PARALLEL]
**Файл:** `2.2_configure_knip_parallel.md`
**Приоритет:** P2 (средний)
**Время:** ~2 часа
**Можно выполнять параллельно с:** 2.1
**Статус:** ✅ ЗАВЕРШЕНО

**Проблема:**
- 21 unused files (scripts, test helpers)
- 42 unused exports (constants, factories)
- 45 unused types (schema types)

**Решение:**
- Создать `knip.config.js` с правильными ignore patterns
- Игнорировать scripts (используются через npm run)
- Игнорировать test helpers (библиотека для тестов)
- Игнорировать factories и schema types (публичный API)

**Включает:**
- Детальный анализ каждой категории unused
- Классификацию: реально unused vs ложноположительные
- Конфигурацию для каждого workspace
- Валидацию и документирование

---

## 🚀 Порядок выполнения

### Неделя 1 (P0 - критично):
```bash
1. Выполнить этап 1.1: Исправление ESLint конфигурации
   - Анализ проблемы
   - Реализация решения
   - Валидация
   ✓ Результат: npm run lint работает для всех пакетов
```

### Неделя 2 (P1 - высокий приоритет):
```bash
2. Выполнить этап 1.2: Исправление trivial проблем
   - Обновить тест EntityCacheKey
   - Добавить mime-types в dependencies
   ✓ Результат: Тесты 97/97 passed, нет unlisted dependencies

3. Параллельно запустить:
   - Этап 2.1: Рефакторинг циркулярных зависимостей (ИИ агент 1)
   - Этап 2.2: Настройка knip (ИИ агент 2)
   ✓ Результат: 0 циркулярных зависимостей, < 25 knip warnings
```

### Финал (валидация):
```bash
4. Полная валидация проекта
   npm run validate

   ✓ Ожидается:
   - lint: успешно
   - typecheck: успешно
   - test: 97/97 passed
   - test:smoke: успешно
   - cpd: 4.87% (допустимо)
   - depcruise: 0 циркулярных зависимостей
   - knip: < 25 warnings
```

---

## 📈 Метрики успеха

### Начало (2025-11-18):
```
❌ ESLint:     226 ошибок парсинга
❌ Tests:      96/97 passed (1 failed)
⚠️  Circular:  17 warnings
⚠️  Unlisted:  1 dependency
⚠️  Knip:      108 warnings
```

### После исправлений (2025-11-19) - ФИНАЛ:
```
✅ ESLint:     0 ошибок
✅ Tests:      455/455 passed в yandex-tracker (100% при запуске --workspace)
✅ Circular:   0 warnings (все устранены!)
✅ Unlisted:   0 dependencies
✅ Knip:       15 unused dependencies (цель < 25 достигнута)
⚠️  Vitest:    133 теста падают при запуске через vitest workspace (будет исправлено в отдельной сессии)
```

**Примечание:** Тесты проходят успешно при запуске через `npm run test:quiet --workspace=@mcp-server/yandex-tracker`, но падают при запуске через vitest workspace для всех пакетов одновременно. Это проблема конфигурации path aliases в vitest workspace, которая будет решена в отдельной сессии.

---

## 🔄 Процесс выполнения

### Для каждого этапа:

1. **Прочитать план** - детально изучить соответствующий .md файл
2. **Анализ** - выполнить все задачи из раздела "Детальный анализ"
3. **Реализация** - следовать пошаговым инструкциям
4. **Валидация** - запустить все тесты и проверки
5. **Commit** - зафиксировать изменения с описательным сообщением
6. **Отметить прогресс** - обновить чек-листы в плане

### Коммиты:

После каждого этапа создавать отдельный commit:

```bash
# После этапа 1.1
git add eslint.config.js
git commit -m "fix(eslint): настроить конфигурацию для всех workspaces monorepo

- Добавить мультипакетную конфигурацию
- Настроить parserOptions для каждого workspace
- Исправить 226 ошибок парсинга

Closes: этап 1.1 плана валидации
"

# После этапа 1.2
git add packages/framework/infrastructure/tests/cache/entity-cache-key.test.ts
git add packages/servers/yandex-tracker/package.json
git commit -m "fix(tests): обновить тест EntityCacheKey и добавить mime-types

- Обновить ожидаемое количество типов: 6 → 7
- Добавить mime-types в dependencies
- Добавить @types/mime-types в devDependencies

Closes: этап 1.2 плана валидации
"

# И так далее для этапов 2.1 и 2.2
```

---

## 💡 Важные замечания

### Параллельное выполнение (этап 2):

Этапы 2.1 и 2.2 можно выполнять **параллельно** в разных ветках:

```bash
# Агент 1 (или сессия 1):
git checkout -b fix/circular-dependencies
# Выполнить этап 2.1

# Агент 2 (или сессия 2):
git checkout -b fix/knip-config
# Выполнить этап 2.2

# После завершения обоих:
# Смержить обе ветки в главную ветку
```

### Если что-то идет не так:

Каждый файл плана содержит раздел **"Возможные проблемы и решения"** с:
- Симптомами проблемы
- Причинами
- Пошаговыми решениями

---

## 📞 Вопросы и изменения

Если в процессе выполнения обнаружены:
- **Новые проблемы** - добавить в соответствующий файл плана
- **Неточности в плане** - обновить план и отметить изменения
- **Альтернативные решения** - задокументировать и обсудить

---

## ✅ Чек-лист готовности к выполнению

Перед началом выполнения убедись:

- [ ] Все файлы плана прочитаны
- [ ] Структура плана понятна
- [ ] Зависимости между этапами ясны
- [ ] Есть доступ ко всем необходимым инструментам
- [ ] Создана резервная копия (или git branch)
- [ ] Понятны критерии успеха для каждого этапа

---

## 🔮 Будущая работа

### Проблема с Vitest Workspace

**Описание:**
При запуске `npm run test` (через vitest workspace), 133 теста падают с ошибками резолюции модулей:
- `Cannot find package '@integration/helpers/mcp-client.js'`
- `Cannot find package '@tools/api/issues/links/create/index.js'`

**Статус:**
- ✅ Все тесты проходят при запуске через `npm run test:quiet --workspace=@mcp-server/yandex-tracker`
- ❌ 133 теста падают при запуске через vitest workspace для всех пакетов

**Причина:**
Проблема с конфигурацией path aliases в vitest.workspace.ts. Vitest не корректно резолвит алиасы путей при запуске в workspace mode.

**Решение (будет выполнено в отдельной сессии):**
1. Исследовать конфигурацию vitest.workspace.ts
2. Проверить настройку resolve.alias для каждого workspace
3. Возможно, потребуется обновить vitest или изменить подход к path aliases
4. Документировать решение

**Приоритет:** P2 (средний) - не блокирует разработку, т.к. тесты можно запускать через --workspace

---

**План исправления проблем валидации завершен! 🎉**
