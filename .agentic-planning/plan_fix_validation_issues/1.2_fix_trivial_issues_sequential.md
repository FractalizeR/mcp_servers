# Этап 1.2: Исправление trivial проблем [SEQUENTIAL]

**Приоритет:** P1 (высокий)
**Статус:** ⏭️ СЛЕДУЮЩИЙ ЭТАП (1.1 ✅ завершен)
**Время выполнения:** ~1 час
**Зависимости:** Этап 1.1 ✅ (ESLint работает корректно)

---

## 📋 Описание проблем

### Проблема 1: Упавший тест EntityCacheKey
- **Файл:** `packages/framework/infrastructure/tests/cache/entity-cache-key.test.ts:89`
- **Ошибка:** `expected [...] to have a length of 8 but got 10`
- **Причина:** Добавлены новые типы `BOARD` и `FIELD`, но тест не обновлен
- **Также:** В тесте (строки 79-87) отсутствуют проверки для BOARD и FIELD

### Проблема 2: Unlisted dependency
- **Пакет:** `@mcp-server/yandex-tracker`
- **Зависимость:** `mime-types`
- **Использование:** `src/tracker_api/utils/file-upload.util.ts:8:25`
- **Причина:** Зависимость используется, но не указана в package.json

---

## 🔍 ЭТАП 1: Детальный анализ проблем

### 1.1. Анализ EntityCacheKey теста

#### Задачи анализа:
- [ ] Прочитать `packages/framework/infrastructure/src/cache/entity-cache-key.ts`
- [ ] Подсчитать реальное количество типов в enum `EntityType`
- [ ] Найти, когда был добавлен `ATTACHMENT`
- [ ] Проверить, не нужно ли обновить другие тесты

#### Команды для анализа:
```bash
# Проверить определение EntityType
grep -A 10 "export enum EntityType" packages/framework/infrastructure/src/cache/entity-cache-key.ts

# Найти все использования EntityType в тестах
grep -r "EntityType" packages/framework/infrastructure/tests/cache/

# Проверить git историю
git log --oneline --all -S "ATTACHMENT" -- packages/framework/infrastructure/src/cache/entity-cache-key.ts
```

#### Ожидаемые находки:
```typescript
export enum EntityType {
  ISSUE = 'issue',       // 1
  QUEUE = 'queue',       // 2
  USER = 'user',         // 3
  COMMENT = 'comment',   // 4
  COMPONENT = 'component', // 5
  SPRINT = 'sprint',     // 6
  BOARD = 'board',       // 7 ← добавлено
  PROJECT = 'project',   // 8
  ATTACHMENT = 'attachment', // 9
  FIELD = 'field',       // 10 ← добавлено
}
```

#### Вопросы для анализа:
1. Есть ли другие тесты, проверяющие количество типов?
2. Нужно ли обновить документацию после добавления BOARD и FIELD?
3. Используются ли BOARD и FIELD корректно в коде?
4. Проверяются ли BOARD и FIELD в тестах (строки 79-87)?

---

### 1.2. Анализ mime-types зависимости

#### Задачи анализа:
- [ ] Найти все импорты `mime-types` в проекте
- [ ] Проверить, используется ли в production коде
- [ ] Определить нужную версию пакета
- [ ] Проверить, нужны ли @types/mime-types

#### Команды для анализа:
```bash
# Найти все использования mime-types
grep -r "mime-types" packages/servers/yandex-tracker/src/

# Проверить, как используется
cat packages/servers/yandex-tracker/src/tracker_api/utils/file-upload.util.ts | grep -A 5 -B 5 "mime-types"

# Проверить package.json
cat packages/servers/yandex-tracker/package.json | grep -A 5 -B 5 "dependencies"
```

#### Ожидаемые находки:
```typescript
// file-upload.util.ts
import mime from 'mime-types';

// Используется для определения MIME типа файла
const mimeType = mime.lookup(filename) || 'application/octet-stream';
```

#### Вопросы для анализа:
1. Используется ли mime-types только в одном файле?
2. Есть ли альтернативы (можно ли использовать встроенные средства)?
3. Какая актуальная версия mime-types?
4. Нужны ли type definitions?

---

### 1.3. Анализ влияния на систему

#### Проверка зависимостей:
- [ ] Влияет ли изменение EntityType на другие части системы?
- [ ] Используется ли mime-types в тестах?
- [ ] Есть ли другие unlisted dependencies?

#### Команды для проверки:
```bash
# Проверить все unlisted dependencies
npm run knip 2>&1 | grep "Unlisted dependencies"

# Проверить, что все используемые пакеты в dependencies
npm ls --all 2>&1 | grep "UNMET"
```

---

## 🛠️ ЭТАП 2: Реализация решений

### 2.1. Исправление теста EntityCacheKey

#### Шаг 1: Обновить тест
```bash
# Открыть файл теста
vim packages/framework/infrastructure/tests/cache/entity-cache-key.test.ts
```

#### Изменения:
```typescript
// До (строка 89):
it('должен иметь 8 типов сущностей', () => {
  const types = Object.values(EntityType);
  expect(types).toHaveLength(8);
});

// После:
it('должен иметь 10 типов сущностей', () => {
  const types = Object.values(EntityType);
  expect(types).toHaveLength(10);
});
```

#### Шаг 2: Добавить проверки для BOARD и FIELD
```typescript
// В тесте "должен содержать все типы сущностей" (строки 78-87)
// Добавить после строки 86:
it('должен содержать все типы сущностей', () => {
  expect(EntityType.ISSUE).toBe('issue');
  expect(EntityType.QUEUE).toBe('queue');
  expect(EntityType.USER).toBe('user');
  expect(EntityType.COMMENT).toBe('comment');
  expect(EntityType.COMPONENT).toBe('component');
  expect(EntityType.SPRINT).toBe('sprint');
  expect(EntityType.BOARD).toBe('board');        // ← Добавить
  expect(EntityType.PROJECT).toBe('project');
  expect(EntityType.ATTACHMENT).toBe('attachment');
  expect(EntityType.FIELD).toBe('field');        // ← Добавить
});
```

#### Шаг 3: Валидация
```bash
# Запустить конкретный тест
npm run test --workspace=@mcp-framework/infrastructure -- entity-cache-key.test.ts

# Проверить, что все тесты проходят
npm run test --workspace=@mcp-framework/infrastructure
```

---

### 2.2. Добавление mime-types в dependencies

#### Шаг 1: Определить актуальную версию
```bash
# Проверить, какая версия используется (через node_modules)
npm ls mime-types

# Найти последнюю stable версию
npm view mime-types versions --json | tail -n 5
```

#### Шаг 2: Добавить в package.json
```bash
# Добавить runtime зависимость
npm install mime-types --workspace=@mcp-server/yandex-tracker

# Добавить type definitions
npm install --save-dev @types/mime-types --workspace=@mcp-server/yandex-tracker
```

#### Шаг 3: Проверить package.json
```bash
# Убедиться, что зависимости добавлены
cat packages/servers/yandex-tracker/package.json | grep -A 2 "mime-types"
```

Ожидается:
```json
{
  "dependencies": {
    "mime-types": "^2.1.35",
    ...
  },
  "devDependencies": {
    "@types/mime-types": "^2.1.4",
    ...
  }
}
```

#### Шаг 4: Валидация зависимости
```bash
# Переустановить зависимости
npm install

# Проверить, что mime-types в dependencies
npm run knip 2>&1 | grep -i "mime-types"

# Должно быть пусто (не unlisted)
```

---

## ✅ ЭТАП 3: Проверка результатов

### 3.1. Тестирование исправлений

#### Тест 1: EntityCacheKey
```bash
# Запустить тесты infrastructure
npm run test:quiet --workspace=@mcp-framework/infrastructure

# Проверить конкретный тест
npm run test --workspace=@mcp-framework/infrastructure -- entity-cache-key.test.ts -t "должен иметь 10 типов"
```

**Ожидается:**
```
✓ должен иметь 10 типов сущностей
✓ должен содержать все типы сущностей (включая BOARD и FIELD)
Test Files  11 passed (11)
Tests       200 passed (200)
```

#### Тест 2: mime-types dependency
```bash
# Проверить, что нет unlisted dependencies
npm run knip 2>&1 | grep "Unlisted"

# Собрать проект (должно работать)
npm run build --workspace=@mcp-server/yandex-tracker

# Запустить typecheck
npm run typecheck --workspace=@mcp-server/yandex-tracker
```

**Ожидается:**
- Нет ошибок "Cannot find module 'mime-types'"
- Сборка успешна
- TypeScript проверка проходит

---

### 3.2. Регрессионное тестирование

#### Полный прогон тестов:
```bash
# Все тесты infrastructure
npm run test --workspace=@mcp-framework/infrastructure

# Все тесты yandex-tracker
npm run test --workspace=@mcp-server/yandex-tracker

# Smoke test
npm run test:smoke --workspace=@mcp-server/yandex-tracker
```

#### Валидация всего monorepo:
```bash
# Полная валидация
npm run validate

# Должно пройти:
# ✓ lint (после этапа 1.1)
# ✓ typecheck
# ✓ test (включая исправленный тест)
# ✓ test:smoke
# ✓ cpd
```

---

### 3.3. Проверка side effects

#### Проверить, что BOARD и FIELD используются правильно:
```bash
# Найти все использования BOARD
grep -r "EntityType.BOARD" packages/

# Найти все использования FIELD
grep -r "EntityType.FIELD" packages/

# Найти использования в cache-related коде
grep -r "board:\|field:" packages/ | grep -i cache
```

#### Проверить mime-types в действии:
```bash
# Найти тесты для file-upload
find packages -name "*file-upload*test*"

# Запустить тесты загрузки файлов
npm run test --workspace=@mcp-server/yandex-tracker -- file-upload
```

---

## 🚨 Возможные проблемы и решения

### Проблема 1: Другие тесты зависят от количества типов

**Симптом:**
```
Other tests failing after changing 6 to 7
```

**Решение:**
```bash
# Найти все упоминания "6" в тестах cache
grep -r "toHaveLength(6)" packages/framework/infrastructure/tests/cache/

# Обновить все найденные места
```

### Проблема 2: mime-types конфликтует с другой зависимостью

**Симптом:**
```
npm ERR! ERESOLVE unable to resolve dependency tree
```

**Решение:**
```bash
# Проверить конфликты
npm ls mime-types

# Принудительная установка (если безопасно)
npm install mime-types --workspace=@mcp-server/yandex-tracker --force

# Или использовать конкретную версию
npm install mime-types@2.1.35 --workspace=@mcp-server/yandex-tracker
```

### Проблема 3: TypeScript не находит types для mime-types

**Симптом:**
```
Could not find a declaration file for module 'mime-types'
```

**Решение:**
```bash
# Установить @types
npm install --save-dev @types/mime-types --workspace=@mcp-server/yandex-tracker

# Или создать собственный .d.ts
echo "declare module 'mime-types';" > packages/servers/yandex-tracker/src/types/mime-types.d.ts
```

---

## 📊 Метрики до/после

### До исправления (2025-11-19):
```
Tests: 199/200 passed (1 failed - EntityCacheKey)
Unlisted dependencies: 1 (mime-types)
```

### После исправления (цель):
```
Tests: 200/200 passed (100%)
Unlisted dependencies: 0
```

---

## 📝 Документирование изменений

### Обновить CHANGELOG (опционально):
```markdown
## Infrastructure v0.1.2

### Fixed
- Обновлен тест EntityCacheKey для поддержки 10 типов (добавлены BOARD, FIELD)
```

### Обновить комментарии:
```typescript
/**
 * Типы сущностей Яндекс.Трекера для кеширования
 *
 * @enum {string}
 * @property ISSUE - Задачи
 * @property QUEUE - Очереди
 * @property USER - Пользователи
 * @property COMMENT - Комментарии
 * @property COMPONENT - Компоненты
 * @property SPRINT - Спринты
 * @property BOARD - Доски (добавлено в v0.1.2)
 * @property PROJECT - Проекты
 * @property ATTACHMENT - Вложения
 * @property FIELD - Поля (добавлено в v0.1.2)
 */
export enum EntityType { ... }
```

---

## ✅ Критерии завершения

- [ ] Тест EntityCacheKey обновлен: 8 → 10 типов
- [ ] Добавлены проверки для BOARD и FIELD в тесте
- [ ] Тест EntityCacheKey проходит успешно
- [ ] mime-types добавлен в dependencies
- [ ] @types/mime-types добавлен в devDependencies
- [ ] Нет unlisted dependencies в knip
- [ ] Все тесты infrastructure проходят (200/200)
- [ ] `npm run validate:quiet` успешен
- [ ] Изменения задокументированы (опционально)

---

## ⏭️ Следующие этапы

После завершения этого этапа можно параллельно выполнять:
- **2.1_refactor_circular_deps_parallel.md** (независимо)
- **2.2_configure_knip_parallel.md** (независимо)
