# Этап 3 — Финализация (sequential)

**Зависимости:** этапы 1–2 завершены и провалидированы.

## Шаги
1. **Документация:**
   - README модулей `tracker_api/utils`, `tools`, `api_operations` — описать `PaginatedResult`, `fetchAll`, лимит `maxItems`, два механизма пагинации (Link/seek).
   - Заметка в `packages/servers/yandex-tracker/CLAUDE.md` о паттерне пагинации для будущих эндпоинтов.
   - Проверить лимиты размера доков (`npm run validate:docs`).
2. **Полная валидация:** `npm run validate` (build, lint, typecheck, test, cpd, depcruise, knip, validate:docs/tools). Убедиться, что `PaginationUtil` больше не мёртвый код (knip).
3. **Расширенное ревью** (по правилам: новый кросс-доменный контракт + изменение интерфейса `IHttpClient` → Claude + Codex + DeepSeek параллельно). Верифицировать каждый issue, устранить CRITICAL→LOW, повторная валидация. В конце — сводка качества ревьюеров.
4. **Коммиты** на отдельной ветке (не `main`), логическими частями: (1) инфраструктура+paginator, (2) общие схемы, (3) доменные группы, (4) доки. Спросить разрешение перед push.
5. **Удаление плана** после успешной финальной валидации (по правилу проекта).

## DoD
- `npm run validate` зелёный.
- Все list-инструменты отдают `pagination`; `fetchAll` работает с лимитом и `truncated`.
- Ревью закрыто, документация актуальна.
