# Этап 6.1: add-checklist-item → batch support

**Execution Type:** `parallel` (может выполняться одновременно с 6.2, 6.3)

**Цель:** Добавить batch support в add-checklist-item с индивидуальными параметрами.

---

## ✅ Чек-лист

- [ ] Schema: массив items
- [ ] Batch operation: executeMany()
- [ ] Tool: unified batch result
- [ ] Facade/service methods
- [ ] Unit-тесты
- [ ] Валидация и коммит

---

## Schema (Стратегия B)

```typescript
{
  items: z.array(
    z.object({
      issueId: IssueKeySchema,
      text: z.string(),
      checked: z.boolean().optional(),
      assignee: z.string().optional(),
      deadline: z.string().optional()
    })
  ).min(1),
  fields: FieldsSchema
}
```

Следовать паттерну этапа 2.1 (add-comment).
