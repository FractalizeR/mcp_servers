# Этап 8.1: Документация и финальная валидация

**Execution Type:** `sequential` (выполняется после этапа 7)

**Цель:** Обновить документацию, провести финальную валидацию, подготовить к использованию.

---

## ✅ Чек-лист выполнения

- [ ] 1. Обновить README.md (user-facing документация)
- [ ] 2. Обновить CLAUDE.md (developer guide)
- [ ] 3. Обновить ARCHITECTURE.md (если требуется)
- [ ] 4. Обновить MIGRATION.md (breaking changes)
- [ ] 5. Проверить лимиты документации
- [ ] 6. Финальная валидация проекта
- [ ] 7. Smoke тесты в реальной среде (если возможно)
- [ ] 8. Финальный коммит
- [ ] 9. Удалить план (если все успешно)

---

## 📋 Детальные шаги

### Шаг 1: Обновление README.md

**Файл:** `packages/servers/yandex-tracker/README.md`

**Добавить секцию "Batch Operations":**

```markdown
## Batch Operations

All read and write operations now support batch mode for improved performance when working with multiple issues.

### Batch GET Operations

Get data from multiple issues in a single tool call:

- `get_comments` - Get comments from multiple issues
- `get_issue_links` - Get links from multiple issues
- `get_issue_changelog` - Get changelog from multiple issues
- `get_worklogs` - Get worklogs from multiple issues
- `get_checklist` - Get checklists from multiple issues
- `get_attachments` - Get attachments from multiple issues

**Example:**
\`\`\`json
{
  "issueIds": ["PROJ-1", "PROJ-2", "PROJ-3"],
  "fields": ["id", "text", "createdAt"]
}
\`\`\`

**Response format (unified):**
\`\`\`json
{
  "total": 3,
  "successful": [
    { "issueId": "PROJ-1", "comments": [...], "count": 5 },
    { "issueId": "PROJ-2", "comments": [...], "count": 3 }
  ],
  "failed": [
    { "issueId": "PROJ-3", "error": "Issue not found" }
  ]
}
\`\`\`

### Batch POST/DELETE Operations

Modify multiple issues with individual parameters:

- `add_comment` - Add comments to multiple issues (each with own text)
- `create_link` - Create multiple links
- `delete_link` - Delete multiple links
- `add_worklog` - Add worklogs to multiple issues
- `delete_comment` - Delete comments from multiple issues
- `add_checklist_item` - Add checklist items to multiple issues
- `delete_attachment` - Delete attachments from multiple issues
- `edit_comment` - Edit comments in multiple issues

**Example (individual parameters):**
\`\`\`json
{
  "comments": [
    { "issueId": "PROJ-1", "text": "Comment for task 1" },
    { "issueId": "PROJ-2", "text": "Comment for task 2", "attachmentIds": ["att1"] }
  ],
  "fields": ["id", "text", "createdAt"]
}
\`\`\`
```

**Обновить секцию "Breaking Changes"** (если есть):
- Перечислить изменения параметров (issueId → issueIds)
- Указать на изменение формата ответа

---

### Шаг 2: Обновление CLAUDE.md

**Файл:** `packages/servers/yandex-tracker/CLAUDE.md`

**Добавить секцию "Batch Operations Pattern":**

```markdown
## 🔄 Batch Operations Pattern

### Когда использовать batch

**GET операции:**
- Используй batch, когда нужны данные от >1 задачи
- Параметры (perPage, page, expand) применяются ко всем задачам одинаково

**POST/DELETE операции:**
- Используй batch для массовых модификаций
- Каждая задача может иметь индивидуальные параметры (Стратегия B)

### Schema conventions

**GET operations:**
\`\`\`typescript
issueIds: IssueKeysSchema  // всегда массив, минимум 1
\`\`\`

**POST/DELETE operations (индивидуальные параметры):**
\`\`\`typescript
comments: z.array(
  z.object({
    issueId: IssueKeySchema,
    text: z.string(),
    // ... другие параметры
  })
).min(1)
\`\`\`

### Unified batch result format

**Все batch операции ОБЯЗАНЫ возвращать:**
\`\`\`typescript
{
  total: number,
  successful: Array<{ issueId: string, ...data }>,
  failed: Array<{ issueId: string, error: string }>,
  fieldsReturned?: string[]  // для GET операций
}
\`\`\`

### ParallelExecutor usage

**ОБЯЗАТЕЛЬНО использовать ParallelExecutor для соблюдения concurrency limits:**
\`\`\`typescript
const operations = issueIds.map(id => ({
  key: id,
  fn: async () => this.httpClient.get(\`/v3/issues/\${id}/...\`)
}));

return this.parallelExecutor.executeParallel(operations, 'operation name');
\`\`\`

### Обработка частичных ошибок

**Batch операции ДОЛЖНЫ обрабатывать частичные ошибки:**
- Если некоторые задачи успешны, а некоторые с ошибками → вернуть обе группы
- НЕ выбрасывать исключение, если хотя бы одна задача успешна
- Использовать BatchResultProcessor для унифицированной обработки
```

---

### Шаг 3: Обновление ARCHITECTURE.md

**Файл:** `ARCHITECTURE.md`

**Добавить в секцию "Data Flow" подсекцию о batch операциях:**

```markdown
### Batch Operations Flow

**GET операции (параллельное получение):**
\`\`\`
Tool (issueIds[]) → Operation.executeMany()
  → ParallelExecutor (respects maxConcurrentRequests)
    → N × API calls (parallel)
  → BatchResult<string, Data>
  → BatchResultProcessor.process()
    → Unified format: { total, successful, failed }
\`\`\`

**POST/DELETE операции (параллельная модификация):**
\`\`\`
Tool (items[]) → Operation.executeMany()
  → ParallelExecutor
    → N × API calls (each with individual params)
  → BatchResult<string, Response>
  → Unified format: { total, successful, failed }
\`\`\`

**Ключевые компоненты:**
- **ParallelExecutor**: соблюдает maxConcurrentRequests из config
- **BatchResultProcessor**: унифицирует обработку результатов
- **Unified Batch Format**: консистентный формат ответа
```

---

### Шаг 4: Обновление MIGRATION.md

**Файл:** `MIGRATION.md`

**Добавить секцию "Migration to Batch Operations (v2.x → v2.y)":**

```markdown
## Migration to Batch Operations

### Breaking Changes

**All operations now require arrays:**

**Before:**
\`\`\`typescript
get_comments({ issueId: "PROJ-1", fields: [...] })
\`\`\`

**After:**
\`\`\`typescript
get_comments({ issueIds: ["PROJ-1"], fields: [...] })
\`\`\`

**Response format changed:**

**Before:**
\`\`\`json
{
  "issueId": "PROJ-1",
  "comments": [...],
  "count": 5
}
\`\`\`

**After (unified batch format):**
\`\`\`json
{
  "total": 1,
  "successful": [
    { "issueId": "PROJ-1", "comments": [...], "count": 5 }
  ],
  "failed": []
}
\`\`\`

### Migration checklist

- [ ] Update all tool calls: `issueId` → `issueIds` (array)
- [ ] Update response parsing: expect `{ total, successful, failed }`
- [ ] Handle `failed` array (partial errors)
- [ ] For POST/DELETE: use array of objects with individual params
```

---

### Шаг 5: Проверка лимитов документации

```bash
npm run validate:docs
```

**Проверить:**
- CLAUDE.md ≤ 400 строк (лимит)
- ARCHITECTURE.md ≤ 700 строк
- README.md ≤ 600 строк

**Если превышен лимит:**
- Применить правила сокращения из CLAUDE.md (секция "Правила сокращения")
- Удалить примеры кода >10 строк → заменить ссылками
- Удалить дублирование
- Сократить объяснения "почему"

---

### Шаг 6: Финальная валидация

```bash
npm run validate
```

**Убедиться:**
- [ ] Все lint checks проходят
- [ ] Type-check успешен
- [ ] Все тесты проходят (unit + integration + smoke)
- [ ] CPD проходит (нет излишнего дублирования кода)
- [ ] Dependency cruiser проходит
- [ ] Документация валидна (лимиты соблюдены)

---

### Шаг 7: Smoke тесты в реальной среде

**Если есть доступ к test environment Yandex Tracker:**

```bash
npm run test:smoke
```

**Вручную протестировать:**
1. Создать 3 тестовые задачи
2. Вызвать `get_comments` с массивом из 3 issueIds
3. Проверить unified batch result
4. Вызвать `add_comment` с массивом комментариев
5. Проверить, что комментарии добавлены корректно

---

### Шаг 8: Финальный коммит

**Формат:**
```
docs: обновить документацию для batch operations

Детали:
- README.md: добавлена секция "Batch Operations" с примерами
- CLAUDE.md: добавлен "Batch Operations Pattern" для разработчиков
- ARCHITECTURE.md: описан data flow для batch операций
- MIGRATION.md: руководство по миграции на batch API
- Все лимиты документации соблюдены

Проверено:
- npm run validate:docs успешна
- npm run validate успешна
- Smoke тесты пройдены

🤖 Generated with Claude Code
Co-Authored-By: Claude <noreply@anthropic.com>
```

```bash
git add .
git commit -m "..."
git push origin claude/yandex-tracker-mcp-tools-019MPZCAtcjW5fpmbAAdQvzz
```

---

### Шаг 9: Удаление плана (если успешно)

**Если все этапы завершены и валидация успешна:**

```bash
rm -rf .agentic-planning/plan_batch_operations
git add .agentic-planning/plan_batch_operations
git commit -m "chore: удалить план batch operations (успешно завершен)"
git push
```

**Согласно CLAUDE.md:**
> Если финальная валидация показывает, что весь план успешно выполнен, после завершения последнего шага план надо удалить.

---

## 🎯 Критерии завершения всего проекта

1. ✅ Все 15 операций поддерживают batch режим
2. ✅ Unified batch format везде консистентен
3. ✅ Документация полная и корректная
4. ✅ Все тесты проходят (unit + integration + smoke)
5. ✅ Покрытие тестами ≥90%
6. ✅ `npm run validate` успешна
7. ✅ Breaking changes задокументированы в MIGRATION.md
8. ✅ План удален

---

## 🎉 После завершения

**Сообщить пользователю:**
- Все batch операции реализованы
- Breaking changes: issueId → issueIds, новый формат ответа
- Документация обновлена
- Тесты покрывают >90% кода
- Готово к использованию

**Предложить пользователю:**
- Протестировать в реальной среде
- Обновить интеграции с новым API
- Ознакомиться с MIGRATION.md

---

## 📚 Справочные материалы

**Документация для проверки:**
- README.md — user-facing guide
- CLAUDE.md — developer guide
- ARCHITECTURE.md — architecture overview
- MIGRATION.md — migration guide
- DOC_STANDARDS.md — documentation standards
