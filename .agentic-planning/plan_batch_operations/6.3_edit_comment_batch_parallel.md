# Этап 6.3: edit-comment → batch support

**Execution Type:** `parallel` (может выполняться одновременно с 6.1, 6.2)

**Цель:** Добавить batch support в edit-comment с индивидуальными параметрами.

---

## ✅ Чек-лист

- [ ] Schema: массив comments для редактирования
- [ ] Batch operation: executeMany()
- [ ] Tool: unified batch result
- [ ] Facade/service methods
- [ ] Unit-тесты
- [ ] Валидация и коммит

---

## Schema (Стратегия B)

```typescript
{
  comments: z.array(
    z.object({
      issueId: IssueKeySchema,
      commentId: z.string(),
      text: z.string()
    })
  ).min(1),
  fields: FieldsSchema
}
```

Следовать паттерну этапа 2.1 (add-comment).
