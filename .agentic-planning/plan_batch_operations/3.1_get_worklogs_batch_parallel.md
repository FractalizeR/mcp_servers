# Этап 3.1: get-worklogs → batch support

**Execution Type:** `parallel` (может выполняться одновременно с 3.2)

**Цель:** Добавить batch support в get-worklogs.

---

## ✅ Чек-лист

- [ ] Schema: issueId → issueIds
- [ ] Batch operation: executeMany()
- [ ] Tool: unified batch result
- [ ] Facade/service methods
- [ ] Unit-тесты
- [ ] Валидация и коммит

---

Следовать паттерну этапа 1.1 (get-comments) — GET операция с одинаковыми параметрами для всех задач.

**Unified result:**
```typescript
{
  total: number,
  successful: Array<{ issueId: string, worklogs: WorklogWithUnknownFields[], count: number }>,
  failed: Array<{ issueId: string, error: string }>
}
```
