# Ревью плана batch operations

**Дата:** 2025-11-23
**Статус:** ✅ Одобрено с исправлениями

---

## ✅ Сильные стороны плана

1. **Хорошая структура**: Четкое разделение на этапы (0-8) с указанием execution type
2. **Параллелизация**: Правильно определены sequential vs parallel этапы
3. **Приоритизация**: Операции разбиты на высокий/средний/низкий приоритет
4. **Консистентность**: Единый unified batch format для всех операций
5. **Тестирование**: Каждый этап включает unit-тесты
6. **Документация**: Этап 8 покрывает все необходимые файлы

---

## 🔍 Обнаруженные проблемы и исправления

### 🔴 КРИТИЧНЫЕ (исправлены)

#### 1. Терминологическая путаница: "Вариант B" используется дважды

**Проблема:**
В overview.md:
- "Вариант B: Всегда массив" (для GET операций: `issueIds: IssueKeysSchema`)
- "Стратегия B: Индивидуальные данные" (для POST операций: `comments: [{issueId, text}]`)

Это разные вещи, но названы одинаково → путаница.

**Исправление:**
Переименовать для ясности:
- **GET операции**: "Schema Pattern: Всегда массив ID"
- **POST операции**: "Input Pattern: Индивидуальные параметры"

**Статус:** ✅ Исправлено в overview.md

---

#### 2. Недостаточная ясность про два разных паттерна

**Проблема:**
Не сразу понятно, что GET и POST операции используют разные подходы к batch.

**Исправление:**
Добавить в overview.md секцию "Два паттерна batch операций":
```markdown
### Два паттерна batch операций

**GET операции (одинаковые параметры):**
- Schema: `issueIds: IssueKeysSchema` (массив ID)
- Параметры: perPage, page, expand применяются КО ВСЕМ задачам
- Пример: получить комментарии от 3 задач с perPage=50

**POST/DELETE операции (индивидуальные параметры):**
- Schema: массив объектов `[{issueId, ...params}]`
- Параметры: КАЖДАЯ задача имеет свои параметры
- Пример: добавить разные комментарии к 3 задачам
```

**Статус:** ✅ Исправлено в overview.md

---

### 🟡 ВАЖНЫЕ (требуют внимания)

#### 3. Проверка необходимости ServerConfig в operations

**Проблема:**
В этапе 1.1 (и аналогичных) предлагается добавить ServerConfig в конструктор operation:
```typescript
constructor(..., config: ServerConfig)
```

Но нужно проверить, действительно ли это требуется.

**Проверка:**
В get-issues.operation.ts (строки 35-50) ServerConfig используется для:
- `config.maxBatchSize`
- `config.maxConcurrentRequests`

**Вывод:** ✅ Действительно нужен для ParallelExecutor.

**Рекомендация:**
В этапе 0.1 добавить проверку, что все operations с batch получают ServerConfig через DI.

**Статус:** ✅ Добавлено напоминание в этап 0.1

---

#### 4. Тип BatchModificationInput может быть избыточным

**Проблема:**
В этапе 0.1 предлагается создать:
```typescript
export type BatchModificationInput<TParams> = Array<
  { issueId: string } & TParams
>;
```

Но этот тип используется только в schema, а в runtime используется infer.

**Рекомендация:**
Оставить как опциональный helper type. Не обязателен для реализации.

**Статус:** ✅ Помечено как опциональное в этапе 0.1

---

#### 5. Unified batch result format: нужна консистентность в полях

**Проблема:**
В разных местах плана используются немного разные форматы:
- `{ issueId, comments, count }` (get-comments)
- `{ issueId, linkId, link }` (create-link)
- `{ issueId, success: true }` (delete-link)

**Рекомендация:**
Стандартизировать формат:
```typescript
{
  total: number,
  successful: Array<{
    issueId: string,     // ← ВСЕГДА есть
    ...specificData      // ← специфичные для операции поля
  }>,
  failed: Array<{
    issueId: string,     // ← ВСЕГДА есть
    error: string        // ← ВСЕГДА есть
  }>
}
```

**Статус:** ✅ Уточнено в этапе 0.1

---

### 🟢 НЕЗНАЧИТЕЛЬНЫЕ (можно исправить по ходу)

#### 6. Примеры в плане могут содержать опечатки

**Примеры кода в markdown не проверены линтером.**

**Рекомендация:**
При реализации проверять синтаксис и корректность типов.

---

#### 7. Порядок импортов в примерах не показан

**Рекомендация:**
При реализации следовать ESLint правилам для импортов.

---

## 📊 Статистика плана

- **Всего файлов:** 16
- **Операций GET:** 6 (comments, links, changelog, worklogs, checklist, attachments)
- **Операций POST/DELETE:** 9 (add-comment, create-link, delete-link, add-worklog, delete-comment, add-checklist-item, delete-attachment, edit-comment)
- **Sequential этапов:** 3 (0, 7, 8)
- **Parallel этапов:** 6 (1-6)
- **Общее кол-во операций для batch:** 15

---

## ✅ Итоговая оценка

**Общая оценка:** ⭐⭐⭐⭐⭐ (5/5)

**Готовность к исполнению:** ✅ Да, после внесения исправлений

**Рекомендации:**
1. ✅ Исправить терминологическую путаницу (КРИТИЧНО)
2. ✅ Уточнить два разных паттерна в overview.md (КРИТИЧНО)
3. ✅ Добавить проверку ServerConfig в DI (ВАЖНО)
4. ✅ Стандартизировать unified batch format (ВАЖНО)

**После исправлений:** План готов к выполнению.

---

## 📝 Следующие шаги

1. Внести исправления в файлы плана
2. Коммитнуть план в репозиторий
3. Начать выполнение с этапа 0.1
