# Этап 2.3: delete-link → batch support

**Execution Type:** `parallel` (может выполняться одновременно с 2.1, 2.2)

**Цель:** Добавить поддержку удаления множества связей.

---

## ✅ Чек-лист

- [x] Обновить schema для массива links
- [x] Batch operation: executeMany()
- [x] Tool: unified batch result (ручная обработка void)
- [x] Facade/service methods
- [x] Unit-тесты
- [x] Валидация и коммит

---

## Schema

```typescript
{
  links: z.array(
    z.object({
      issueId: IssueKeySchema,
      linkId: z.string()
    })
  ).min(1)
}
```

**Unified result:**
```typescript
{
  total: number,
  successful: Array<{ issueId: string, linkId: string, success: true }>,
  failed: Array<{ issueId: string, linkId: string, error: string }>
}
```

Следовать паттерну этапа 2.1 (add-comment).
