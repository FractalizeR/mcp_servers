# Этап 4.1: add-worklog → batch support

**Execution Type:** `parallel` (может выполняться одновременно с 4.2)

**Цель:** Добавить batch support в add-worklog с индивидуальными параметрами.

---

## ✅ Чек-лист

- [ ] Schema: массив worklogs с индивидуальными параметрами
- [ ] Batch operation: executeMany()
- [ ] Tool: unified batch result
- [ ] Facade/service methods
- [ ] Unit-тесты
- [ ] Валидация и коммит

---

## Schema (Стратегия B)

```typescript
{
  worklogs: z.array(
    z.object({
      issueId: IssueKeySchema,
      start: z.string(),
      duration: z.string(),
      comment: z.string().optional()
    })
  ).min(1),
  fields: FieldsSchema
}
```

Следовать паттерну этапа 2.1 (add-comment) — POST операция с индивидуальными параметрами.
