# Этап 2.2: create-link → batch support

**Execution Type:** `parallel` (может выполняться одновременно с 2.1, 2.3)

**Цель:** Добавить поддержку массива связей с индивидуальными параметрами.

---

## ✅ Чек-лист

- [x] Обновить schema для массива links с индивидуальными параметрами
- [x] Batch operation: executeMany()
- [x] Tool: unified batch result
- [x] Facade/service methods
- [x] Unit-тесты
- [x] Валидация и коммит

**Примечание:** Integration тесты обновлены частично (unit тесты проходят, integration тесты для create-link требуют обновления отдельно).

---

## Schema

```typescript
{
  links: z.array(
    z.object({
      issueId: IssueKeySchema,
      relationship: z.string(),
      targetIssue: IssueKeySchema
    })
  ).min(1),
  fields: FieldsSchema
}
```

**Unified result:**
```typescript
{
  total: number,
  successful: Array<{ issueId: string, linkId: string, link: LinkWithUnknownFields }>,
  failed: Array<{ issueId: string, error: string }>
}
```

Следовать паттерну этапа 2.1 (add-comment).
