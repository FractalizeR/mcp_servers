# Этап 6.2: delete-attachment → batch support

**Execution Type:** `parallel` (может выполняться одновременно с 6.1, 6.3)

**Цель:** Добавить batch support в delete-attachment.

---

## ✅ Чек-лист

- [ ] Schema: массив attachments
- [ ] Batch operation: executeMany()
- [ ] Tool: unified batch result
- [ ] Facade/service methods
- [ ] Unit-тесты
- [ ] Валидация и коммит

---

## Schema

```typescript
{
  attachments: z.array(
    z.object({
      issueId: IssueKeySchema,
      attachmentId: z.string()
    })
  ).min(1)
}
```

Следовать паттерну этапа 2.3 (delete-link).
