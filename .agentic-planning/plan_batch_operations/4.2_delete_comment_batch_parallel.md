# Этап 4.2: delete-comment → batch support

**Execution Type:** `parallel` (может выполняться одновременно с 4.1)

**Цель:** Добавить batch support в delete-comment.

---

## ✅ Чек-лист

- [ ] Schema: массив comments для удаления
- [ ] Batch operation: executeMany()
- [ ] Tool: unified batch result
- [ ] Facade/service methods
- [ ] Unit-тесты
- [ ] Валидация и коммит

---

## Schema

```typescript
{
  comments: z.array(
    z.object({
      issueId: IssueKeySchema,
      commentId: z.string()
    })
  ).min(1)
}
```

Следовать паттерну этапа 2.3 (delete-link).
