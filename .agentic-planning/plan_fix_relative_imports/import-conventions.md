# Соглашение об импортах в проекте

**Дата:** 2025-11-19
**Статус:** 🟡 Черновик (требует согласования)

---

## 🎯 Цель соглашения

Установить единые правила использования относительных импортов и path aliases во всех пакетах monorepo для:
- Улучшения читаемости кода
- Упрощения рефакторинга
- Снижения количества ошибок при перемещении файлов
- Консистентности стиля кода

---

## 📐 Правила импортов

### 1. Межпакетные импорты (между npm пакетами)

**Правило:** ✅ **ВСЕГДА используй npm package names**

```typescript
// ✅ ПРАВИЛЬНО
import { BaseTool } from '@mcp-framework/core';
import { HttpClient } from '@mcp-framework/infrastructure';
import { ToolSearchEngine } from '@mcp-framework/search';

// ❌ НЕПРАВИЛЬНО
import { BaseTool } from '../../../core/src/tools/base/base-tool.js';
import { HttpClient } from '../../infrastructure/src/http/http-client.js';
```

**Почему:**
- Публичный API пакета
- Инкапсуляция
- Не ломается при изменении внутренней структуры пакета

---

### 2. Внутрипакетные импорты (в пределах одного пакета)

#### 2.1. Короткие импорты (≤2 уровня вложенности)

**Правило:** ✅ **Используй относительные пути**

```typescript
// ✅ ПРАВИЛЬНО - показывает локальную структуру
import { validateInput } from './utils.js';              // 1 уровень
import { BaseOperation } from '../base-operation.js';    // 2 уровня
import { IssueEntity } from '../entities/issue.js';      // 2 уровня
```

**Почему:**
- Стандартная практика в TypeScript/JavaScript
- Показывает логическую связь между файлами
- Проще для review
- Не требует настройки алиасов

#### 2.2. Глубокие импорты (≥3 уровня вложенности)

**Правило:** ✅ **Используй path aliases**

```typescript
// ✅ ПРАВИЛЬНО
import { MCP_TOOL_PREFIX } from '@constants';
import { FieldsSchema } from '@common/schemas/index.js';
import { createQueueFixture } from '@helpers/queue.fixture.js';

// ❌ НЕПРАВИЛЬНО
import { MCP_TOOL_PREFIX } from '../../../constants.js';              // 4 уровня
import { FieldsSchema } from '../../../common/schemas/index.js';      // 4 уровня
import { createQueueFixture } from '../../../helpers/queue.fixture.js'; // 4 уровня
```

**Почему:**
- Хрупкий код при рефакторинге
- Сложно отследить глубину вложенности
- Легко ошибиться при переносе файлов
- Плохая читаемость

---

### 3. Импорты корневых файлов monorepo

**Правило:** ✅ **Используй относительные пути (нет альтернативы)**

```typescript
// ✅ ПРАВИЛЬНО (единственный способ)
// В packages/*/vitest.config.ts:
import { sharedConfig } from '../../../vitest.shared';
```

**Почему:**
- Корневые файлы (vitest.shared.ts) не входят ни в один workspace пакет
- У них нет своего tsconfig.json с path aliases
- Технически нельзя использовать алиасы

---

## 🗂️ Path aliases по пакетам

### yandex-tracker (packages/servers/yandex-tracker)

#### Production код (tsconfig.json):
```json
{
  "compilerOptions": {
    "paths": {
      "@tracker_api/*": ["./src/tracker_api/*"],
      "@tools/*": ["./src/tools/*"],
      "@composition-root/*": ["./src/composition-root/*"],
      "@cli/*": ["./src/cli/*"],
      "@constants": ["./src/constants.ts"],
      "@common/*": ["./src/common/*"]
    }
  }
}
```

#### Тесты (tsconfig.tests.json):
```json
{
  "compilerOptions": {
    "paths": {
      // Все из production +
      "@integration/*": ["./tests/integration/*"],
      "@helpers/*": ["./tests/helpers/*"],
      "@test-utils/*": ["./tests/utils/*"]
    }
  }
}
```

### Framework пакеты (infrastructure, core, search)

**Текущее состояние:** Без внутренних path aliases (не требуется, нет глубоких импортов)

**Если понадобится в будущем:** Добавить алиасы аналогично yandex-tracker

---

## 📊 Статистика проекта (на момент анализа)

| Категория | Количество файлов | Действие |
|-----------|------------------|----------|
| **Production код с глубокими импортами** | 94 файла | 🔄 Заменить на алиасы |
| **Тесты с глубокими импортами** | 45 файлов | 🔄 Заменить на алиасы |
| **Междупакетные относительные импорты** | 9 файлов | 🔄 Заменить на npm names |
| **Импорты корневых конфигов** | 4 файла | ✅ Оставить как есть |
| **Короткие импорты (≤2 уровня)** | ~300+ файлов | ✅ Оставить как есть |

**Итого к замене:** ~148 файлов

---

## 🛠️ Реализация

### Автоматизация

**Скрипт:** `scripts/fix-relative-imports.ts`

**Использование:**
```bash
# Показать изменения без применения
npm run fix-imports -- --dry-run

# Применить изменения для конкретного пакета
npm run fix-imports -- --package=yandex-tracker --apply

# Verbose режим
npm run fix-imports -- --apply --verbose
```

### Проверка соблюдения (CI)

**Скрипт:** `scripts/check-deep-imports.ts`

**Использование:**
```bash
# Проверить, что нет глубоких импортов в коде
npm run check-deep-imports

# Интеграция в CI
npm run validate  # включает check-deep-imports
```

---

## 📝 Чек-лист для разработчиков

При написании нового кода:

- [ ] Импорт из другого пакета? → Используй npm package name (`@mcp-framework/core`)
- [ ] Импорт в том же пакете, ≤2 уровня? → Используй относительный путь (`./utils.js`, `../types.js`)
- [ ] Импорт в том же пакете, ≥3 уровня? → Используй path alias (`@helpers/*`, `@constants`)
- [ ] Импорт корневого файла monorepo? → Используй относительный путь (нет выбора)

---

## 🔗 Связанные документы

- **[CLAUDE.md](../../CLAUDE.md)** — Основные правила monorepo
- **[ARCHITECTURE.md](../../ARCHITECTURE.md)** — Архитектура проекта
- **План реализации:** [1.1_analyze_and_prepare_sequential.md](./1.1_analyze_and_prepare_sequential.md)

---

## 🚦 Статус согласования

- [ ] Согласовано с командой/maintainer
- [ ] Скрипт реализован и протестирован
- [ ] Все импорты приведены в соответствие
- [ ] Добавлена CI проверка
- [ ] Документация обновлена

**Следующий шаг:** Согласовать с пользователем и реализовать скрипт
