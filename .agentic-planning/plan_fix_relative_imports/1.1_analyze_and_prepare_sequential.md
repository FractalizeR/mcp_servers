# План: Исправление относительных импортов на алиасы

**Цель:** Заменить глубокие относительные импорты (`../../../+`) на path aliases во всех пакетах monorepo

**Масштаб проблемы:**
- **yandex-tracker:** 45 файлов с относительными импортами ≥3 уровней
- **framework пакеты:** 9 файлов
- **Максимальная глубина:** 8 уровней (`../../../../../../helpers/`)

---

## Этап 1.1: Анализ и подготовка (sequential)

**Цель:** Понять паттерны импортов и подготовить конфигурацию

### ✅ Подэтапы:

- [x] Проанализировать паттерны относительных импортов во всех пакетах
- [x] Определить необходимые path aliases для каждого пакета
- [ ] Создать таблицу маппинга: `паттерн относительного пути → алиас`
- [ ] Определить порядок применения изменений (чтобы не сломать билд)

### 📊 Найденные паттерны:

#### yandex-tracker (tests):
```
../../../../../../helpers/*.fixture.js   → @helpers/*.fixture.js   (8 уровней, 4 файла)
../../../../../helpers/*.fixture.js      → @helpers/*.fixture.js   (6 уровней, 2 файла)
../../../../helpers/*.fixture.js         → @helpers/*.fixture.js   (5 уровней, 6 файлов)
../../../helpers/*.fixture.js            → @helpers/*.fixture.js   (4 уровня, 33 файла)
../helpers/*.ts                          → ./helpers/* или @helpers/* (2 уровня, оставить как есть)
```

#### framework пакеты (tests):
```
../../../core/src/tools/base/tool-metadata.js → @mcp-framework/core   (междупакетный)
../../../vitest.shared                         → ../../../vitest.shared (корень monorepo, оставить)
```

### 📋 Необходимые алиасы по пакетам:

#### 1. yandex-tracker (`tsconfig.tests.json`):
```json
{
  "compilerOptions": {
    "paths": {
      // Существующие (оставить):
      "@tracker_api/*": ["./src/tracker_api/*"],
      "@tools/*": ["./src/tools/*"],
      "@composition-root/*": ["./src/composition-root/*"],
      "@cli/*": ["./src/cli/*"],
      "@constants": ["./src/constants.ts"],
      "@integration/*": ["./tests/integration/*"],

      // ДОБАВИТЬ:
      "@helpers/*": ["./tests/helpers/*"],
      "@test-utils/*": ["./tests/utils/*"]  // если есть
    }
  }
}
```

#### 2. yandex-tracker (`vitest.config.ts`):
```typescript
// Добавить в resolve.alias:
'@helpers': path.resolve(__dirname, './tests/helpers'),
'@helpers/*': path.resolve(__dirname, './tests/helpers/*'),
```

#### 3. framework packages (если нужно создать `tsconfig.test.json`):
- **infrastructure**, **core**, **search** — не требуется, т.к. у них нет глубоких импортов внутри

---

## Этап 1.2: Создание скрипта замены (sequential)

**Цель:** Написать Node.js скрипт для автоматической замены импортов

### ✅ Подэтапы:

- [ ] Создать скрипт `scripts/fix-relative-imports.ts`
- [ ] Реализовать функции:
  - [ ] `findFilesWithDeepImports(directory, minDepth)` — поиск файлов
  - [ ] `replaceImports(filePath, replacements)` — замена в файле
  - [ ] `generateReplacementMap(patterns)` — маппинг паттернов
- [ ] Добавить dry-run режим (показать изменения без применения)
- [ ] Добавить verbose режим (показать каждую замену)
- [ ] Добавить валидацию: проверить, что файл по новому алиасу существует

### 🎯 Требования к скрипту:

1. **Безопасность:**
   - Dry-run по умолчанию
   - Создавать backup перед изменением (опционально)
   - Валидировать синтаксис после замены

2. **Гибкость:**
   - Параметры: `--package <name>`, `--apply`, `--verbose`
   - Поддержка списка исключений (skip patterns)

3. **Отчётность:**
   - Сколько файлов изменено
   - Сколько импортов заменено
   - Список изменённых файлов

### 📝 Структура замены:

```typescript
// Паттерн: найти импорты с определённой глубиной
const PATTERNS = [
  {
    // 7-8 уровней до helpers
    regex: /from ['"](?:\.\.\/){6,7}helpers\/([^'"]+)['"]/g,
    replacement: 'from \'@helpers/$1\'',
    depth: '6-7',
  },
  {
    // 5 уровней до helpers
    regex: /from ['"](?:\.\.\/){4}helpers\/([^'"]+)['"]/g,
    replacement: 'from \'@helpers/$1\'',
    depth: '4',
  },
  {
    // 4 уровня до helpers
    regex: /from ['"](?:\.\.\/){3}helpers\/([^'"]+)['"]/g,
    replacement: 'from \'@helpers/$1\'',
    depth: '3',
  },
  // ... и т.д.
];
```

---

## Этап 1.3: Обновление конфигурации (sequential)

**Цель:** Добавить недостающие path aliases в конфигурацию TypeScript

### ✅ Подэтапы:

- [ ] Обновить `packages/servers/yandex-tracker/tsconfig.tests.json`
  - [ ] Добавить `@helpers/*` path alias
- [ ] Обновить `packages/servers/yandex-tracker/vitest.config.ts`
  - [ ] Добавить `@helpers` и `@helpers/*` в `resolve.alias`
- [ ] Запустить `npm run typecheck --workspace=@mcp-server/yandex-tracker`
- [ ] Убедиться, что конфигурация корректна (не сломана)

### ⚠️ Важно:

- НЕ применять замены импортов на этом этапе!
- Только добавить алиасы в конфигурацию
- После этого этапа и старые (относительные), и новые (алиасы) пути должны работать

---

## Этап 2.1: Применение замены в yandex-tracker (parallel-ready)

**Цель:** Заменить относительные импорты на алиасы в yandex-tracker

### ✅ Подэтапы:

- [ ] Запустить скрипт в dry-run режиме:
  ```bash
  npm run fix-imports -- --package=yandex-tracker --dry-run --verbose
  ```
- [ ] Проверить предложенные изменения
- [ ] Применить изменения:
  ```bash
  npm run fix-imports -- --package=yandex-tracker --apply
  ```
- [ ] Запустить валидацию:
  ```bash
  npm run validate:quiet --workspace=@mcp-server/yandex-tracker
  ```
- [ ] Исправить ошибки (если есть)
- [ ] Коммит: `refactor(yandex-tracker): заменить глубокие относительные импорты на @helpers alias`

### 📊 Ожидаемый результат:

- 45 файлов изменено
- ~60-80 импортов заменено
- Все тесты проходят
- Typecheck успешен

---

## Этап 2.2: Применение замены в framework пакетах (parallel-ready)

**Цель:** Заменить междупакетные относительные импорты на npm package names

### ✅ Подэтапы:

- [ ] Найти междупакетные относительные импорты:
  ```bash
  find packages/framework -name "*.ts" -exec grep -h "from ['\"]\.\..*src/" {} \;
  ```
- [ ] Заменить вручную или скриптом:
  ```
  ../../../core/src/tools/base/tool-metadata.js → @mcp-framework/core
  ```
- [ ] Запустить валидацию:
  ```bash
  npm run validate:quiet --workspace=@mcp-framework/search
  ```
- [ ] Коммит: `refactor(framework): заменить относительные междупакетные импорты на npm names`

---

## Этап 3: Финальная проверка (sequential)

**Цель:** Убедиться, что всё работает корректно во всём monorepo

### ✅ Подэтапы:

- [ ] Запустить полную валидацию:
  ```bash
  npm run validate:quiet
  ```
- [ ] Запустить билд всех пакетов:
  ```bash
  npm run build
  ```
- [ ] Проверить, что не осталось глубоких импортов:
  ```bash
  npm run check-deep-imports  # скрипт для проверки
  ```
- [ ] Обновить документацию (если нужно):
  - [ ] Добавить упоминание `@helpers` в `tests/README.md`
- [ ] Финальный коммит: `docs: обновить документацию по path aliases`

---

## Этап 4: Документирование и очистка (sequential)

**Цель:** Задокументировать изменения и добавить скрипт в npm scripts

### ✅ Подэтапы:

- [ ] Добавить скрипт в корневой `package.json`:
  ```json
  {
    "scripts": {
      "fix-imports": "tsx scripts/fix-relative-imports.ts",
      "check-deep-imports": "tsx scripts/check-deep-imports.ts"
    }
  }
  ```
- [ ] Создать `scripts/check-deep-imports.ts` — CI проверка на будущее
- [ ] Обновить `CLAUDE.md` (если нужно) — добавить правило про `@helpers`
- [ ] Создать changelog запись

---

## 📋 Чек-лист готовности к следующей сессии

- [ ] План согласован с пользователем
- [ ] Таблица маппинга создана
- [ ] Скрипт написан и протестирован в dry-run
- [ ] Конфигурация обновлена
- [ ] Готов к применению изменений

---

## 🎯 Критерии успеха

1. ✅ Все относительные импорты ≥3 уровней заменены на алиасы
2. ✅ `npm run validate` проходит успешно
3. ✅ `npm run build` собирает все пакеты
4. ✅ Нет деградации в тестовом покрытии
5. ✅ Документация обновлена

---

## 📌 Примечания

- **Относительные пути ≤2 уровня** (`../`, `./`) — НЕ заменять, это нормальная практика
- **Междупакетные импорты** в framework пакетах — заменить на npm package names
- **Скрипт** должен быть reusable для будущих изменений
