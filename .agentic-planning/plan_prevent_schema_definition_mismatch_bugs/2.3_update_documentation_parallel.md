# Этап 2.3: Обновление документации (Parallel)

**Тип:** Parallel (можно выполнять отдельным агентом)
**Время:** ~0.5-1 час
**Ветка:** `feature/adapt-docs` (создать от основной ветки после рефакторинга)
**Зависимости:** Требует завершения Этапа 1 (рефакторинг)

---

## 🎯 Цель

Обновить всю документацию проекта для отражения нового подхода автогенерации definition из schema.

---

## 📋 Чек-лист выполнения

### Обновление основной документации

- [ ] Обновить `CLAUDE.md`
  - Добавить секцию про автогенерацию definition
  - Обновить чек-лист создания инструмента
  - Добавить примеры (кратко, ≤5 строк)

- [ ] Обновить `ARCHITECTURE.md`
  - Добавить описание генератора schema → definition
  - Диаграмму потока данных (как schema превращается в definition)
  - Объяснение "почему" (DRY принцип, исключение багов)

- [ ] Обновить `README.md` (корневой)
  - Упомянуть автогенерацию definition в разделе Features
  - Добавить краткий пример

### Обновление документации yandex-tracker пакета

- [ ] Обновить `packages/servers/yandex-tracker/README.md`
  - Секция про создание инструментов
  - Примеры использования автогенерации
  - Преимущества нового подхода

- [ ] Обновить `packages/servers/yandex-tracker/CLAUDE.md`
  - Правила создания инструментов
  - Обновить чек-листы
  - Удалить упоминания `*.definition.ts` файлов

### Обновление модульной документации

- [ ] Обновить `packages/servers/yandex-tracker/src/tools/README.md`
  - Секция "Создание нового инструмента"
  - Обновленный чек-лист
  - Примеры кода (краткие!)
  - Раздел "Schema как источник истины"

- [ ] Обновить `packages/framework/core/README.md`
  - Добавить документацию генератора
  - API generateDefinitionFromSchema()
  - Примеры использования

### Создание migration guide

- [ ] Создать `MIGRATION_SCHEMA_DEFINITION.md` (опционально)
  - Как мигрировать существующие инструменты
  - Что изменилось?
  - Пошаговая инструкция
  - Примеры до/после

### Обновление комментариев в коде

- [ ] Проверить комментарии в BaseTool
  - Обновить JSDoc для методов
  - Добавить примеры автогенерации

- [ ] Обновить комментарии в шаблонах
  - Указать, что definition генерируется автоматически
  - Примеры использования

---

## 📝 Примеры обновлений документации

### CLAUDE.md (добавить секцию):

```markdown
## 🔄 Автогенерация MCP Definition

**Принцип:** Schema = единственный источник истины

**Старый подход (НЕ используй!):**
- Создавать отдельный `*.definition.ts` файл
- Вручную синхронизировать с `*.schema.ts`
- Риск schema-definition mismatch

**Новый подход:**
- Только `*.schema.ts` с Zod schema
- Definition генерируется автоматически
- Физически невозможен mismatch

**Пример:**
```typescript
getDefinition(): ToolDefinition {
  return generateDefinitionFromSchema(this.metadata, MyToolSchema);
}
```

### packages/servers/yandex-tracker/src/tools/README.md (обновить):

**До:**
```markdown
## Создание нового инструмента

1. Создать `*.schema.ts` — Zod schema
2. Создать `*.definition.ts` — MCP definition  ← удалить!
3. Создать `*.tool.ts` — класс инструмента
```

**После:**
```markdown
## Создание нового инструмента

### Принцип: Schema → Definition (автогенерация)

**Зод schema = единственный источник истины!**
MCP definition генерируется автоматически, отдельный `*.definition.ts` НЕ нужен.

### Шаги:

1. **Создать `*.schema.ts`** — Zod schema
   - Добавить `.describe()` для всех полей
   - Это источник истины для definition

2. **Создать `*.tool.ts`** — класс инструмента
   - Использовать `generateDefinitionFromSchema()`
   - Definition генерируется из schema

3. **Создать `*.metadata.ts`** — метаданные
4. **Создать `*.operation.ts`** — API операция
5. **Добавить тесты** — включая проверку генерации

### Пример:
```typescript
export class MyTool extends BaseTool<typeof MyToolSchema> {
  getDefinition(): ToolDefinition {
    return generateDefinitionFromSchema(this.metadata, MyToolSchema);
  }
}
```
```

### ARCHITECTURE.md (добавить секцию):

```markdown
## Schema-to-Definition Generator

**Проблема:**
- Ручное создание MCP definition → риск mismatch с schema
- Дублирование кода (DRY нарушение)
- Тесты не всегда ловят несоответствие

**Решение:**
- Автоматическая генерация definition из Zod schema
- Schema = единственный источник истины
- Физически невозможен mismatch

**Архитектура:**
```
Zod Schema (*.schema.ts)
    ↓
generateDefinitionFromSchema()
    ↓
MCP Definition (runtime)
```

**Преимущества:**
- ✅ DRY принцип
- ✅ Исключение багов schema-definition mismatch
- ✅ Упрощение создания инструментов
- ✅ Автоматическая синхронизация
```

---

## ✅ Критерии завершения

- [ ] Все основные документы обновлены
  - `CLAUDE.md`
  - `ARCHITECTURE.md`
  - `README.md` (корневой)

- [ ] Документация yandex-tracker обновлена
  - `packages/servers/yandex-tracker/README.md`
  - `packages/servers/yandex-tracker/CLAUDE.md`

- [ ] Модульная документация обновлена
  - `packages/servers/yandex-tracker/src/tools/README.md`
  - `packages/framework/core/README.md`

- [ ] Проверка лимитов размера
  - `npm run validate:docs` успешна
  - Все файлы в пределах лимитов

- [ ] Комментарии в коде обновлены (опционально)

- [ ] Коммит с описанием изменений

- [ ] Пуш в ветку `feature/adapt-docs`

---

## 📝 Файлы для изменения

### Основные документы:
- `CLAUDE.md`
- `ARCHITECTURE.md`
- `README.md`

### Документация yandex-tracker:
- `packages/servers/yandex-tracker/README.md`
- `packages/servers/yandex-tracker/CLAUDE.md`

### Модульная документация:
- `packages/servers/yandex-tracker/src/tools/README.md`
- `packages/framework/core/README.md`

### Опционально:
- `MIGRATION_SCHEMA_DEFINITION.md` (новый файл)

---

## ⚠️ Важно

1. **Не блокируй другие этапы:**
   - Этот этап независим от 2.1 и 2.2
   - Можешь выполнять параллельно

2. **Соблюдай лимиты размера:**
   - `CLAUDE.md` ≤400 строк
   - `ARCHITECTURE.md` ≤700 строк
   - Module `README.md` ≤500 строк
   - Проверка: `npm run validate:docs`

3. **Правила сокращения (если превышен лимит):**
   - Удалить примеры кода >10 строк (заменить ссылкой)
   - Удалить дублирование информации
   - Сократить объяснения "почему" (оставить в ARCHITECTURE.md)

4. **Краткость:**
   - Примеры ≤5 строк для ясности
   - Чек-листы вместо пошаговых инструкций
   - Ссылки вместо повторений

---

## 🔗 Связанные этапы

- **Зависит от:** Этап 1 (рефакторинг)
- **Параллельно с:** Этапы 2.1, 2.2
- **Предшествует:** Этап 3 (интеграция)

---

## 📋 Чек-лист валидации документации

После обновления ОБЯЗАТЕЛЬНО проверить:

```bash
# Проверка лимитов размера
npm run validate:docs

# Если превышен лимит - применить правила сокращения
# Повторить проверку
```

**Не коммитить, пока `validate:docs` не успешна!**
