# Этап 3.1: Архитектурный рефакторинг (Optional, Sequential)

**Приоритет:** Низкий (опционально)
**Тип выполнения:** Sequential (после завершения этапа 2)
**Время:** 4-8 часов
**Зависимости:** Требует завершения этапа 2.1

---

## ⚠️ ВАЖНО: Опциональный этап

Этот этап является **опциональным** и должен выполняться **только после согласования с пользователем**.

**Преимущества рефакторинга:**
- ✅ Полное исключение возможности schema-definition mismatch (DRY)
- ✅ Упрощение создания новых инструментов
- ✅ Автоматическая синхронизация schema ↔ definition

**Недостатки рефакторинга:**
- ❌ Требует изменения архитектуры всех существующих инструментов
- ❌ Может нарушить обратную совместимость
- ❌ Требует много времени и тестирования
- ❌ Увеличивает сложность понимания для новых разработчиков

**Рекомендация:** Обсудить с пользователем перед началом.

---

## 🎯 Цель

Рефакторинг архитектуры для автоматической генерации MCP definition из Zod schema (или наоборот), что полностью исключит возможность несоответствия.

---

## 📋 Возможные подходы к рефакторингу

### Вариант A: Генерация Definition из Schema (рекомендуется)

**Идея:** Zod schema является source of truth, definition генерируется автоматически.

**Архитектура:**
```typescript
// schema.ts (source of truth)
export const GetIssuesParamsSchema = z.object({
  issueKeys: IssueKeysSchema,
  fields: FieldsSchema,
});

// tool.ts (использует schema)
export class GetIssuesTool extends BaseTool {
  // Definition генерируется автоматически из schema
  protected buildDefinition(): ToolDefinition {
    return generateDefinitionFromSchema(
      GetIssuesParamsSchema,
      this.METADATA,
      {
        // Дополнительные параметры для генерации
        fieldDescriptions: {
          issueKeys: 'Массив ключей задач',
          fields: 'Поля для возврата',
        },
      }
    );
  }
}
```

**Преимущества:**
- Schema уже содержит всю информацию о типах и required/optional
- Schema используется для runtime валидации
- Один источник истины

**Недостатки:**
- Нужно добавлять метаинформацию для description, examples и т.д.
- Может быть сложно для сложных типов

### Вариант B: Генерация Schema из Definition

**Идея:** Definition является source of truth, schema генерируется автоматически.

**Недостатки:**
- Definition не содержит runtime валидации
- Сложнее извлечь типы для TypeScript
- Менее предсказуемо

**Не рекомендуется.**

### Вариант C: Общий источник (DSL)

**Идея:** Создать DSL (Domain Specific Language) для описания инструмента, из которого генерируются и schema, и definition.

**Недостатки:**
- Слишком сложно
- Требует создания собственного DSL
- Избыточно для наших нужд

**Не рекомендуется.**

---

## ✅ Чек-лист задач (Вариант A)

### 1. Проектирование генератора

- [ ] Изучить структуру Zod schema:
  - Как извлечь типы полей
  - Как определить required/optional
  - Как получить вложенные объекты
- [ ] Определить, какая информация нужна для MCP definition:
  - name (из metadata)
  - description (отдельная конфигурация)
  - inputSchema.properties (из schema)
  - inputSchema.required (из schema)
  - examples (отдельная конфигурация)
- [ ] Спроектировать API генератора:
  ```typescript
  function generateDefinitionFromSchema(
    schema: z.ZodObject<any>,
    metadata: StaticToolMetadata,
    config: DefinitionGeneratorConfig
  ): ToolDefinition;
  ```

### 2. Реализация генератора

- [ ] Создать файл `packages/framework/core/src/tools/definition-generator.ts`
- [ ] Реализовать функцию `generateDefinitionFromSchema()`:
  - Извлечение полей из `schema.shape`
  - Определение required полей
  - Генерация JSON Schema для каждого поля
  - Применение descriptions и examples из config
- [ ] Реализовать поддержку различных Zod типов:
  - `z.string()`, `z.number()`, `z.boolean()`
  - `z.array()`
  - `z.object()` (вложенные)
  - `z.enum()`
  - `z.optional()`, `z.nullable()`
- [ ] Создать unit тесты для генератора

### 3. Обновление BaseTool

- [ ] Обновить `BaseTool` класс:
  - Добавить метод `protected getParamsSchema(): z.ZodObject<any>`
  - Обновить `buildDefinition()` для использования генератора
- [ ] Обратная совместимость:
  - Сохранить возможность ручного определения definition
  - Добавить флаг для выбора режима

### 4. Миграция существующих инструментов

- [ ] Создать скрипт миграции `scripts/migrate-tools-to-generated-definition.ts`
- [ ] Для каждого инструмента:
  - Удалить файл `*.definition.ts`
  - Добавить config для descriptions и examples
  - Обновить tool класс для использования генератора
- [ ] Мигрировать инструменты поэтапно:
  - Начать с простых (1-2 параметра)
  - Затем средние (3-5 параметров)
  - Затем сложные (>5 параметров, вложенные объекты)

### 5. Обновление тестов

- [ ] Обновить все существующие тесты инструментов
- [ ] Удалить явные тесты definition (они теперь генерируются)
- [ ] Добавить тесты генератора для всех типов Zod schema

### 6. Обновление документации

- [ ] Обновить ARCHITECTURE.md:
  - Описание нового подхода
  - Преимущества автоматической генерации
- [ ] Обновить src/tools/README.md:
  - Инструкции по созданию инструментов с генератором
  - Примеры config для descriptions
- [ ] Обновить шаблоны (из этапа 1.3):
  - Использовать новый подход с генератором

### 7. Финальная валидация

- [ ] Запустить `npm run validate` — всё должно работать
- [ ] Убедиться, что все инструменты работают корректно
- [ ] Провести регрессионное тестирование

---

## ⚠️ Риски и миграционные проблемы

### Возможные проблемы

1. **Потеря кастомных описаний:**
   - В definition могут быть детальные descriptions
   - Нужно перенести их в config

2. **Сложные типы:**
   - Union types могут быть сложны для генерации
   - Требуется fallback на ручной definition

3. **Обратная совместимость:**
   - Старые инструменты могут сломаться
   - Требуется тщательное тестирование

### Минимизация рисков

- [ ] Реализовать постепенную миграцию (не все инструменты сразу)
- [ ] Сохранить возможность ручного definition для edge cases
- [ ] Создать подробную документацию миграции
- [ ] Провести thorough регрессионное тестирование

---

## ⚠️ Критерии завершения

- [ ] Генератор реализован и протестирован
- [ ] BaseTool обновлен с поддержкой генерации
- [ ] Все инструменты успешно мигрированы
- [ ] Все тесты проходят
- [ ] `npm run validate` — без ошибок
- [ ] Документация обновлена
- [ ] Регрессионное тестирование пройдено
- [ ] Создан коммит и пуш
- [ ] Согласовано с пользователем

---

## 🔗 Решение о выполнении

**Перед началом этого этапа обязательно согласовать с пользователем:**

Вопросы для обсуждения:
1. Стоит ли делать архитектурный рефакторинг?
2. Какой подход выбрать (вариант A, B или C)?
3. Мигрировать все инструменты или только новые?
4. Каковы приоритеты (стабильность vs DRY)?

**Если пользователь не одобрил этот этап — план считается завершенным после этапа 2.1.**
