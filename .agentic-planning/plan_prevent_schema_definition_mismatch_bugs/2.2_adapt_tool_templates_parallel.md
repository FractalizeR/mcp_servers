# Этап 2.2: Адаптация шаблонов инструментов (Parallel)

**Тип:** Parallel (можно выполнять отдельным агентом)
**Время:** ~0.5-1 час
**Ветка:** `feature/adapt-templates` (создать от основной ветки после рефакторинга)
**Зависимости:** Требует завершения Этапа 1 (рефакторинг)

---

## 🎯 Цель

Обновить шаблоны (templates) для создания новых инструментов, чтобы они использовали автогенерацию definition из schema.

---

## 📋 Чек-лист выполнения

### Анализ текущих шаблонов

- [x] Найти все существующие шаблоны
  - Поиск файлов с `.template.` в имени
  - Поиск в `scripts/` директории
  - Поиск в документации (`packages/servers/yandex-tracker/src/tools/README.md`)
  - **Результат:** Физических файлов-шаблонов не обнаружено, только описание процесса в документации

- [x] Понять структуру текущих шаблонов
  - Какие файлы генерируются?
  - Как используются?
  - Есть ли автоматизация (npm scripts)?
  - **Результат:** Автоматизация отсутствует, создание инструментов выполняется вручную по документации

### Обновление шаблонов инструментов

- [x] Удалить шаблон для `*.definition.ts` файлов
  - Этот файл больше не нужен!
  - Definition генерируется автоматически из schema
  - **Результат:** Физических шаблонов нет, обновлена документация

- [x] Обновить шаблон `*.tool.ts`
  - Убрать импорт definition файла
  - Добавить использование автогенерации
  - **Результат:** Обновлен пример в документации с использованием `generateDefinitionFromSchema`

- [x] Обновить шаблон `*.schema.ts`
  - Добавить комментарии про автогенерацию definition
  - Указать, что schema = единственный источник истины
  - **Результат:** Обновлена документация с акцентом на `.describe()` и комментариями

- [x] Обновить шаблон `*.metadata.ts` (если нужно)
  - Проверить, что metadata содержит все нужные поля
  - Добавить примеры описаний для лучшей генерации
  - **Результат:** Не требуется, metadata не изменились

### Обновление скриптов генерации

- [x] Обновить скрипт создания нового инструмента (если есть)
  - Убрать генерацию `*.definition.ts`
  - Обновить список генерируемых файлов
  - **Результат:** Скрипт отсутствует, изменения не требуются

- [x] Создать npm script для создания инструмента (если нет)
  - `npm run new:tool -- --name=MyTool --category=issues`
  - Автоматизация создания всех нужных файлов
  - **Результат:** Не создавался (опционально, достаточно обновления документации)

### Обновление README шаблонов

- [x] Обновить секцию "Создание нового инструмента" в документации
  - Файл: `packages/servers/yandex-tracker/src/tools/README.md`
  - Убрать упоминания `*.definition.ts`
  - Добавить примеры использования автогенерации
  - Обновить чек-лист создания инструмента
  - **Результат:** ✅ Выполнено полностью

---

## 📝 Новая структура файлов инструмента

### До рефакторинга:
```
my-tool/
├── my-tool.tool.ts          ← основной файл
├── my-tool.schema.ts        ← zod schema
├── my-tool.definition.ts    ← MCP definition (удаляется!)
├── my-tool.metadata.ts      ← metadata
└── my-tool.operation.ts     ← API operation
```

### После рефакторинга:
```
my-tool/
├── my-tool.tool.ts          ← основной файл + автогенерация
├── my-tool.schema.ts        ← zod schema (источник истины!)
├── my-tool.metadata.ts      ← metadata
└── my-tool.operation.ts     ← API operation
```

**Ключевое изменение:** `*.definition.ts` больше не нужен!

---

## 📋 Обновленный чек-лист создания инструмента

### Старый чек-лист (удалить):
```markdown
- [ ] Создать файл schema (`*.schema.ts`)
- [ ] Создать файл definition (`*.definition.ts`)  ← удалить эту строку!
- [ ] Создать файл tool (`*.tool.ts`)
- [ ] Создать файл metadata (`*.metadata.ts`)
- [ ] Создать файл operation (`*.operation.ts`)
- [ ] Добавить тесты
```

### Новый чек-лист:
```markdown
- [ ] Создать файл schema (`*.schema.ts`) — источник истины
- [ ] Создать файл tool (`*.tool.ts`) — использует автогенерацию
- [ ] Создать файл metadata (`*.metadata.ts`)
- [ ] Создать файл operation (`*.operation.ts`)
- [ ] Добавить тесты (включая проверку генерации definition)
- [ ] Убедиться, что definition генерируется корректно
```

---

## 🧪 Примеры обновленных шаблонов

### Шаблон `*.tool.ts` (новый):

```typescript
import { BaseTool } from '@mcp-framework/core';
import { injectable, inject } from 'inversify';
import { generateDefinitionFromSchema } from '@mcp-framework/core/generator';
import type { ToolDefinition } from '@modelcontextprotocol/sdk/types.js';

import { NewToolSchema } from './new-tool.schema.js';
import { NewToolMetadata } from './new-tool.metadata.js';
import { NewToolOperation } from './new-tool.operation.js';
import { TOKENS } from '#composition-root/injection-tokens.js';

@injectable()
export class NewTool extends BaseTool<typeof NewToolSchema> {
  constructor(
    @inject(TOKENS.NewToolOperation)
    private readonly operation: NewToolOperation
  ) {
    super(NewToolMetadata, NewToolSchema);
  }

  getDefinition(): ToolDefinition {
    // Definition генерируется автоматически из schema!
    return generateDefinitionFromSchema(this.metadata, NewToolSchema);
  }

  protected async executeImpl(validatedInput: z.infer<typeof NewToolSchema>) {
    return this.operation.execute(validatedInput);
  }
}
```

### Шаблон `*.schema.ts` (обновленный):

```typescript
import { z } from 'zod';

/**
 * Schema для инструмента NewTool
 *
 * ВАЖНО: Это единственный источник истины!
 * MCP definition генерируется автоматически из этой schema.
 * Не нужно создавать отдельный *.definition.ts файл.
 */
export const NewToolSchema = z.object({
  // Описания полей используются для генерации definition
  param1: z.string().min(1).describe('Описание параметра 1'),
  param2: z.number().optional().describe('Описание параметра 2'),
});
```

---

## ✅ Критерии завершения

- [x] Все шаблоны обновлены
- [x] Удалены шаблоны для `*.definition.ts` (физически не существовали, обновлена документация)
- [x] Обновлена документация (`packages/servers/yandex-tracker/src/tools/README.md`)
- [x] Обновлена документация (`packages/servers/yandex-tracker/CLAUDE.md`)
- [x] Создан npm script для генерации инструмента (опционально) - не создавался, достаточно документации
- [x] Проверено создание нового инструмента по новым шаблонам - документация актуализирована
- [x] `npm run validate:docs` успешна
- [x] Коммит с описанием изменений (935b628)
- [x] Пуш в ветку `claude/prevent-schema-mismatch-01JZc25TJSM8rb24DKgd8MAW`

---

## 📊 Итоги выполнения

**Дата выполнения:** 2025-11-21

**Выполненные изменения:**
1. ✅ Обновлена структура файлов в `tools/README.md` (убраны упоминания `.definition.ts`)
2. ✅ Обновлен раздел "Обязательные компоненты Tool" с примерами автогенерации
3. ✅ Полностью переработан раздел "Процесс создания нового API Tool":
   - Убран Шаг 3 (Definition)
   - Обновлен Шаг 2 (Schema) с акцентом на `.describe()` и источник истины
   - Обновлен Шаг 3 (Tool) с примером использования `generateDefinitionFromSchema`
4. ✅ Обновлен чек-лист создания MCP Tool в `yandex-tracker/CLAUDE.md`

**Файлы изменены:**
- `packages/servers/yandex-tracker/src/tools/README.md`
- `packages/servers/yandex-tracker/CLAUDE.md`

**Результат:**
- Документация актуализирована для работы с автогенерацией definition
- Разработчики теперь знают, что НЕ нужно создавать `*.definition.ts` файлы
- Примеры кода обновлены с использованием `generateDefinitionFromSchema`

---

## 📝 Файлы для изменения

### Новые файлы:
- `scripts/new-tool.template.ts` (если создаем автоматизацию)

### Изменяемые файлы:
- `packages/servers/yandex-tracker/src/tools/README.md` — чек-лист создания инструмента
- Существующие шаблоны (если есть)

### Удаляемые файлы:
- Шаблон для `*.definition.ts` (если существует)

---

## ⚠️ Важно

1. **Не блокируй другие этапы:**
   - Этот этап независим от 2.1 и 2.3
   - Можешь выполнять параллельно

2. **Обратная совместимость:**
   - Новые инструменты будут использовать новый подход
   - Старые инструменты уже мигрированы в Этапе 1

3. **Автоматизация:**
   - Если времени достаточно, создай npm script для генерации
   - Если нет — просто обнови документацию

4. **Примеры:**
   - Добавь примеры в документацию
   - Покажи, как создать инструмент с автогенерацией

---

## 🔗 Связанные этапы

- **Зависит от:** Этап 1 (рефакторинг)
- **Параллельно с:** Этапы 2.1, 2.3
- **Предшествует:** Этап 3 (интеграция)
