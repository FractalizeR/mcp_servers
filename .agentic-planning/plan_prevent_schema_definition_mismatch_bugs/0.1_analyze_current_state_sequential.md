# Этап 0.1: Анализ текущего состояния (Sequential)

**Приоритет:** Высокий
**Тип выполнения:** Sequential (должен быть выполнен перед этапом 1)
**Время:** ~30 минут

---

## 🎯 Цель

Проанализировать текущую архитектуру инструментов и определить требования к генератору автоматической конверсии schema → definition для refactoring-first подхода.

---

## ✅ Чек-лист задач

### 1. Анализ архитектуры инструментов

- [ ] Изучить структуру всех инструментов в `packages/servers/yandex-tracker/src/tools/api/`
- [ ] Определить типичную структуру файлов для одного инструмента:
  - `*.tool.ts` — основной класс инструмента
  - `*.definition.ts` — MCP definition
  - `*.schema.ts` — Zod схема валидации
  - `*.metadata.ts` — метаданные
- [ ] Проверить, все ли инструменты следуют этому паттерну

### 2. Изучение существующих тестов

- [ ] Изучить структуру unit тестов в `tests/tools/`
- [ ] Определить, какие аспекты уже покрыты тестами:
  - Бизнес-логика `execute()`
  - Структура `getDefinition()`
  - Валидация параметров через Zod
- [ ] Определить, что НЕ покрыто тестами:
  - Соответствие Zod schema ↔ MCP definition
  - Соответствие `required` полей между schema и definition

### 3. Анализ исправленного бага

- [ ] Изучить коммит de41f2c с исправлением
- [ ] Понять паттерн бага:
  - Zod schema: `fields: FieldsSchema` (обязательное поле)
  - MCP definition: отсутствует `fields: this.buildFieldsParam()`
  - MCP definition: отсутствует `'fields'` в массиве `required`
- [ ] Проверить, могут ли быть другие типы несоответствий:
  - Optional в schema, но required в definition
  - Разные типы данных
  - Разные имена полей

### 4. Определение требований к генератору

- [ ] Определить, что должен уметь генератор definition:
  - **Обязательное:** Извлекать все поля из Zod schema и создавать properties
  - **Обязательное:** Определять required/optional поля и формировать required[]
  - **Обязательное:** Конвертировать типы Zod → JSON Schema
  - **Желательно:** Поддерживать nested objects, arrays, enums
  - **Желательно:** Использовать .describe() из Zod для descriptions
- [ ] Определить архитектуру генератора:
  - Где будет находиться (packages/framework/core)
  - API функции generateDefinitionFromSchema()
  - Как интегрируется с BaseTool

### 5. Определение подхода к извлечению информации

- [ ] Определить, как извлекать информацию из Zod schema:
  - Использовать `z.infer<typeof Schema>` для типов
  - Использовать `Schema.shape` для доступа к полям
  - Проверить методы `.isOptional()`, `.isNullable()` и т.д.
- [ ] Определить, как извлекать информацию из MCP definition:
  - Создать экземпляр tool
  - Вызвать `tool.getDefinition()`
  - Проверить `inputSchema.properties` и `inputSchema.required`

### 6. Создание спецификации для следующих этапов

- [ ] Определить архитектуру генератора definition (для этапа 1.1)
- [ ] Спроектировать API generateDefinitionFromSchema() (для этапа 1.1)
- [ ] Определить план миграции инструментов (для этапа 1.1)
- [ ] Определить требования к адаптации тестов (для этапа 2.1)
- [ ] Определить изменения в шаблонах (для этапа 2.2)
- [ ] Определить разделы документации для обновления (для этапа 2.3)

---

## 📝 Выходные артефакты

Создать файл `ANALYSIS_RESULTS.md` в директории плана со следующей информацией:

1. **Список всех инструментов** с указанием структуры файлов
2. **Типичный паттерн** создания инструмента
3. **Паттерн бага** с конкретными примерами
4. **Архитектура генератора** (где, как, API)
5. **Технический подход** к конверсии Zod → JSON Schema
6. **Спецификации для этапа 1.1** (рефакторинг)
7. **Требования для этапов 2.1-2.3** (адаптация)

---

## ⚠️ Критерии завершения

- [ ] Файл `ANALYSIS_RESULTS.md` создан и содержит всю необходимую информацию
- [ ] Определена архитектура генератора definition
- [ ] Определен технический подход к конверсии Zod → JSON Schema
- [ ] Созданы спецификации для этапа 1.1 (рефакторинг)
- [ ] Определены требования для этапов 2.1-2.3 (адаптация)
- [ ] Этап согласован с пользователем

---

## 🔗 Следующий шаг

После завершения этого этапа и согласования с пользователем переходить к **этапу 1.1 (архитектурный рефакторинг)** — критичный этап, 4-8 часов работы.
