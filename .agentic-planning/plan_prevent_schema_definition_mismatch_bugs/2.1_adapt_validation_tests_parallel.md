# Этап 2.1: Адаптация валидационных тестов (Parallel)

**Тип:** Parallel (можно выполнять отдельным агентом)
**Время:** ~1-1.5 часа
**Ветка:** `feature/adapt-tests` (создать от основной ветки после рефакторинга)
**Зависимости:** Требует завершения Этапа 1 (рефакторинг)

---

## 🎯 Цель

Адаптировать существующие валидационные тесты для работы с новой архитектурой автогенерации definition из schema.

---

## 📋 Чек-лист выполнения

### Анализ текущих тестов

- [ ] Прочитать текущие тесты schema-definition validation
  - `tests/tools/validation/schema-definition.test.ts` (если существует)
  - Тесты в `tests/tools/api/*/` на наличие проверок definition
- [ ] Понять, какие тесты нужно адаптировать
- [ ] Определить, какие тесты нужно удалить (устаревшие)

### Создание тестов для генератора

- [ ] Создать тест-сюит для генератора definition
  - Файл: `tests/framework/core/generator/schema-to-definition.test.ts`
  - Тесты:
    - [ ] Генерация `type: 'object'` из zod object
    - [ ] Генерация `properties` из zod shape
    - [ ] Генерация `required` из zod required fields
    - [ ] Обработка optional полей
    - [ ] Обработка nullable полей
    - [ ] Обработка array полей (zod.array())
    - [ ] Обработка enum полей (zod.enum())
    - [ ] Обработка nested objects
    - [ ] Обработка discriminated unions
    - [ ] Edge cases:
      - [ ] Empty schema
      - [ ] Schema with all optional fields
      - [ ] Schema with complex nested structures

### Обновление тестов инструментов

- [ ] Обновить тесты для всех инструментов
  - Паттерн: вместо ручной проверки definition, проверять, что:
    - [ ] `tool.getDefinition()` возвращает корректную структуру
    - [ ] Definition соответствует schema (проверка через генератор)
    - [ ] Required поля в definition совпадают с required в schema

- [ ] Удалить устаревшие проверки
  - Удалить тесты, которые проверяли ручное соответствие
  - Удалить моки для `*.definition.ts` файлов (их больше нет)

### Создание helper для тестирования

- [ ] Создать test helper для проверки schema ↔ definition
  - Файл: `tests/helpers/schema-definition-matcher.ts`
  - Функции:
    - `expectDefinitionMatchesSchema(definition, schema)` — проверяет соответствие
    - `validateGeneratedDefinition(definition)` — проверяет корректность сгенерированного definition

### Обновление smoke тестов

- [ ] Адаптировать smoke тесты для новой архитектуры
  - Проверить, что все инструменты корректно генерируют definition
  - Убедиться, что нет orphaned `*.definition.ts` файлов

### Документация тестов

- [ ] Обновить `tests/README.md`
  - Добавить секцию про тестирование генератора
  - Обновить примеры тестов инструментов
  - Добавить рекомендации по тестированию новых инструментов

---

## 🧪 Примеры тестов

### Тест генератора (новый)

```typescript
describe('SchemaToDefinitionGenerator', () => {
  it('должен генерировать definition из простой zod schema', () => {
    const schema = z.object({
      name: z.string().min(1),
      age: z.number().optional(),
    });

    const definition = generateDefinition(schema);

    expect(definition).toEqual({
      type: 'object',
      properties: {
        name: {
          type: 'string',
          minLength: 1,
        },
        age: {
          type: 'number',
        },
      },
      required: ['name'],
    });
  });

  it('должен обрабатывать array полей', () => {
    const schema = z.object({
      tags: z.array(z.string()).min(1),
    });

    const definition = generateDefinition(schema);

    expect(definition.properties.tags).toEqual({
      type: 'array',
      items: { type: 'string' },
      minItems: 1,
    });
  });
});
```

### Тест инструмента (обновленный)

```typescript
describe('GetQueueFieldsTool', () => {
  it('должен генерировать корректный definition из schema', () => {
    const tool = new GetQueueFieldsTool(/* deps */);
    const definition = tool.getDefinition();

    // Проверяем, что definition соответствует schema
    expectDefinitionMatchesSchema(definition.inputSchema, GetQueueFieldsSchema);
  });
});
```

---

## ✅ Критерии завершения

- [ ] Все новые тесты написаны и проходят
- [ ] Устаревшие тесты удалены
- [ ] Test helper создан и используется
- [ ] `npm run test` успешна (все тесты проходят)
- [ ] `npm run test:coverage` показывает покрытие ≥90%
- [ ] `tests/README.md` обновлен
- [ ] Коммит с описанием изменений
- [ ] Пуш в ветку `feature/adapt-tests`

---

## 📝 Файлы для изменения

### Новые файлы:
- `tests/framework/core/generator/schema-to-definition.test.ts`
- `tests/helpers/schema-definition-matcher.ts`

### Изменяемые файлы:
- `tests/tools/api/*/` (все тесты инструментов)
- `tests/README.md`

### Удаляемые файлы:
- Устаревшие тесты ручной проверки definition (если есть)

---

## ⚠️ Важно

1. **Не блокируй другие этапы:**
   - Этот этап независим от 2.2 и 2.3
   - Можешь выполнять параллельно

2. **Сохраняй обратную совместимость:**
   - Не ломай существующие тесты без необходимости
   - Адаптируй постепенно

3. **Проверяй покрытие:**
   - Новые тесты должны поддерживать покрытие ≥90%

4. **Используй TypeScript:**
   - Все тесты должны быть типобезопасными

---

## 🔗 Связанные этапы

- **Зависит от:** Этап 1 (рефакторинг)
- **Параллельно с:** Этапы 2.2, 2.3
- **Предшествует:** Этап 3 (интеграция)
