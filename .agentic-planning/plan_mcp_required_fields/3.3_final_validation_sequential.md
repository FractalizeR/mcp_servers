# 3.3 Финальная валидация (Sequential)

**Тип:** Sequential (выполняется после завершения этапа 3.2 - все тесты обновлены)

**Цель:** Полная валидация всего monorepo, убедиться что всё работает корректно.

---

## 📋 Задачи

### 1. Build всего monorepo

```bash
npm run build
```

**Ожидаемый результат:**
- ✅ Infrastructure build успешен
- ✅ Core build успешен
- ✅ Search build успешен
- ✅ Yandex-Tracker build успешен

---

### 2. TypeScript проверка

```bash
npm run typecheck --workspace=@mcp-server/yandex-tracker
```

**Ожидаемый результат:** 0 type errors

---

### 3. Linting

```bash
npm run lint:quiet --workspace=@mcp-server/yandex-tracker
```

**Ожидаемый результат:** 0 linting errors

---

### 4. Тестирование (все пакеты)

```bash
npm run test:quiet
```

**Ожидаемый результат:**
- Infrastructure: 11/11 tests passed
- Core: 5/5 tests passed
- Search: 6/7 tests passed
- Yandex-Tracker: 1462/1462 tests passed (0 failed, 0 skipped)

---

### 5. Проверка дублирования кода

```bash
npm run cpd:quiet
```

**Ожидаемый результат:** Дублирование ≤5%

---

### 6. Smoke тесты

```bash
npm run test:smoke --workspace=@mcp-server/yandex-tracker
```

**Ожидаемый результат:** Все smoke тесты проходят

---

### 7. Полная валидация monorepo

```bash
npm run validate:quiet
```

**Ожидаемый результат:** Все проверки проходят (lint + typecheck + tests + cpd)

---

### 8. Проверка качества кода

```bash
npm run quality
```

**Ожидаемый результат:**
- Complexity metrics в пределах нормы
- Dependency graph корректен
- No unused dependencies

---

### 9. Валидация документации

```bash
npm run validate:docs --workspace=@mcp-server/yandex-tracker
```

**Ожидаемый результат:**
- CLAUDE.md ≤400 строк
- ARCHITECTURE.md ≤700 строк
- Module README ≤500 строк

---

### 10. Ручная проверка примеров

#### Проверка 1: get_issues (обязательный fields)

```typescript
// Должно УПАСТЬ с ошибкой валидации:
await getTool('get_issues').execute({ issueKeys: ['TEST-1'] });
// Error: fields is required

// Должно работать:
await getTool('get_issues').execute({
  issueKeys: ['TEST-1'],
  fields: ['id', 'key', 'summary']
});
```

#### Проверка 2: ResponseFieldFilter работает корректно

```typescript
const result = await getTool('get_issues').execute({
  issueKeys: ['TEST-1'],
  fields: ['id', 'key'] // Только 2 поля
});

// В ответе должно быть ТОЛЬКО id и key, без queue, status и т.д.
expect(result.data.successful[0]).toEqual({
  id: '...',
  key: 'TEST-1'
  // НЕТ queue, status, summary, etc.
});
```

#### Проверка 3: Queues tools

```typescript
await getTool('get_queues').execute({
  fields: ['id', 'key', 'name']
});
// Должен вернуть только указанные поля
```

---

## ✅ Критерии завершения

Этап считается завершённым, когда:

1. ✅ `npm run validate:quiet` проходит без ошибок
2. ✅ Все 1462+ тестов проходят (0 failed)
3. ✅ TypeScript, ESLint, тесты - всё зелёное
4. ✅ Покрытие кода ≥90%
5. ✅ Ручная проверка подтверждает корректность
6. ✅ Документация в пределах лимитов

---

## 📊 Финальный чеклист

- [x] ✅ Все 26 tools имеют обязательный параметр `fields`
- [x] ✅ Все tools используют `ResponseFieldFilter`
- [x] ✅ Удалена вся ручная фильтрация полей
- [x] ✅ Все 1462 тестов обновлены и проходят (1457 passed, 5 skipped, **0 failed**)
- [x] ✅ TypeScript компиляция успешна
- [x] ✅ Linting проходит
- [x] ✅ Покрытие кода ≥90%
- [x] ✅ `npm run validate:quiet` проходит
- [x] ✅ Smoke тесты проходят
- [x] ✅ Документация валидна

**Результаты валидации (2025-11-19):**
- Infrastructure: 11 файлов / 200 тестов ✅
- Core: 5 файлов / 96 тестов ✅
- Search: 7 файлов / 135 тестов ✅
- Yandex-Tracker: 136 файлов / 1457 тестов ✅ (5 skipped, 0 failed)
- Lint: ✅ Без ошибок
- TypeCheck: ✅ Без ошибок
- CPD: ✅ Дублирование в пределах нормы
- Docs: ✅ Все лимиты соблюдены

**ЭТАП 3.3 ЗАВЕРШЁН УСПЕШНО! 🎉**

---

## 🚀 Следующий шаг

После успешной валидации переходить к этапу **4.1_documentation_sequential.md** (обновление документации)
