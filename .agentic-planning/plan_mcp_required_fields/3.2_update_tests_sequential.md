# 3.2 Обновление тестов для обязательного `fields` (Sequential)

**Тип:** Sequential (выполняется после завершения этапа 2 - все tools обновлены)

**Цель:** Обновить 150 проваленных тестов (30 test файлов), чтобы они соответствовали новой логике с обязательным параметром `fields`.

---

## 📊 Статистика проблемы

**Провалено:** 150 тестов в 30 файлах

**Причины:**
1. Тестовые вызовы НЕ передают обязательный параметр `fields`
2. Assertion helpers hardcoded на ожидание определённых полей (например, `queue`, `status`)
3. Ожидания не соответствуют фильтрации - тесты ожидают все поля, но получают только запрошенные

---

## 🎯 Стратегия обновления

### Шаг 1: Создать стандартные наборы полей для тестов

**Файл:** `tests/helpers/test-fields.ts` (новый)

```typescript
/**
 * Стандартные наборы полей для unit тестов
 * Обеспечивают консистентность и переиспользование в тестах
 */

export const STANDARD_ISSUE_FIELDS = [
  'id',
  'key',
  'summary',
  'description',
  'status',
  'queue',
  'createdAt',
  'updatedAt',
  'createdBy',
  'assignee',
  'priority',
] as const;

export const MINIMAL_ISSUE_FIELDS = ['id', 'key', 'summary'] as const;

export const STANDARD_QUEUE_FIELDS = ['id', 'key', 'name', 'description', 'lead'] as const;

export const MINIMAL_QUEUE_FIELDS = ['id', 'key', 'name'] as const;

export const STANDARD_COMMENT_FIELDS = ['id', 'text', 'createdAt', 'createdBy'] as const;

export const STANDARD_CHECKLIST_FIELDS = ['id', 'text', 'checked', 'assignee'] as const;

export const STANDARD_ATTACHMENT_FIELDS = ['id', 'name', 'size', 'createdAt', 'createdBy'] as const;

export const STANDARD_LINK_FIELDS = ['id', 'type', 'direction', 'object'] as const;

export const STANDARD_PROJECT_FIELDS = ['id', 'key', 'name', 'description', 'lead'] as const;

export const STANDARD_WORKLOG_FIELDS = ['id', 'duration', 'createdAt', 'createdBy'] as const;

export const STANDARD_TRANSITION_FIELDS = ['id', 'to', 'display'] as const;

export const STANDARD_CHANGELOG_FIELDS = ['updatedAt', 'updatedBy', 'fields'] as const;

// Хелпер для создания кастомного набора полей
export function createFieldsSet(...fields: string[]): string[] {
  return fields;
}
```

---

### Шаг 2: Обновить assertion helpers (гибкие проверки)

**Файл:** `tests/helpers/assertion-helpers.ts`

**Изменения:**

```typescript
/**
 * Проверить что объект имеет указанные поля
 * @param obj - Объект для проверки
 * @param expectedFields - Массив ожидаемых полей
 */
export function assertHasFields(obj: unknown, expectedFields: string[]): void {
  for (const field of expectedFields) {
    expect(obj).toHaveProperty(field);
  }
}

/**
 * Проверить что задача имеет запрошенные поля (НЕ больше, НЕ меньше)
 */
export function assertIssueStructure(issue: unknown, requestedFields?: string[]): void {
  if (!requestedFields) {
    // Дефолтная проверка для обратной совместимости
    expect(issue).toHaveProperty('key');
    expect(issue).toHaveProperty('summary');
    expect(issue).toHaveProperty('status');
    expect(issue).toHaveProperty('queue');
  } else {
    // Гибкая проверка только запрошенных полей
    assertHasFields(issue, requestedFields);
  }
}

/**
 * Проверить что transitions имеют правильную структуру
 */
export function assertTransitionsStructure(
  transitions: unknown[],
  requestedFields?: string[]
): void {
  expect(Array.isArray(transitions)).toBe(true);
  if (transitions.length > 0) {
    const transition = transitions[0] as Record<string, unknown>;
    if (requestedFields) {
      assertHasFields(transition, requestedFields);
    } else {
      // Дефолт для обратной совместимости
      expect(transition).toHaveProperty('id');
      expect(transition).toHaveProperty('to');
    }
  }
}

/**
 * Проверить что changelog имеет правильную структуру
 */
export function assertChangelogStructure(
  changelog: unknown[],
  requestedFields?: string[]
): void {
  expect(Array.isArray(changelog)).toBe(true);
  if (changelog.length > 0) {
    const entry = changelog[0] as Record<string, unknown>;
    if (requestedFields) {
      assertHasFields(entry, requestedFields);
    } else {
      // Дефолт для обратной совместимости
      expect(entry).toHaveProperty('updatedAt');
      expect(entry).toHaveProperty('updatedBy');
    }
  }
}
```

---

### Шаг 3: Обновить unit тесты (категории API)

**Для каждой категории API обновить тесты:**

#### 3.1 Issues tests

**Файлы:**
- `tests/tools/api/issues/get/get-issues.tool.test.ts`
- `tests/tools/api/issues/get/get-issue.tool.test.ts`
- `tests/tools/api/issues/create/create-issue.tool.test.ts`
- `tests/tools/api/issues/update/update-issue.tool.test.ts`
- `tests/tools/api/issues/changelog/get-issue-changelog.tool.test.ts`
- `tests/tools/api/issues/transitions/get-issue-transitions.tool.test.ts`

**Шаблон изменений:**

```typescript
// ДО:
const result = await tool.execute({ issueKeys: ['TEST-1'] });

// ПОСЛЕ:
import { STANDARD_ISSUE_FIELDS } from '../../../../helpers/test-fields.js';

const result = await tool.execute({
  issueKeys: ['TEST-1'],
  fields: STANDARD_ISSUE_FIELDS,
});
```

#### 3.2 Workflows tests

**Файлы:**
- `tests/workflows/issue-lifecycle.test.ts`
- `tests/workflows/issue-search.test.ts`
- `tests/workflows/issue-tracking.test.ts`

**Шаблон:**

```typescript
import { STANDARD_ISSUE_FIELDS } from '../helpers/test-fields.js';

const result = await getTool('get_issues').execute({
  issueKeys: ['TEST-1'],
  fields: STANDARD_ISSUE_FIELDS,
});

// В assertions передавать requestedFields
assertIssueStructure(issue, STANDARD_ISSUE_FIELDS);
```

#### 3.3 Tool Registry tests

**Файл:** `tests/tool-registry.test.ts`

**Изменения:**

```typescript
import { STANDARD_ISSUE_FIELDS } from './helpers/test-fields.js';

// Для get_issues
const params: ToolCallParams = {
  issueKeys: ['TEST-1'],
  fields: STANDARD_ISSUE_FIELDS,
};
```

---

### Шаг 4: Обновить smoke тесты

**Файлы:**
- `tests/smoke/basic.test.ts` (если есть)

**Добавить параметр `fields` во все вызовы tools**

---

### Шаг 5: Обновить интеграционные тесты

**Файлы под `tests/integration/` (если есть)**

**Добавить `fields` во все вызовы**

---

## 📋 Пошаговый план выполнения

### Этап 1: Подготовка инфраструктуры

- [x] Создать `tests/helpers/test-fields.ts` со стандартными наборами полей ✅
- [x] Обновить `tests/helpers/assertion-helpers.ts` для гибких проверок ✅
- [x] Запустить тесты, убедиться что старые тесты всё ещё падают (150 failed) ✅

### Этап 2: Обновление unit тестов (по категориям)

**Можно выполнять параллельно в разных ветках:**

- [x] **Issues** (8 файлов) - `tests/tools/api/issues/**/*.test.ts` ✅
- [x] **Checklists** (3 файла) - `tests/tools/api/checklists/**/*.test.ts` ✅
- [x] **Comments** (3 файла) - `tests/tools/api/comments/**/*.test.ts` ✅
- [x] **Components** (3 файла) - `tests/tools/api/components/**/*.test.ts` ✅
- [x] **Projects** (4 файла) - `tests/tools/api/projects/**/*.test.ts` ✅
- [x] **Queues** (6 файлов) - `tests/tools/api/queues/**/*.test.ts` ✅
- [x] **Исправлены пути импортов** (28 файлов) ✅
- [x] **Исправлены syntax errors** (3 файла) ✅
- [x] **Исправлены тесты валидации** (6 файлов) ✅

### Этап 3: Обновление workflow тестов

- [x] `tests/workflows/issue-lifecycle.test.ts` ✅ (используют WorkflowClient, уже обновлен)
- [x] `tests/workflows/issue-search.test.ts` ✅ (используют WorkflowClient, уже обновлен)
- [x] `tests/workflows/issue-tracking.test.ts` ✅ (используют WorkflowClient, уже обновлен)

### Этап 4: Обновление других тестов

- [x] `tests/tool-registry.test.ts` ✅ (не требует изменений - не вызывает execute)
- [ ] **Integration тесты** ⏳ (26 файлов, ~88 failed тестов) - **ТРЕБУЕТСЯ ДОРАБОТКА**
  - `tests/integration/tools/api/checklists/**/*.test.ts`
  - `tests/integration/tools/api/components/**/*.test.ts`
  - `tests/integration/tools/api/comments/**/*.test.ts`
  - `tests/integration/tools/api/projects/**/*.test.ts`
  - `tests/integration/tools/api/queues/**/*.test.ts`

### Этап 5: Валидация

- [x] Запустить `npm run test:quiet --workspace=@mcp-server/yandex-tracker` ✅
- [ ] **Частично выполнено:** Unit тесты проходят, integration тесты требуют доработки
  - **Результат:** 88 failed из 1429 тестов (было 150 failed)
  - **Прогресс:** Unit тесты в `tests/tools/api/` обновлены и проходят
  - **Осталось:** Integration тесты в `tests/integration/tools/api/` (26 файлов)
- [ ] Проверить покрытие кода (должно остаться ≥90%) ⏳

---

## 🔍 Автоматизация (опционально)

**Использовать find/replace для массового обновления:**

### Шаблон 1: Добавление импорта

```bash
# В начало каждого test файла добавить:
import { STANDARD_ISSUE_FIELDS } from '../../../helpers/test-fields.js';
```

### Шаблон 2: Обновление вызовов execute()

**Найти:**
```typescript
tool.execute({ issueKeys: ['TEST-1'] })
```

**Заменить на:**
```typescript
tool.execute({ issueKeys: ['TEST-1'], fields: STANDARD_ISSUE_FIELDS })
```

**Повторить для каждого tool:**
- `issueId` → добавить `fields: STANDARD_ISSUE_FIELDS`
- `queueId` → добавить `fields: STANDARD_QUEUE_FIELDS`
- `commentId` → добавить `fields: STANDARD_COMMENT_FIELDS`
- и т.д.

---

## ✅ Критерии завершения этапа

Этап считается завершённым, когда:

1. ✅ Созданы `test-fields.ts` и обновлены `assertion-helpers.ts`
2. ✅ Все 30 test файлов обновлены
3. ✅ `npm run test:quiet` проходит без ошибок (0 failed из 455+)
4. ✅ Покрытие кода ≥90% (lines, functions, branches, statements)
5. ✅ TypeScript компиляция успешна
6. ✅ Linting проходит

---

## 📊 Ожидаемый результат

**До:**
```
Test Files  30 failed | 106 passed | 1 skipped (137)
     Tests  150 failed | 1307 passed | 5 skipped (1462)
```

**После:**
```
Test Files  0 failed | 137 passed | 0 skipped (137)
     Tests  0 failed | 1462 passed | 0 skipped (1462)
```

---

## 🚀 Следующий шаг

После успешного обновления всех тестов переходить к этапу **3.3_final_validation_sequential.md** (финальная валидация всего monorepo)
