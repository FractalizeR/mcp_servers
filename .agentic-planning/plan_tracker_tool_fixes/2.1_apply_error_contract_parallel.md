# Этап 2.1 (PARALLEL, после 1.1) — перевод схем на общий контракт ошибки

## Задача
Заменить `error: z.string()` на общий `BatchErrorValueSchema` во всех схемах из таблицы
`0_enumeration.md`, КРОМЕ чеклистов (их владелец — пакет 2.2, чтобы не пересекать файлы).

## Файлы (13 схем трекера)
comments/add, comments/get, comments/edit, comments/delete, issues/get,
issues/links/create, issues/links/delete, issues/links/get, issues/changelog,
issues/attachments/get, users/get-users, worklog/add, worklog/get — файлы `*.schema.ts`.

Плюс `packages/servers/ticktick/src/tools/tasks/batch-create-tasks/batch-create-tasks.schema.ts`
— сначала проверить, что источник значения тот же `BatchResultProcessor`; если нет —
зафиксировать факт в отчёте и не трогать. Ticktick — отдельный пакет, общий контракт из
трекера ему недоступен: либо локальный аналог, либо подъём контракта в framework
(решение принимает исполнитель, обосновав в отчёте).

## Definition of Done
- [ ] `grep -rn "error: z.string()" packages/servers/*/src/tools` не находит ничего в
      блоках `errors:`/`failed:` (кроме чеклистов — их закрывает 2.2)
- [ ] `npm run typecheck` и `npm run test:quiet` в затронутых пакетах зелёные
- [ ] Ни одного изменения в `*.tool.ts`, `*.metadata.ts` и в чеклистах
