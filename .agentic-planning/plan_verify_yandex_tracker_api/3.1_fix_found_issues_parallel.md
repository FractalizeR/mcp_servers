# Шаг 3.1: Исправление найденных проблем

**Тип:** Parallel (можно выполнять параллельно разными агентами)
**Приоритет:** P1-P2 (в зависимости от проблемы)
**Время выполнения:** ~2-3 часа

**Зависимости:** Требует завершения шагов 1.1 и 2.1

---

## 🎯 Цель

Исправить все выявленные проблемы с использованием API на основе результатов проверки из шага 2.1.

---

## 📋 Задачи

### Задача 3.1.1: Стандартизация именования параметров

**Проблема:** Inconsistency между `{issueId}` и `{issueKey}` в разных операциях

#### Текущее состояние:
- v2 endpoints: используют `{issueId}` (но принимают и ключи типа 'QUEUE-123')
- v3 endpoints: используют `{issueKey}` или `{issueId}` непоследовательно

#### Действия:

1. **Проанализировать все operations:**
   ```bash
   cd packages/servers/yandex-tracker
   grep -r "issueId\|issueKey" src/tracker_api/api_operations/ --include="*.ts"
   ```

2. **Выбрать единый стандарт:**
   - Рекомендация: использовать `issueIdOrKey` в TypeScript коде
   - Причина: API принимает оба формата

3. **Обновить код операций (если необходимо):**
   - GetIssuesOperation
   - CreateIssueOperation
   - UpdateIssueOperation
   - GetCommentsOperation
   - и т.д.

4. **Обновить DTO:**
   - Параметры должны называться `issueIdOrKey: string`
   - Добавить JSDoc комментарий с пояснением

#### Пример изменения:
```typescript
// Было:
async execute(issueId: string): Promise<...> {
  return this.httpClient.get(`/v3/issues/${issueId}`);
}

// Стало:
/**
 * @param issueIdOrKey - ID задачи (число) или ключ ('QUEUE-123')
 */
async execute(issueIdOrKey: string): Promise<...> {
  return this.httpClient.get(`/v3/issues/${issueIdOrKey}`);
}
```

#### Чек-лист:
- [x] Проанализированы все операции с issues
- [x] Выбран стандарт именования (решено: текущее состояние `issueId` приемлемо с поясняющими комментариями, массовое переименование на `issueIdOrKey` не требуется как P2 приоритет)
- [x] Проверены параметры в operations (корректны)
- [x] Проверены параметры в DTO (корректны)
- [x] Комментарии JSDoc достаточно детальны
- [x] Тесты не требуют обновления
- [x] `npm run validate:quiet` проходит (unit тесты успешны)

---

### Задача 3.1.2: Проверка корректности HTTP методов

**Цель:** Убедиться, что все операции используют правильные HTTP методы

#### Действия:

1. **Проверить CRUD операции:**
   - **CREATE** → POST
   - **READ** → GET
   - **UPDATE** → PATCH (не PUT!)
   - **DELETE** → DELETE

2. **Специальные случаи:**
   - **Search** → POST `/v3/issues/_search` (правильно ✅)
   - **Execute transition** → POST `/v3/issues/{id}/transitions/{id}/_execute` (правильно ✅)

3. **Проверить все операции:**
   ```bash
   cd packages/servers/yandex-tracker/src/tracker_api/api_operations

   # Найти все POST запросы
   grep -r "httpClient.post" . --include="*.ts"

   # Найти все PATCH запросы
   grep -r "httpClient.patch" . --include="*.ts"

   # Найти все DELETE запросы
   grep -r "deleteRequest\|httpClient.delete" . --include="*.ts"
   ```

4. **Сверить с официальной документацией:**
   - Для каждой операции проверить HTTP метод в документации
   - Если найдено несоответствие — исправить

#### Чек-лист:
- [x] Проверены все CREATE операции (используют POST) ✅
- [x] Проверены все UPDATE операции (используют PATCH) ✅
- [x] Проверены все DELETE операции (используют DELETE) ✅
- [x] Специальные endpoints проверены (search, execute transition используют POST) ✅
- [x] Несоответствий не найдено
- [x] Тесты не требуют обновления
- [x] `npm run validate:quiet` проходит

---

### Задача 3.1.3: Валидация структур DTO

**Цель:** Убедиться, что все DTO соответствуют официальному API

#### Действия:

1. **Проверить CreateIssueDto:**
   - Обязательные поля: `queue`, `summary`
   - Опциональные поля: `description`, `type`, `priority`, `assignee`, etc.
   - Index signature для кастомных полей

2. **Проверить UpdateIssueDto:**
   - Все поля должны быть опциональными (partial update)
   - Index signature для кастомных полей

3. **Проверить другие DTO:**
   - CreateLinkDto
   - CreateCommentDto
   - CreateQueueDto
   - и т.д.

4. **Сверка с официальной документацией:**
   ```
   Для каждого DTO:
   1. Найти соответствующую страницу в документации
   2. Сравнить список полей
   3. Проверить обязательность полей
   4. Проверить типы полей
   ```

#### Чек-лист:
- [x] Проверены все Create DTO (CreateIssue, CreateQueue, CreateLink, CreateComponent, AddComment, AddChecklist, AddWorklog) ✅
- [x] Проверены все Update DTO (все поля опциональны) ✅
- [x] Проверены все Search/Find DTO (FindIssues корректен) ✅
- [x] Несоответствий не найдено
- [x] Все обязательные поля присутствуют
- [x] Index signature для кастомных полей есть везде
- [x] `npm run validate:quiet` проходит

---

### Задача 3.1.4: Проверка обработки ошибок

**Цель:** Убедиться, что все операции корректно обрабатывают ошибки API

#### Текущее состояние:
- HttpClient автоматически преобразует HTTP ошибки в ApiError
- Operations не должны иметь try-catch для обработки API ошибок

#### Действия:

1. **Найти избыточные try-catch блоки:**
   ```bash
   cd packages/servers/yandex-tracker/src/tracker_api/api_operations
   grep -r "try {" . --include="*.ts" -A 10
   ```

2. **Проанализировать каждый try-catch:**
   - Если ловит только API ошибки → удалить (обрабатывается в HttpClient)
   - Если ловит для логирования → можно оставить, но лучше убрать
   - Если ловит для трансформации → оставить

3. **Проверить GetCommentsOperation:**
   ```typescript
   // Найдено в коде:
   try {
     const comments = await this.httpClient.get<...>(endpoint);
     return Array.isArray(comments) ? comments : [comments];
   } catch (error) {
     this.logger.error(`Ошибка при получении комментариев...`, error);
     throw error; // Просто пробрасывает дальше
   }
   ```
   **Проблема:** Избыточный try-catch, ошибка просто пробрасывается

4. **Рефакторинг:**
   ```typescript
   // Правильный вариант:
   const comments = await this.httpClient.get<...>(endpoint);
   this.logger.info(`Получено ${comments.length} комментариев`);
   return Array.isArray(comments) ? comments : [comments];
   // HttpClient уже залогирует ошибку и пробросит ApiError
   ```

#### Чек-лист:
- [x] Найдены все избыточные try-catch блоки (выполнено ранее)
- [x] Удалены try-catch, которые только пробрасывают ошибки (выполнено ранее)
- [x] Проверено, что HttpClient правильно обрабатывает все типы ошибок ✅
- [x] Тесты проверяют обработку ошибок корректно ✅
- [x] `npm run validate:quiet` проходит

---

### Задача 3.1.5: Проверка параметров запросов и query strings

**Цель:** Убедиться в корректности формирования URL с query параметрами

#### Действия:

1. **Найти все операции с query params:**
   ```bash
   grep -r "queryParams\|queryString" packages/servers/yandex-tracker/src/tracker_api/api_operations --include="*.ts"
   ```

2. **Проверить FindIssuesOperation:**
   - Правильное разделение body и query параметров
   - Body: `query`, `filter`, `keys`, `queue`, `filterId`, `order`
   - Query: `perPage`, `page`, `expand`

3. **Проверить GetCommentsOperation:**
   - Query: `perPage`, `page`, `expand`

4. **Проверить корректность encoding:**
   ```typescript
   // Должно быть:
   const queryString = Object.entries(queryParams)
     .map(([key, value]) => `${encodeURIComponent(key)}=${encodeURIComponent(value)}`)
     .join('&');
   ```

5. **Проверить массивы в query params:**
   ```typescript
   // Для expand: ['attachments', 'transitions']
   // Должно стать: expand=attachments,transitions
   if (Array.isArray(params.expand)) {
     queryParams.expand = params.expand.join(',');
   }
   ```

#### Чек-лист:
- [x] Проверены все операции с query параметрами (GetComments, FindIssues, GetQueues, GetQueue, GetProjects, GetProject, DownloadAttachment) ✅
- [x] Правильное разделение body/query (FindIssues: body={query,filter,keys,order}, query={perPage,page,expand}) ✅
- [x] Корректный encoding параметров (encodeURIComponent + URLSearchParams) ✅
- [x] Правильная обработка массивов (expand преобразуется в строку через .join(',')) ✅
- [x] Дублирования параметров нет ✅
- [x] Тесты не требуют обновления
- [x] `npm run validate:quiet` проходит

---

## ✅ Критерии завершения

- [x] Все задачи 3.1.1 - 3.1.5 выполнены ✅
- [x] Проблем, требующих исправления, не найдено ✅
- [x] Тесты не требуют обновления (unit тесты проходят) ✅
- [x] `npm run validate:quiet` успешен (lint + typecheck + unit tests) ✅
- [ ] Изменения закоммичены (в процессе)

---

## 🔄 После выполнения

1. **Закоммитить изменения:**
   ```bash
   git add packages/servers/yandex-tracker/src/tracker_api/
   git commit -m "fix: исправить проблемы с использованием API

   - Стандартизировать именование параметров (issueIdOrKey)
   - Проверить корректность HTTP методов
   - Валидировать структуры DTO
   - Удалить избыточные try-catch блоки
   - Проверить параметры запросов и query strings

   🤖 Generated with Claude Code
   Co-Authored-By: Claude <noreply@anthropic.com>"
   ```

2. **Пуш изменений:**
   ```bash
   git push -u origin claude/verify-yandex-tracker-api-016zyAHRcedufXhynbFTzbgF
   ```

3. **Обновить 0_SUMMARY.md с результатами**
