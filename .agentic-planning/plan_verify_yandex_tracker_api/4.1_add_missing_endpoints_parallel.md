# Шаг 4.1: Добавление недостающих API endpoints

**Тип:** Parallel (можно выполнять параллельно разными агентами)
**Приоритет:** P2 (Средний)
**Время выполнения:** ~4-6 часов

**Зависимости:** Требует завершения шага 2.1 (для понимания доступных endpoints)

---

## 🎯 Цель

Добавить поддержку важных API endpoints, которые отсутствуют в текущей реализации.

---

## 📋 Недостающие категории

### 1. Boards API (Доски и спринты)

**Приоритет:** P2 (важно для Agile команд)

#### Endpoints для реализации:

| Endpoint | HTTP | Описание |
|----------|------|----------|
| `GET /v2/boards` | GET | Получить список досок |
| `GET /v2/boards/{boardId}` | GET | Получить доску по ID |
| `POST /v2/boards` | POST | Создать доску |
| `PATCH /v2/boards/{boardId}` | PATCH | Обновить доску |
| `DELETE /v2/boards/{boardId}` | DELETE | Удалить доску |
| `GET /v2/boards/{boardId}/sprints` | GET | Получить спринты доски |
| `GET /v2/sprints/{sprintId}` | GET | Получить спринт |
| `POST /v2/sprints` | POST | Создать спринт |

#### Задачи:
- [ ] Создать Board entity
- [ ] Создать Sprint entity
- [ ] Создать Board DTO (Create, Update, Get)
- [ ] Создать Sprint DTO
- [ ] Реализовать GetBoardsOperation
- [ ] Реализовать GetBoardOperation
- [ ] Реализовать CreateBoardOperation
- [ ] Реализовать UpdateBoardOperation
- [ ] Реализовать GetSprintsOperation
- [ ] Добавить методы в YandexTrackerFacade
- [ ] Создать MCP tools для работы с досками
- [ ] Написать тесты
- [ ] Обновить документацию

---

### 2. Fields API (Поля трекера)

**Приоритет:** P2 (нужно для работы с кастомными полями)

#### Endpoints для реализации:

| Endpoint | HTTP | Описание |
|----------|------|----------|
| `GET /v2/fields` | GET | Получить список всех полей |
| `GET /v2/fields/{fieldId}` | GET | Получить поле по ID |
| `POST /v2/fields` | POST | Создать кастомное поле |
| `PATCH /v2/fields/{fieldId}` | PATCH | Обновить поле |
| `DELETE /v2/fields/{fieldId}` | DELETE | Удалить поле |

#### Задачи:
- [ ] Создать Field entity
- [ ] Создать Field DTO
- [ ] Реализовать GetFieldsOperation
- [ ] Реализовать GetFieldOperation
- [ ] Реализовать CreateFieldOperation
- [ ] Реализовать UpdateFieldOperation
- [ ] Реализовать DeleteFieldOperation
- [ ] Добавить методы в Facade
- [ ] Создать MCP tools
- [ ] Написать тесты
- [ ] Обновить документацию

---

### 3. Macros API (Макросы)

**Приоритет:** P3 (полезно для автоматизации)

#### Endpoints для реализации:

| Endpoint | HTTP | Описание |
|----------|------|----------|
| `GET /v2/queues/{queueId}/macros` | GET | Получить макросы очереди |
| `GET /v2/queues/{queueId}/macros/{macroId}` | GET | Получить макрос |
| `POST /v2/queues/{queueId}/macros` | POST | Создать макрос |
| `PATCH /v2/queues/{queueId}/macros/{macroId}` | PATCH | Обновить макрос |
| `DELETE /v2/queues/{queueId}/macros/{macroId}` | DELETE | Удалить макрос |

#### Задачи:
- [ ] Создать Macro entity
- [ ] Создать Macro DTO
- [ ] Реализовать операции для макросов
- [ ] Добавить методы в Facade
- [ ] Создать MCP tools
- [ ] Написать тесты
- [ ] Обновить документацию

---

### 4. Bulk Change API (Массовые операции)

**Приоритет:** P2 (важно для производительности)

#### Endpoints для реализации:

| Endpoint | HTTP | Описание |
|----------|------|----------|
| `POST /v2/bulkchange/_update` | POST | Массовое обновление задач |
| `POST /v2/bulkchange/_transition` | POST | Массовый переход статусов |
| `POST /v2/bulkchange/_move` | POST | Массовое перемещение в другую очередь |
| `GET /v2/bulkchange/{operationId}` | GET | Получить статус операции |

#### Особенности:
- Асинхронные операции (возвращают operationId)
- Нужно реализовать polling для проверки статуса
- Лимиты на количество задач в одной операции

#### Задачи:
- [ ] Создать BulkChangeOperation entity
- [ ] Создать BulkChange DTO
- [ ] Реализовать BulkUpdateIssuesOperation
- [ ] Реализовать BulkTransitionIssuesOperation
- [ ] Реализовать BulkMoveIssuesOperation
- [ ] Реализовать GetBulkChangeStatusOperation
- [ ] Добавить поддержку polling
- [ ] Добавить методы в Facade
- [ ] Создать MCP tools
- [ ] Написать тесты
- [ ] Обновить документацию

---

### 5. Import API (Импорт задач)

**Приоритет:** P3 (специализированный функционал)

#### Endpoints для реализации:

| Endpoint | HTTP | Описание |
|----------|------|----------|
| `POST /v2/issues/import` | POST | Импорт задач из CSV/JSON |
| `GET /v2/imports/{importId}` | GET | Статус импорта |

---

### 6. Attachments для комментариев

**Приоритет:** P2 (расширение существующего функционала)

#### Текущее состояние:
- Реализованы attachments для задач
- НЕ реализованы attachments для комментариев

#### Endpoints для реализации:

| Endpoint | HTTP | Описание |
|----------|------|----------|
| `POST /v3/issues/{issueId}/comments/{commentId}/attachments` | POST | Добавить файл к комментарию |
| `GET /v3/issues/{issueId}/comments/{commentId}/attachments` | GET | Получить файлы комментария |

#### Задачи:
- [ ] Расширить AddCommentOperation для поддержки attachments
- [ ] Реализовать UploadCommentAttachmentOperation
- [ ] Обновить DTO
- [ ] Написать тесты
- [ ] Обновить документацию

---

### 7. Notifications API (Уведомления)

**Приоритет:** P3 (дополнительный функционал)

#### Endpoints для реализации:

| Endpoint | HTTP | Описание |
|----------|------|----------|
| `GET /v2/myself/subscriptions` | GET | Получить подписки пользователя |
| `POST /v2/issues/{issueId}/subscriptions` | POST | Подписаться на задачу |
| `DELETE /v2/issues/{issueId}/subscriptions` | DELETE | Отписаться от задачи |

---

## 📊 Приоритизация реализации

### Фаза 1 (P2 - Высокий приоритет):
1. **Bulk Change API** — критично для производительности
2. **Fields API** — нужно для работы с кастомными полями
3. **Boards API** — важно для Agile команд
4. **Comment Attachments** — расширение существующего функционала

### Фаза 2 (P3 - Средний приоритет):
1. **Macros API** — полезно для автоматизации
2. **Notifications API** — дополнительный функционал
3. **Import API** — специализированный функционал

---

## ✅ Критерии завершения

Для каждой категории:
- [ ] Entity созданы
- [ ] DTO созданы
- [ ] Operations реализованы
- [ ] Методы добавлены в Facade
- [ ] MCP tools созданы
- [ ] Тесты написаны (coverage ≥80%)
- [ ] Документация обновлена
- [ ] `npm run validate` проходит

---

## 🔄 После выполнения

1. **Коммит для каждой категории отдельно:**
   ```bash
   git add packages/servers/yandex-tracker/src/tracker_api/
   git commit -m "feat: добавить поддержку [Category] API

   - Создать [Category] entity и DTO
   - Реализовать операции для работы с [Category]
   - Добавить методы в YandexTrackerFacade
   - Создать MCP tools для [Category]
   - Добавить тесты (coverage 85%)

   🤖 Generated with Claude Code
   Co-Authored-By: Claude <noreply@anthropic.com>"
   ```

2. **Пуш изменений:**
   ```bash
   git push -u origin claude/verify-yandex-tracker-api-016zyAHRcedufXhynbFTzbgF
   ```

3. **Обновить 0_SUMMARY.md с результатами**

---

## 📝 Примечания

- Эти задачи можно выполнять параллельно разными агентами
- Каждая категория независима от других
- Рекомендуется начинать с Фазы 1 (P2 приоритет)
- Можно создать отдельные ветки для каждой категории
