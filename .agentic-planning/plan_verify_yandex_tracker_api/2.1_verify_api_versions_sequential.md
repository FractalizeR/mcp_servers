# Шаг 2.1: Проверка версий API endpoints

**Тип:** Sequential (должен выполняться последовательно)
**Приоритет:** P1 (Высокий)
**Время выполнения:** ~1-2 часа

---

## 🎯 Цель

Проверить официальную документацию Яндекс.Трекер и выяснить:
1. Существуют ли v3 версии для endpoints, которые мы используем в v2
2. Есть ли deprecated endpoints в v2, которые нужно мигрировать на v3
3. Какие новые endpoints доступны в API, но не реализованы в нашем проекте

---

## 📋 Задачи

### Задача 2.1.1: Проверить Attachments API

**Текущее состояние:** Используем v2
**Endpoints:**
- `GET /v2/issues/{issueId}/attachments`
- `POST /v2/issues/{issueId}/attachments`
- `GET /v2/issues/{issueId}/attachments/{attachmentId}/{filename}`
- `DELETE /v2/issues/{issueId}/attachments/{attachmentId}`
- `GET /v2/issues/{issueId}/thumbnails/{attachmentId}`

#### Действия:
1. Поискать в документации Яндекс.Трекер информацию об Attachments API
2. Проверить, существует ли `/v3/issues/{id}/attachments`
3. Если существует v3 — сравнить различия с v2
4. Зафиксировать результаты в таблицу ниже

#### Результаты:
| Endpoint | v2 существует | v3 существует | Рекомендация |
|----------|---------------|---------------|--------------|
| GET attachments | ✅ | ❓ | Проверить |
| POST attachment | ✅ | ❓ | Проверить |
| GET attachment file | ✅ | ❓ | Проверить |
| DELETE attachment | ✅ | ❓ | Проверить |
| GET thumbnail | ✅ | ❓ | Проверить |

---

### Задача 2.1.2: Проверить Checklists API

**Текущее состояние:** Используем v2
**Endpoints:**
- `GET /v2/issues/{issueId}/checklistItems`
- `POST /v2/issues/{issueId}/checklistItems`
- `PATCH /v2/issues/{issueId}/checklistItems/{checklistItemId}`
- `DELETE /v2/issues/{issueId}/checklistItems/{checklistItemId}`

#### Действия:
1. Поискать в документации информацию о Checklists
2. Проверить, существует ли `/v3/issues/{id}/checklistItems`
3. Зафиксировать результаты

#### Результаты:
| Endpoint | v2 существует | v3 существует | Рекомендация |
|----------|---------------|---------------|--------------|
| GET checklist | ✅ | ❓ | Проверить |
| POST checklist item | ✅ | ❓ | Проверить |
| PATCH checklist item | ✅ | ❓ | Проверить |
| DELETE checklist item | ✅ | ❓ | Проверить |

---

### Задача 2.1.3: Проверить Components API

**Текущее состояние:** Используем v2
**Endpoints:**
- `GET /v2/queues/{queueId}/components`
- `POST /v2/queues/{queueId}/components`
- `PATCH /v2/components/{componentId}`
- `DELETE /v2/components/{componentId}`

#### Действия:
1. Проверить документацию по Components
2. Проверить существование v3 версии
3. Зафиксировать результаты

#### Результаты:
| Endpoint | v2 существует | v3 существует | Рекомендация |
|----------|---------------|---------------|--------------|
| GET components | ✅ | ❓ | Проверить |
| POST component | ✅ | ❓ | Проверить |
| PATCH component | ✅ | ❓ | Проверить |
| DELETE component | ✅ | ❓ | Проверить |

---

### Задача 2.1.4: Проверить Projects API

**Текущее состояние:** Используем v2
**Endpoints:**
- `GET /v2/projects`
- `GET /v2/projects/{projectId}`
- `POST /v2/projects`
- `PATCH /v2/projects/{projectId}`
- `DELETE /v2/projects/{projectId}`

#### Действия:
1. Проверить документацию по Projects
2. Проверить существование v3 версии
3. Зафиксировать результаты

#### Результаты:
| Endpoint | v2 существует | v3 существует | Рекомендация |
|----------|---------------|---------------|--------------|
| GET projects list | ✅ | ❓ | Проверить |
| GET project | ✅ | ❓ | Проверить |
| POST project | ✅ | ❓ | Проверить |
| PATCH project | ✅ | ❓ | Проверить |
| DELETE project | ✅ | ❓ | Проверить |

---

### Задача 2.1.5: Проверить Worklogs API

**Текущее состояние:** Используем v2
**Endpoints:**
- `GET /v2/issues/{issueId}/worklog`
- `POST /v2/issues/{issueId}/worklog`
- `PATCH /v2/issues/{issueId}/worklog/{worklogId}`
- `DELETE /v2/issues/{issueId}/worklog/{worklogId}`

#### Действия:
1. Проверить документацию по Worklogs
2. Проверить существование v3 версии
3. Зафиксировать результаты

#### Результаты:
| Endpoint | v2 существует | v3 существует | Рекомендация |
|----------|---------------|---------------|--------------|
| GET worklogs | ✅ | ❓ | Проверить |
| POST worklog | ✅ | ❓ | Проверить |
| PATCH worklog | ✅ | ❓ | Проверить |
| DELETE worklog | ✅ | ❓ | Проверить |

---

### Задача 2.1.6: Проверить Comments API (для подтверждения v3)

**Текущее состояние:** Используем v3
**Endpoints:**
- `GET /v3/issues/{issueId}/comments`
- `POST /v3/issues/{issueId}/comments`
- `PATCH /v3/issues/{issueId}/comments/{commentId}`
- `DELETE /v3/issues/{issueId}/comments/{commentId}`

#### Действия:
1. Подтвердить, что v3 — правильная версия для Comments
2. Проверить, не deprecated ли v2 версия
3. Зафиксировать результаты

#### Результаты:
| Endpoint | v2 существует | v3 существует | Текущее использование | Статус |
|----------|---------------|---------------|----------------------|--------|
| GET comments | ❓ | ✅ | v3 | ✅ OK |
| POST comment | ❓ | ✅ | v3 | ✅ OK |
| PATCH comment | ❓ | ✅ | v3 | ✅ OK |
| DELETE comment | ❓ | ✅ | v3 | ✅ OK |

---

## 📝 Источники для проверки

1. **Официальная документация:**
   - https://cloud.yandex.ru/docs/tracker/about-api
   - https://yandex.ru/support/tracker/ru/user/API

2. **Python клиент (reference implementation):**
   - https://github.com/yandex/yandex_tracker_client
   - Анализ используемых endpoints в коде

3. **Альтернативные клиенты:**
   - https://github.com/intensa/yandex-tracker (PHP)
   - https://github.com/Olegt0rr/YaTracker (Python asyncio)

---

## 📊 Итоговая таблица рекомендаций

После выполнения всех проверок заполнить:

| Категория | Текущая версия | Рекомендуемая версия | Действие | Приоритет |
|-----------|----------------|---------------------|----------|-----------|
| Attachments | v2 | ❓ | ❓ | ❓ |
| Checklists | v2 | ❓ | ❓ | ❓ |
| Components | v2 | ❓ | ❓ | ❓ |
| Projects | v2 | ❓ | ❓ | ❓ |
| Worklogs | v2 | ❓ | ❓ | ❓ |

**Легенда действий:**
- ✅ **Оставить как есть** — текущая версия оптимальна
- 🔄 **Мигрировать на v3** — существует v3, нужна миграция
- ⏸️ **Отложить миграцию** — v3 существует, но миграция не критична

---

## ✅ Критерии завершения

- [ ] Проверены все 5 категорий на v2 (Attachments, Checklists, Components, Projects, Worklogs)
- [ ] Подтверждено использование v3 для Comments
- [ ] Заполнена итоговая таблица рекомендаций
- [ ] Зафиксированы ссылки на официальную документацию
- [ ] Созданы issue/задачи для миграции на v3 (если требуется)

---

## 🔄 После выполнения

1. Создать файл с результатами проверки:
   ```bash
   # Файл: .agentic-planning/plan_verify_yandex_tracker_api/2.1_RESULTS.md
   ```

2. Обновить файл 0_SUMMARY.md с результатами проверки

3. Если найдены endpoints для миграции на v3:
   - Создать отдельный план миграции
   - Оценить трудозатраты
   - Определить breaking changes
