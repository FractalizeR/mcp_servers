# Этап 6.1: Обновление корневой документации (PARALLEL)

**Execution Type:** Parallel
**Зависимости:** Этапы 1-5 завершены
**Можно начинать:** После успешного завершения этапов 4.x и 5.x
**Можно выполнять параллельно с:** 6.2, 6.3, 6.4
**Ожидаемое время:** 20 минут

---

## 🎯 Цель этапа

Обновить всю корневую документацию monorepo для отражения нового названия пакета.

---

## ✅ Чек-лист выполнения

### 6.1.1 Обновление ARCHITECTURE.md

**Файл:** `ARCHITECTURE.md`

- [ ] Заменить все упоминания `@mcp-framework/core` на `@mcp-framework/tooling`
- [ ] Обновить описание пакета
- [ ] Обновить диаграммы dependency graph
- [ ] Обновить примеры кода

**Секции для обновления:**

1. **Monorepo Structure** (строки ~23-40):
   ```markdown
   packages/
   ├── infrastructure/
   ├── tooling/              → @mcp-framework/tooling  (было: core/)
   ├── search/
   └── yandex-tracker/
   ```

2. **Dependency Graph** (строки ~43-75):
   ```markdown
   infrastructure → tooling → search → yandex-tracker
   ```

3. **Package Details** (строки ~95-108):
   - Обновить заголовок секции
   - Обновить описание
   - Обновить ссылку на README

4. Все примеры импортов:
   ```typescript
   import { BaseTool } from '@mcp-framework/tooling';
   ```

**Команды для поиска всех упоминаний:**
```bash
grep -n "@mcp-framework/core" ARCHITECTURE.md
grep -n "packages/framework/core" ARCHITECTURE.md
grep -n "core/" ARCHITECTURE.md | grep -v "core/src"  # исключить внутренние пути
```

### 6.1.2 Обновление CLAUDE.md

**Файл:** `CLAUDE.md`

- [ ] Обновить секцию "Структура monorepo"
- [ ] Обновить граф зависимостей
- [ ] Обновить ссылки на документацию
- [ ] Обновить примеры импортов

**Секции для обновления:**

1. **Структура monorepo** (~линия 25):
   ```markdown
   packages/
   ├── tooling/              → @mcp-framework/tooling
   ```

2. **Граф зависимостей** (~линия 47):
   ```markdown
   infrastructure → tooling → search → yandex-tracker
   ```

3. **Импорты между пакетами** (~линия 60):
   ```typescript
   import { BaseTool } from '@mcp-framework/tooling';
   ```

4. **Ссылки на документацию** (~линия 34):
   - `packages/framework/tooling/README.md`

**Команды:**
```bash
grep -n "core" CLAUDE.md | grep -v "core responsibility"  # исключить "core responsibility"
```

### 6.1.3 Обновление README.md

**Файл:** `README.md` (root)

- [ ] Обновить секцию "Package Overview"
- [ ] Обновить quick start примеры
- [ ] Обновить ссылки на документацию
- [ ] Обновить badges (если есть)

**Секции для обновления:**

1. **Package listing:**
   ```markdown
   - [@mcp-framework/tooling](./packages/framework/tooling) - MCP tools framework
   ```

2. **Quick Start примеры:**
   ```typescript
   import { BaseTool } from '@mcp-framework/tooling';
   ```

3. **Ссылки:**
   - Обновить все ссылки на core → tooling

**Команды:**
```bash
grep -n "@mcp-framework/core" README.md
grep -n "framework/core" README.md
```

### 6.1.4 Обновление DOCS.md

**Файл:** `DOCS.md`

- [ ] Обновить секцию "Framework Packages"
- [ ] Обновить ссылки на tooling package

**Секции для обновления:**

1. **Framework Packages** (~линия 30):
   ```markdown
   - **[Tooling](./packages/framework/tooling/README.md)** — MCP tools framework
   ```

**Команды:**
```bash
grep -n "core" DOCS.md
```

### 6.1.5 Обновление DOC_STANDARDS.md (если есть упоминания)

**Файл:** `DOC_STANDARDS.md`

- [ ] Проверить наличие упоминаний core
- [ ] Обновить примеры путей

**Команды:**
```bash
grep -n "core" DOC_STANDARDS.md
```

### 6.1.6 Обновление .github/CONTRIBUTING.md (если есть)

**Файл:** `.github/CONTRIBUTING.md`

- [ ] Проверить наличие упоминаний core
- [ ] Обновить примеры разработки

**Команды:**
```bash
grep -n "core" .github/CONTRIBUTING.md
```

### 6.1.7 Проверка других markdown файлов в корне

- [ ] Найти все .md файлы в корне
- [ ] Проверить каждый на упоминания core

**Команды:**
```bash
find . -maxdepth 1 -name "*.md" -exec grep -l "core" {} \;
```

### 6.1.8 Валидация ссылок

- [ ] Проверить что все ссылки на документацию валидны
- [ ] Проверить что файлы по ссылкам существуют

**Команды:**
```bash
# Проверить существование файлов
ls packages/framework/tooling/README.md
ls packages/framework/tooling/CHANGELOG.md
```

### 6.1.9 Валидация размера документации

- [ ] Запустить валидацию размера документов
- [ ] Убедиться что лимиты не превышены

**Команды:**
```bash
npm run validate:docs
```

**Ожидаемый результат:** Все документы в пределах лимитов.

### 6.1.10 Финальная проверка

- [ ] Убедиться что все упоминания обновлены
- [ ] Проверить консистентность терминологии

**Команды:**
```bash
grep -r "@mcp-framework/core" . --include="*.md" --exclude-dir=node_modules --exclude-dir=.agentic-planning --max-depth=2
grep -r "packages/framework/core" . --include="*.md" --exclude-dir=node_modules --exclude-dir=.agentic-planning --max-depth=2
```

**Ожидаемый результат:** Нет совпадений (кроме CHANGELOG файлов, которые могут содержать историю).

---

## 📊 Критерии завершения этапа

- ✅ `ARCHITECTURE.md` обновлен полностью
- ✅ `CLAUDE.md` обновлен полностью
- ✅ `README.md` обновлен полностью
- ✅ `DOCS.md` обновлен полностью
- ✅ Другие корневые .md файлы проверены и обновлены
- ✅ Все ссылки валидны
- ✅ `npm run validate:docs` проходит
- ✅ Нет упоминаний старого названия (кроме CHANGELOG)

---

## 🚨 Важные замечания

1. Этот этап **можно выполнять параллельно** с этапами 6.2, 6.3, 6.4
2. Если выполняется параллельно с другими агентами:
   - Работать в отдельной ветке
   - Merge после завершения всех этапов 6.x
3. **НЕ коммитить** до завершения всех этапов 6.x и валидации
4. **НЕ обновлять CHANGELOG** на этом этапе (будет в этапе 8)

---

## ⚠️ Возможные проблемы

**Проблема:** Ссылки на несуществующие файлы
**Решение:** Проверить что этап 2 завершен (директория переименована)

**Проблема:** Превышен лимит размера документа
**Решение:** Применить правила сокращения из CLAUDE.md

---

## ➡️ Следующие шаги

После завершения этого этапа:
- Если работаешь **последовательно**: перейти к `6.2_update_tooling_docs_parallel.md`
- Если работаешь **параллельно**: дождаться завершения этапов 6.2, 6.3, 6.4 перед валидацией
