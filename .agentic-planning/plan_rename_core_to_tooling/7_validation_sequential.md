# Этап 7: Валидация (SEQUENTIAL)

**Execution Type:** Sequential
**Зависимости:** Все этапы 1-6 завершены
**Можно начинать:** После успешного завершения этапов 6.1, 6.2, 6.3, 6.4
**Ожидаемое время:** 20 минут

---

## 🎯 Цель этапа

Полная валидация всех изменений: сборка, тесты, линтинг, dependency graph, документация.

---

## ✅ Чек-лист выполнения

### 7.1 Подготовка к валидации

- [ ] Убедиться что все этапы 1-6 завершены
- [ ] Если работали параллельные агенты — выполнить merge веток
- [ ] Разрешить конфликты (если есть)

**Команды:**
```bash
git status
git log --oneline -10
```

### 7.2 Очистка артефактов

- [ ] Удалить все build артефакты
- [ ] Очистить кэши
- [ ] Переустановить зависимости

**Команды:**
```bash
# Очистка
npm run clean:all  # удаляет dist и node_modules

# Переустановка
npm install

# Очистка Turborepo cache
rm -rf .turbo
```

### 7.3 Проверка финальных замен

- [ ] Убедиться что нигде не осталось упоминаний старого названия
- [ ] Проверить все типы файлов

**Команды полного поиска:**
```bash
# TypeScript файлы
grep -r "@mcp-framework/core" packages/*/src --include="*.ts" || echo "✓ No matches in .ts files"

# package.json файлы
grep -r "@mcp-framework/core" packages/*/package.json || echo "✓ No matches in package.json"

# Markdown файлы (исключая CHANGELOG)
grep -r "@mcp-framework/core" . --include="*.md" --exclude="CHANGELOG.md" --exclude-dir=node_modules --exclude-dir=dist --exclude-dir=.agentic-planning || echo "✓ No matches in .md files"

# Конфигурационные файлы
grep -r "framework/core" . --include="*.json" --include="*.cjs" --exclude-dir=node_modules --exclude-dir=dist || echo "✓ No matches in config files"
```

**Ожидаемый результат:** Все команды выводят "✓ No matches" (кроме CHANGELOG файлов).

### 7.4 Валидация сборки

- [ ] Запустить сборку всех пакетов
- [ ] Убедиться что сборка проходит успешно
- [ ] Проверить что артефакты создаются

**Команды:**
```bash
npm run build
```

**Ожидаемый порядок сборки:**
```
✓ infrastructure
✓ tooling (было: core)
✓ search
✓ yandex-tracker
```

**Проверка артефактов:**
```bash
ls packages/framework/tooling/dist/
ls packages/framework/search/dist/
ls packages/servers/yandex-tracker/dist/
```

### 7.5 Валидация TypeScript

- [ ] Запустить typecheck для всех пакетов
- [ ] Убедиться что нет ошибок типов

**Команды:**
```bash
npm run typecheck
```

**Ожидаемый результат:** 0 errors во всех пакетах.

### 7.6 Валидация линтинга

- [ ] Запустить eslint для всех пакетов
- [ ] Убедиться что нет ошибок линтинга

**Команды:**
```bash
npm run lint:quiet
```

**Ожидаемый результат:** 0 errors, 0 warnings (или только expected warnings).

### 7.7 Валидация тестов

- [ ] Запустить все unit тесты
- [ ] Убедиться что все тесты проходят

**Команды:**
```bash
npm run test:quiet
```

**Ожидаемый результат:** Все тесты проходят (0 failures).

**Если тесты падают:**
- Проверить что все импорты обновлены
- Проверить test файлы на упоминания старого пакета
- Запустить тесты с verbose: `npm run test:verbose`

### 7.8 Валидация smoke tests (yandex-tracker)

- [ ] Запустить smoke tests для yandex-tracker
- [ ] Убедиться что server запускается

**Команды:**
```bash
cd packages/servers/yandex-tracker
npm run test:smoke
```

**Ожидаемый результат:** Smoke tests проходят.

### 7.9 Валидация dependency graph

- [ ] Запустить dependency-cruiser
- [ ] Убедиться что все правила проходят

**Команды:**
```bash
npm run depcruise
```

**Ожидаемый результат:**
- Все правила проходят
- Граф зависимостей валиден
- infrastructure → tooling → search → yandex-tracker

### 7.10 Валидация качества кода

- [ ] Запустить jscpd (copy-paste detection)
- [ ] Проверить метрики дублирования

**Команды:**
```bash
npm run cpd:quiet
```

**Ожидаемый результат:** Дублирование < 5% (как обычно).

### 7.11 Валидация документации

- [ ] Запустить validate:docs для проверки размеров
- [ ] Убедиться что все лимиты соблюдены

**Команды:**
```bash
npm run validate:docs
```

**Ожидаемый результат:** Все документы в пределах лимитов.

### 7.12 Полная валидация

- [ ] Запустить полную валидацию всего monorepo
- [ ] Убедиться что все проверки проходят

**Команды:**
```bash
npm run validate:quiet
```

**Эта команда включает:**
- lint
- typecheck
- test
- cpd

**Ожидаемый результат:** Все проверки успешны.

### 7.13 Проверка symlinks

- [ ] Убедиться что symlinks созданы корректно
- [ ] Проверить что tooling доступен для других пакетов

**Команды:**
```bash
ls -la node_modules/@mcp-framework/ | grep tooling
ls -la packages/framework/search/node_modules/@mcp-framework/ | grep tooling
ls -la packages/servers/yandex-tracker/node_modules/@mcp-framework/ | grep tooling
```

**Ожидаемый результат:**
- `tooling` symlinks существуют
- `core` symlinks НЕ существуют

### 7.14 Финальная проверка файловой структуры

- [ ] Убедиться что старая директория не существует
- [ ] Новая директория существует и содержит все файлы

**Команды:**
```bash
# Старая директория НЕ должна существовать
ls packages/framework/core 2>&1 | grep "No such file"

# Новая директория должна существовать
ls -la packages/framework/tooling

# Проверить количество файлов
find packages/framework/tooling/src -name "*.ts" | wc -l
```

**Ожидаемое количество .ts файлов:** ~24 (как было в core)

---

## 📊 Критерии завершения этапа

- ✅ Нет упоминаний `@mcp-framework/core` в коде (кроме CHANGELOG)
- ✅ `npm run build` проходит успешно
- ✅ `npm run typecheck` — 0 errors
- ✅ `npm run lint:quiet` — 0 errors
- ✅ `npm run test:quiet` — все тесты проходят
- ✅ `npm run test:smoke` — smoke tests проходят
- ✅ `npm run depcruise` — dependency graph валиден
- ✅ `npm run validate:quiet` — все проверки успешны
- ✅ `npm run validate:docs` — документация в пределах лимитов
- ✅ Symlinks созданы корректно
- ✅ Файловая структура корректна

---

## 🚨 Важные замечания

1. Этот этап **ОБЯЗАТЕЛЬНО sequential** — валидация перед финализацией
2. **НЕ переходить к этапу 8** если хотя бы одна проверка не проходит
3. Если есть ошибки — вернуться к соответствующим этапам и исправить
4. Это последняя возможность найти проблемы перед коммитом

---

## ⚠️ Возможные проблемы

**Проблема:** Build падает с "Cannot find module '@mcp-framework/core'"
**Решение:**
- Проверить что этап 3 завершен (dependencies обновлены)
- Запустить `npm install`
- Проверить что этап 4 завершен (imports обновлены)

**Проблема:** Тесты падают
**Решение:**
- Проверить импорты в test файлах
- Запустить с --verbose для деталей
- Проверить mock/stub объекты

**Проблема:** Dependency graph показывает ошибки
**Решение:**
- Проверить что этап 5.1 завершен (depcruise config обновлен)
- Проверить что все path patterns корректны

**Проблема:** Symlinks не созданы
**Решение:**
- Удалить node_modules: `rm -rf node_modules packages/*/node_modules`
- Запустить `npm install`

---

## ➡️ Следующий этап

После успешного завершения перейти к: `8_finalization_sequential.md`
