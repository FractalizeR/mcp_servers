# Этап 4.1: Обновление импортов в search пакете (PARALLEL)

**Execution Type:** Parallel
**Зависимости:** Этап 3 завершен
**Можно начинать:** После успешного завершения этапа 3
**Можно выполнять параллельно с:** 4.2, 5.1, 5.2, 5.3
**Ожидаемое время:** 10 минут

---

## 🎯 Цель этапа

Обновить все импорты из `@mcp-framework/core` на `@mcp-framework/tooling` в пакете search.

---

## ✅ Чек-лист выполнения

### 4.1.1 Поиск импортов в search пакете

- [ ] Найти все TypeScript файлы с импортами из `@mcp-framework/core`
- [ ] Подсчитать количество файлов для обновления

**Команды:**
```bash
cd packages/framework/search
grep -r "from '@mcp-framework/core" src --include="*.ts"
grep -r "from '@mcp-framework/core" src --include="*.ts" | wc -l
```

**Ожидаемое количество:** ~10-15 файлов

### 4.1.2 Автоматическое обновление импортов

- [ ] Использовать find+sed для замены всех импортов
- [ ] Проверить результат замены

**Команды для автоматической замены:**
```bash
cd packages/framework/search

# Замена в TypeScript файлах
find src -name "*.ts" -type f -exec sed -i "s|from '@mcp-framework/core'|from '@mcp-framework/tooling'|g" {} +
find src -name "*.ts" -type f -exec sed -i 's|from "@mcp-framework/core"|from "@mcp-framework/tooling"|g' {} +
```

**Проверка после замены:**
```bash
# Должно быть 0 результатов
grep -r "from '@mcp-framework/core" src --include="*.ts"

# Должны быть новые импорты
grep -r "from '@mcp-framework/tooling" src --include="*.ts"
```

### 4.1.3 Проверка конкретных файлов

Вручную проверить ключевые файлы на корректность импортов:

- [ ] `src/engine/tool-search-engine.ts`
- [ ] `src/tools/search-tools.tool.ts`
- [ ] `src/tools/search-tools.definition.ts`
- [ ] `src/tools/search-tools.metadata.ts`
- [ ] `src/utils/build-index-from-registry.ts`
- [ ] `src/types.ts`

**Команды для проверки:**
```bash
head -20 src/engine/tool-search-engine.ts | grep import
head -20 src/tools/search-tools.tool.ts | grep import
head -20 src/utils/build-index-from-registry.ts | grep import
```

**Ожидаемый результат:** Все импорты используют `@mcp-framework/tooling`.

### 4.1.4 Проверка re-exports

- [ ] Проверить index.ts файлы на наличие re-exports из core
- [ ] Обновить если необходимо

**Файлы для проверки:**
```bash
grep -r "from '@mcp-framework/core" src --include="index.ts"
```

**Ожидаемый результат:** Нет re-exports из core (обычно их нет в search).

### 4.1.5 Проверка TypeScript компиляции

- [ ] Запустить typecheck для проверки корректности импортов
- [ ] Убедиться что нет ошибок TypeScript

**Команды:**
```bash
cd packages/framework/search
npm run typecheck
```

**Ожидаемый результат:** Typecheck проходит успешно (0 errors).

### 4.1.6 Запуск тестов search пакета

- [ ] Запустить тесты для проверки работоспособности
- [ ] Убедиться что все тесты проходят

**Команды:**
```bash
cd packages/framework/search
npm run test:quiet
```

**Ожидаемый результат:** Все тесты проходят.

### 4.1.7 Проверка линтинга

- [ ] Запустить eslint для проверки quality
- [ ] Убедиться что нет новых ошибок линтинга

**Команды:**
```bash
cd packages/framework/search
npm run lint:quiet
```

### 4.1.8 Финальная проверка

- [ ] Убедиться что все импорты обновлены
- [ ] Никаких упоминаний `@mcp-framework/core` не осталось в src/

**Команды:**
```bash
cd packages/framework/search
grep -r "@mcp-framework/core" src --include="*.ts"
```

**Ожидаемый результат:** Нет совпадений (empty output).

---

## 📊 Критерии завершения этапа

- ✅ Все импорты из `@mcp-framework/core` заменены на `@mcp-framework/tooling`
- ✅ Typecheck проходит: `npm run typecheck`
- ✅ Тесты проходят: `npm run test:quiet`
- ✅ Линтинг проходит: `npm run lint:quiet`
- ✅ Нет упоминаний `@mcp-framework/core` в src/

---

## 🚨 Важные замечания

1. Этот этап **можно выполнять параллельно** с этапами 4.2, 5.x
2. Если выполняется параллельно с другими агентами:
   - Работать в отдельной ветке
   - Merge после завершения всех параллельных этапов
3. **НЕ коммитить** до завершения этапа 6 (или координации с другими агентами)

---

## ⚠️ Возможные проблемы

**Проблема:** sed не находит файлы
**Решение:** Проверить что находишься в директории `packages/framework/search`

**Проблема:** TypeScript errors после замены
**Решение:** Проверить что этап 3 завершен (dependencies обновлены, npm install выполнен)

---

## ➡️ Следующие шаги

После завершения этого этапа:
- Если работаешь **последовательно**: перейти к `4.2_update_imports_yandex_tracker_parallel.md`
- Если работаешь **параллельно**: дождаться завершения других параллельных этапов (4.2, 5.x) перед merge
