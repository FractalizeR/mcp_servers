# Этап 6.4: Обновление документации yandex-tracker пакета (PARALLEL)

**Execution Type:** Parallel
**Зависимости:** Этапы 1-5 завершены
**Можно начинать:** После успешного завершения этапов 4.x и 5.x
**Можно выполнять параллельно с:** 6.1, 6.2, 6.3
**Ожидаемое время:** 15 минут

---

## 🎯 Цель этапа

Обновить документацию yandex-tracker пакета для отражения изменения зависимости с core на tooling.

---

## ✅ Чек-лист выполнения

### 6.4.1 Обновление README.md в yandex-tracker пакете

**Файл:** `packages/servers/yandex-tracker/README.md`

- [ ] Найти все упоминания `@mcp-framework/core`
- [ ] Заменить на `@mcp-framework/tooling`
- [ ] Обновить примеры кода
- [ ] Обновить секцию Architecture/Dependencies

**Команды для поиска:**
```bash
cd packages/servers/yandex-tracker
grep -n "@mcp-framework/core" README.md
```

**Секции для обновления:**

1. **Dependencies секция (если есть):**
   ```markdown
   **Built on @mcp-framework:**
   - `@mcp-framework/infrastructure` — HTTP, logging, cache
   - `@mcp-framework/tooling` — BaseTool, ToolRegistry
   - `@mcp-framework/search` — Tool search engine
   ```

2. **Примеры импортов (если есть):**
   ```typescript
   import { BaseTool } from '@mcp-framework/tooling';
   ```

3. **Architecture описание (если упоминается core):**
   - Заменить упоминания на tooling

### 6.4.2 Обновление CLAUDE.md в yandex-tracker

**Файл:** `packages/servers/yandex-tracker/CLAUDE.md`

- [ ] Найти все упоминания `@mcp-framework/core`
- [ ] Заменить на `@mcp-framework/tooling`
- [ ] Обновить примеры импортов
- [ ] Обновить ссылки

**Команды:**
```bash
cd packages/servers/yandex-tracker
grep -n "@mcp-framework/core" CLAUDE.md
```

**Секции для обновления:**

1. **Импорты:**
   ```typescript
   import { BaseTool, ResponseFieldFilter } from '@mcp-framework/tooling';
   ```

2. **Ссылки на framework пакеты:**
   ```markdown
   - [Tooling API](../../framework/tooling/README.md)
   ```

### 6.4.3 Обновление tests/README.md

**Файл:** `packages/servers/yandex-tracker/tests/README.md`

- [ ] Проверить наличие упоминаний core
- [ ] Обновить примеры тестов (если есть)

**Команды:**
```bash
cd packages/servers/yandex-tracker
grep -n "@mcp-framework/core" tests/README.md
```

### 6.4.4 Обновление module README файлов

Проверить README в модулях yandex-tracker:

- [ ] `src/tools/README.md`
- [ ] `src/tracker_api/api_operations/README.md`
- [ ] `src/tracker_api/entities/README.md`
- [ ] `src/tracker_api/dto/README.md`
- [ ] `src/composition-root/README.md`

**Команды для каждого:**
```bash
grep -n "@mcp-framework/core" src/tools/README.md
grep -n "@mcp-framework/core" src/tracker_api/api_operations/README.md
# ... и т.д.
```

**Для каждого файла:** заменить упоминания на `@mcp-framework/tooling`

### 6.4.5 Проверка CHANGELOG.md

**Файл:** `packages/servers/yandex-tracker/CHANGELOG.md`

- [ ] **НЕ изменять** CHANGELOG на этом этапе
- [ ] Запланировать обновление на этапе 8

### 6.4.6 Проверка комментариев в коде

- [ ] Проверить JSDoc комментарии на упоминания core
- [ ] Проверить inline comments

**Команды:**
```bash
cd packages/servers/yandex-tracker/src
grep -r "@mcp-framework/core" . --include="*.ts" | grep -E "//|/\*"
```

**Если найдены:**
- Обновить для консистентности

### 6.4.7 Проверка примеров в документации

- [ ] Проверить examples/ директорию (если есть)
- [ ] Проверить code snippets в документации

**Команды:**
```bash
cd packages/servers/yandex-tracker
find . -name "*.md" -not -path "*/node_modules/*" -not -path "*/dist/*" -exec grep -l "@mcp-framework/core" {} \;
```

### 6.4.8 Валидация ссылок

- [ ] Проверить что все ссылки на framework пакеты валидны
- [ ] Убедиться что файлы по ссылкам существуют

**Ключевые ссылки для проверки:**
```bash
ls ../../framework/tooling/README.md
ls ../../framework/infrastructure/README.md
ls ../../framework/search/README.md
ls ../../../ARCHITECTURE.md
```

### 6.4.9 Валидация размеров документов

- [ ] Проверить размеры всех README файлов
- [ ] Убедиться что лимиты не превышены

**Команды:**
```bash
cd packages/servers/yandex-tracker
wc -l README.md CLAUDE.md tests/README.md src/*/README.md
```

**Ожидаемые лимиты:**
- README.md: ≤ 600 строк
- CLAUDE.md: ≤ 400 строк
- tests/README.md: ≤ 500 строк
- Module README: ≤ 600 строк

### 6.4.10 Финальная проверка

- [ ] Убедиться что все упоминания обновлены
- [ ] Никаких ссылок на старое название не осталось

**Команды:**
```bash
cd packages/servers/yandex-tracker
grep -r "@mcp-framework/core" . --include="*.md" --exclude-dir=node_modules --exclude-dir=dist
```

**Ожидаемый результат:** Нет совпадений (empty output, кроме CHANGELOG).

---

## 📊 Критерии завершения этапа

- ✅ `README.md` обновлен
- ✅ `CLAUDE.md` обновлен
- ✅ `tests/README.md` обновлен
- ✅ Все module README обновлены
- ✅ Примеры импортов используют `@mcp-framework/tooling`
- ✅ Комментарии в коде проверены
- ✅ Все ссылки валидны
- ✅ Размеры документов в пределах лимитов
- ✅ Нет упоминаний `@mcp-framework/core` (кроме CHANGELOG)

---

## 🚨 Важные замечания

1. Этот этап **можно выполнять параллельно** с этапами 6.1, 6.2, 6.3
2. Если выполняется параллельно с другими агентами:
   - Работать в отдельной ветке
   - Merge после завершения всех этапов 6.x
3. **НЕ коммитить** до завершения всех этапов 6.x и валидации
4. **НЕ обновлять CHANGELOG** на этом этапе (будет в этапе 8)
5. Большое количество документов — важно проверить все

---

## ⚠️ Возможные проблемы

**Проблема:** Много файлов с упоминаниями
**Решение:** Использовать автоматизацию (find + sed) для массовой замены

**Проблема:** Битые ссылки после переименования
**Решение:** Проверить что этап 2 завершен, ссылки должны использовать новый путь

---

## ➡️ Следующие шаги

После завершения этого этапа:
- Если работаешь **последовательно**: перейти к `7_validation_sequential.md`
- Если работаешь **параллельно**: дождаться завершения этапов 6.1, 6.2, 6.3, затем перейти к этапу 7
