# Этап 5.3: Обновление TypeScript конфигурации (PARALLEL)

**Execution Type:** Parallel
**Зависимости:** Этап 3 завершен
**Можно начинать:** После успешного завершения этапа 3
**Можно выполнять параллельно с:** 4.1, 4.2, 5.1, 5.2
**Ожидаемое время:** 10 минут

---

## 🎯 Цель этапа

Обновить TypeScript конфигурации (project references, path mappings) для корректной работы с переименованным пакетом.

---

## ✅ Чек-лист выполнения

### 5.3.1 Проверка корневого tsconfig.json

- [ ] Открыть корневой `tsconfig.json`
- [ ] Проверить секцию `references` на упоминания core

**Файл:** `tsconfig.json` (root)

**Команды:**
```bash
cat tsconfig.json | grep -A20 "references"
```

**Типичная структура:**
```json
{
  "references": [
    { "path": "./packages/framework/infrastructure" },
    { "path": "./packages/framework/core" },  // <- обновить на tooling
    { "path": "./packages/framework/search" },
    { "path": "./packages/servers/yandex-tracker" }
  ]
}
```

- [ ] Обновить `"path": "./packages/framework/core"` на `"./packages/framework/tooling"`

### 5.3.2 Проверка path mappings в корневом tsconfig

- [ ] Проверить секцию `compilerOptions.paths` (если есть)
- [ ] Убедиться что нет жестко закодированных путей к core

**Команды:**
```bash
grep -A10 "paths" tsconfig.json
```

**Ожидаемый результат:**
- Обычно monorepo НЕ использует path mappings в корневом tsconfig
- Если есть — обновить упоминания core на tooling

### 5.3.3 Обновление tsconfig в search пакете

- [ ] Открыть `packages/framework/search/tsconfig.json`
- [ ] Проверить секцию `references`

**Файл:** `packages/framework/search/tsconfig.json`

**Команды:**
```bash
cat packages/framework/search/tsconfig.json | grep -A10 "references"
```

**Типичная структура:**
```json
{
  "references": [
    { "path": "../infrastructure" },
    { "path": "../core" }  // <- обновить на ../tooling
  ]
}
```

- [ ] Обновить `"path": "../core"` на `"../tooling"`

### 5.3.4 Обновление tsconfig в yandex-tracker пакете

- [ ] Открыть `packages/servers/yandex-tracker/tsconfig.json`
- [ ] Проверить секцию `references`

**Файл:** `packages/servers/yandex-tracker/tsconfig.json`

**Команды:**
```bash
cat packages/servers/yandex-tracker/tsconfig.json | grep -A20 "references"
```

**Типичная структура:**
```json
{
  "references": [
    { "path": "../../framework/infrastructure" },
    { "path": "../../framework/core" },  // <- обновить на tooling
    { "path": "../../framework/search" }
  ]
}
```

- [ ] Обновить `"path": "../../framework/core"` на `"../../framework/tooling"`

### 5.3.5 Проверка tsconfig в tooling пакете

- [ ] Открыть `packages/framework/tooling/tsconfig.json`
- [ ] Убедиться что нет самореференций на старое название

**Файл:** `packages/framework/tooling/tsconfig.json`

**Команды:**
```bash
grep -i "core" packages/framework/tooling/tsconfig.json
```

**Ожидаемый результат:** Нет упоминаний "core" (или только в комментариях).

### 5.3.6 Поиск других tsconfig файлов

- [ ] Найти все tsconfig*.json файлы в проекте
- [ ] Проверить каждый на упоминания core

**Команды:**
```bash
find . -name "tsconfig*.json" -not -path "*/node_modules/*" -not -path "*/dist/*"
```

**Для каждого найденного файла:**
```bash
grep -n "core" <файл>
```

### 5.3.7 Валидация TypeScript конфигураций

- [ ] Запустить typecheck в корне для проверки всех references
- [ ] Убедиться что нет ошибок конфигурации

**Команды:**
```bash
# Из корня проекта
npm run typecheck
```

**Ожидаемый результат:**
- Typecheck проходит успешно
- Все project references разрешаются корректно
- Нет ошибок "Cannot find project"

**Если есть ошибки:**
- Проверить что все path в references корректны
- Проверить что этапы 2-4 завершены
- Попробовать удалить все `*.tsbuildinfo` файлы: `find . -name "*.tsbuildinfo" -delete`

### 5.3.8 Проверка build с project references

- [ ] Запустить build с `-b` флагом для проверки references
- [ ] Убедиться что топологический порядок корректен

**Команды:**
```bash
# Очистить старые build artifacts
npm run clean

# Build с project references
npm run build
```

**Ожидаемый порядок сборки:**
```
1. infrastructure
2. tooling
3. search
4. yandex-tracker
```

### 5.3.9 Финальная проверка

- [ ] Убедиться что все tsconfig файлы обновлены
- [ ] Никаких упоминаний старого пути не осталось

**Команды:**
```bash
find . -name "tsconfig*.json" -not -path "*/node_modules/*" -exec grep -l "framework/core" {} \;
grep -r "framework/core" . --include="tsconfig*.json" --exclude-dir=node_modules
```

**Ожидаемый результат:** Нет совпадений (empty output).

---

## 📊 Критерии завершения этапа

- ✅ Корневой `tsconfig.json` обновлен (references)
- ✅ `packages/framework/search/tsconfig.json` обновлен
- ✅ `packages/servers/yandex-tracker/tsconfig.json` обновлен
- ✅ Все другие tsconfig файлы проверены
- ✅ `npm run typecheck` проходит успешно
- ✅ `npm run build` проходит успешно
- ✅ Нет упоминаний старого пути в tsconfig файлах

---

## 🚨 Важные замечания

1. Этот этап **можно выполнять параллельно** с этапами 4.x, 5.1, 5.2
2. Если выполняется параллельно с другими агентами:
   - Работать в отдельной ветке
   - Merge после завершения всех параллельных этапов
3. **НЕ коммитить** до завершения этапа 6 (или координации с другими агентами)
4. Project references критичны для корректной работы TypeScript compiler

---

## ⚠️ Возможные проблемы

**Проблема:** "Cannot find project" ошибка
**Решение:**
- Проверить что path в references корректен
- Проверить что директория существует
- Удалить *.tsbuildinfo файлы

**Проблема:** Typecheck проходит, но build падает
**Решение:**
- Очистить все dist директории: `npm run clean`
- Удалить *.tsbuildinfo: `find . -name "*.tsbuildinfo" -delete`
- Запустить build заново

**Проблема:** Циклические references
**Решение:** Это не должно произойти при переименовании, но если произошло:
- Проверить граф зависимостей
- Убедиться что references соответствуют dependencies в package.json

---

## ➡️ Следующие шаги

После завершения этого этапа:
- Если работаешь **последовательно**: перейти к этапам 6.x (обновление документации)
- Если работаешь **параллельно**: дождаться завершения других параллельных этапов (4.x, 5.1, 5.2) перед merge
