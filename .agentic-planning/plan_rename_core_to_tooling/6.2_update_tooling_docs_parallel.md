# Этап 6.2: Обновление документации tooling пакета (PARALLEL)

**Execution Type:** Parallel
**Зависимости:** Этапы 1-5 завершены
**Можно начинать:** После успешного завершения этапов 4.x и 5.x
**Можно выполнять параллельно с:** 6.1, 6.3, 6.4
**Ожидаемое время:** 15 минут

---

## 🎯 Цель этапа

Обновить всю документацию внутри tooling пакета для отражения нового названия.

---

## ✅ Чек-лист выполнения

### 6.2.1 Обновление README.md в tooling пакете

**Файл:** `packages/framework/tooling/README.md`

**ВАЖНО:** Большая часть должна быть обновлена на этапе 2.5, но нужна финальная проверка.

- [ ] Проверить что заголовок обновлен: `# @mcp-framework/tooling`
- [ ] Проверить что description обновлен
- [ ] Проверить все примеры импортов
- [ ] Проверить все ссылки на другие документы

**Секции для проверки:**

1. **Заголовок и badges:**
   ```markdown
   # @mcp-framework/tooling

   [![npm version](https://img.shields.io/npm/v/@mcp-framework/tooling.svg)]
   ```

2. **Installation:**
   ```bash
   npm install @mcp-framework/tooling
   ```

3. **Все примеры кода:**
   ```typescript
   import { BaseTool } from '@mcp-framework/tooling';
   ```

4. **Exports секция** (~линия 320):
   ```typescript
   export { BaseTool } from './tools/base/base-tool.js';
   ```
   (должна остаться без изменений — это внутренние пути)

5. **Ссылки на другие пакеты:**
   ```markdown
   - **Monorepo root:** [../../README.md](../../README.md)
   - **Infrastructure package:** [../infrastructure/README.md](../infrastructure/README.md)
   ```

**Команды для финальной проверки:**
```bash
cd packages/framework/tooling
grep -n "@mcp-framework/core" README.md
```

**Ожидаемый результат:** Нет совпадений.

### 6.2.2 Проверка CHANGELOG.md

**Файл:** `packages/framework/tooling/CHANGELOG.md`

- [ ] **НЕ изменять** CHANGELOG на этом этапе
- [ ] Запланировать обновление на этапе 8

**Примечание:** В этапе 8 добавим entry о переименовании.

### 6.2.3 Обновление внутренней документации (если есть)

Проверить наличие других markdown файлов в tooling пакете:

- [ ] Найти все .md файлы
- [ ] Проверить каждый на упоминания старого названия

**Команды:**
```bash
cd packages/framework/tooling
find . -name "*.md" -not -path "*/node_modules/*" -not -path "*/dist/*"
```

**Для каждого файла:**
```bash
grep -n "@mcp-framework/core" <файл>
```

### 6.2.4 Проверка комментариев в коде (опционально)

Проверить наличие комментариев со старым названием в TypeScript файлах:

- [ ] Найти комментарии с упоминанием старого пакета
- [ ] Обновить при необходимости

**Команды:**
```bash
cd packages/framework/tooling/src
grep -r "@mcp-framework/core" . --include="*.ts" | grep "//"
```

**Примечание:** Обычно комментарии не упоминают package name, но стоит проверить.

### 6.2.5 Проверка JSDoc комментариев

- [ ] Проверить JSDoc на наличие примеров с старым названием

**Команды:**
```bash
cd packages/framework/tooling/src
grep -r "@example" . --include="*.ts" -A5 | grep "@mcp-framework/core"
```

### 6.2.6 Валидация ссылок в README

- [ ] Проверить что все относительные ссылки валидны
- [ ] Убедиться что файлы существуют

**Ключевые ссылки для проверки:**
```bash
ls ../../README.md
ls ../infrastructure/README.md
ls ../../ARCHITECTURE.md
ls CHANGELOG.md
```

### 6.2.7 Валидация размера README

- [ ] Проверить размер README.md
- [ ] Убедиться что не превышен лимит (600 строк)

**Команды:**
```bash
cd packages/framework/tooling
wc -l README.md
```

**Ожидаемый результат:** ≤ 600 строк (или ≤ 660 с исключением).

### 6.2.8 Проверка examples/ директории (если есть)

- [ ] Проверить наличие examples/ директории
- [ ] Обновить примеры при необходимости

**Команды:**
```bash
cd packages/framework/tooling
ls examples/ 2>/dev/null
```

**Если examples/ существует:**
```bash
grep -r "@mcp-framework/core" examples/
```

### 6.2.9 Финальная проверка всего пакета

- [ ] Поиск всех упоминаний старого названия в пакете
- [ ] Убедиться что все обновлено

**Команды:**
```bash
cd packages/framework/tooling
grep -r "@mcp-framework/core" . --include="*.md" --include="*.ts" --exclude-dir=node_modules --exclude-dir=dist
```

**Ожидаемый результат:** Нет совпадений (empty output).

---

## 📊 Критерии завершения этапа

- ✅ `README.md` полностью обновлен
- ✅ Все примеры импортов используют `@mcp-framework/tooling`
- ✅ Все ссылки валидны
- ✅ Размер README в пределах лимита
- ✅ Комментарии в коде проверены
- ✅ Нет упоминаний `@mcp-framework/core` в пакете (кроме CHANGELOG)

---

## 🚨 Важные замечания

1. Этот этап **можно выполнять параллельно** с этапами 6.1, 6.3, 6.4
2. Если выполняется параллельно с другими агентами:
   - Работать в отдельной ветке
   - Merge после завершения всех этапов 6.x
3. **НЕ коммитить** до завершения всех этапов 6.x и валидации
4. **НЕ обновлять CHANGELOG** на этом этапе (будет в этапе 8)

---

## ⚠️ Возможные проблемы

**Проблема:** README уже обновлен на этапе 2
**Решение:** Это ожидаемо. Просто проверить что все корректно.

**Проблема:** Найдены упоминания в комментариях
**Решение:** Обновить комментарии для консистентности

---

## ➡️ Следующие шаги

После завершения этого этапа:
- Если работаешь **последовательно**: перейти к `6.3_update_search_docs_parallel.md`
- Если работаешь **параллельно**: дождаться завершения этапов 6.1, 6.3, 6.4 перед валидацией
