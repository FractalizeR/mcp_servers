# План переименования @mcp-framework/core → @mcp-framework/tooling

## 🎯 Цель

Переименовать пакет `@mcp-framework/core` в `@mcp-framework/tooling` для более точного отражения его назначения (tool framework, а не общее ядро).

## 📋 Общая структура плана

### Этап 1: Подготовка (sequential) ✓
**Файл:** `1_preparation_sequential.md`
- Валидация текущего состояния
- Создание резервной ветки
- Проверка чистоты рабочей директории

### Этап 2: Переименование файловой структуры (sequential) ✓
**Файл:** `2_rename_structure_sequential.md`
- Физическое переименование директории
- Обновление package.json в tooling пакете
- Обновление exports и paths

### Этап 3: Обновление зависимостей (sequential) ✓
**Файл:** `3_update_dependencies_sequential.md`
- Обновить dependencies в search
- Обновить dependencies в yandex-tracker
- Обновить root package.json workspaces

### Этап 4: Обновление импортов (parallel) ⚡
**Файлы:**
- `4.1_update_imports_search_parallel.md`
- `4.2_update_imports_yandex_tracker_parallel.md`

Можно выполнять одновременно в разных ветках/сессиях.

### Этап 5: Обновление конфигурации (parallel) ⚡
**Файлы:**
- `5.1_update_depcruise_config_parallel.md`
- `5.2_update_turbo_config_parallel.md`
- `5.3_update_tsconfig_parallel.md`

Можно выполнять одновременно в разных ветках/сессиях.

### Этап 6: Обновление документации (parallel) ⚡
**Файлы:**
- `6.1_update_root_docs_parallel.md`
- `6.2_update_tooling_docs_parallel.md`
- `6.3_update_search_docs_parallel.md`
- `6.4_update_yandex_tracker_docs_parallel.md`

Можно выполнять одновременно в разных ветках/сессиях.

### Этап 7: Валидация (sequential) ✓
**Файл:** `7_validation_sequential.md`
- Запуск тестов для всех пакетов
- Проверка dependency graph
- Smoke tests

### Этап 8: Финализация (sequential) ✓
**Файл:** `8_finalization_sequential.md`
- Bump версий пакетов
- Обновление CHANGELOG
- Создание коммита и push

## 🔄 Стратегия выполнения

### Вариант 1: Последовательное выполнение (одна сессия)
```
1 → 2 → 3 → 4.1+4.2 → 5.1+5.2+5.3 → 6.1+6.2+6.3+6.4 → 7 → 8
```
**Время:** ~4 часа в одной сессии

### Вариант 2: Параллельное выполнение (несколько агентов)
```
Агент 1: 1 → 2 → 3 → [wait] → merge → 7 → 8
Агент 2:              4.1 → 5.1 → 6.1, 6.2
Агент 3:              4.2 → 5.2, 5.3 → 6.3, 6.4
```
**Время:** ~2 часа с параллелизацией

## ⚠️ Критические правила

1. **Этапы 1-3 ОБЯЗАТЕЛЬНО sequential** — создают основу для остальных
2. **Этапы 4-6 можно parallel** — независимы друг от друга
3. **Этапы 7-8 ОБЯЗАТЕЛЬНО sequential после всех** — валидация и коммит
4. **После каждого этапа:** проверить что код компилируется (если применимо)
5. **НЕ коммитить** до завершения этапа 8
6. **При параллельном выполнении:** использовать разные ветки, merge в конце этапа 6

## 📊 Метрики успеха

- [ ] Все тесты проходят: `npm run validate`
- [ ] Dependency graph валиден: `npm run depcruise`
- [ ] Нет упоминаний `@mcp-framework/core` в коде (кроме CHANGELOG)
- [ ] Документация обновлена и консистентна
- [ ] Версии пакетов обновлены (breaking change)
- [ ] Build проходит успешно: `npm run build`

## 🔗 Связанные файлы

После завершения плана, следующие файлы будут изменены:
- ~300+ TypeScript файлов (импорты)
- ~10 package.json файлов
- ~15 документационных файлов
- ~3 конфигурационных файла

## 📝 Примечания

- План НЕ содержит примеров кода — только инструкции
- Каждый шаг содержит чек-листы для отслеживания прогресса
- Для автоматизации использовать find/sed/awk где возможно
- Валидация после каждого критического этапа обязательна
