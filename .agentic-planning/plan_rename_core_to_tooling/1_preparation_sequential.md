# Этап 1: Подготовка (SEQUENTIAL)

**Execution Type:** Sequential
**Зависимости:** Нет
**Можно начинать:** Сразу
**Ожидаемое время:** 10 минут

---

## 🎯 Цель этапа

Подготовить проект к переименованию: проверить текущее состояние, создать точку восстановления, убедиться в отсутствии конфликтов.

---

## ✅ Чек-лист выполнения

### 1.1 Проверка текущего состояния репозитория

- [ ] Выполнить `git status` и убедиться что нет незакоммиченных изменений
- [ ] Если есть изменения — закоммитить или stash перед началом
- [ ] Убедиться что текущая ветка `claude/analyze-framework-core-*`
- [ ] Проверить что все удаленные изменения синхронизированы: `git fetch origin`

**Команды:**
```bash
git status
git fetch origin
```

### 1.2 Валидация текущей сборки

- [ ] Убедиться что проект собирается: `npm run build`
- [ ] Запустить валидацию: `npm run validate:quiet`
- [ ] Если есть ошибки — исправить перед началом переименования

**Команды:**
```bash
npm run build
npm run validate:quiet
```

**Ожидаемый результат:** Все проверки проходят успешно.

### 1.3 Проверка текущей структуры

- [ ] Убедиться что директория `packages/framework/core` существует
- [ ] Убедиться что пакет называется `@mcp-framework/core` в package.json
- [ ] Проверить что нет директории `packages/framework/tooling`

**Команды:**
```bash
ls -la packages/framework/core
cat packages/framework/core/package.json | grep '"name"'
ls packages/framework/ | grep tooling
```

**Ожидаемый результат:**
- `packages/framework/core` существует
- package.json содержит `"name": "@mcp-framework/core"`
- `tooling` директории нет

### 1.4 Анализ зависимостей

- [ ] Найти все пакеты, зависящие от `@mcp-framework/core`
- [ ] Подсчитать количество файлов с импортами из core
- [ ] Создать список затрагиваемых пакетов

**Команды для поиска:**
```bash
# Пакеты с зависимостью в package.json
grep -r "@mcp-framework/core" packages/*/package.json

# Файлы с импортами
grep -r "from '@mcp-framework/core" packages/ --include="*.ts" | wc -l
```

**Ожидаемый результат:**
- Зависимости найдены в: search, yandex-tracker
- Количество файлов с импортами: ~300+

### 1.5 Создание точки восстановления

- [ ] Создать тег для текущего состояния (на случай отката)
- [ ] Задокументировать начальное состояние

**Команды:**
```bash
git tag -a backup-before-rename -m "Backup before core→tooling rename"
git log --oneline -1
```

**Ожидаемый результат:** Тег создан, можно откатиться командой `git reset --hard backup-before-rename`

### 1.6 Проверка конфигурационных файлов

- [ ] Найти все упоминания "core" в конфигурационных файлах
- [ ] Создать список файлов для обновления

**Файлы для проверки:**
- `.dependency-cruiser.cjs` — dependency rules
- `turbo.json` — build pipeline
- `tsconfig.json` (root) — project references
- Package `tsconfig.json` файлы с path references

**Команды:**
```bash
grep -n "core" .dependency-cruiser.cjs
grep -n "core" turbo.json
grep -n "@mcp-framework/core" tsconfig*.json
```

---

## 📊 Критерии завершения этапа

- ✅ Git репозиторий в чистом состоянии (no uncommitted changes)
- ✅ Текущая сборка успешна (`npm run build`)
- ✅ Валидация проходит (`npm run validate:quiet`)
- ✅ Создана точка восстановления (git tag)
- ✅ Список зависимых пакетов составлен
- ✅ Список конфигурационных файлов для обновления готов

---

## 🚨 Важные замечания

1. **НЕ переходить к следующему этапу** если есть незакоммиченные изменения
2. **НЕ начинать переименование** если валидация не проходит
3. **Сохранить вывод команд** для документирования процесса
4. Этот этап **ОБЯЗАТЕЛЬНО sequential** — база для всех остальных

---

## ➡️ Следующий этап

После успешного завершения перейти к: `2_rename_structure_sequential.md`
