# Этап 6.3: Обновление документации search пакета (PARALLEL)

**Execution Type:** Parallel
**Зависимости:** Этапы 1-5 завершены
**Можно начинать:** После успешного завершения этапов 4.x и 5.x
**Можно выполнять параллельно с:** 6.1, 6.2, 6.4
**Ожидаемое время:** 10 минут

---

## 🎯 Цель этапа

Обновить документацию search пакета для отражения изменения зависимости с core на tooling.

---

## ✅ Чек-лист выполнения

### 6.3.1 Обновление README.md в search пакете

**Файл:** `packages/framework/search/README.md`

- [ ] Найти все упоминания `@mcp-framework/core`
- [ ] Заменить на `@mcp-framework/tooling`
- [ ] Обновить примеры кода
- [ ] Обновить секцию Dependencies

**Команды для поиска:**
```bash
cd packages/framework/search
grep -n "@mcp-framework/core" README.md
```

**Секции для обновления:**

1. **Dependencies секция:**
   ```markdown
   **Dependencies:**
   - `@mcp-framework/tooling` (ToolRegistry, tool types)
   - `@mcp-framework/infrastructure` (HTTP, logging)
   ```

2. **Примеры импортов:**
   ```typescript
   import type { ToolRegistry } from '@mcp-framework/tooling';
   ```

3. **Usage примеры (если есть):**
   ```typescript
   import { ToolSearchEngine } from '@mcp-framework/search';
   import { ToolRegistry } from '@mcp-framework/tooling';
   ```

4. **Ссылки на другие пакеты:**
   ```markdown
   - **Tooling package:** [../tooling/README.md](../tooling/README.md)
   ```

### 6.3.2 Проверка CHANGELOG.md

**Файл:** `packages/framework/search/CHANGELOG.md`

- [ ] **НЕ изменять** CHANGELOG на этом этапе
- [ ] Запланировать обновление на этапе 8

### 6.3.3 Проверка других markdown файлов

- [ ] Найти все .md файлы в search пакете
- [ ] Проверить каждый на упоминания core

**Команды:**
```bash
cd packages/framework/search
find . -name "*.md" -not -path "*/node_modules/*" -not -path "*/dist/*"
```

**Для каждого найденного файла:**
```bash
grep -n "@mcp-framework/core" <файл>
```

### 6.3.4 Проверка комментариев в коде

- [ ] Проверить комментарии и JSDoc на упоминания старого пакета

**Команды:**
```bash
cd packages/framework/search/src
grep -r "@mcp-framework/core" . --include="*.ts" | grep -E "//|/\*"
```

**Если найдены:**
- Обновить для консистентности

### 6.3.5 Проверка типов и интерфейсов

Проверить что в type comments нет упоминаний старого пакета:

- [ ] Проверить JSDoc с `@see` тегами
- [ ] Проверить примеры в комментариях

**Команды:**
```bash
cd packages/framework/search/src
grep -r "@see.*core" . --include="*.ts"
grep -r "@example" . --include="*.ts" -A10 | grep "@mcp-framework/core"
```

### 6.3.6 Валидация ссылок

- [ ] Проверить что все ссылки в README валидны
- [ ] Убедиться что файлы по ссылкам существуют

**Ключевые ссылки:**
```bash
ls ../tooling/README.md
ls ../infrastructure/README.md
ls ../../README.md
ls ../../ARCHITECTURE.md
```

### 6.3.7 Валидация размера README

- [ ] Проверить размер README.md
- [ ] Убедиться что не превышен лимит (600 строк)

**Команды:**
```bash
cd packages/framework/search
wc -l README.md
```

### 6.3.8 Финальная проверка

- [ ] Убедиться что все упоминания обновлены
- [ ] Никаких ссылок на старое название не осталось

**Команды:**
```bash
cd packages/framework/search
grep -r "@mcp-framework/core" . --include="*.md" --include="*.ts" --exclude-dir=node_modules --exclude-dir=dist
```

**Ожидаемый результат:** Нет совпадений (empty output).

---

## 📊 Критерии завершения этапа

- ✅ `README.md` обновлен
- ✅ Dependencies секция обновлена
- ✅ Все примеры импортов используют `@mcp-framework/tooling`
- ✅ Комментарии в коде проверены
- ✅ Все ссылки валидны
- ✅ Размер README в пределах лимита
- ✅ Нет упоминаний `@mcp-framework/core` в пакете (кроме CHANGELOG)

---

## 🚨 Важные замечания

1. Этот этап **можно выполнять параллельно** с этапами 6.1, 6.2, 6.4
2. Если выполняется параллельно с другими агентами:
   - Работать в отдельной ветке
   - Merge после завершения всех этапов 6.x
3. **НЕ коммитить** до завершения всех этапов 6.x и валидации
4. **НЕ обновлять CHANGELOG** на этом этапе (будет в этапе 8)

---

## ⚠️ Возможные проблемы

**Проблема:** Много упоминаний в комментариях
**Решение:** Обновить все для консистентности

**Проблема:** Битые ссылки на tooling пакет
**Решение:** Проверить что этап 2 завершен (директория переименована)

---

## ➡️ Следующие шаги

После завершения этого этапа:
- Если работаешь **последовательно**: перейти к `6.4_update_yandex_tracker_docs_parallel.md`
- Если работаешь **параллельно**: дождаться завершения этапов 6.1, 6.2, 6.4 перед валидацией
