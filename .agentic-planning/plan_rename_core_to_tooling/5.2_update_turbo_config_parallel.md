# Этап 5.2: Обновление Turborepo конфигурации (PARALLEL)

**Execution Type:** Parallel
**Зависимости:** Этап 3 завершен
**Можно начинать:** После успешного завершения этапа 3
**Можно выполнять параллельно с:** 4.1, 4.2, 5.1, 5.3
**Ожидаемое время:** 5 минут

---

## 🎯 Цель этапа

Проверить и при необходимости обновить `turbo.json` конфигурацию для корректной работы с переименованным пакетом.

---

## ✅ Чек-лист выполнения

### 5.2.1 Открыть конфигурационный файл

- [ ] Открыть `turbo.json` в корне проекта
- [ ] Проверить структуру конфигурации

**Файл:** `turbo.json`

**Команды:**
```bash
cat turbo.json
```

### 5.2.2 Поиск упоминаний "core"

- [ ] Найти все упоминания "core" в turbo.json
- [ ] Определить требуется ли обновление

**Команды:**
```bash
grep -n "core" turbo.json
```

**Ожидаемый результат:**
- Возможно нет упоминаний (Turborepo обычно использует glob patterns)
- Если есть упоминания — обновить их

### 5.2.3 Проверка pipeline конфигурации

Проверить секцию `pipeline` на наличие package-specific конфигураций.

- [ ] Проверить что нет жестко закодированных ссылок на `@mcp-framework/core`
- [ ] Убедиться что используются glob patterns или workspace references

**Типичная структура pipeline:**
```json
{
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": ["dist/**"]
    },
    "test": {
      "dependsOn": ["build"]
    }
    // ... другие tasks
  }
}
```

**Ожидаемый результат:** Pipeline использует generic patterns — **НЕ требует изменений**.

### 5.2.4 Проверка task configurations

- [ ] Проверить секции для отдельных tasks
- [ ] Убедиться что нет package-specific overrides для core

**Команды:**
```bash
grep -A5 -B5 "core" turbo.json
```

**Если найдены упоминания:**
- Заменить `@mcp-framework/core` на `@mcp-framework/tooling`
- Заменить `packages/framework/core` на `packages/framework/tooling`

### 5.2.5 Проверка cache конфигурации

- [ ] Проверить секцию cache/remoteCache
- [ ] Убедиться что нет специфичных правил для core пакета

**Ожидаемый результат:** Cache конфигурация generic — не требует изменений.

### 5.2.6 Валидация конфигурации

- [ ] Очистить Turborepo cache
- [ ] Запустить build для проверки конфигурации

**Команды:**
```bash
# Очистить cache
rm -rf .turbo

# Запустить build через Turborepo
npm run build
```

**Ожидаемый результат:**
- Build проходит успешно
- Все пакеты собираются в правильном порядке
- Новый cache создается корректно

### 5.2.7 Проверка topological order

- [ ] Убедиться что Turborepo правильно определяет порядок сборки
- [ ] tooling должен собираться раньше search и yandex-tracker

**Команды для проверки порядка:**
```bash
npm run build -- --dry-run
# или
turbo run build --dry-run
```

**Ожидаемый порядок:**
```
1. infrastructure
2. tooling (было: core)
3. search
4. yandex-tracker
```

### 5.2.8 Финальная проверка

- [ ] Убедиться что нет упоминаний старого названия
- [ ] Конфигурация валидна

**Команды:**
```bash
grep -i "@mcp-framework/core" turbo.json
grep -i "framework/core" turbo.json
```

**Ожидаемый результат:** Нет совпадений (empty output).

---

## 📊 Критерии завершения этапа

- ✅ `turbo.json` проверен на упоминания "core"
- ✅ Все package-specific references обновлены (если были)
- ✅ Turborepo cache очищен
- ✅ `npm run build` проходит успешно
- ✅ Topological order корректен
- ✅ Нет упоминаний старого названия в конфигурации

---

## 🚨 Важные замечания

1. Этот этап **можно выполнять параллельно** с этапами 4.x, 5.1, 5.3
2. Если выполняется параллельно с другими агентами:
   - Работать в отдельной ветке
   - Merge после завершения всех параллельных этапов
3. **НЕ коммитить** до завершения этапа 6 (или координации с другими агентами)
4. В большинстве случаев `turbo.json` **НЕ требует изменений** (использует glob patterns)

---

## ⚠️ Возможные проблемы

**Проблема:** Build не проходит через Turborepo
**Решение:**
- Проверить что этапы 2-4 завершены (пакет переименован, dependencies и imports обновлены)
- Очистить cache: `rm -rf .turbo node_modules/.cache`
- Запустить `npm install`

**Проблема:** Неправильный порядок сборки
**Решение:**
- Проверить dependencies в package.json файлах
- Turborepo автоматически определяет порядок по dependencies

---

## ➡️ Следующие шаги

После завершения этого этапа:
- Если работаешь **последовательно**: перейти к `5.3_update_tsconfig_parallel.md`
- Если работаешь **параллельно**: дождаться завершения других параллельных этапов перед merge
