# Этап 3: Обновление зависимостей (SEQUENTIAL)

**Execution Type:** Sequential
**Зависимости:** Этап 2 завершен
**Можно начинать:** После успешного завершения этапа 2
**Ожидаемое время:** 15 минут

---

## 🎯 Цель этапа

Обновить зависимости во всех пакетах, которые используют `@mcp-framework/core`, заменив их на `@mcp-framework/tooling`.

---

## ✅ Чек-лист выполнения

### 3.1 Поиск всех зависимостей

- [ ] Найти все package.json файлы с зависимостью от `@mcp-framework/core`
- [ ] Составить список файлов для обновления

**Команды:**
```bash
grep -r "@mcp-framework/core" packages/*/package.json
```

**Ожидаемые файлы:**
- `packages/framework/search/package.json`
- `packages/servers/yandex-tracker/package.json`
- Возможно корневой `package.json` (workspaces)

### 3.2 Обновление dependencies в search пакете

Обновить `packages/framework/search/package.json`:

- [ ] Заменить `@mcp-framework/core` на `@mcp-framework/tooling` в dependencies
- [ ] Проверить что версия зависимости корректна (обычно `"*"` для monorepo)

**Файл:** `packages/framework/search/package.json`

**Секция dependencies:**
```json
{
  "dependencies": {
    "@mcp-framework/tooling": "*",  // было: @mcp-framework/core
    "@mcp-framework/infrastructure": "*",
    // ... остальные зависимости
  }
}
```

**Команды для проверки:**
```bash
grep "@mcp-framework" packages/framework/search/package.json
```

### 3.3 Обновление dependencies в yandex-tracker пакете

Обновить `packages/servers/yandex-tracker/package.json`:

- [ ] Заменить `@mcp-framework/core` на `@mcp-framework/tooling` в dependencies
- [ ] Проверить что версия зависимости корректна

**Файл:** `packages/servers/yandex-tracker/package.json`

**Секция dependencies:**
```json
{
  "dependencies": {
    "@mcp-framework/cli": "*",
    "@mcp-framework/infrastructure": "*",
    "@mcp-framework/search": "*",
    "@mcp-framework/tooling": "*",  // было: @mcp-framework/core
    // ... остальные зависимости
  }
}
```

**Команды для проверки:**
```bash
grep "@mcp-framework" packages/servers/yandex-tracker/package.json
```

### 3.4 Проверка корневого package.json

- [ ] Открыть корневой `package.json`
- [ ] Проверить секцию `workspaces`
- [ ] Убедиться что workspaces использует glob patterns (не жестко закодированные имена)

**Файл:** `package.json` (root)

**Секция workspaces:**
```json
{
  "workspaces": [
    "packages/framework/*",  // glob pattern — не требует изменений
    "packages/servers/*"     // glob pattern — не требует изменений
  ]
}
```

**Ожидаемый результат:** Workspaces используют glob patterns — **НЕ требуют изменений**.

**Команды для проверки:**
```bash
cat package.json | grep -A5 "workspaces"
```

### 3.5 Переустановка зависимостей

- [ ] Удалить node_modules и lock файлы для чистой установки
- [ ] Запустить `npm install` в корне проекта
- [ ] Убедиться что установка прошла успешно

**Команды:**
```bash
rm -rf node_modules package-lock.json
rm -rf packages/*/node_modules
npm install
```

**Ожидаемый результат:**
- Все пакеты установлены
- Symlinks в node_modules обновлены
- `@mcp-framework/tooling` доступен для других пакетов

### 3.6 Проверка symlinks

- [ ] Проверить что symlink создан корректно в node_modules

**Команды:**
```bash
ls -la node_modules/@mcp-framework/ | grep tooling
ls -la packages/framework/search/node_modules/@mcp-framework/ | grep tooling
```

**Ожидаемый результат:**
- `tooling` symlink существует
- `core` symlink больше не существует

### 3.7 Валидация package.json файлов

- [ ] Проверить что все package.json валидны после изменений
- [ ] Убедиться что нет синтаксических ошибок

**Команды:**
```bash
cd packages/framework/search && npm pkg get dependencies
cd packages/servers/yandex-tracker && npm pkg get dependencies
```

### 3.8 Финальная проверка зависимостей

- [ ] Убедиться что нигде не осталось упоминаний `@mcp-framework/core`
- [ ] Проверить все package.json файлы

**Команды:**
```bash
grep -r "@mcp-framework/core" packages/*/package.json
grep -r "@mcp-framework/core" package.json
```

**Ожидаемый результат:** Нет совпадений (empty output).

---

## 📊 Критерии завершения этапа

- ✅ `packages/framework/search/package.json` обновлен
- ✅ `packages/servers/yandex-tracker/package.json` обновлен
- ✅ Корневой `package.json` проверен (workspaces не требуют изменений)
- ✅ `npm install` выполнен успешно
- ✅ Symlinks созданы корректно
- ✅ Нет упоминаний `@mcp-framework/core` в package.json файлах

---

## 🚨 Важные замечания

1. **НЕ обновлять imports** на этом этапе (будет в этапах 4.x)
2. **НЕ запускать build/test** на этом этапе (imports еще сломаны)
3. После этого этапа зависимости обновлены, но **imports в .ts файлах еще сломаны**
4. Этот этап **ОБЯЗАТЕЛЬНО sequential** — следующие этапы зависят от обновленных dependencies

---

## ⚠️ Ожидаемые проблемы

После этого этапа:
- ✅ Dependencies обновлены
- ✅ npm install работает
- ❌ `npm run build` все еще ЛОМАЕТСЯ (imports не обновлены) — это нормально
- ❌ TypeScript errors в IDE — это нормально

**НЕ паниковать!** Следующие этапы исправят imports.

---

## ➡️ Следующие этапы (можно параллельно)

После успешного завершения **МОЖНО** выполнять параллельно:

1. **Этап 4: Обновление импортов**
   - `4.1_update_imports_search_parallel.md`
   - `4.2_update_imports_yandex_tracker_parallel.md`

2. **Этап 5: Обновление конфигурации**
   - `5.1_update_depcruise_config_parallel.md`
   - `5.2_update_turbo_config_parallel.md`
   - `5.3_update_tsconfig_parallel.md`

Если выполнять **последовательно**, начать с этапов 4.x.
