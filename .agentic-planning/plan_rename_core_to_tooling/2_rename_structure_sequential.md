# Этап 2: Переименование файловой структуры (SEQUENTIAL)

**Execution Type:** Sequential
**Зависимости:** Этап 1 завершен
**Можно начинать:** После успешного завершения этапа 1
**Ожидаемое время:** 15 минут

---

## 🎯 Цель этапа

Физически переименовать директорию пакета и обновить его внутренние метаданные (package.json, exports, paths).

---

## ✅ Чек-лист выполнения

### 2.1 Переименование директории

- [ ] Переименовать `packages/framework/core` → `packages/framework/tooling`
- [ ] Убедиться что переименование прошло успешно
- [ ] Проверить что старая директория больше не существует

**Команды:**
```bash
cd /home/user/mcp_server_yandex_tracker
mv packages/framework/core packages/framework/tooling
ls packages/framework/ | grep -E "(core|tooling)"
```

**Ожидаемый результат:**
- Директория `tooling` существует
- Директория `core` не существует

### 2.2 Обновление package.json основного пакета

Обновить `packages/framework/tooling/package.json`:

- [ ] Изменить `"name"` с `@mcp-framework/core` на `@mcp-framework/tooling`
- [ ] Обновить `"description"` для отражения нового назначения
- [ ] НЕ изменять версию (это будет в этапе 8)

**Что обновить:**

```json
{
  "name": "@mcp-framework/tooling",  // было: @mcp-framework/core
  "description": "MCP tools framework: base classes, registry, utilities for building tools"  // обновить описание
}
```

**Команда для проверки:**
```bash
cat packages/framework/tooling/package.json | grep -E '"name"|"description"'
```

### 2.3 Обновление keywords в package.json

- [ ] Проверить секцию `keywords` в package.json
- [ ] Добавить "tooling" если нужно
- [ ] Убрать "core" если есть

**Секция keywords должна содержать:**
```json
"keywords": [
  "mcp",
  "model-context-protocol",
  "framework",
  "tools",
  "tooling",
  "ai"
]
```

### 2.4 Проверка exports в package.json

- [ ] Убедиться что exports не содержат жестко закодированных путей к "core"
- [ ] Все exports должны использовать относительные пути

**Проверить секцию exports:**
```json
{
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    },
    "./tools": {
      "types": "./dist/tools/index.d.ts",
      "default": "./dist/tools/index.js"
    },
    "./utils": {
      "types": "./dist/utils/index.d.ts",
      "default": "./dist/utils/index.js"
    },
    "./*": "./dist/*"
  }
}
```

**Ожидаемый результат:** Все exports используют `./dist/*` (относительные пути) — не требуют изменений.

### 2.5 Обновление README.md в tooling пакете

- [ ] Обновить заголовок: `# @mcp-framework/tooling`
- [ ] Обновить badge с названием пакета
- [ ] Обновить инструкции установки

**Что обновить в README.md:**

1. Заголовок:
   - Было: `# @mcp-framework/core`
   - Стало: `# @mcp-framework/tooling`

2. Описание:
   - Было: `Core framework for building MCP tools: base classes, utilities, registry`
   - Стало: `MCP tools framework: base classes, registry, utilities for building tools`

3. Badges:
   - Обновить npm badge URL (если есть)

4. Installation секция:
   ```bash
   npm install @mcp-framework/tooling
   ```

5. Import примеры во всем README:
   ```typescript
   import { BaseTool } from '@mcp-framework/tooling';
   ```

**Команды для поиска всех упоминаний:**
```bash
grep -n "@mcp-framework/core" packages/framework/tooling/README.md
```

### 2.6 Проверка CHANGELOG.md

- [ ] НЕ изменять CHANGELOG.md на этом этапе
- [ ] Запланировать обновление на этапе 8 (финализация)

### 2.7 Проверка других файлов в директории tooling

Проверить следующие файлы на упоминания "core":

- [ ] `CHANGELOG.md` — оставить как есть
- [ ] `.npmignore` — проверить отсутствие упоминаний "core"
- [ ] Любые скрипты в директории пакета

**Команды:**
```bash
cd packages/framework/tooling
grep -r "core" . --include="*.json" --include="*.md" --exclude-dir=node_modules --exclude-dir=dist
```

### 2.8 Валидация структуры

- [ ] Проверить что package.json валиден: `npm pkg get name` в директории tooling
- [ ] Убедиться что нет синтаксических ошибок в package.json

**Команды:**
```bash
cd packages/framework/tooling
npm pkg get name
npm pkg get description
cat package.json | jq '.'  # если jq установлен
```

---

## 📊 Критерии завершения этапа

- ✅ Директория переименована: `packages/framework/tooling` существует
- ✅ Старая директория удалена: `packages/framework/core` не существует
- ✅ `package.json` обновлен: name = `@mcp-framework/tooling`
- ✅ `README.md` обновлен: все примеры используют `@mcp-framework/tooling`
- ✅ Keywords обновлены
- ✅ Exports в package.json проверены (не требуют изменений)
- ✅ package.json валиден (no syntax errors)

---

## 🚨 Важные замечания

1. **НЕ обновлять версию** на этом этапе (только в этапе 8)
2. **НЕ обновлять CHANGELOG** на этом этапе (только в этапе 8)
3. **НЕ запускать build/test** на этом этапе (будет сломано до обновления зависимостей)
4. Этот этап **ОБЯЗАТЕЛЬНО sequential** — создает основу для обновления зависимостей

---

## ⚠️ Ожидаемые проблемы

После этого этапа:
- ❌ `npm run build` будет ЛОМАТЬСЯ — это нормально
- ❌ Импорты в других пакетах будут СЛОМАНЫ — это нормально
- ✅ Следующий этап исправит зависимости

**НЕ паниковать!** Продолжить с этапом 3.

---

## ➡️ Следующий этап

После успешного завершения перейти к: `3_update_dependencies_sequential.md`
