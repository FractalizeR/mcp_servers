# Этап 5. Resources и Prompts (parallel)

Зависит от этапов 3 и 4: Resources требуют `structuredContent` и владельца cache-полей в adapter.

Рабочее дерево: пакеты по серверам — **основной чекаут** при strict file isolation; framework-часть
(5.1.A) делается первой и последовательно.

## Пакет 5.1.A — Resources в framework (sequential)

Новые методы у adapter: `resources/list`, `resources/read`, `resources/templates/list`. Все три обязаны
возвращать `ttlMs` и `cacheScope` — это требование спеки, владелец тот же adapter.

```
ResourceProvider
  listResources(cursor?): { resources[], nextCursor? }
  readResource(uri): { contents[] }
  listTemplates(): ResourceTemplate[]
```

- Пагинация `resources/list` — тем же opaque-cursor, что уже используется в list-эндпоинтах Трекера.
- Ошибка «ресурс не найден» — код **`-32602`**, а не `-32002`: код сменился в этой ревизии.
- Регистрация провайдеров — как у инструментов, через composition root.

**Факты исполнения (пакет 5.1.A, принят).**

- **Требование «тот же курсор» было сформулировано неисполнимо.** `CursorCodec` живёт в Трекере, а
  граф зависимостей монорепо запрещает импорт из сервера во framework — переиспользовать его
  физически нельзя, да и его полезная нагрузка (HTTP-путь Трекера) не обобщается. Исполнитель
  воспроизвёл **тот же механизм**: версионный префикс, base64url(JSON), тег семейства, явное
  исключение на битом курсоре вместо тихого отката на первую страницу. Принято; требование в плане
  надо было формулировать как «тот же механизм», а не «тот же код».
- **`-32602` даёт сам SDK:** `ResourceNotFoundError` из `@modelcontextprotocol/server` сериализуется
  этим кодом на любой ревизии, а `-32002` в этой версии SDK только принимается, но никогда не
  отдаётся. Проверено сквозным тестом, а не по документации. Свой код с числом не заводили.
- **`server.connect(transport)` не регистрирует `server/discover`** и не делает cache-стемпинг
  2026-эпохи: это поведение обёртки `serveStdio`, а не самого `Server`. Вскрылось тем, что первая
  версия сквозного теста упала с `-32601`. Тест переписан на `serveStdio` с подставным транспортом.
- **Контракт для волны 5.1.C:** `ResourceProvider { id, listResources(cursor?), readResource(uri),
  listTemplates() }`, где `readResource` возвращающий `undefined` означает «не мой URI», а не ошибку,
  и обязан разрешать любой URI своей схемы напрямую — независимо от того, был ли он в списке.
  Регистрация — `ResourceRegistry`, зеркалит `ToolRegistry`; поле в опциях adapter опциональное,
  поэтому три существующих `server.ts` не сломаны, а capability `resources` объявляется безусловно и
  отвечает честно, а не `-32601`.
- Сборка `Server` вынесена из `create-mcp-server-adapter.ts` в `build-mcp-server.ts` — механическая
  экстракция ради тестируемости, поведение `tools/list` и `tools/call` не менялось.

## Пакет 5.1.B — ResourceLink в результатах инструментов (главный выигрыш)

**Проблема, которую решаем.** `find_issues` на 200 задач сейчас вываливает 200 объектов в контекст
модели. Это дорого и вытесняет всё остальное.

**Решение.** Инструменты, возвращающие коллекции, отдают компактную сводку плюс массив
`resource_link` вместо полных тел. Агент подтягивает тела выборочно через `resources/read`, который
к тому же кешируется по `ttlMs`.

Схемы URI (проектируются здесь, множество порождается планом):
- `tracker://issue/{key}`, `tracker://queue/{key}`, `tracker://project/{id}`
- `wiki://page/{slug}`
- `ticktick://task/{id}`, `ticktick://project/{id}`

**Граничные условия.**
- Инструмент должен уметь оба режима: ссылки и полные тела. Управление — параметром, а не глобальной
  настройкой: агент сам знает, нужны ли ему тела.
- Режим по умолчанию — решение с последствиями. Ссылки экономят контекст, но добавляют круг
  «список → чтение»; для маленьких результатов это чистый проигрыш. Разумный дефолт — порог по
  количеству элементов, и он должен быть виден в описании инструмента.
- `resource_link`, возвращённый инструментом, **не обязан** появляться в `resources/list` — спека это
  прямо разрешает. Значит `readResource` должен уметь отдать то, чего нет в списке.

## Пакеты 5.1.C.{tracker,wiki,ticktick} — провайдеры и prompts (parallel)

Наборы файлов не пересекаются.

**Resources** каждого сервера: сущности из схем URI выше.

**Prompts** — самый дешёвый способ вовлечь команду: в клиенте они выглядят как слэш-команды и не
требуют нового API, это обёртки над существующими инструментами.

- Трекер: triage очереди, дейли-сводка, сводка по спринту/проекту, разбор связей эпика.
- Вики: сводка раздела, подготовка обновления документа.
- TickTick: дневной и недельный обзор, GTD-разбор входящих.

Prompts тоже возвращают `ttlMs`/`cacheScope` в `prompts/list` — владелец adapter.

**Уточнено при исполнении: только в `prompts/list`, и это не мелочь формулировки.** Спека 2026-07-28
(SEP-2549) намеренно не включает `prompts/get` в кешируемые результаты — `GetPromptResult` не
наследует `CacheableResult`, потому что результат зависит от аргументов. SDK отражает это закрытым
типом `CacheableResultMethod`, куда `'prompts/get'` не входит: попытка добавить даёт ошибку
компиляции, а не молчаливый no-op. Проставлять cache-поля на `prompts/get` вручную в обход типизации
нельзя — посредник может закешировать ответ, который кешировать запрещено. Зафиксировано тестом,
утверждающим ОТСУТСТВИЕ этих полей, иначе их вернут обратно «для единообразия».

**Границы для исполнителя.** Формулировки самих промптов — на усмотрение исполнителя, но каждый
обязан быть проверен вручную хотя бы один раз на реальных данных: промпт, который не проверяли,
почти всегда даёт не тот результат, который задумывался.

## Факты исполнения по серверам

**Вики (пакет 5.1.C.wiki, принят).**

- Два провайдера: страницы по `wiki://page/{slug}` и не-табличные ресурсы страницы по
  `wiki://page-resource/{pageId}/{type}/{name}`. Второй заведён потому, что линковать вложение через
  `wiki://page/{slug}` семантически неверно — у вложения нет слага.
- `listResources()` у страниц **честно пуст**: в реализованной части API Вики нет ручки «список всех
  страниц», только точечное чтение. Контракт это допускает — `readResource` обязан резолвить URI
  напрямую независимо от списка. То есть страницы Вики достижимы по ссылке, но не перечисляются.
- Таблицы заморозку соблюли: провайдер отклоняет URI с `grid`, а инструмент кладёт табличные
  элементы отдельным полем сводки, мимо механизма ссылок. Проверяется тестами явно.
- `description` из дескриптора ссылки убран: с ним экономия объёма не дотягивала до заявленной.
  Предупреждение плана «для маленьких результатов ссылки — чистый проигрыш» оказалось верным и на
  уровне размера одного дескриптора, а не только всей коллекции.

**Трекер (пакет 5.1.C.tracker, принят).**

- Три провайдера: `tracker://issue/{key}`, `tracker://queue/{key}`, `tracker://project/{id}`.
  Задачи намеренно **не перечисляются** в `listResources()` — у них нет естественной границы без
  критериев поиска, и перечислять их значило бы сделать ресурсом всё подряд, ровно как предупреждает
  план. Очереди и проекты перечисляются через уже существующую курсорную пагинацию Трекера, её
  курсор пробрасывается как есть — второй слой кодирования не нужен, реестр оборачивает его в свой.
- `404` от API превращается в «не мой URI», а `5xx` и сетевые ошибки пробрасываются. Разница
  существенна: маскировать сбой под «не найдено» — значит врать вызывающему.
- **Измеренная экономия на мотивирующем случае плана:** `find_issues` на 200 задачах — 204 801 байт
  телами против 40 782 байт ссылками, то есть 80%. Тест закрепляет консервативный порог (впятеро),
  чтобы не сломаться от изменения размера сущности.
- `key` и `summary` теперь включаются в выборку полей всегда, даже если агент их не просил: без них
  у `resource_link` не было бы ни адреса, ни заголовка.
- `get_queues` и `get_projects` на ссылки НЕ переведены осознанно: их списки типично малы, выигрыш
  ниже порога окупаемости. Дешёвый кандидат на отдельный пакет, если понадобится.

**Находка: первая страница `resources/list` у Трекера пуста.** Реестр отдаёт одну страницу ОДНОГО
провайдера за вызов, провайдеры обходятся по порядку id, а `tracker-issues` идёт первым и всегда
пуст. Значит первый же `resources/list` без курсора возвращает пустой список с `nextCursor` — по
спеке законно, клиент обязан идти по курсору, но выглядит как «ресурсов нет». Стоит либо
пропускать пустых провайдеров при агрегации, либо сознательно оставить и задокументировать.

**Находка: `buildCollectionOutputSchema()` не универсален.** Хелпер требует `z.ZodObject` для схемы
элемента, а у Трекера форма отфильтрованной сущности — `z.record(...)`, потому что состав полей
задаётся вызывающим. Исполнитель не стал обходить хелпер хаком, а собрал ту же форму вручную и
зафиксировал несовместимость. Если такая форма встретится ещё раз — хелпер надо обобщать.

**Замечание к общей тестовой оснастке (не чинилось, кандидат на отдельный пакет).** Заглушка ответа
в утилите проверки достижимости — глубокий Proxy, у которого правдоподобен только `.length`. Вызов
`.filter()` или `.map()` на теле ответа роняет её. Первый же инструмент, фильтрующий ответ, в это
упёрся, и обошёл защитной проверкой `Array.isArray` у себя. Обход законен сам по себе (тело HTTP-ответа
у нас не валидируется Zod), но если так будет упираться каждый следующий инструмент — чинить надо
заглушку, а не расставлять защиту по инструментам.

## DoD этапа

- `resources/list`, `resources/read`, `resources/templates/list`, `prompts/list`, `prompts/get`
  работают на трёх серверах и возвращают cache-поля.
- Инструменты-коллекции умеют возвращать `resource_link`; тест сравнивает объём ответа в двух режимах
  и подтверждает экономию.
- `resources/read` по ссылке, отсутствующей в `resources/list`, работает.
- Несуществующий ресурс → `-32602`.
- Промпты видны как слэш-команды хотя бы в одном реальном клиенте (проверка руками).
- `npm run validate:quiet` зелёный.

## Риски

- Соблазн сделать ресурсом всё подряд. Ресурс оправдан там, где содержимое адресуемо и переиспользуемо;
  для одноразовых вычисляемых ответов это лишний слой.
- Дефолтный режим `resource_link` может ухудшить UX на маленьких выборках — отсюда требование порога.
