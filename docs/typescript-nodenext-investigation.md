# ⚠️ УСТАРЕВШИЙ ДОКУМЕНТ — ИСТОРИЯ РЕШЕНИЯ ПРОБЛЕМЫ

**Дата создания:** 2025-11-14 08:23
**Дата решения:** 2025-11-14 19:11 (через 11 часов)
**Статус:** ✅ **РЕШЕНО** (миграция на Vitest + применение NodeNext)

---

## 📌 Актуальное состояние (на 2025-11-14)

**Проблема решена путём миграции на Vitest:**
- ✅ **Test runner:** Jest → Vitest (коммит c8fe883)
- ✅ **TypeScript config:** `module: "NodeNext"` + `moduleResolution: "NodeNext"` (коммит da6b9c5)
- ✅ **Результат:** Все 404 теста проходят, NodeNext работает корректно
- ✅ **Бонус:** Vitest имеет нативную поддержку ESM + TypeScript

**Этот документ сохранён как история исследования проблемы Jest + NodeNext + ESM.**

---

# Исследование: TypeScript module: "NodeNext" + Jest + ESM (ИСТОРИЧЕСКИЙ КОНТЕКСТ)

**Дата исследования:** 2025-11-14 08:23
**Проект:** yandex-tracker-mcp
**Статус на момент создания:** ОТЛОЖЕНО (известная проблема экосистемы)
**Итоговое решение:** Миграция на Vitest + NodeNext

---

## 📋 Контекст

Попытка внедрения рекомендаций из `recomm.md`, в частности изменения:
- `module: "ES2022"` → `module: "NodeNext"`
- `moduleResolution: "node"` → `moduleResolution: "NodeNext"`

**Цель:** Улучшить совместимость с нативным Node.js ESM runtime.

---

## ⚠️ Проблема

`module: "NodeNext"` **требует явные расширения `.js` в импортах**, даже для TypeScript файлов:

```typescript
// NodeNext требует:
import { Foo } from './foo.js';  // ✅ файл физически foo.ts

// ES2022 позволяет:
import { Foo } from './foo';     // ✅ автоматический resolve
```

**Источник:** [TypeScript Handbook - Module Resolution](https://www.typescriptlang.org/docs/handbook/modules/reference.html#node16-nodenext)

---

## 🔧 Попытки решения

### Попытка 1: Добавить `.js` к импортам в исходниках и тестах

**Действия:**
1. Изменён `tsconfig.json`: `module: "NodeNext"` + `moduleResolution: "NodeNext"`
2. Добавлены `.js` расширения ко всем импортам в `src/`
3. Добавлены `.js` расширения ко всем импортам в `tests/`

**Результат:** ❌ **Тесты падают**

**Ошибка:**
```
Cannot find module '../../../../src/infrastructure/logger.js'
from 'tests/unit/infrastructure/logger.test.ts'
```

**Причина:**
Jest (даже с `ts-jest` + ESM preset) **не может резолвить `.ts` файлы из импортов с `.js`** в ESM mode.

---

### Попытка 2: Использовать `moduleNameMapper`

**Действия:**
```javascript
// jest.config.js
moduleNameMapper: {
  '^(\\.{1,2}/.*)\\.js$': '$1',  // Убираем .js
}
```

**Результат:** ❌ **Тесты падают**

**Ошибка:**
```
Configuration error:
Could not locate module ../../../../src/infrastructure/logger.js mapped as:
$1.
```

**Причина:**
`moduleNameMapper` убирает `.js`, но Jest **не добавляет автоматически `.ts`** в ESM mode.

---

### Попытка 3: Custom Jest resolver

**Действия:**
```javascript
// jest-resolver.cjs
module.exports = (request, options) => {
  if (request.endsWith('.js')) {
    const tsRequest = request.replace(/\.js$/, '.ts');
    try {
      return options.defaultResolver(tsRequest, options);
    } catch (e) {}
  }
  return options.defaultResolver(request, options);
};
```

**Результат:** ❌ **Не сработало**

**Причина:**
Resolver не вызывается для относительных путей или вызывается слишком поздно.

---

### Попытка 4: Пакет `ts-jest-resolver`

**Действия:**
```bash
npm install --save-dev ts-jest-resolver
```

```javascript
// jest.config.js
resolver: 'ts-jest-resolver',
```

**Результат:** ❌ **Тесты всё ещё падают**

**Причина:**
Пакет не решает проблему резолвинга `.js` → `.ts` в ESM mode.

---

### Попытка 5: Переопределение `tsconfig` в `ts-jest`

**Действия:**
```javascript
// jest.config.js
transform: {
  '^.+\\.ts$': [
    'ts-jest',
    {
      useESM: true,
      tsconfig: {
        module: 'ES2022',
        moduleResolution: 'node',
      },
    },
  ],
},
```

**Результат:** ❌ **Частично работает, но падает на импортах из исходников**

**Ошибка:**
```
Cannot find module './base-tool.js' from 'src/mcp/tools/get-issues.tool.ts'
```

**Причина:**
Исходники (`src/`) используют `.js` импорты (для NodeNext), но `ts-jest` с `module: "ES2022"` не понимает `.js` импорты к `.ts` файлам.

---

## 🎯 Корневая причина

**TypeScript + Jest + ESM + NodeNext** — это **несовместимая комбинация** на данный момент:

1. **NodeNext требует:** `.js` в импортах (даже для `.ts` файлов)
2. **Jest ESM mode:** Не резолвит `.ts` файлы из импортов с `.js`
3. **ts-jest:** Не имеет встроенной поддержки этого сценария

**Известная проблема:**
- [ts-jest#4081](https://github.com/kulshekhar/ts-jest/issues/4081)
- [TypeScript#49083](https://github.com/microsoft/TypeScript/issues/49083)
- [Jest#9430](https://github.com/jestjs/jest/issues/9430)

---

## ✅ Рабочее решение (применено)

**Откат к `module: "ES2022"` + `moduleResolution: "node"`**

**Обоснование:**
- ✅ Стабильно работает с Jest + ts-jest
- ✅ Полная поддержка ESM (Node 22 нативно поддерживает ES2022)
- ✅ Не требует `.js` в импортах
- ✅ Никаких практических недостатков для данного проекта

**Недостатки `ES2022` vs `NodeNext`:**
- ⚠️ Менее строгий резолвинг (но не критично для чистого ESM проекта)
- ⚠️ Потенциальные проблемы с mixed ESM/CJS зависимостями (но таких нет в проекте)

---

## 📊 Применённые улучшения

Из `recomm.md` были применены **только безопасные** изменения:

### ✅ Применено

1. **Удалены дублирующиеся флаги строгости**
   - `noImplicitAny`, `strictNullChecks`, и т.д. уже включены через `strict: true`

2. **`downlevelIteration: false`**
   - Не нужен для target ES2022
   - Уменьшает размер и повышает производительность

3. **`types: ["node"]` в основном tsconfig.json**
   - Предотвращает попадание DOM типов
   - Явная защита от случайного импорта browser API

4. **`types: ["node", "jest"]` в tsconfig.tests.json**
   - Обеспечивает правильную работу Jest типов
   - Улучшает автодополнение в IDE

5. **`engines: { "node": ">=22.0.0" }`**
   - Явное требование Node.js 22+ (подтверждено пользователем)

6. **Удалён `skipDefaultLibCheck`**
   - Дублирует `skipLibCheck`

### ❌ Не применено (отложено)

1. **`module: "NodeNext"` + `moduleResolution: "NodeNext"`**
   - Причина: Несовместимость с Jest (см. выше)

2. **`importHelpers` + `noEmitHelpers`**
   - Причина: Избыточно для серверного приложения

3. **`useDefineForClassFields: false`**
   - Причина: Workaround для edge case, проблем не обнаружено

---

## 🔮 Будущее решение

**Когда можно будет вернуться к NodeNext:**

1. **ts-jest выпустит нативную поддержку** NodeNext + ESM
   - Следить за: https://github.com/kulshekhar/ts-jest/issues/4081

2. **Jest добавит поддержку** TypeScript `.js` → `.ts` маппинга
   - Следить за: https://github.com/jestjs/jest/issues/9430

3. **Альтернатива:** Переход на другой test runner
   - [Vitest](https://vitest.dev/) — нативная поддержка ESM + TypeScript
   - [Node.js Test Runner](https://nodejs.org/api/test.html) — встроенный в Node 22

---

## 📝 Команды для воспроизведения проблемы

```bash
# 1. Применить NodeNext конфигурацию
git stash  # сохранить текущие изменения

# В tsconfig.json:
# "module": "NodeNext"
# "moduleResolution": "NodeNext"

# 2. Добавить .js к импортам (скрипт)
python3 /tmp/fix_imports.py

# 3. Запустить тесты
npm run test

# Результат: тесты падают с ошибками резолвинга
```

---

## 🎓 Выводы

1. **`module: "NodeNext"` технически правильное решение**, но экосистема ещё не готова
2. **`module: "ES2022"` практичное решение** для текущего момента
3. **Нет реальных недостатков** для данного проекта при использовании ES2022
4. **Мониторить прогресс** в ts-jest и Jest для будущего апгрейда

---

## 🔗 Ссылки

- [TypeScript NodeNext Documentation](https://www.typescriptlang.org/docs/handbook/modules/reference.html#node16-nodenext)
- [ts-jest ESM Support](https://kulshekhar.github.io/ts-jest/docs/guides/esm-support/)
- [Jest ESM Support](https://jestjs.io/docs/ecmascript-modules)
- [Node.js ESM Documentation](https://nodejs.org/api/esm.html)
